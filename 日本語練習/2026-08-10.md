## 2026-08-10

### 练习 1

> [!question] 题目
> 在 Unreal 中设计闪避动作时，无敌时间、位移曲线和输入锁定应该由同一个动作状态管理。

> [!example] 自己的答案
> Unrealで回避アクションを設計する時に、無敵時間、移動カブー、入力ロックは同じステートで管理します。

> [!warning] 指正
> 意思基本清楚。`設計する時に` 建议改为更自然的 `設計するときは`。`移動カブー` 应为 `移動カーブ`。题目里的“由同一个动作状态管理”建议明确为 `同じアクション状態で一元的に管理する`，比单纯 `同じステートで管理します` 更完整。

> [!tip] 参考回答
> Unrealで回避アクションを設計するときは、無敵時間、移動カーブ、入力ロックを同じアクション状態で一元的に管理するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的技能冷却显示应该根据剩余时间刷新，不要让按钮组件自己计算战斗规则。

> [!example] 自己的答案
>　Unityのスキルクールダウンは残り時間に応じて更新し、ボタンコンポーネントがバトルルールを計算するようにしません。

> [!warning] 指正
> 大意正确。`スキルクールダウン` 少了“显示”，建议说 `スキルのクールダウン表示`。`残り時間に応じて更新し` 很自然。后半 `計算するようにしません` 稍硬，可以改成 `計算させないようにします`，更符合“不要让组件自己做规则计算”的意思。

> [!tip] 参考回答
> Unityのスキルのクールダウン表示は残り時間に応じて更新し、ボタンコンポーネントに戦闘ルールを計算させないようにします。

---

### 练习 3

> [!question] 题目
> 如果角色转向动画和实际朝向不一致，请比较控制器旋转、根运动设置和动画蓝图变量。

> [!example] 自己的答案
>　キャラクター回転アニメーションと実際の向きが不一致する時に、コントローラの回転、ルートモーション、アニメーションブループリントの変数を比較してください。

> [!warning] 指正
> 题意能传达。`キャラクター回転アニメーション` 建议加 `の`，写成 `キャラクターの回転アニメーション`。`不一致する時に` 不自然，应为 `一致しない場合は`。`コントローラ` 可写成长音 `コントローラー`，但不影响理解。

> [!tip] 参考回答
> キャラクターの回転アニメーションと実際の向きが一致しない場合は、コントローラーの回転、ルートモーション設定、アニメーションブループリントの変数を比較してください。

---

### 练习 4

> [!question] 题目
> 在实现范围伤害时，请先收集命中对象，再按队伍、距离和免疫状态过滤目标。

> [!example] 自己的答案
> ダメージ範囲を実装する時に、ヒットオブジェクトを先に収集し、チーム、距離、免疫状態に応じてターゲットを絞り込みます。

> [!warning] 指正
> 主要意思正确。`ダメージ範囲` 会偏“伤害范围”，题目是“范围伤害”，更自然是 `範囲ダメージ`。`ヒットオブジェクト` 可以理解，但游戏判定里更常说 `命中した対象`。`免疫状態` 可以用，也可以说 `無敵状態` 或 `耐性状態`，按系统语境选择。

> [!tip] 参考回答
> 範囲ダメージを実装するときは、まず命中した対象を収集し、その後でチーム、距離、免疫状態に応じてターゲットを絞り込みます。

---

### 练习 5

> [!question] 题目
> 为了降低背包界面的刷新成本，可以只更新数量变化的格子，而不是重建整个列表。

> [!example] 自己的答案
>　インベントリーの更新コストを軽減するため、変化されたセルだけを更新し、リスト全体を再構築するのではありません。

> [!warning] 指正
> 意思基本明确。`インベントリー` 可以用，常见写法也有 `インベントリ`。`変化されたセル` 不自然，应为 `数量が変わったセル` 或 `変更されたセル`。`再構築するのではありません` 语气偏说明，建议用 `再構築しないようにします` 或 `再構築する必要はありません`。

> [!tip] 参考回答
> インベントリ画面の更新コストを下げるために、リスト全体を再構築するのではなく、数量が変わったセルだけを更新できます。

---

### 练习 6

> [!question] 题目
> 当 Boss 进入第二阶段时，攻击模式、镜头演出和背景音效需要按固定顺序切换。

> [!example] 自己的答案
>　ボスが第二段階に入る時、攻撃モード、カメラ演出、背景効果音は決まった順番で切り替えます。

> [!warning] 指正
> 表达整体自然。`第二段階に入る時` 可改为 `第二段階に入るときは`。`背景効果音` 能懂，但 Boss 战语境里 `BGM` 或 `背景音` 更自然。题目是“需要按固定顺序切换”，建议加 `切り替える必要があります`。

> [!tip] 参考回答
> ボスが第二段階に入るときは、攻撃パターン、カメラ演出、BGMを決まった順序で切り替える必要があります。

---

### 练习 7

> [!question] 题目
> Unreal 的 Gameplay Ability 应该从效果规格中读取伤害参数，而不是直接访问武器蓝图里的临时变量。

> [!example] 自己的答案
>　UnrealのGameplay Abilityは効果設定からダメージパラメータを読み込み、武器のブループリント内の一時的な変数を直接アクセスするのではありません。

> [!warning] 指正
> 大意正确。`効果設定` 稍泛，GAS 语境里的“效果规格”更适合说 `Gameplay Effect Spec` 或 `効果スペック`。`変数を直接アクセスする` 助词不自然，应为 `変数に直接アクセスする`。句尾建议用 `べきではありません`，更符合“应该/不应该”的技术建议。

> [!tip] 参考回答
> UnrealのGameplay Abilityは、効果スペックからダメージパラメータを読み込み、武器ブループリント内の一時的な変数に直接アクセスするべきではありません。

---

### 练习 8

> [!question] 题目
> 在优化草地渲染时，请分别记录实例数量、LOD 切换距离和阴影开销。

> [!example] 自己的答案
>　草の描画を最適化する時に、インスタンスの数、LOD切り替え距離、シャドウコストをそれぞれ記録してください。

> [!warning] 指正
> 意思完整。`最適化する時に` 建议写成 `最適化するときは`。`インスタンスの数` 可以，但性能记录里 `インスタンス数` 更紧凑。`シャドウコスト` 可用，也可以说 `影のコスト`。

> [!tip] 参考回答
> 草の描画を最適化するときは、インスタンス数、LODの切り替え距離、シャドウコストをそれぞれ記録してください。

---

### 练习 9

> [!question] 题目
> 如果敌人追击时频繁丢失目标，请检查感知更新时间、最后已知位置和路径请求状态。

> [!example] 自己的答案
>　敵が追跡中に頻繁にターゲットを失う場合、検知更新時間、最後検知位置、ルート請求状態を確認してください。

> [!warning] 指正
> 主要意思能看出，但几个术语需要调整。`検知更新時間` 可以理解，不过 AI 感知语境里更自然是 `感知の更新タイミング`。`最後検知位置` 应为 `最後に確認した位置` 或 `最後に把握した位置`。`ルート請求状態` 不自然，路径请求建议说 `経路リクエストの状態`。

> [!tip] 参考回答
> 敵が追跡中に頻繁にターゲットを見失う場合は、<mark style="background: #FF5582A6;">感知の更新タイミング</mark>、最後に把握した位置、経路リクエストの状態を確認してください。

---

### 练习 10

> [!question] 题目
> Unity 的任务面板应该监听任务数据变化，再刷新标题、进度文本和奖励图标。

> [!example] 自己的答案
>　Unityのクエストパネルはクエストのデータ変化イベントを登録してからタイトル、進捗テキスト、報酬アイコンを更新します。

> [!warning] 指正
> 题意基本正确。`データ変化イベントを登録してから` 会让人觉得只是在注册事件，题目是“监听数据变化”，更自然是 `データの変化を監視して` 或 `変更通知を受けて`。后半三个 UI 要素的并列很清楚。

> [!tip] 参考回答
> Unityのクエストパネルは、<mark style="background: #FF5582A6;">クエストデータの変化を監視し、その変更を受けて</mark>タイトル、進捗テキスト、報酬アイコンを更新するべきです。

---

### 练习 11

> [!question] 题目
> 在制作受击反馈时，伤害计算、生命值扣除和屏幕震动不应该写在同一个 UI 回调里。

> [!example] 自己的答案
>　被弾フィードバックを作成する時に、ダメージ計算、HPの減る、スクリンシェークを同じUIコールバック内に実装するのではありません。

> [!warning] 指正
> 意思方向正确，但自然度和术语有几处问题。`HPの減る` 是名词化不完整，应为 `HPの減少処理` 或 `HPを減らす処理`。`スクリンシェーク` 应为 `スクリーンシェイク`。`作成する時に` 可改为 `作成するときは`。

> [!tip] 参考回答
> 被弾フィードバックを作成するときは、ダメージ計算、<mark style="background: #FF5582A6;">HPの減少処理</mark>、スクリーンシェイクを同じUIコールバック内に実装するべきではありません。

---

### 练习 12

> [!question] 题目
> 如果读取旧存档后装备属性缺失，请为新增字段准备默认值和兼容转换逻辑。

> [!example] 自己的答案
>　旧セーブデータを読み込んだ後装備の属性が欠落している場合、新しく追加されたフィールドにデフォルト値と互換性移行ロジックを用意してください。

> [!warning] 指正
> 大意正确。`読み込んだ後装備の属性` 中间建议加 `に`。`属性` 可以用，但装备语境里 `装備プロパティ` 或 `装備ステータス` 也自然。`互換性移行ロジック` 稍硬，建议说 `互換用の変換ロジック`。

> [!tip] 参考回答
> 旧セーブデータを読み込んだ後に装備属性が欠落している場合は、新しく追加されたフィールド向けにデフォルト値と互換用の変換ロジックを用意してください。

---

### 练习 13

> [!question] 题目
> 在调试连招取消规则时，请记录当前段数、输入缓存时间和可取消窗口是否开启。

> [!example] 自己的答案
>　コンボキャンセル決まりをデバッグする時に、現在の段落番号、入力バッファタイムスタンプ、キャンセルウィンドを記録してください。

> [!warning] 指正
> 题意能传达，但关键搭配需要修正。`コンボキャンセル決まり` 不自然，建议说 `コンボキャンセルのルール`。`段落番号` 会像文章段落，连招段数更自然是 `現在の段数`。`キャンセルウィンド` 应为 `キャンセルウィンドウ`，并且题目有“是否开启”，需要补出 `開いているかどうか`。

> [!tip] 参考回答
> コンボキャンセルのルールをデバッグするときは、現在の段数、入力バッファの時間、キャンセルウィンドウが開いているかどうかを記録してください。

---

### 练习 14

> [!question] 题目
> 如果今天晚上不下雨，我们吃完饭后去附近的公园走一会儿吧。

> [!example] 自己的答案
>　今晩雨が降らないなら、食事後近くの公園で散歩しましょう

> [!warning] 指正
> 日常意思清楚。`雨が降らないなら` 可以用，但“如果不下雨”更自然是 `雨が降らなければ`。`食事後` 偏书面，日常对话里 `ご飯を食べた後で` 更自然。句末需要句号。

> [!tip] 参考回答
> 今晩雨が降らなければ、ご飯を食べた後で近くの公園を少し散歩しましょう。

---

### 练习 15

> [!question] 题目
> 我先确认这两个问题的负责人，明天上午再把处理顺序发到群里。

> [!example] 自己的答案
>　二つの問題の担当者を確認しておき、明日午前処理順番をチャンネルで共有します。

> [!warning] 指正
> 职场意思基本完整。`確認しておき` 自然，但这里更常说 `確認しておいて`。`明日午前` 应为 `明日の午前中に`。`処理順番` 不自然，建议用 `対応順` 或 `対応する順番`。`チャンネルで共有します` 可以表达“发到群里”。

> [!tip] 参考回答
> 先にこの二つの問題の担当者を確認しておいて、明日の午前中に対応順をチャンネルで共有します。

## 2026-08-11

### 练习 1

> [!question] 题目
> 在 Unreal 中实现锁定目标时，镜头旋转、角色朝向和目标选择规则应该分别维护。

> [!example] 自己的答案
> Unrealでターゲットロックを実装する時に、カメラ回転、キャラクター向き、ターゲット絞り込みルールは分けて保守します。

> [!warning] 指正
> 意思基本清楚。`実装する時に` 建议改为 `実装するときは`。`キャラクター向き` 少了 `の`，应为 `キャラクターの向き`。`ターゲット絞り込みルール` 能表达“筛选规则”，但题目是“目标选择规则”，更自然是 `ターゲット選択ルール`。`保守します` 偏维护代码，规则职责上用 `管理します` 或 `別々に管理します` 更自然。

> [!tip] 参考回答
> Unrealでターゲットロックを実装するときは、カメラの回転、キャラクターの向き、ターゲット選択ルールをそれぞれ別々に管理するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的对象池应该在关卡开始前预热常用特效，避免战斗中频繁实例化。

> [!example] 自己的答案
>　Unityのオブジェクトプールはレベルに入る前によく使用するエフェクトを予熱するべきで、バトル中に頻繁にインスタンス化を防ぎます。

> [!warning] 指正
> 大意正确。`レベルに入る前` 可以理解，但 Unity/关卡语境里 `レベル開始前` 或 `シーン開始前` 更自然。`予熱する` 是直译，技术上常说 `事前にウォームアップする`。后半 `インスタンス化を防ぎます` 前面缺少目的结构，建议改成 `頻繁なインスタンス化を避けます`。

> [!tip] 参考回答
> Unityのオブジェクトプールは、レベル開始前によく使うエフェクトを事前にウォームアップし、戦闘中の頻繁なインスタンス化を避けるべきです。

---

### 练习 3

> [!question] 题目
> 如果技能命中特效的位置偏离目标，请检查插槽名称、坐标空间和特效生成参数。

> [!example] 自己的答案
>　スキルヒットエフェクトの位置がターゲットと外れる場合、スロット名、座標系、エフェクト生成パラメータを確認してください。

> [!warning] 指正
> 题意能传达。`ターゲットと外れる` 不自然，位置偏离目标应说 `ターゲットからずれている`。`スロット名` 可以用，但 Unreal 里也常写 `ソケット名`，要看具体系统。`座標系` 可以表达坐标空间，也可写成 `座標空間` 更贴近题目。

> [!tip] 参考回答
> スキルのヒットエフェクトの位置がターゲットからずれている場合は、ソケット名、座標空間、エフェクト生成パラメータを確認してください。

---

### 练习 4

> [!question] 题目
> 在设计状态机时，待机、移动和攻击状态之间的切换条件要保持简单明确。

> [!example] 自己的答案
>　ステートマシンを設計する時に、待機、移動、攻撃状態の間での切り替え条件を簡潔で明確にするようにします。

> [!warning] 指正
> 表达整体自然。`設計する時に` 建议统一为 `設計するときは`。`状態の間での切り替え条件` 可以理解，但更简洁是 `状態間の遷移条件`。`簡潔で明確にするようにします` 稍绕，建议说 `シンプルで明確にします`。

> [!tip] 参考回答
> ステートマシンを設計するときは、待機、移動、攻撃の状態間の遷移条件をシンプルで明確にする必要があります。

---

### 练习 5

> [!question] 题目
> Unreal 的动画蒙太奇结束后，应该清理临时标签并恢复角色的输入状态。

> [!example] 自己的答案
>　UnrealのMontageが終了した後、一時的なラベルをクリアしてキャラクターの入力状態をリセットするべきです。

> [!warning] 指正
> 主要意思正确。`Montage` 最好写成 `アニメーションモンタージュ`，更完整。`ラベル` 对 Unreal 的 Gameplay Tag 来说不准确，建议用 `タグ`。`リセットする` 可以表达恢复，但输入状态更自然是 `元に戻す` 或 `復元する`。

> [!tip] 参考回答
> Unrealのアニメーションモンタージュが終了した後は、一時的なタグをクリアし、キャラクターの入力状態を元に戻すべきです。

---

### 练习 6

> [!question] 题目
> Unity 的设置界面可以把音量、画质和操作灵敏度保存到同一个配置对象里。

> [!example] 自己的答案
>　Unityの設定画面で音量、画面品質、操作精度を同じ設定オブジェクト内に保存します。

> [!warning] 指正
> 意思大致正确。`画面品質` 会偏“屏幕质量”，画质通常说 `画質`。`操作精度` 是“操作精度”，题目的“操作灵敏度”更自然是 `操作感度`。`設定画面で...保存します` 少了“可以”的语气，可用 `保存できます`。

> [!tip] 参考回答
> Unityの設定画面では、音量、画質、操作感度を同じ設定オブジェクトに保存できます。

---

### 练习 7

> [!question] 题目
> 在优化 UI 动画时，请先确认是否存在过多的布局重算和透明度叠加。

> [!example] 自己的答案
>　UIアニメーションを最適化する時は、まず過剰なレイアウト再計算と透明度重なりを確認してください。

> [!warning] 指正
> 题意清楚。`最適化する時は` 可以改为 `最適化するときは`。`透明度重なり` 不太自然，建议说 `透明度の重なり` 或 `アルファの重なり`。如果强调性能问题，`過剰なレイアウト再計算` 很合适。

> [!tip] 参考回答
> UIアニメーションを最適化するときは、まず過剰なレイアウト再計算や<mark style="background: #FF5582A6;">透明度の重なり</mark>が発生していないか確認してください。

---

### 练习 8

> [!question] 题目
> 如果道具拾取后没有进入背包，请从碰撞检测、实例创建和格子占用三个步骤排查。

> [!example] 自己的答案
>　アイテムを拾得下後もインベントリに入っていない場合は、コリジョン検出、インスタンスの生成、セルをこの3つ要素から確認してください

> [!warning] 指正
> 主要意思能看出，但有几处需要修正。`拾得下後` 是错字，应为 `拾った後` 或 `取得した後`。`入っていない場合は` 可以。`セルをこの3つ要素から` 结构不成立，应说 `セルの占有状態という三つの手順から`。句末需要句号。

> [!tip] 参考回答
> アイテムを拾った後もインベントリに入っていない場合は、コリジョン検出、インスタンス生成、<mark style="background: #FF5582A6;">セルの占有状態</mark>という<mark style="background: #FF5582A6;">三つの手順</mark>から確認してください。

---

### 练习 9

> [!question] 题目
> Unreal 的角色属性变化应该通过事件通知 UI，而不是让 UI 每帧读取属性值。

> [!example] 自己的答案
>　UIはイベント経由でUnrealのキャラクター属性変化を受け取るようにし、毎フレーム属性を読み込むのではありません。

> [!warning] 指正
> 意思基本正确。主语写成 `UIは` 后，`Unrealのキャラクター属性変化` 会稍不自然；题目强调“角色属性变化应该通知 UI”，建议以 `Unrealのキャラクター属性の変化は...` 开头。`毎フレーム属性を読み込むのではありません` 语气偏硬，建议用 `読み取らせるべきではありません`。

> [!tip] 参考回答
> Unrealのキャラクター属性の変化はイベント経由でUIに通知し、UIに毎フレーム属性値を読み取らせるべきではありません。

---

### 练习 10

> [!question] 题目
> 在调试跳跃手感时，请分别调整起跳速度、重力倍率和落地缓冲时间。

> [!example] 自己的答案
>　ジャンプ手応えをデバッグする時に、ジャンプ速度、重力倍率、着地バッファ時間をそれぞれ調整してください。

> [!warning] 指正
> 大意能传达。`ジャンプ手応え` 不自然，“跳跃手感”更自然是 `ジャンプの操作感` 或 `ジャンプの手触り`。`デバッグする時に` 建议改成 `調整するときは` 或 `デバッグするときは`。`ジャンプ速度` 比“起跳速度”稍泛，可用 `ジャンプ開始速度`。

> [!tip] 参考回答
> ジャンプの操作感をデバッグするときは、ジャンプ開始速度、重力倍率、着地バッファ時間をそれぞれ調整してください。

---

### 练习 11

> [!question] 题目
> Unity 的资源加载流程应该区分常驻资源和临时资源，方便统计内存占用。

> [!example] 自己的答案
>　Unityのリソースロードフローは常駐リソースと一時的リソースを切り分けるべきでメモリ使用量を計測しやすくなります。

> [!warning] 指正
> 题意正确。`リソースロードフロー` 可以理解，但更自然是 `リソース読み込みフロー`。`一時的リソース` 应为 `一時リソース` 或 `一時的なリソース`。`切り分けるべきでメモリ...` 中间缺少连接，建议用 `切り分けることで...しやすくなります`。

> [!tip] 参考回答
> Unityのリソース読み込みフローでは、常駐リソースと一時リソースを切り分けることで、メモリ使用量を集計しやすくなります。

---

### 练习 12

> [!question] 题目
> 如果战斗日志缺少关键字段，请在伤害结算前记录技能编号、攻击者和目标。

> [!example] 自己的答案
>　バトルログには肝心なフィールドが足りない場合、スキル番号、攻撃者、ターゲットをダメージ計算する前に記録してください。

> [!warning] 指正
> 意思基本完整。`バトルログには` 用 `に` 更自然：`バトルログに重要なフィールドが足りない場合`。`肝心なフィールド` 可以懂，但技术文档中 `重要なフィールド` 或 `必要なフィールド` 更自然。`ダメージ計算` 与题目的“结算”相比略窄，可用 `ダメージ処理` 或 `ダメージ確定処理`。

> [!tip] 参考回答
> バトルログに重要なフィールドが足りない場合は、<mark style="background: #FF5582A6;">ダメージ確定処理</mark>の前に、スキル番号、攻撃者、ターゲットを記録してください。

---

### 练习 13

> [!question] 题目
> 在制作教程关卡时，引导提示、交互限制和完成条件需要由任务流程统一管理。

> [!example] 自己的答案
>　チュートリアルレベルを作成するときは、導き提示、インタラクション制限、完成条件はクエストフローで一元的に管理します。

> [!warning] 指正
> 大意正确。`導き提示` 不自然，教程引导提示建议说 `ガイド表示` 或 `案内表示`。`完成条件` 可以理解，但任务/教程语境里 `完了条件` 更自然。`クエストフローで一元的に管理します` 很好。

> [!tip] 参考回答
> チュートリアルレベルを作成するときは、<mark style="background: #FF5582A6;">ガイド表示</mark>、インタラクション制限、完了条件をクエストフローで一元的に管理する必要があります。

---

### 练习 14

> [!question] 题目
> 周末如果你有空，我们一起去看那家新开的展览吧。

> [!example] 自己的答案
>　週末に時間があれば、一緒に新しくできた展覧会を見に行きましょう。

> [!warning] 指正
> 表达自然，意思完整。`新しくできた展覧会` 可以表达“新开的展览”，如果想更口语一些，也可以说 `新しく始まった展示`。`見に行きましょう` 很自然。

> [!tip] 参考回答
> 週末に時間があれば、一緒に新しく始まった展覧会を見に行きましょう。

---

### 练习 15

> [!question] 题目
> 我把今天的修改点整理好了，会议前会发给大家确认。

> [!example] 自己的答案
>　本日に改修点をまとめました。ミーティング前に皆さんに送って確認していただきます。

> [!warning] 指正
> 职场意思基本清楚。`本日に` 不自然，应为 `今日の`。`改修点` 偏程序修改也可以，但题目的“修改点”更通用可说 `修正点`。`皆さんに送って確認していただきます` 表达可以，但更自然是 `皆さんに送って確認してもらいます` 或礼貌一点 `確認していただきます`。

> [!tip] 参考回答
> 今日の修正点を整理しました。会議前に皆さんに送って確認していただきます。

## 2026-08-12

### 练习 1

> [!question] 题目
> 在 Unreal 中处理受击硬直时，角色移动、动画播放和输入恢复应该按状态变化统一控制。

> [!example] 自己的答案
> Unrealで被弾硬直を処理する時に、キャラクターの移動、アニメーション再生、入力復元は状態変化による一元的に制御します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 2

> [!question] 题目
> Unity 的伤害数字显示只负责表现，不应该直接修改敌人的生命值数据。

> [!example] 自己的答案
>　Unityのダメージ数字の表示は表現だけを担当し、敵のHPを直接変更するのではありません。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 3

> [!question] 题目
> 如果技能范围预览和实际判定不一致，请比较碰撞形状、缩放比例和角色朝向。

> [!example] 自己的答案
>　スキル範囲のプレビューと実際の判定が一致していない場合、コリジョン形、スケール率、キャラクターの向きを比較してください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 4

> [!question] 题目
> 在设计连击系统时，下一段攻击能否开始应该根据当前动作帧和输入缓存决定。

> [!example] 自己的答案
> コンボシステムを設計する時に、次の段階に入るかどうかは現在アクションフレームと入力バッファによって決定します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 5

> [!question] 题目
> Unreal 的技能消耗应该先验证资源是否足够，再扣除数值并播放失败或成功表现。

> [!example] 自己的答案
>　Unrealのスキルコストではまず消耗を検証し、数値を減らしてから成功・失敗表現を再生します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 6

> [!question] 题目
> Unity 的小地图图标可以根据实体状态变化刷新颜色、透明度和显示优先级。

> [!example] 自己的答案
>　Unityのミニマップはインスタンスの状態に応じて色の更新、透明度、表示優先度を変更します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 7

> [!question] 题目
> 在优化角色材质时，请分别记录贴图尺寸、材质实例数量和动态参数更新频率。

> [!example] 自己的答案
>　キャラクターのマテリアルを最適化する時に、テクスチャのサイズ、マテリアルインスタンスの数、動的パラメータの更新頻度をそれぞれ記録してください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 8

> [!question] 题目
> 如果敌人的巡逻行为突然停止，请检查当前路点、等待计时器和行为树条件。

> [!example] 自己的答案
>　敵のパトロールが急に止まった場合、現在の経路ポイント、待ちタイマー、ビヘイビアツリーを確認してください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 9

> [!question] 题目
> Unreal 的属性面板刷新应该由属性变化事件触发，而不是在 Tick 中反复重建控件。

> [!example] 自己的答案
>　Unrealの属性画面の更新は属性変化イベントによって発火するべきで、Tick内でウィジェットを繰り返して生成するのではありません。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 10

> [!question] 题目
> 在保存玩家设置时，请把画质、语言和按键配置写入带版本号的数据结构。

> [!example] 自己的答案
>　プレイヤーの設定を保存する時は、画質、言語、マッピングをバージョン付きデータ構造に書き込んでください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 11

> [!question] 题目
> Unity 的关卡加载界面应该显示进度和提示文本，但不要承担资源依赖检查的职责。

> [!example] 自己的答案
>　Unityのレベルロード画面は進捗と提示テキストを表示するべきで、リソースロードと依存チェックを担当するのではありません。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 12

> [!question] 题目
> 如果投射物没有命中目标，请记录生成位置、初速度、碰撞通道和生命周期。

> [!example] 自己的答案
>　飛び道具が外れた場合、生成位置、初速度、コリジョンチャンネルとライフサイクルを記録してください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 13

> [!question] 题目
> 在制作任务奖励界面时，奖励计算结果和领取按钮状态应该来自同一份任务数据。

> [!example] 自己的答案
>　クエスト報酬画面を生成する時に、報酬決定の受け取るボタン状態は同じクエストを参照するべきです。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 14

> [!question] 题目
> 明天早上如果天气不错，我们早点出门，顺便买杯咖啡吧。

> [!example] 自己的答案
>　明日朝、天気が良ければ、早めに出かけてついでにコーヒーを買いましょう。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 15

> [!question] 题目
> 我会先整理今天讨论过的事项，下午发给大家确认有没有遗漏。

> [!example] 自己的答案
>　本日まず相談した事項をまとめ、午後に皆さんに送って抜け漏れがないかを確認していただきます。

> [!warning] 指正
>

> [!tip] 参考回答
>
