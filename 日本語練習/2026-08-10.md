## 2026-08-10

### 练习 1

> [!question] 题目
> 在 Unreal 中设计闪避动作时，无敌时间、位移曲线和输入锁定应该由同一个动作状态管理。

> [!example] 自己的答案
> Unrealで回避アクションを設計する時に、無敵時間、移動カブー、入力ロックは同じステートで管理します。

> [!warning] 指正
> 意思基本清楚。`設計する時に` 建议改为更自然的 `設計するときは`。`移動カブー` 应为 `移動カーブ`。题目里的“由同一个动作状态管理”建议明确为 `同じアクション状態で一元的に管理する`，比单纯 `同じステートで管理します` 更完整。

> [!tip] 参考回答
> Unrealで回避アクションを設計するときは、無敵時間、移動カーブ、入力ロックを同じアクション状態で一元的に管理するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的技能冷却显示应该根据剩余时间刷新，不要让按钮组件自己计算战斗规则。

> [!example] 自己的答案
>　Unityのスキルクールダウンは残り時間に応じて更新し、ボタンコンポーネントがバトルルールを計算するようにしません。

> [!warning] 指正
> 大意正确。`スキルクールダウン` 少了“显示”，建议说 `スキルのクールダウン表示`。`残り時間に応じて更新し` 很自然。后半 `計算するようにしません` 稍硬，可以改成 `計算させないようにします`，更符合“不要让组件自己做规则计算”的意思。

> [!tip] 参考回答
> Unityのスキルのクールダウン表示は残り時間に応じて更新し、ボタンコンポーネントに戦闘ルールを計算させないようにします。

---

### 练习 3

> [!question] 题目
> 如果角色转向动画和实际朝向不一致，请比较控制器旋转、根运动设置和动画蓝图变量。

> [!example] 自己的答案
>　キャラクター回転アニメーションと実際の向きが不一致する時に、コントローラの回転、ルートモーション、アニメーションブループリントの変数を比較してください。

> [!warning] 指正
> 题意能传达。`キャラクター回転アニメーション` 建议加 `の`，写成 `キャラクターの回転アニメーション`。`不一致する時に` 不自然，应为 `一致しない場合は`。`コントローラ` 可写成长音 `コントローラー`，但不影响理解。

> [!tip] 参考回答
> キャラクターの回転アニメーションと実際の向きが一致しない場合は、コントローラーの回転、ルートモーション設定、アニメーションブループリントの変数を比較してください。

---

### 练习 4

> [!question] 题目
> 在实现范围伤害时，请先收集命中对象，再按队伍、距离和免疫状态过滤目标。

> [!example] 自己的答案
> ダメージ範囲を実装する時に、ヒットオブジェクトを先に収集し、チーム、距離、免疫状態に応じてターゲットを絞り込みます。

> [!warning] 指正
> 主要意思正确。`ダメージ範囲` 会偏“伤害范围”，题目是“范围伤害”，更自然是 `範囲ダメージ`。`ヒットオブジェクト` 可以理解，但游戏判定里更常说 `命中した対象`。`免疫状態` 可以用，也可以说 `無敵状態` 或 `耐性状態`，按系统语境选择。

> [!tip] 参考回答
> 範囲ダメージを実装するときは、まず命中した対象を収集し、その後でチーム、距離、免疫状態に応じてターゲットを絞り込みます。

---

### 练习 5

> [!question] 题目
> 为了降低背包界面的刷新成本，可以只更新数量变化的格子，而不是重建整个列表。

> [!example] 自己的答案
>　インベントリーの更新コストを軽減するため、変化されたセルだけを更新し、リスト全体を再構築するのではありません。

> [!warning] 指正
> 意思基本明确。`インベントリー` 可以用，常见写法也有 `インベントリ`。`変化されたセル` 不自然，应为 `数量が変わったセル` 或 `変更されたセル`。`再構築するのではありません` 语气偏说明，建议用 `再構築しないようにします` 或 `再構築する必要はありません`。

> [!tip] 参考回答
> インベントリ画面の更新コストを下げるために、リスト全体を再構築するのではなく、数量が変わったセルだけを更新できます。

---

### 练习 6

> [!question] 题目
> 当 Boss 进入第二阶段时，攻击模式、镜头演出和背景音效需要按固定顺序切换。

> [!example] 自己的答案
>　ボスが第二段階に入る時、攻撃モード、カメラ演出、背景効果音は決まった順番で切り替えます。

> [!warning] 指正
> 表达整体自然。`第二段階に入る時` 可改为 `第二段階に入るときは`。`背景効果音` 能懂，但 Boss 战语境里 `BGM` 或 `背景音` 更自然。题目是“需要按固定顺序切换”，建议加 `切り替える必要があります`。

> [!tip] 参考回答
> ボスが第二段階に入るときは、攻撃パターン、カメラ演出、BGMを<mark style="background: #FF5582A6;">決まった順序で</mark>切り替える必要があります。

---

### 练习 7

> [!question] 题目
> Unreal 的 Gameplay Ability 应该从效果规格中读取伤害参数，而不是直接访问武器蓝图里的临时变量。

> [!example] 自己的答案
>　UnrealのGameplay Abilityは効果設定からダメージパラメータを読み込み、武器のブループリント内の一時的な変数を直接アクセスするのではありません。

> [!warning] 指正
> 大意正确。`効果設定` 稍泛，GAS 语境里的“效果规格”更适合说 `Gameplay Effect Spec` 或 `効果スペック`。`変数を直接アクセスする` 助词不自然，应为 `変数に直接アクセスする`。句尾建议用 `べきではありません`，更符合“应该/不应该”的技术建议。

> [!tip] 参考回答
> UnrealのGameplay Abilityは、効果スペックからダメージパラメータを読み込み、武器ブループリント内の一時的な変数に直接アクセスするべきではありません。

---

### 练习 8

> [!question] 题目
> 在优化草地渲染时，请分别记录实例数量、LOD 切换距离和阴影开销。

> [!example] 自己的答案
>　草の描画を最適化する時に、インスタンスの数、LOD切り替え距離、シャドウコストをそれぞれ記録してください。

> [!warning] 指正
> 意思完整。`最適化する時に` 建议写成 `最適化するときは`。`インスタンスの数` 可以，但性能记录里 `インスタンス数` 更紧凑。`シャドウコスト` 可用，也可以说 `影のコスト`。

> [!tip] 参考回答
> 草の描画を最適化するときは、インスタンス数、LODの切り替え距離、シャドウコストをそれぞれ記録してください。

---

### 练习 9

> [!question] 题目
> 如果敌人追击时频繁丢失目标，请检查感知更新时间、最后已知位置和路径请求状态。

> [!example] 自己的答案
>　敵が追跡中に頻繁にターゲットを失う場合、検知更新時間、最後検知位置、ルート請求状態を確認してください。

> [!warning] 指正
> 主要意思能看出，但几个术语需要调整。`検知更新時間` 可以理解，不过 AI 感知语境里更自然是 `感知の更新タイミング`。`最後検知位置` 应为 `最後に確認した位置` 或 `最後に把握した位置`。`ルート請求状態` 不自然，路径请求建议说 `経路リクエストの状態`。

> [!tip] 参考回答
> 敵が追跡中に頻繁にターゲットを見失う場合は、<mark style="background: #FF5582A6;">感知の更新タイミング</mark>、最後に把握した位置、経路リクエストの状態を確認してください。

---

### 练习 10

> [!question] 题目
> Unity 的任务面板应该监听任务数据变化，再刷新标题、进度文本和奖励图标。

> [!example] 自己的答案
>　Unityのクエストパネルはクエストのデータ変化イベントを登録してからタイトル、進捗テキスト、報酬アイコンを更新します。

> [!warning] 指正
> 题意基本正确。`データ変化イベントを登録してから` 会让人觉得只是在注册事件，题目是“监听数据变化”，更自然是 `データの変化を監視して` 或 `変更通知を受けて`。后半三个 UI 要素的并列很清楚。

> [!tip] 参考回答
> Unityのクエストパネルは、<mark style="background: #FF5582A6;">クエストデータの変化を監視し、その変更を受けて</mark>タイトル、進捗テキスト、報酬アイコンを更新するべきです。

---

### 练习 11

> [!question] 题目
> 在制作受击反馈时，伤害计算、生命值扣除和屏幕震动不应该写在同一个 UI 回调里。

> [!example] 自己的答案
>　被弾フィードバックを作成する時に、ダメージ計算、HPの減る、スクリンシェークを同じUIコールバック内に実装するのではありません。

> [!warning] 指正
> 意思方向正确，但自然度和术语有几处问题。`HPの減る` 是名词化不完整，应为 `HPの減少処理` 或 `HPを減らす処理`。`スクリンシェーク` 应为 `スクリーンシェイク`。`作成する時に` 可改为 `作成するときは`。

> [!tip] 参考回答
> 被弾フィードバックを作成するときは、ダメージ計算、<mark style="background: #FF5582A6;">HPの減少処理</mark>、スクリーンシェイクを同じUIコールバック内に実装するべきではありません。

---

### 练习 12

> [!question] 题目
> 如果读取旧存档后装备属性缺失，请为新增字段准备默认值和兼容转换逻辑。

> [!example] 自己的答案
>　旧セーブデータを読み込んだ後装備の属性が欠落している場合、新しく追加されたフィールドにデフォルト値と互換性移行ロジックを用意してください。

> [!warning] 指正
> 大意正确。`読み込んだ後装備の属性` 中间建议加 `に`。`属性` 可以用，但装备语境里 `装備プロパティ` 或 `装備ステータス` 也自然。`互換性移行ロジック` 稍硬，建议说 `互換用の変換ロジック`。

> [!tip] 参考回答
> 旧セーブデータを読み込んだ後に装備属性が欠落している場合は、新しく追加されたフィールド向けにデフォルト値と互換用の変換ロジックを用意してください。

---

### 练习 13

> [!question] 题目
> 在调试连招取消规则时，请记录当前段数、输入缓存时间和可取消窗口是否开启。

> [!example] 自己的答案
>　コンボキャンセル決まりをデバッグする時に、現在の段落番号、入力バッファタイムスタンプ、キャンセルウィンドを記録してください。

> [!warning] 指正
> 题意能传达，但关键搭配需要修正。`コンボキャンセル決まり` 不自然，建议说 `コンボキャンセルのルール`。`段落番号` 会像文章段落，连招段数更自然是 `現在の段数`。`キャンセルウィンド` 应为 `キャンセルウィンドウ`，并且题目有“是否开启”，需要补出 `開いているかどうか`。

> [!tip] 参考回答
> コンボキャンセルのルールをデバッグするときは、現在の段数、入力バッファの時間、キャンセルウィンドウが開いているかどうかを記録してください。

---

### 练习 14

> [!question] 题目
> 如果今天晚上不下雨，我们吃完饭后去附近的公园走一会儿吧。

> [!example] 自己的答案
>　今晩雨が降らないなら、食事後近くの公園で散歩しましょう

> [!warning] 指正
> 日常意思清楚。`雨が降らないなら` 可以用，但“如果不下雨”更自然是 `雨が降らなければ`。`食事後` 偏书面，日常对话里 `ご飯を食べた後で` 更自然。句末需要句号。

> [!tip] 参考回答
> 今晩雨が降らなければ、ご飯を食べた後で近くの公園を少し散歩しましょう。

---

### 练习 15

> [!question] 题目
> 我先确认这两个问题的负责人，明天上午再把处理顺序发到群里。

> [!example] 自己的答案
>　二つの問題の担当者を確認しておき、明日午前処理順番をチャンネルで共有します。

> [!warning] 指正
> 职场意思基本完整。`確認しておき` 自然，但这里更常说 `確認しておいて`。`明日午前` 应为 `明日の午前中に`。`処理順番` 不自然，建议用 `対応順` 或 `対応する順番`。`チャンネルで共有します` 可以表达“发到群里”。

> [!tip] 参考回答
> 先にこの二つの問題の担当者を確認しておいて、明日の午前中に対応順をチャンネルで共有します。

## 2026-08-11

### 练习 1

> [!question] 题目
> 在 Unreal 中实现锁定目标时，镜头旋转、角色朝向和目标选择规则应该分别维护。

> [!example] 自己的答案
> Unrealでターゲットロックを実装する時に、カメラ回転、キャラクター向き、ターゲット絞り込みルールは分けて保守します。

> [!warning] 指正
> 意思基本清楚。`実装する時に` 建议改为 `実装するときは`。`キャラクター向き` 少了 `の`，应为 `キャラクターの向き`。`ターゲット絞り込みルール` 能表达“筛选规则”，但题目是“目标选择规则”，更自然是 `ターゲット選択ルール`。`保守します` 偏维护代码，规则职责上用 `管理します` 或 `別々に管理します` 更自然。

> [!tip] 参考回答
> Unrealでターゲットロックを実装するときは、カメラの回転、キャラクターの向き、ターゲット選択ルールをそれぞれ別々に管理するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的对象池应该在关卡开始前预热常用特效，避免战斗中频繁实例化。

> [!example] 自己的答案
>　Unityのオブジェクトプールはレベルに入る前によく使用するエフェクトを予熱するべきで、バトル中に頻繁にインスタンス化を防ぎます。

> [!warning] 指正
> 大意正确。`レベルに入る前` 可以理解，但 Unity/关卡语境里 `レベル開始前` 或 `シーン開始前` 更自然。`予熱する` 是直译，技术上常说 `事前にウォームアップする`。后半 `インスタンス化を防ぎます` 前面缺少目的结构，建议改成 `頻繁なインスタンス化を避けます`。

> [!tip] 参考回答
> Unityのオブジェクトプールは、レベル開始前によく使うエフェクトを事前にウォームアップし、戦闘中の頻繁なインスタンス化を避けるべきです。

---

### 练习 3

> [!question] 题目
> 如果技能命中特效的位置偏离目标，请检查插槽名称、坐标空间和特效生成参数。

> [!example] 自己的答案
>　スキルヒットエフェクトの位置がターゲットと外れる場合、スロット名、座標系、エフェクト生成パラメータを確認してください。

> [!warning] 指正
> 题意能传达。`ターゲットと外れる` 不自然，位置偏离目标应说 `ターゲットからずれている`。`スロット名` 可以用，但 Unreal 里也常写 `ソケット名`，要看具体系统。`座標系` 可以表达坐标空间，也可写成 `座標空間` 更贴近题目。

> [!tip] 参考回答
> スキルのヒットエフェクトの位置がターゲットからずれている場合は、ソケット名、座標空間、エフェクト生成パラメータを確認してください。

---

### 练习 4

> [!question] 题目
> 在设计状态机时，待机、移动和攻击状态之间的切换条件要保持简单明确。

> [!example] 自己的答案
>　ステートマシンを設計する時に、待機、移動、攻撃状態の間での切り替え条件を簡潔で明確にするようにします。

> [!warning] 指正
> 表达整体自然。`設計する時に` 建议统一为 `設計するときは`。`状態の間での切り替え条件` 可以理解，但更简洁是 `状態間の遷移条件`。`簡潔で明確にするようにします` 稍绕，建议说 `シンプルで明確にします`。

> [!tip] 参考回答
> ステートマシンを設計するときは、待機、移動、攻撃の状態間の遷移条件をシンプルで明確にする必要があります。

---

### 练习 5

> [!question] 题目
> Unreal 的动画蒙太奇结束后，应该清理临时标签并恢复角色的输入状态。

> [!example] 自己的答案
>　UnrealのMontageが終了した後、一時的なラベルをクリアしてキャラクターの入力状態をリセットするべきです。

> [!warning] 指正
> 主要意思正确。`Montage` 最好写成 `アニメーションモンタージュ`，更完整。`ラベル` 对 Unreal 的 Gameplay Tag 来说不准确，建议用 `タグ`。`リセットする` 可以表达恢复，但输入状态更自然是 `元に戻す` 或 `復元する`。

> [!tip] 参考回答
> Unrealのアニメーションモンタージュが終了した後は、一時的なタグをクリアし、キャラクターの入力状態を元に戻すべきです。

---

### 练习 6

> [!question] 题目
> Unity 的设置界面可以把音量、画质和操作灵敏度保存到同一个配置对象里。

> [!example] 自己的答案
>　Unityの設定画面で音量、画面品質、操作精度を同じ設定オブジェクト内に保存します。

> [!warning] 指正
> 意思大致正确。`画面品質` 会偏“屏幕质量”，画质通常说 `画質`。`操作精度` 是“操作精度”，题目的“操作灵敏度”更自然是 `操作感度`。`設定画面で...保存します` 少了“可以”的语气，可用 `保存できます`。

> [!tip] 参考回答
> Unityの設定画面では、音量、画質、操作感度を同じ設定オブジェクトに保存できます。

---

### 练习 7

> [!question] 题目
> 在优化 UI 动画时，请先确认是否存在过多的布局重算和透明度叠加。

> [!example] 自己的答案
>　UIアニメーションを最適化する時は、まず過剰なレイアウト再計算と透明度重なりを確認してください。

> [!warning] 指正
> 题意清楚。`最適化する時は` 可以改为 `最適化するときは`。`透明度重なり` 不太自然，建议说 `透明度の重なり` 或 `アルファの重なり`。如果强调性能问题，`過剰なレイアウト再計算` 很合适。

> [!tip] 参考回答
> UIアニメーションを最適化するときは、まず過剰なレイアウト再計算や<mark style="background: #FF5582A6;">透明度の重なり</mark>が発生していないか確認してください。

---

### 练习 8

> [!question] 题目
> 如果道具拾取后没有进入背包，请从碰撞检测、实例创建和格子占用三个步骤排查。

> [!example] 自己的答案
>　アイテムを拾得下後もインベントリに入っていない場合は、コリジョン検出、インスタンスの生成、セルをこの3つ要素から確認してください

> [!warning] 指正
> 主要意思能看出，但有几处需要修正。`拾得下後` 是错字，应为 `拾った後` 或 `取得した後`。`入っていない場合は` 可以。`セルをこの3つ要素から` 结构不成立，应说 `セルの占有状態という三つの手順から`。句末需要句号。

> [!tip] 参考回答
> アイテムを拾った後もインベントリに入っていない場合は、コリジョン検出、インスタンス生成、<mark style="background: #FF5582A6;">セルの占有状態</mark>という<mark style="background: #FF5582A6;">三つの手順</mark>から確認してください。

---

### 练习 9

> [!question] 题目
> Unreal 的角色属性变化应该通过事件通知 UI，而不是让 UI 每帧读取属性值。

> [!example] 自己的答案
>　UIはイベント経由でUnrealのキャラクター属性変化を受け取るようにし、毎フレーム属性を読み込むのではありません。

> [!warning] 指正
> 意思基本正确。主语写成 `UIは` 后，`Unrealのキャラクター属性変化` 会稍不自然；题目强调“角色属性变化应该通知 UI”，建议以 `Unrealのキャラクター属性の変化は...` 开头。`毎フレーム属性を読み込むのではありません` 语气偏硬，建议用 `読み取らせるべきではありません`。

> [!tip] 参考回答
> Unrealのキャラクター属性の変化はイベント経由でUIに通知し、UIに毎フレーム属性値を読み取らせるべきではありません。

---

### 练习 10

> [!question] 题目
> 在调试跳跃手感时，请分别调整起跳速度、重力倍率和落地缓冲时间。

> [!example] 自己的答案
>　ジャンプ手応えをデバッグする時に、ジャンプ速度、重力倍率、着地バッファ時間をそれぞれ調整してください。

> [!warning] 指正
> 大意能传达。`ジャンプ手応え` 不自然，“跳跃手感”更自然是 `ジャンプの操作感` 或 `ジャンプの手触り`。`デバッグする時に` 建议改成 `調整するときは` 或 `デバッグするときは`。`ジャンプ速度` 比“起跳速度”稍泛，可用 `ジャンプ開始速度`。

> [!tip] 参考回答
> ジャンプの操作感をデバッグするときは、ジャンプ開始速度、重力倍率、着地バッファ時間をそれぞれ調整してください。

---

### 练习 11

> [!question] 题目
> Unity 的资源加载流程应该区分常驻资源和临时资源，方便统计内存占用。

> [!example] 自己的答案
>　Unityのリソースロードフローは常駐リソースと一時的リソースを切り分けるべきでメモリ使用量を計測しやすくなります。

> [!warning] 指正
> 题意正确。`リソースロードフロー` 可以理解，但更自然是 `リソース読み込みフロー`。`一時的リソース` 应为 `一時リソース` 或 `一時的なリソース`。`切り分けるべきでメモリ...` 中间缺少连接，建议用 `切り分けることで...しやすくなります`。

> [!tip] 参考回答
> Unityのリソース読み込みフローでは、常駐リソースと一時リソースを切り分けることで、メモリ使用量を集計しやすくなります。

---

### 练习 12

> [!question] 题目
> 如果战斗日志缺少关键字段，请在伤害结算前记录技能编号、攻击者和目标。

> [!example] 自己的答案
>　バトルログには肝心なフィールドが足りない場合、スキル番号、攻撃者、ターゲットをダメージ計算する前に記録してください。

> [!warning] 指正
> 意思基本完整。`バトルログには` 用 `に` 更自然：`バトルログに重要なフィールドが足りない場合`。`肝心なフィールド` 可以懂，但技术文档中 `重要なフィールド` 或 `必要なフィールド` 更自然。`ダメージ計算` 与题目的“结算”相比略窄，可用 `ダメージ処理` 或 `ダメージ確定処理`。

> [!tip] 参考回答
> バトルログに重要なフィールドが足りない場合は、<mark style="background: #FF5582A6;">ダメージ確定処理</mark>の前に、スキル番号、攻撃者、ターゲットを記録してください。

---

### 练习 13

> [!question] 题目
> 在制作教程关卡时，引导提示、交互限制和完成条件需要由任务流程统一管理。

> [!example] 自己的答案
>　チュートリアルレベルを作成するときは、導き提示、インタラクション制限、完成条件はクエストフローで一元的に管理します。

> [!warning] 指正
> 大意正确。`導き提示` 不自然，教程引导提示建议说 `ガイド表示` 或 `案内表示`。`完成条件` 可以理解，但任务/教程语境里 `完了条件` 更自然。`クエストフローで一元的に管理します` 很好。

> [!tip] 参考回答
> チュートリアルレベルを作成するときは、<mark style="background: #FF5582A6;">ガイド表示</mark>、インタラクション制限、完了条件をクエストフローで一元的に管理する必要があります。

---

### 练习 14

> [!question] 题目
> 周末如果你有空，我们一起去看那家新开的展览吧。

> [!example] 自己的答案
>　週末に時間があれば、一緒に新しくできた展覧会を見に行きましょう。

> [!warning] 指正
> 表达自然，意思完整。`新しくできた展覧会` 可以表达“新开的展览”，如果想更口语一些，也可以说 `新しく始まった展示`。`見に行きましょう` 很自然。

> [!tip] 参考回答
> 週末に時間があれば、一緒に新しく始まった展覧会を見に行きましょう。

---

### 练习 15

> [!question] 题目
> 我把今天的修改点整理好了，会议前会发给大家确认。

> [!example] 自己的答案
>　本日に改修点をまとめました。ミーティング前に皆さんに送って確認していただきます。

> [!warning] 指正
> 职场意思基本清楚。`本日に` 不自然，应为 `今日の`。`改修点` 偏程序修改也可以，但题目的“修改点”更通用可说 `修正点`。`皆さんに送って確認していただきます` 表达可以，但更自然是 `皆さんに送って確認してもらいます` 或礼貌一点 `確認していただきます`。

> [!tip] 参考回答
> 今日の修正点を整理しました。会議前に皆さんに送って確認していただきます。

## 2026-08-12

### 练习 1

> [!question] 题目
> 在 Unreal 中处理受击硬直时，角色移动、动画播放和输入恢复应该按状态变化统一控制。

> [!example] 自己的答案
> Unrealで被弾硬直を処理する時に、キャラクターの移動、アニメーション再生、入力復元は状態変化による一元的に制御します。

> [!warning] 指正
> 意思基本清楚。`処理する時に` 建议改为 `処理するときは`。`入力復元` 可以理解，但输入状态恢复更自然是 `入力の復元` 或 `入力状態の復元`。`状態変化による一元的に制御します` 助词不自然，应为 `状態変化に基づいて一元的に制御します`。

> [!tip] 参考回答
> Unrealで被弾硬直を処理するときは、キャラクターの移動、アニメーション再生、入力状態の復元を状態変化に基づいて一元的に制御するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的伤害数字显示只负责表现，不应该直接修改敌人的生命值数据。

> [!example] 自己的答案
>　Unityのダメージ数字の表示は表現だけを担当し、敵のHPを直接変更するのではありません。

> [!warning] 指正
> 大意正确。`ダメージ数字` 能懂，但 UI 表现里更自然是 `ダメージ数値` 或 `ダメージ表示`。`表現だけを担当し` 很好。后半 `変更するのではありません` 稍硬，建议用 `変更するべきではありません`，更符合技术规范的语气。

> [!tip] 参考回答
> Unityのダメージ数値表示は表現だけを担当し、敵のHPデータを直接変更するべきではありません。

---

### 练习 3

> [!question] 题目
> 如果技能范围预览和实际判定不一致，请比较碰撞形状、缩放比例和角色朝向。

> [!example] 自己的答案
>　スキル範囲のプレビューと実際の判定が一致していない場合、コリジョン形、スケール率、キャラクターの向きを比較してください。

> [!warning] 指正
> 题意能传达。`一致していない場合` 可以，技术排查里更常用 `一致しない場合は`。`コリジョン形` 不自然，建议说 `コリジョン形状`。`スケール率` 可理解，但 `スケール値` 或 `拡大縮小率` 更自然。

> [!tip] 参考回答
> スキル範囲のプレビューと実際の判定が一致しない場合は、コリジョン形状、スケール値、キャラクターの向きを比較してください。

---

### 练习 4

> [!question] 题目
> 在设计连击系统时，下一段攻击能否开始应该根据当前动作帧和输入缓存决定。

> [!example] 自己的答案
> コンボシステムを設計する時に、次の段階に入るかどうかは現在アクションフレームと入力バッファによって決定します。

> [!warning] 指正
> 主要意思正确。`設計する時に` 建议写成 `設計するときは`。`次の段階に入るかどうか` 能表达，但连击语境里更自然是 `次の段の攻撃を開始できるかどうか`。`現在アクションフレーム` 少了 `の`，应为 `現在のアクションフレーム`。

> [!tip] 参考回答
> コンボシステムを設計するときは、次の段の攻撃を開始できるかどうかを、現在のアクションフレームと入力バッファに基づいて決定します。

---

### 练习 5

> [!question] 题目
> Unreal 的技能消耗应该先验证资源是否足够，再扣除数值并播放失败或成功表现。

> [!example] 自己的答案
>　Unrealのスキルコストではまず消耗を検証し、数値を減らしてから成功・失敗表現を再生します。

> [!warning] 指正
> 题意有一部分缺失。`消耗を検証し` 没有明确“资源是否足够”，建议说 `リソースが足りているかを検証し`。另外题目顺序是先验证、再扣除、并播放失败或成功表现；失败时通常不会扣数值，所以参考回答里会把成功/失败分支说清楚。

> [!tip] 参考回答
> Unrealのスキルコスト処理では、まずリソースが足りているかを検証し、成功した場合は数値を消費して成功演出を再生し、足りない場合は失敗演出を再生します。

---

### 练习 6

> [!question] 题目
> Unity 的小地图图标可以根据实体状态变化刷新颜色、透明度和显示优先级。

> [!example] 自己的答案
>　Unityのミニマップはインスタンスの状態に応じて色の更新、透明度、表示優先度を変更します。

> [!warning] 指正
> 大意基本正确。题目是“小地图图标”，所以 `ミニマップ` 后最好补出 `アイコン`。`インスタンスの状態` 可以理解，但这里更贴近题意的是 `エンティティの状態`。`色の更新、透明度、表示優先度を変更します` 并列稍不齐，建议说 `色、透明度、表示優先度を更新します`。

> [!tip] 参考回答
> Unityのミニマップアイコンは、エンティティの状態変化に応じて、色、透明度、表示優先度を更新できます。

---

### 练习 7

> [!question] 题目
> 在优化角色材质时，请分别记录贴图尺寸、材质实例数量和动态参数更新频率。

> [!example] 自己的答案
>　キャラクターのマテリアルを最適化する時に、テクスチャのサイズ、マテリアルインスタンスの数、動的パラメータの更新頻度をそれぞれ記録してください。

> [!warning] 指正
> 表达整体自然。`最適化する時に` 建议统一为 `最適化するときは`。`マテリアルインスタンスの数` 可以，性能记录里也可更紧凑地说 `マテリアルインスタンス数`。其他术语都比较准确。

> [!tip] 参考回答
> キャラクターのマテリアルを最適化するときは、テクスチャサイズ、マテリアルインスタンス数、動的パラメータの更新頻度をそれぞれ記録してください。

---

### 练习 8

> [!question] 题目
> 如果敌人的巡逻行为突然停止，请检查当前路点、等待计时器和行为树条件。

> [!example] 自己的答案
>　敵のパトロールが急に止まった場合、現在の経路ポイント、待ちタイマー、ビヘイビアツリーを確認してください。

> [!warning] 指正
> 意思基本清楚。`現在の経路ポイント` 可以表达路点，但巡逻语境里更自然是 `現在のウェイポイント`。`待ちタイマー` 可以。题目还有“行为树条件”，你的 `ビヘイビアツリー` 少了“条件”，建议写成 `ビヘイビアツリーの条件`。

> [!tip] 参考回答
> 敵のパトロール行動が急に止まった場合は、現在の<mark style="background: #FF5582A6;">ウェイポイント</mark>、待機タイマー、ビヘイビアツリーの条件を確認してください。

---

### 练习 9

> [!question] 题目
> Unreal 的属性面板刷新应该由属性变化事件触发，而不是在 Tick 中反复重建控件。

> [!example] 自己的答案
>　Unrealの属性画面の更新は属性変化イベントによって発火するべきで、Tick内でウィジェットを繰り返して生成するのではありません。

> [!warning] 指正
> 主要意思正确。`属性画面` 能懂，但题目是“属性面板”，更自然是 `属性パネル`。`イベントによって発火する` 通常说事件“触发处理”，这里建议用 `イベントをきっかけに更新される`。`繰り返して生成する` 应为 `繰り返し生成する`。

> [!tip] 参考回答
> Unrealの属性パネルの更新は属性変化イベントをきっかけに行い、Tick内でウィジェットを繰り返し再生成するべきではありません。

---

### 练习 10

> [!question] 题目
> 在保存玩家设置时，请把画质、语言和按键配置写入带版本号的数据结构。

> [!example] 自己的答案
>　プレイヤーの設定を保存する時は、画質、言語、マッピングをバージョン付きデータ構造に書き込んでください。

> [!warning] 指正
> 大意正确。`保存する時は` 建议写成 `保存するときは`。`マッピング` 可以指按键映射，但题目是“按键配置”，更清楚的是 `キー設定` 或 `キーコンフィグ`。`バージョン付きデータ構造` 很自然。

> [!tip] 参考回答
> プレイヤー設定を保存するときは、画質、言語、キー設定をバージョン付きのデータ構造に書き込んでください。

---

### 练习 11

> [!question] 题目
> Unity 的关卡加载界面应该显示进度和提示文本，但不要承担资源依赖检查的职责。

> [!example] 自己的答案
>　Unityのレベルロード画面は進捗と提示テキストを表示するべきで、リソースロードと依存チェックを担当するのではありません。

> [!warning] 指正
> 题意基本正确。`提示テキスト` 在中文影响下稍不自然，加载界面的“提示文本”更常说 `ヒントテキスト`。后半多写了 `リソースロード`，题目强调的是不要承担“资源依赖检查”的职责，建议直接说 `リソース依存関係のチェックを担当するべきではありません`。

> [!tip] 参考回答
> Unityのレベルロード画面は進捗とヒントテキストを表示するべきですが、リソース依存関係のチェックを担当するべきではありません。

---

### 练习 12

> [!question] 题目
> 如果投射物没有命中目标，请记录生成位置、初速度、碰撞通道和生命周期。

> [!example] 自己的答案
>　飛び道具が外れた場合、生成位置、初速度、コリジョンチャンネルとライフサイクルを記録してください。

> [!warning] 指正
> 意思大体清楚。`飛び道具` 可以用于投射物，不过技术语境里 `投射物` 或 `プロジェクタイル` 更直接。`外れた場合` 是“打偏了”，题目是“没有命中目标”，可说 `ターゲットに命中しない場合`。`コリジョンチャンネルとライフサイクル` 并列时用顿号更自然。

> [!tip] 参考回答
> 投射物がターゲットに命中しない場合は、生成位置、初速度、コリジョンチャンネル、ライフサイクルを記録してください。

---

### 练习 13

> [!question] 题目
> 在制作任务奖励界面时，奖励计算结果和领取按钮状态应该来自同一份任务数据。

> [!example] 自己的答案
>　クエスト報酬画面を生成する時に、報酬決定の受け取るボタン状態は同じクエストを参照するべきです。

> [!warning] 指正
> 这题的核心意思表达不完整。`報酬決定の受け取るボタン状態` 结构不成立，也遗漏了“奖励计算结果”。建议拆成 `報酬の計算結果` 和 `受け取りボタンの状態` 两个并列项。`同じクエストを参照する` 少了“数据”，应为 `同じクエストデータを参照する`。

> [!tip] 参考回答
> クエスト報酬画面を作成するときは、報酬の計算結果と受け取りボタンの状態を、同じクエストデータから取得するべきです。

---

### 练习 14

> [!question] 题目
> 明天早上如果天气不错，我们早点出门，顺便买杯咖啡吧。

> [!example] 自己的答案
>　明日朝、天気が良ければ、早めに出かけてついでにコーヒーを買いましょう。

> [!warning] 指正
> 日常表达自然，意思完整。`明日朝` 可以，但口语里 `明日の朝` 更自然。`ついでにコーヒーを買いましょう` 很好。整体只需要小幅调整语感。

> [!tip] 参考回答
> 明日の朝、天気が良ければ、早めに出かけて、ついでにコーヒーを買いましょう。

---

### 练习 15

> [!question] 题目
> 我会先整理今天讨论过的事项，下午发给大家确认有没有遗漏。

> [!example] 自己的答案
>　本日まず相談した事項をまとめ、午後に皆さんに送って抜け漏れがないかを確認していただきます。

> [!warning] 指正
> 职场语境大体自然。`本日まず` 语序稍硬，建议说 `まず本日話し合った内容を整理し`。`相談した事項` 更像“咨询过的事项”，会议讨论内容用 `話し合った内容` 或 `議論した事項` 更自然。`抜け漏れがないか` 很好，适合职场确认。

> [!tip] 参考回答
> まず本日話し合った内容を整理し、午後に皆さんへ送って、抜け漏れがないか確認していただきます。

## 2026-08-13

### 练习 1

> [!question] 题目
> 在 Unreal 的角色状态机里，受击硬直结束后，是否能继续连击应该由 gameplay 状态统一判断。

> [!example] 自己的答案
> Unrealのステートマシンでは被弾硬直が終了した後、コンボが続けるかどかはgameplay状態で一元的に判断するべきです。

> [!warning] 指正
> 意思基本能传达。`続けるかどか` 应为 `続けられるかどうか`，这里要表达“能不能继续”，需要可能形。`gameplay状態` 建议写成 `ゲームプレイ状態` 或 `Gameplay状態`，更符合日语技术文档习惯。整句可以用 `〜が一元的に判断するべきです`。

> [!tip] 参考回答
> Unrealのキャラクターステートマシンでは、被弾硬直が終了した後にコンボを<mark style="background: #FF5582A6;">継続できる</mark>かどうかを、Gameplay状態で一元的に判断するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的技能冷却 UI 应该根据技能数据的变化更新，而不是每一帧重新计算所有按钮。

> [!example] 自己的答案
>　UnityのスキルクールダウンUIは　スキルデータの変化に応じて更新します。マイフレーム全てのボタンを再計算するのではありません。

> [!warning] 指正
> 题意正确，但有几个自然度和拼写问题。`マイフレーム` 应为 `毎フレーム`。`全てのボタン` 前面最好加 `すべての` 或 `全ボタン`。前后两句可以用 `〜べきで、〜するべきではありません` 连接，表达“应该怎样，而不是怎样”更自然。

> [!tip] 参考回答
> UnityのスキルクールダウンUIは、スキルデータの変化に応じて更新されるべきで、毎フレームすべてのボタンを再計算するべきではありません。

---

### 练习 3

> [!question] 题目
> 如果敌人的警戒状态切换得太频繁，请先记录感知范围、视野角和当前目标。

> [!example] 自己的答案
>　敵の警戒状態が頻繁に切り替わる場合、検知範囲、視野角度、現在のターゲットを先に記録してください。

> [!warning] 指正
> 表达清楚。`視野角度` 可以理解，但技术表达里更常说 `視野角`。`先に記録してください` 自然；如果强调“先做记录再调查”，可以说 `まず〜を記録してください`。整体只需小幅调整术语。

> [!tip] 参考回答
> 敵の警戒状態が頻繁に切り替わる場合は、まず<mark style="background: #FF5582A6;">感知範囲</mark>、視野角、現在のターゲットを記録してください。

---

### 练习 4

> [!question] 题目
> 背包格子的占用信息应该先在数据层更新，然后再通知 UI 刷新显示。

> [!example] 自己的答案
>　インベントリの占有状態はまずデータ層で更新してからUIに通知して表示を更新します。

> [!warning] 指正
> 很自然，意思完整。`占有状態` 能表达“占用状态”，但背包格子语境里 `占有情報` 或 `占有状態の情報` 更贴近“占用信息”。`UIに通知して表示を更新します` 结构很好。

> [!tip] 参考回答
> インベントリスロットの占有情報は、まずデータ層で更新してから、UIに通知して表示を更新します。

---

### 练习 5

> [!question] 题目
> 制作武器换弹逻辑时，请把弹药数量的计算和动画表现分开处理。

> [!example] 自己的答案
>　武器リロードロジックを作成する時に、残弾数の計算とアニメーション再生を切り分けて処理してください。

> [!warning] 指正
> 表达基本正确。`作成する時に` 可以换成更自然的 `実装するときは`。`残弾数` 是“剩余弹数”，题目是“弹药数量”，这里可用 `弾薬数`，也可以根据语境继续用 `残弾数`。`切り分けて処理してください` 很好。

> [!tip] 参考回答
> 武器のリロードロジックを実装するときは、弾薬数の計算とアニメーション表現を切り分けて処理してください。

---

### 练习 6

> [!question] 题目
> 为了降低粒子效果的开销，可以限制发射数量，并根据目标平台调整特效质量。

> [!example] 自己的答案
>　パーティクル負荷を軽減するため、エミッタ数を制限できる、対象プラとフォームに応じてエフェクト品質を調整します。

> [!warning] 指正
> 主要意思能看懂，但需要修正搭配。`エミッタ数` 是发射器数量，题目更像“发射数量”，可说 `エミッション数`。`制限できる` 后面不能直接接下一句，应用 `制限し`。`対象プラとフォーム` 是拼写错误，应为 `対象プラットフォーム`。

> [!tip] 参考回答
> パーティクル効果の負荷を軽減するために、<mark style="background: #FF5582A6;">エミッション数</mark>を制限し、対象プラットフォームに応じてエフェクト品質を調整できます。

---

### 练习 7

> [!question] 题目
> 任务追踪界面只需要显示当前目标，不应该直接决定任务是否完成。

> [!example] 自己的答案
> クエスト追跡画面では現在の目標を表示するだけで、クエストが完成するかを判断するべきではありません。

> [!warning] 指正
> 句子方向正确。`完成する` 在任务语境里可以理解，但 `完了する` 更自然。`判断するべきではありません` 可以表达“不应该决定”，不过这里用 `直接決定するべきではありません` 更贴近题目里的“直接决定”。

> [!tip] 参考回答
> クエスト追跡画面は現在の目標を表示するだけで、クエストが完了したかどうかを直接決定するべきではありません。

---

### 练习 8

> [!question] 题目
> 当存档字段发生变化时，请为旧数据准备默认值和转换逻辑。

> [!example] 自己的答案
> セーブデータのフィールドが変化した場合、旧データに対するデーフォルト値と変換ロジックを用意してください。

> [!warning] 指正
> 意思完整。`デーフォルト` 拼写应为 `デフォルト`。`フィールドが変化した場合` 可以，但存档字段“发生变化”在版本兼容语境里说 `変更された場合` 更自然。`旧データに対する` 用得可以，也可说 `古いデータ向けに`。

> [!tip] 参考回答
> セーブデータのフィールドが変更された場合は、古いデータ向けにデフォルト値と変換ロジックを用意してください。

---

### 练习 9

> [!question] 题目
> 如果角色在导航区域边缘卡住，请检查胶囊体大小和导航网格是否匹配。

> [!example] 自己的答案
>　キャラクターがナビーエリアの境界で引っかかった場合、カプセルのサイズとナビーメッシュが一致しているかを確認してください。

> [!warning] 指正
> 题意清楚，但术语拼写需要修正。`ナビーエリア` 应为 `ナビエリア`，`ナビーメッシュ` 应为 `ナビメッシュ`。`境界` 可用，不过游戏开发语境里“边缘”常说 `端`。`一致しているか` 自然。

> [!tip] 参考回答
> キャラクターがナビエリア<mark style="background: #FF5582A6;">の端</mark>で引っかかる場合は、カプセルのサイズとナビメッシュが一致しているか確認してください。

---

### 练习 10

> [!question] 题目
> 在实现教程弹窗时，显示文本可以由配置数据提供，按钮行为则交给控制逻辑处理。

> [!example] 自己的答案
>　チュートリアルのポップアップを実装する時に、表示テキストは設定データを参照し、ボタン動作をロジック制御で処理します。

> [!warning] 指正
> 意思基本正确。`設定データを参照し` 是“参考配置数据”，但题目是“由配置数据提供”，可以说 `設定データから取得し`。`ボタン動作をロジック制御で処理します` 稍硬，建议用 `ボタンの挙動は制御ロジック側で処理します`。

> [!tip] 参考回答
> チュートリアルのポップアップを実装するときは、表示テキストを設定データから取得し、ボタン<mark style="background: #FF5582A6;">の挙動</mark>は制御ロジック側で処理します。

---

### 练习 11

> [!question] 题目
> 渲染调试时，请分别记录平均帧时间、峰值帧时间和主要开销来源。

> [!example] 自己的答案
>　描画をデバッグする時に,平均フレーム時間、ピーク時間、メインコスト元をそれぞれ記録してください。

> [!warning] 指正
> 主要意思能传达。日语逗号应使用 `、`。`ピーク時間` 最好明确为 `ピークフレーム時間`。`メインコスト元` 不自然，建议说 `主な負荷要因` 或 `主なコスト要因`。`描画をデバッグする時に` 可以换成 `レンダリングをデバッグするときは`。

> [!tip] 参考回答
> レンダリングをデバッグするときは、平均フレーム時間、ピークフレーム時間、主な<mark style="background: #FF5582A6;">負荷要因</mark>をそれぞれ記録してください。


---

### 练习 12

> [!question] 题目
> 对象池中的投射物重新使用前，必须重置位置、速度、碰撞状态和生命周期。

> [!example] 自己的答案
>　オプジェクトプールの投射物を再利用する前に、ポジション、速度、コリジョン状態、ライフサイクルをリセットする必要があります。

> [!warning] 指正
> 意思正确。`オプジェクト` 拼写应为 `オブジェクト`。`ポジション` 能懂，但技术文档中 `位置` 更自然。`投射物を再利用する前に` 表达很好，后面的并列也清楚。

> [!tip] 参考回答
> オブジェクトプール内の投射物を再利用する前に、位置、速度、コリジョン状態、ライフサイクルをリセットする必要があります。

---

### 练习 13

> [!question] 题目
> 设计 Boss 阶段切换时，请让阶段数据决定技能列表、移动速度和演出开始时机。

> [!example] 自己的答案
>　ボスのフェーズの切り替えを設計する時に、スキルリスト、移動速度、演出開始時はフェーズデータで決まってください。

> [!warning] 指正
> 题意大致明白，但谓语不自然。`演出開始時` 应改为 `演出の開始タイミング`。`フェーズデータで決まってください` 不成立，应该说 `フェーズデータで決めるようにしてください` 或 `フェーズデータに基づいて決めてください`。

> [!tip] 参考回答
> ボスのフェーズ切り替えを設計するときは、スキルリスト、移動速度、演出の開始タイミングを<mark style="background: #FF5582A6;">フェーズデータに基づいて決めて</mark>ください。

---

### 练习 14

> [!question] 题目
> 今天下班后如果还有精神，我们去车站附近吃点热的东西吧。

> [!example] 自己的答案
>　仕事が終わった後まだ元気だったら、駅の近くで温かいものを食べに行きましょう。

> [!warning] 指正
> 日常表达自然，意思完整。`仕事が終わった後` 没问题，但“下班后”口语里也常说 `仕事のあと`。`まだ元気だったら` 自然，`温かいものを食べに行きましょう` 也很顺。

> [!tip] 参考回答
> 今日、仕事のあとまだ元気だったら、駅の近くで温かいものを食べに行きましょう。

---

### 练习 15

> [!question] 题目
> 我会在会议前把确认事项整理成列表，方便大家快速看完。

> [!example] 自己的答案
>　ミーティング前に確認事項をリストに整理し、皆さんが早く確認するようにします。

> [!warning] 指正
> 意思基本正确。`リストに整理し` 可以理解，但更自然是 `リストにまとめて`。`早く確認するようにします` 有“让大家快速确认”的强制感，题目是“方便大家快速看完”，建议说 `皆さんがすぐ確認できるようにします`。

> [!tip] 参考回答
> 会議の前に確認事項をリストにまとめて、皆さんがすぐ確認できるようにします。

## 2026-08-14

### 练习 1

> [!question] 题目
> 在 Unreal 的 Ability 设计里，技能是否可以释放应该根据标签、冷却和资源消耗一起判断。

> [!example] 自己的答案
> UnrealのAbilityの設計では、スキル発動判定がタグ、クールダウン、コストを一元的に判断します。

> [!warning] 指正
> 意思大致正确，但主语关系需要调整。`スキル発動判定が〜判断します` 不自然，应该说“系统/処理が判断する”或“発動可否を判断する”。题目里的“是否可以释放”建议用 `発動できるかどうか`。`コスト` 可以表达资源消耗，但更明确可写 `リソース消費`。

> [!tip] 参考回答
> UnrealのAbility設計では、スキルを発動できるかどうかを、タグ、クールダウン、リソース消費に基づいてまとめて判断するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的角色控制代码不要直接修改 UI 文本，而应该发送状态变化事件。

> [!example] 自己的答案
>　UnityのキャラクターコントーロラではUIテキストを直接変更しない、状態変化イベントを送信するべきです。

> [!warning] 指正
> 题意能传达，但 `コントーロラ` 应为 `コントローラー`。`直接変更しない、状態変化イベントを送信するべきです` 中间连接不自然，建议用 `〜せず、〜するべきです`。`UIテキスト` 前加助词 `を` 后整体更清楚。

> [!tip] 参考回答
> Unityのキャラクターコントローラーでは、UIテキストを直接変更せず、状態変化イベントを送信するべきです。

---

### 练习 3

> [!question] 题目
> 调整透明材质的渲染顺序时，请同时确认排序规则和实际的绘制结果。

> [!example] 自己的答案
>　透明マテリアルを調整する時には、ソートルールと実際の描画結果を同時に確認してください。

> [!warning] 指正
> 表达基本自然。题目是“调整渲染顺序”，你的句子少了 `レンダリング順序`，只说了“调整透明材质”。`同時に確認してください` 没问题，但这里 `あわせて確認してください` 更像技术说明里的“同时确认”。

> [!tip] 参考回答
> 透明マテリアルのレンダリング順序を調整するときは、ソートルールと実際の描画結果をあわせて確認してください。

---

### 练习 4

> [!question] 题目
> 如果命中判定偶尔失败，请先记录攻击范围、碰撞通道和触发时机。

> [!example] 自己的答案
>　命中判定がたまに外れる場合、攻撃範囲、コリジョンチャンネル、発火タイミングを先にきろくしてください。

> [!warning] 指正
> 意思清楚。`発火タイミング` 用在事件触发可以，但命中判定语境里 `発生タイミング` 或 `判定が出るタイミング` 更自然。`きろく` 应写成汉字 `記録`。`たまに外れる` 可以理解，但“偶尔失败”更常说 `たまに失敗する`。

> [!tip] 参考回答
> 命中判定がたまに失敗する場合は、まず攻撃範囲、コリジョンチャンネル、<mark style="background: #FF5582A6;">判定が発生するタイミング</mark>を記録してください。

---

### 练习 5

> [!question] 题目
> UMG 小部件创建后，初始化数据和事件绑定最好放在明确的生命周期函数里处理。

> [!example] 自己的答案
>　UMGウィジェットが生成された後、データ初期化とイベントバインディングを明確なライフサイクルメソッド内に実装したほうがいいです。

> [!warning] 指正
> 技术意思完整。`ライフサイクルメソッド` 可以用，不过 Unreal/UMG 语境里也常说 `ライフサイクル関数`。`実装したほうがいいです` 稍口语，技术建议里 `処理するのが望ましいです` 或 `処理するべきです` 更自然。

> [!tip] 参考回答
> UMGウィジェットを生成した後は、データの初期化とイベントバインドを明確なライフサイクル関数内で処理するのが望ましいです。

---

### 练习 6

> [!question] 题目
> 制作装备强化系统时，强化结果应该先写入数据，再播放成功或失败的演出。

> [!example] 自己的答案
>　装備強化システムを実装する時に、まず強化結果をデータに書き込んで成功や失敗演出を再生するべきです。

> [!warning] 指正
> 表达基本正确。`実装する時に` 可换成更自然的 `実装するときは`。`書き込んで成功や失敗演出を再生する` 少了“再/然后”的层次，建议用 `書き込んでから、成功または失敗の演出を再生する`。

> [!tip] 参考回答
> 装備強化システムを実装するときは、まず強化結果をデータに書き込んでから、<mark style="background: #FF5582A6;">成功または失敗</mark>の演出を再生するべきです。

---

### 练习 7

> [!question] 题目
> 为了减少运行时加载压力，可以把常用特效提前放入资源缓存。

> [!example] 自己的答案
>　ランタイムの負荷を軽減するために、よく使われるエフェクトを事前にキャッシュします。

> [!warning] 指正
> 句子自然，意思也完整。`ランタイムの負荷` 可以，但题目强调“加载压力”，更准确是 `ランタイムのロード負荷`。`キャッシュします` 没问题；如果强调“可以”，可用 `キャッシュしておくことができます`。

> [!tip] 参考回答
> ランタイムのロード負荷を減らすために、よく使うエフェクトを事前にリソースキャッシュへ入れておくことができます。

---

### 练习 8

> [!question] 题目
> 当敌人的行为树没有进入预期分支时，请检查黑板变量和条件装饰器。

> [!example] 自己的答案
>　敵のビヘイビアツリーが想定した分岐に入らない場合、ブラックボード変数と条件装飾機を確認してください。

> [!warning] 指正
> 主要意思正确。`条件装飾機` 不自然，应为 `条件デコレーター` 或 `条件付きデコレーター`。`想定した分岐` 可以理解，但 `想定どおりの分岐` 更自然。`確認してください` 用得很好。

> [!tip] 参考回答
> 敵のビヘイビアツリーが想定どおりの分岐に入らない場合は、ブラックボード変数と<mark style="background: #FF5582A6;">条件デコレーター</mark>を確認してください。

---

### 练习 9

> [!question] 题目
> 存档版本升级时，需要保留旧字段的读取逻辑，直到旧数据全部迁移完成。

> [!example] 自己的答案
>　セーブデータがアップグレードする時に、旧データの移行が完了するまで旧フィールドを読み込むロジックを残す必要があります。

> [!warning] 指正
> 意思清楚。`セーブデータがアップグレードする時に` 听起来像存档自己升级，建议说 `セーブデータのバージョンを上げるとき` 或 `バージョンアップ時`。`旧フィールドを読み込むロジック` 很好，和题目对应。

> [!tip] 参考回答
> セーブデータのバージョンアップ時には、古いデータの移行がすべて完了するまで、旧フィールドを読み込むロジックを残しておく必要があります。

---

### 练习 10

> [!question] 题目
> 在分析移动端性能时，请把 CPU 开销、GPU 开销和内存峰值分开记录。

> [!example] 自己的答案
>　モバイル端末のパフォーマンスを分析する時に、CPU負荷、GPU負荷、メモリピーク使用量を分けて記録してください。

> [!warning] 指正
> 表达自然，技术意思完整。`モバイル端末` 很好。`メモリピーク使用量` 可以理解，但更常见说法是 `メモリ使用量のピーク` 或 `ピークメモリ使用量`。`分けて記録してください` 很准确。

> [!tip] 参考回答
> モバイル端末のパフォーマンスを分析するときは、CPU負荷、GPU負荷、メモリ使用量のピークを分けて記録してください。

---

### 练习 11

> [!question] 题目
> 任务奖励的计算逻辑应该可以单独测试，不要依赖奖励弹窗是否显示。

> [!example] 自己的答案
>　クエスト報酬計算ロジックを独立してテストできるように、報酬ポップアップの表示に依存しないでください。

> [!warning] 指正
> 意思基本准确。题目主语是“计算逻辑应该可以单独测试”，你的句子用 `〜できるように、〜依存しないでください` 能表达，但更贴近原句的是 `単体でテストできるべきです`。`報酬ポップアップの表示` 很自然。

> [!tip] 参考回答
> クエスト報酬の計算ロジックは<mark style="background: #FF5582A6;">単体で</mark>テストできるべきで、報酬ポップアップが表示されるかどうかに依存してはいけません。

---

### 练习 12

> [!question] 题目
> 如果动画通知没有触发，请确认动画片段、通知名称和状态切换条件。

> [!example] 自己的答案
>　AnimNotifyが発火しない場合、アニメーションセクション、イベント名、状態切替条件をかくにんしてください。

> [!warning] 指正
> 题意能传达，但术语需要调整。`アニメーションセクション` 更像动画中的 section，不是“动画片段”，这里建议用 `アニメーションクリップ` 或 Unreal 语境的 `アニメーションシーケンス`。`イベント名` 对应“通知名称”不够准确，建议说 `通知名`。`かくにん` 应写成 `確認`。

> [!tip] 参考回答
> AnimNotifyが発火しない場合は、アニメーションクリップ、通知名、状態切り替え条件を確認してください。

---

### 练习 13

> [!question] 题目
> 设计道具使用流程时，请把数量扣除、效果应用和 UI 刷新按顺序处理。

> [!example] 自己的答案
>　アイテムの使用フローを設計する時に、アイテム数の減少、効果の適用、UI更新を順番で処理してください。

> [!warning] 指正
> 意思完整。`アイテム数の減少` 能懂，但“数量扣除”在处理流程里更自然是 `数量の減算` 或 `数量を減らす処理`。`順番で処理` 稍不自然，建议用 `順番に処理`。`UI更新` 前后最好统一为名词并列。

> [!tip] 参考回答
> アイテムの使用フローを設計するときは、<mark style="background: #FF5582A6;">数量の減算</mark>、効果の適用、UIの更新を順番に処理してください。

---

### 练习 14

> [!question] 题目
> 周末如果时间合适，我想去附近的书店看看新出的漫画。

> [!example] 自己的答案
>　週末に都合が良ければ、近くの本屋で新しくできた漫画を見に行きたいです。

> [!warning] 指正
> 大体能表达意思。`都合が良ければ` 很自然。`本屋` 口语可以，但标准表达可用 `書店`。`新しくできた漫画` 表示“新开/新做出来的漫画”，题目是“新出的漫画”，应说 `新しく出た漫画` 或 `新刊の漫画`。

> [!tip] 参考回答
> 週末、都合がよければ、近くの書店へ新しく出た漫画を見に行きたいです。

---

### 练习 15

> [!question] 题目
> 如果今天的确认还没结束，我会明天上午继续整理并发给你。

> [!example] 自己的答案
>　本日確認が終わっていなければ、明日午前に引き続き整理して送ります。

> [!warning] 指正
> 职场表达整体自然。`本日確認` 少了助词，建议说 `本日の確認` 或 `今日の確認作業`。`明日午前` 可以，但更自然是 `明日の午前中`。`送ります` 如果对象明确可用，职场里也可以说 `お送りします` 更礼貌。

> [!tip] 参考回答
> 本日の確認がまだ終わっていなければ、明日の午前中に引き続き整理してお送りします。

## 2026-08-15

### 练习 1

> [!question] 题目
> 在 Unreal 中处理持续型 Gameplay Cue 时，效果结束后必须清除对应的视觉表现，避免角色身上残留特效。

> [!example] 自己的答案
>　Unrealで持続的なGameplay Cueを処理する時に、効果が終了後に対するビジュアル表現を必ずクリアします。そうすることで、キャラクターにエフェクトが残ることを避けられます。

> [!warning] 指正
> 技术意思基本成立，但 `効果が終了後に対する` 不自然。应改为 `効果が終了した後は`，而“对应的视觉表现”应说 `対応するビジュアル表現`。`処理する時に` 在说明规则时用 `処理するときは` 更自然。

> [!tip] 参考回答
> Unrealで持続型のGameplay Cueを処理するときは、効果の終了後に対応するビジュアル表現を必ず削除し、キャラクターにエフェクトが残らないようにします。

---

### 练习 2

> [!question] 题目
> Unity 场景卸载后，应释放只在该场景使用的 Addressables 句柄，避免资源长期占用内存。

> [!example] 自己的答案
>　Unityシーンをアンロードした後に、対象シーンに使われていたAddressablesハンドルだけを解放するべきで、リソースがメモリ領域に長時間残るのを避けます。

> [!warning] 指正
> 意思清楚。`対象シーンに使われていた` 建议改为 `そのシーンでのみ使用していた`，才能准确表达“只在该场景使用”。`解放するべきで、〜を避けます` 连接略生硬，可用 `解放し、〜を防ぎます`。

> [!tip] 参考回答
> Unityのシーンをアンロードした後は、そのシーンでのみ使用していたAddressablesのハンドルを解放し、リソースが長時間メモリを占有するのを防ぐべきです。

---

### 练习 3

> [!question] 题目
> 如果 Niagara 特效颜色没有按照角色状态变化，请检查用户参数名称、绑定时机和材质参数。

> [!example] 自己的答案
>　Niagara エフェクトの色がキャラクター状態に応じて変わらない場合、ユーザーパラメータ名、バインディングタイミング、マテリアルパラメータを確認してください。

> [!warning] 指正
> 整体正确自然。`キャラクター状態` 最好加 `の`，写成 `キャラクターの状態`。`バインディングタイミング` 可以理解，但 `バインドするタイミング` 更自然。

> [!tip] 参考回答
> Niagaraエフェクトの色がキャラクターの状態に応じて変化しない場合は、ユーザーパラメータ名、バインドするタイミング、マテリアルパラメータを確認してください。

---

### 练习 4

> [!question] 题目
> 上半身播放攻击动画时，下半身仍要保持移动，因此需要正确设置骨骼混合范围。

> [!example] 自己的答案
>　上半身がアニメーションを再生する時に、下半身が移動し続け、ボーンブレンド範囲を正しく設定する必要があります。

> [!warning] 指正
> 大意能传达，但遗漏了“攻击动画”。`上半身がアニメーションを再生する` 不够自然，应说 `上半身で攻撃アニメーションを再生する`。题目是“仍要保持移动”，用 `下半身の移動を維持する` 更准确。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">上半身で</mark>攻撃アニメーションを再生している間も下半身の移動を維持するため、ボーンのブレンド範囲を正しく設定する必要があります。

---

### 练习 5

> [!question] 题目
> 在为敌人选择掩体位置时，可以根据距离、可达性和暴露程度给候选点打分。

> [!example] 自己的答案
>　敵にカーバー位置を選択する時に、距離、到着可能性、バレ程度をまとめて点数を付けます。

> [!warning] 指正
> 核心意思能理解，但有几处术语问题。`カーバー` 应为 `カバー`，`敵に〜を選択する` 应改为 `敵の〜を選ぶ`。`到着可能性` 更适合说 `到達可能性`，`バレ程度` 太口语，应使用 `露出度`。

> [!tip] 参考回答
> 敵のカバー位置を選ぶときは、距離、<mark style="background: #FF5582A6;">到達可能性露出度に基づいて</mark>候補地点を評価できます。

---

### 练习 6

> [!question] 题目
> 打开确认弹窗时，应保存原来的 UI 焦点，关闭弹窗后再恢复到之前的控件。

> [!example] 自己的答案
>　確認ポップアップを開く時に、元のUIフォーカスを保持し、ポップアップが閉じた後も元のウィジェットに戻れるようにします。

> [!warning] 指正
> 意思基本正确。这里的“保存原焦点”是暂存信息，因此 `保持する` 不如 `保存する` 准确。关闭后应明确说 `元のウィジェットにフォーカスを戻す`，而不是只说“能够返回”。

> [!tip] 参考回答
> 確認ポップアップを開くときは元のUIフォーカスを保存し、ポップアップを閉じた後に以前のウィジェットへフォーカスを戻します。

---

### 练习 7

> [!question] 题目
> 如果室内场景出现物体突然消失的问题，请检查包围盒大小、遮挡剔除和摄像机位置。

> [!example] 自己的答案
>　室内シーンでオブジェクトが急にきえる場合、ボクスのサイズ、オクルージョンカリング、カメラの位置を確認してください。

> [!warning] 指正
> 句子结构正确。`きえる` 应写成 `消える`。`ボクス` 拼写错误，而且“包围盒”应使用 `バウンディングボックス`，不能只写普通的“箱子”。

> [!tip] 参考回答
> 室内シーンでオブジェクトが突然消える場合は、<mark style="background: #FF5582A6;">バウンディングボックス</mark>のサイズ、オクルージョンカリング、カメラの位置を確認してください。

---

### 练习 8

> [!question] 题目
> 角色站在移动平台上发生抖动时，请比较物理更新顺序、平台速度和角色附着方式。

> [!example] 自己的答案
>　キャラクターが移動プラットフォームで揺れる場合、物理演算の実行順番、プラットフォームの速度、キャラクターをくっつける方式を比較してください。

> [!warning] 指正
> 技术要点完整。`移動プラットフォームで` 应为 `移動プラットフォームの上で`。抖动可说 `ガタつく`；`実行順番` 更自然的技术表达是 `更新順序`，`くっつける方式` 应改为 `アタッチ方法` 或 `追従方法`。

> [!tip] 参考回答
> キャラクターが移動プラットフォームの上で<mark style="background: #FF5582A6;">ガタつく</mark>場合は、物理演算の更新順序、プラットフォームの速度、キャラクターの<mark style="background: #FF5582A6;">アタッチ方法</mark>を比較してください。

---

### 练习 9

> [!question] 题目
> 大量脚步声同时播放时，可以设置并发数量和优先级，避免重要音效被淹没。

> [!example] 自己的答案
>　大量の足音を同時に再生する時に、並行数と優先度が設定できるように、重要な効果音が隠れることを避けます。

> [!warning] 指正
> 主要意思能传达，但音频语境中的“并发数量”应说 `同時再生数`，不是 `並行数`。`設定できるように` 表示“为了能够设置”，逻辑不对；这里直接用 `設定し`。音效被淹没可自然表达为 `埋もれる` 或 `かき消される`。

> [!tip] 参考回答
> 大量の足音を同時に再生するときは、<mark style="background: #FF5582A6;">同時再生数</mark>と優先度を設定し、重要な<mark style="background: #FF5582A6;">効果音が埋もれない</mark>ようにします。

---

### 练习 10

> [!question] 题目
> 写入存档时，可以先生成临时文件，确认写入成功后再替换旧文件，降低数据损坏风险。

> [!example] 自己的答案
>　セーブデータを書き込む時に、まず一時的なファイルに書き込んでおきます。書き込みが完了したことを確認してから旧ファイルを差し替えます。こうすることでデータが破損するリスクを下げます。

> [!warning] 指正
> 表达清楚，执行顺序也正确。`一時的なファイル` 可以理解，但固定说法是 `一時ファイル`。题目强调确认写入成功，可说 `正常に完了したことを確認してから`；`旧ファイル` 换成 `既存のファイル` 更自然。

> [!tip] 参考回答
> セーブデータを書き込むときは、まず一時ファイルを作成し、書き込みが正常に完了したことを確認してから既存のファイルを置き換えることで、データ破損のリスクを下げられます。

---

### 练习 11

> [!question] 题目
> Unity 的本地化文本更新后，还要重新检查 UI 布局，避免较长字符串超出按钮范围。

> [!example] 自己的答案
>　Unityのローカライズファイルが更新した後、UIレイアウトを再び確認し、長いテキストがボタン表示範囲を超えることを避けます。

> [!warning] 指正
> 技术意思基本正确。`ローカライズファイルが更新した` 的主谓关系错误，应为 `ローカライズテキストが更新された` 或 `〜を更新した`。`再び確認し` 可换成 `改めて確認し`；“超出按钮范围”更自然地说 `ボタンからはみ出さないようにする`。

> [!tip] 参考回答
> Unityのローカライズテキストを更新した後は、長い文字列がボタンの範囲から<mark style="background: #FF5582A6;">はみ出さない</mark>ように、UIレイアウトを改めて確認する必要があります。

---

### 练习 12

> [!question] 题目
> 为了让战斗回放结果保持稳定，需要记录随机数种子、玩家输入和关键状态变化。

> [!example] 自己的答案
>　バトルリプレイが安定に再生するために、乱数シード、プレイヤーの入力、重要な状態変化を記録する必要があります。

> [!warning] 指正
> 要素列举完整。`安定に再生する` 不自然，应使用副词形式 `安定して再生する`。这里强调回放结果可稳定复现，用 `同じ結果を安定して再現できるように` 更贴合技术含义。

> [!tip] 参考回答
> 戦闘リプレイで同じ結果を安定して再現できるように、乱数シード、プレイヤー入力、重要な状態変化を記録する必要があります。

---

### 练习 13

> [!question] 题目
> 武器最终属性应按照基础值、装备加成和临时增益的顺序计算，并保留各阶段结果以便调试。

> [!example] 自己的答案
>　武器の最終的な属性は基礎値、装備加算値、一時的バフの順番で決定します。デバッグしやすいため、計算ごとの結果を残します。

> [!warning] 指正
> 核心意思正确。游戏属性通常用 `ステータス`；`装備加算値` 不够自然，可用 `装備ボーナス`。题目是按顺序“计算”，因此 `順番で決定します` 应改为 `順に計算します`。`デバッグしやすいため` 应说 `デバッグしやすくするために`。

> [!tip] 参考回答
> 武器の<mark style="background: #FF5582A6;">最終ステータス</mark>は、基礎値、装備ボーナス、一時的なバフの順に計算し、デバッグしやすくするために<mark style="background: #FF5582A6;">各段階の計算結果</mark>を残します。

---

### 练习 14

> [!question] 题目
> 我好像把耳机落在咖啡店了，吃完午饭陪我回去找一下好吗？

> [!example] 自己的答案
>　イヤホンがコーヒー屋に落ちたみたい、昼ご飯終わった後一緒に探しに行ってもらえますか。

> [!warning] 指正
> 大意可以理解，但“落在店里”是忘带走，应说 `イヤホンをカフェに忘れた`，不是耳机自己掉落的 `イヤホンが〜に落ちた`。`昼ご飯終わった後` 缺少助词，可说 `昼ご飯を食べ終わったら`。两个分句之间应使用句号或连接表达。

> [!tip] 参考回答
> イヤホンをカフェに忘れたみたい。昼ご飯を食べ終わったら、一緒に戻って探してくれない？

---

### 练习 15

> [!question] 题目
> 这个问题的影响范围还没有确认清楚，我会先补充测试结果，再决定是否调整发布日期。

> [!example] 自己的答案
>　この問題による影響範囲がまだ確認中なので、先にテスト結果を補充してからリリース時間を調整するかどうかを決めます。

> [!warning] 指正
> 整体逻辑清楚。`影響範囲がまだ確認中` 略生硬，可说 `影響範囲をまだ十分に確認できていない`。`テスト結果を補充する` 是中文直译，建议用 `テスト結果を追加する`。发布日期是 `リリース日`，`リリース時間` 表示具体时刻。

> [!tip] 参考回答
> この問題の影響範囲をまだ十分に確認できていないため、まずテスト結果を追加してから、リリース日を調整するかどうかを判断します。

## 2026-08-16

### 练习 1

> [!question] 题目
> 在 Unreal 中实现锁定镜头的过渡时，应分别调整插值速度、目标偏移和玩家输入恢复时机。

> [!example] 自己的答案
>　Unrealでカメラロック遷移を実装する時に、補間速度、ターゲットオフセット、プレイヤー入力状態の復元をそれぞれに調整できるべきです。

> [!warning] 指正
> 技术要点基本完整。`カメラロック遷移` 更自然地说 `ロックオンカメラの遷移`。`それぞれに` 中的 `に` 多余；题目是“分别调整”，不是“应该能够调整”，所以 `調整できるべきです` 应改为 `それぞれ調整するべきです`。恢复时机可明确为 `入力を戻すタイミング`。

> [!tip] 参考回答
> Unrealでロックオンカメラの遷移を実装するときは、補間速度、ターゲットオフセット、プレイヤー入力を戻すタイミングをそれぞれ調整するべきです。

---

### 练习 2

> [!question] 题目
> Unity 运行时需要修改 ScriptableObject 数据时，最好先复制实例，避免影响原始资源。

> [!example] 自己的答案
>　Unityランタイム時にScriptableObjectデータの変更必要になった場合、インスタンスをコピーしておいくことで、元リソースに影響しないようにします。

> [!warning] 指正
> 意思清楚，但有两个语法问题。`ランタイム時` 略重复，可用 `実行時`；`データの変更必要になった` 应为 `データを変更する必要が生じた`。`コピーしておいく` 是拼写错误，应为 `コピーしておく`。原始资源在 Unity 语境中用 `元のアセット` 更自然。

> [!tip] 参考回答
> Unityの実行時にScriptableObjectのデータを変更する必要がある場合は、先にインスタンスを複製し、元のアセットに影響を与えないようにするのが望ましいです。

---

### 练习 3

> [!question] 题目
> 如果处决动画与目标位置对不准，请检查 Motion Warping 目标、根运动设置和角色朝向。

> [!example] 自己的答案
>　処刑アニメーションとターゲットの位置が不一致している場合、MotionWarpingのターゲット、ルートモーション、キャラクターの向きを確認してください。

> [!warning] 指正
> 技术意思能准确传达。`位置が不一致している` 不够自然，建议说 `位置が合っていない`。题目包含“根运动设置”，你的 `ルートモーション` 少了“设置”，可补成 `ルートモーションの設定`。`Motion Warping` 建议保留空格。

> [!tip] 参考回答
> 処刑アニメーションとターゲットの位置が合っていない場合は、Motion Warpingのターゲット、ルートモーションの設定、キャラクターの向きを確認してください。

---

### 练习 4

> [!question] 题目
> 显示很长的道具列表时，可以只复用屏幕内可见的 UI 元素，减少控件创建数量。

> [!example] 自己的答案
> 長いアイテムリストを表示する時に、表示範囲内のUI要素を再利用することでウィジェット生成数を減らします。

> [!warning] 指正
> 表达自然，技术意思也正确。为了准确体现“只复用可见元素”，可在 `表示範囲内のUI要素` 后加 `だけ`。说明一般规则时，`表示する時に` 改为 `表示するときは` 更自然。

> [!tip] 参考回答
> 長いアイテムリストを表示するときは、画面内に表示されているUI要素だけを再利用することで、生成するウィジェットの数を減らせます。

---

### 练习 5

> [!question] 题目
> 打包游戏之前，应统计实际使用的 Shader Variant，并移除不需要的组合以缩短加载时间。

> [!example] 自己的答案
>　パーケット前に、実際に使用するShaderVariantを集計し、未使用組み合わせを外してロード時間を短縮します。

> [!warning] 指正
> 核心意思正确，但 `パーケット` 是拼写错误，应为 `パッケージ化`，也可以说 `ビルド前`。`未使用組み合わせ` 缺少助词 `の`；`外す` 在这里不如 `削除する` 准确。`Shader Variant` 建议分开书写。

> [!tip] 参考回答
> ゲームをパッケージ化する前に、実際に使用されているShader Variantを集計し、不要な組み合わせを削除してロード時間を短縮するべきです。

---

### 练习 6

> [!question] 题目
> 制作受击停顿时，应把战斗演出的时间控制与 UI 计时分开，避免冷却显示停止。

> [!example] 自己的答案
>　被弾停止を作成する時に、バトル演出時間制御とUIタイマーを分けるべきでクールダウンが停止することを避けます。

> [!warning] 指正
> 大意可以理解。游戏开发中“受击停顿”通常说 `ヒットストップ`。`バトル演出時間制御` 应加 `の`，写成 `バトル演出の時間制御`。`分けるべきで` 连接错误，应使用 `分け、`；题目是避免“冷却显示”停止，要明确写 `クールダウン表示`。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">ヒットストップ</mark>を実装するときは、バトル演出の時間制御とUIの時間計測を分け、クールダウン表示が停止しないようにするべきです。

---

### 练习 7

> [!question] 题目
> 多个敌人同时申请寻路时，可以分批处理路径请求，避免单帧出现明显的性能峰值。

> [!example] 自己的答案
>　複数の敵が同時に経路探索をリクエストする時に、バッチ処理でリクエストを受け取ります。そうすることで１フレーム内に著しいピークが発生することを防ぎます。

> [!warning] 指正
> 原因和目的表达清楚，但 `バッチ処理でリクエストを受け取る` 只是“成批接收”，没有表达“分批处理”。建议说 `複数のフレームに分けて処理する`。`著しいピーク` 可理解，但性能语境中 `大きな負荷のピーク` 更具体。

> [!tip] 参考回答
> 複数の敵が同時に経路探索を要求する場合は、パス要求を複数のフレームに分けて処理し、1フレーム内に大きな負荷のピークが発生するのを防げます。

---

### 练习 8

> [!question] 题目
> 计算最终伤害时，请明确固定减伤、百分比抗性和暴击倍率的应用顺序。

> [!example] 自己的答案
>　最終ダメージを計算する時に、固定減少、抵抗パーセント、クリティカル倍率の適用順を明確にしてください。

> [!warning] 指正
> 句子结构正确，但术语需要调整。`固定減少` 不明确，应说 `固定値のダメージ軽減`；`抵抗パーセント` 是中文式组合，可用 `割合耐性` 或 `パーセント耐性`。`適用順` 很自然。

> [!tip] 参考回答
> 最終ダメージを計算するときは、固定値の<mark style="background: #FF5582A6;">ダメージ軽減</mark>、<mark style="background: #FF5582A6;">割合耐性</mark>、クリティカル倍率の適用順を明確にしてください。

---

### 练习 9

> [!question] 题目
> 如果一个小型资源意外带入了大量内容，请检查硬引用链和资源依赖关系。

> [!example] 自己的答案
>　小さいリソースが大量のコンテンツを意図なしにロードした場合、ハード参照と依存関係を確認してください。

> [!warning] 指正
> 技术意思基本成立。`意図なしに` 不自然，应使用副词 `意図せず`。题目是“小资源意外带入大量内容”，用 `大量のコンテンツを読み込んでしまう` 更贴切。`硬引用链` 建议明确为 `ハード参照のチェーン`。

> [!tip] 参考回答
> 小さなリソースが意図せず大量のコンテンツを読み込んでしまう場合は、ハード参照のチェーンとリソースの依存関係を確認してください。

---

### 练习 10

> [!question] 题目
> 当第三人称摄像机靠近墙壁时，应平滑缩短镜头距离，避免画面突然跳动。

> [!example] 自己的答案
>　第三人称カメラが壁に近づく時に、スムーズにカメラ距離を縮めるべきです。画面がガタつきを防ぎます。

> [!warning] 指正
> 第一处技术表达正确。第二句 `画面がガタつきを防ぎます` 助词关系错误，变成了“画面防止抖动”。应说 `画面が急に揺れないようにします` 或 `画面の急な揺れを防ぎます`。两句合并会更自然。

> [!tip] 参考回答
> 三人称カメラが壁に近づいたときは、画面が<mark style="background: #FF5582A6;">突然跳ねない</mark>ように、カメラとの距離を滑らかに縮めるべきです。

---

### 练习 11

> [!question] 题目
> 在 Unity 中排查垃圾回收峰值时，请记录每帧的临时内存分配和对象创建位置。

> [!example] 自己的答案
>　UnityでGCスパイクを特定する時に、毎フレーム一時的メモリの割り当てとオブジェクトの生成元を記録してください。

> [!warning] 指正
> 技术意思完整。`毎フレーム一時的メモリの割り当て` 的修饰关系略生硬，建议说 `フレームごとの一時メモリ割り当て`。题目的“对象创建位置”用 `オブジェクトの生成箇所` 比 `生成元` 更准确。

> [!tip] 参考回答
> UnityでGCスパイクを調査するときは、フレームごとの一時メモリ割り当てと、オブジェクトが生成されている<mark style="background: #FF5582A6;">箇所</mark>を記録してください。

---

### 练习 12

> [!question] 题目
> 对象销毁之前必须解除事件绑定，否则旧对象可能继续接收状态变化通知。

> [!example] 自己的答案
>　オブジェクトを廃棄する前にイベントバンディングを必ず解除します。そうしないとオブジェクトが状態変化を継続的に受け取ります。

> [!warning] 指正
> 意思基本正确。`イベントバンディング` 是拼写错误，应为 `イベントバインディング`。对象生命周期语境中 `破棄する` 比 `廃棄する` 更常见。第二句应保留题目中的“旧对象”和可能性，可说 `古いオブジェクトが〜を受け取り続ける可能性があります`。

> [!tip] 参考回答
> オブジェクトを破棄する前にイベントバインディングを必ず解除しないと、古いオブジェクトが状態変化の通知を受け取り続ける可能性があります。

---

### 练习 13

> [!question] 题目
> 为了验证连招判定是否稳定，可以使用固定的输入序列测试各个取消窗口。

> [!example] 自己的答案
>　コンボ判定は安定して挙動しているかどうか検証するため、固定入力順で各キャンセルウィンドウをテストできるようにします。

> [!warning] 指正
> 测试目的表达清楚。`安定して挙動している` 不自然，应说 `安定して動作している`。`固定入力順` 可改为 `固定された入力シーケンス`。`テストできるようにします` 表示“使之能够测试”，题目只是说明可采用该方法，直接说 `テストできます` 即可。

> [!tip] 参考回答
> コンボ判定が<mark style="background: #FF5582A6;">安定して動作している</mark>か検証するために、<mark style="background: #FF5582A6;">固定された入力シーケンス</mark>を使って各キャンセルウィンドウをテストできます。

---

### 练习 14

> [!question] 题目
> 冰箱里没有牛奶了，你回来的时候能顺便买一盒吗？

> [!example] 自己的答案
>　冷蔵庫に牛乳がなかった、帰ってくる時についでに買ってくれませんか

> [!warning] 指正
> 意思能理解，但 `なかった` 表示过去“当时没有”，这里说明当前状态应使用 `ない`。两个分句不能只用逗号连接。题目中的“一盒牛奶”日语通常按瓶/纸盒计数说 `一本`，并用 `買ってきて` 表达“买回来”。

> [!tip] 参考回答
> 冷蔵庫に牛乳がないから、帰ってくるときに、ついでに一本買ってきてくれない？

---

### 练习 15

> [!question] 题目
> 我发现需求说明和当前实现有一处不一致，确认后会把修改方案发给大家。

> [!example] 自己的答案
>　要件定義と実際実装が一つ違いがあるので、確認した後に修正提案を皆さんに共有します。

> [!warning] 指正
> 工作沟通意图清楚，但 `実際実装が一つ違いがある` 语法错误。应说 `要件仕様と現在の実装に一か所不一致がある`。`修正提案` 可以理解，但“修改方案”更自然是 `修正案`；“确认后”用 `確認したうえで` 更符合职场表达。

> [!tip] 参考回答
> 要件仕様と現在の実装に一か所不一致があることに気づきました。確認したうえで、修正案を皆さんに共有します。
