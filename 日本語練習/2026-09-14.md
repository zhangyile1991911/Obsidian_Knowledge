## 2026-09-14

### 练习 1

> [!question] 题目
> 玩家准备在敌人攻击命中的瞬间进行精准格挡时，战斗组件应确认攻击来自角色正面、玩家的输入时间位于允许的时间范围内，并且角色仍有足够耐力；只有格挡状态成功开始后才能扣除耐力，如果任意一个条件不满足，则继续使用普通受击结果。

> [!example] 自己的答案
> プレイやーが敵の攻撃が命中する瞬間にブロックしようとするときは、バトルコンポーネントは攻撃方向がキャラクターの正面から来ること、プレイヤーの入力が許容時間内であること、キャラクターの体力が足りていることを確認します。ブロックに成功した場合にのみ耐力を減らし、いずれか一つでも満たさない場合、通常攻撃で結果を受け入れます

> [!warning] 指正
> 三个条件、成功后扣除以及失败后采用普通受击结果的结构基本保留。`プレイやー` 应为 `プレイヤー`。`プレイヤーが敵の攻撃が` 出现两个主语助词，应改成 `敵の攻撃が命中する瞬間にプレイヤーが`。这里的耐力是 `スタミナ`，`体力` 容易被理解为生命值或身体体力。题目要求格挡状态成功开始后才扣除，应说 `ガード状態への移行に成功した場合にのみ`，不只是格挡结果成功。`いずれか一つでも満たさない場合` 应为 `条件を満たしていない場合は`；普通受击结果应使用 `通常の被弾結果`，不是 `通常攻撃で結果を受け入れる`。

> [!tip] 参考回答
> 敵の攻撃が命中する瞬間にプレイヤーがジャストガードを行おうとするときは、戦闘コンポーネントは、攻撃がキャラクターの正面から来ていること、プレイヤーの入力時刻が許容時間内にあること、キャラクターに十分なスタミナが残っていることを確認します。ガード状態への移行に成功した場合にのみスタミナを消費し、いずれか一つでも条件を満たしていない場合は、通常の被弾結果を適用します。

---

### 练习 2

> [!question] 题目
> 辅助瞄准评分组件负责根据目标与准星之间的夹角、目标距离和目标能够被看见的程度计算候选分数，武器瞄准组件只把已经确认的最高分目标用于修正瞄准方向；武器瞄准组件不能改变候选分数，也不能采用已经被障碍物遮挡的目标。

> [!example] 自己的答案
>　エイム補助評価コンポーネントはターゲットとクロスヘア間の角度、ターゲットとの距離、見える程度に基づいてスコアを計算します。武器エイムコンポーネントは確定されたスコアの最も高いターゲットをエイム方向に適用します。武器エイムコンポーネントは候補スコアを変更しなく、まだオブジェクトに遮られているターゲットを採用してはいけません。

> [!warning] 指正
> 评分组件与武器瞄准组件的职责边界表达清楚，三个评分依据也都保留了。`見える程度` 可以理解，更准确的是 `可視性`。`確定されたスコアの最も高いターゲット` 应为 `確定した候補のうち、スコアが最も高いターゲット`。目标不能直接“应用到瞄准方向”，应根据目标 `エイム方向を補正する`。`変更しなく` 应改为并列禁止结构 `変更したり、〜したりしてはいけません`。`まだオブジェクトに遮られている` 中的 `まだ` 没有必要，障碍物宜明确为 `障害物`。

> [!tip] 参考回答
> エイムアシスト評価コンポーネントは、ターゲットとクロスヘアの間の角度、ターゲットまでの距離、ターゲットの可視性に基づいて候補スコアを計算します。武器エイムコンポーネントは、確認済みの候補のうちスコアが最も高いターゲットだけを使ってエイム方向を補正します。武器エイムコンポーネントは、候補スコアを変更したり、障害物に遮られたターゲットを採用したりしてはいけません。

---

### 练习 3

> [!question] 题目
> 调查相机从昏暗房间移动到明亮室外时自动曝光造成画面短暂过亮的问题时，请记录画面亮度分布的统计结果、系统计算出的目标曝光值、曝光适应速度，以及画面亮度恢复到正常范围所需的帧数，并比较修改适应速度前后的结果。

> [!example] 自己的答案
>カメラが室内から室外に移動するときに自動暴露の輝度が高すぎる問題を調査するときは、画面輝度分散の結果、目標暴露値、暴露適応速度、画面輝度が正常範囲に戻るフレーム数を記録し、変更前後の適応速度を比較してください。

> [!warning] 指正
> 四个记录项目大致都有对应，但曝光术语和最后的比较对象需要修正。图形领域的“曝光”是 `露出`，因此自动曝光、目标曝光值和曝光适应速度分别是 `自動露出`、`目標露出値`、`露出の適応速度`；`暴露` 用于秘密或危险物质等被暴露的语境。`画面輝度分散` 是亮度方差，题目说的是亮度分布的统计结果，应使用 `画面の輝度分布に関する統計情報`。最后要求比较修改适应速度前后的画面结果，不是比较两个适应速度数值本身。

> [!tip] 参考回答
> カメラが暗い室内から明るい屋外へ移動したときに、自動露出によって画面が一時的に<mark style="background: #FF5582A6;">明るくなりすぎる</mark>問題を調査する場合は、画面の輝度分布に関する統計情報、システムが算出した目標露出値、露出の<mark style="background: #FF5582A6;">適応速度</mark>、画面の明るさが正常な範囲に戻るまでのフレーム数を記録します。そして、適応速度を変更する前後の結果を比較します。

---

### 练习 4

> [!question] 题目
> 临时教学提示对象创建后会登记角色移动事件和跳跃事件；提示完成或场景关闭时，教学管理器应先解除这两项事件登记，再清除界面对提示对象的引用，最后销毁该对象，清理完成后发生的移动事件或跳跃事件不得再次访问它。

> [!example] 自己的答案
>　一時的チュートリアルオブジェクトが生成されるとキャラクターのジャンプイベントと移動イベントを登録します。完成又はシーンを閉じる時に、チュートリアルマネージャーがまずイベント購読を解除し、そして画面がヒントへの参照をクリアし、最後に該当オブジェクトを廃棄します。クリアが完了した後移動又はジャンプイベントが再びにそれにアクセスしてはいけません。

> [!warning] 指正
> 事件登记、解除、清除引用和销毁对象的顺序基本完整。`完成又はシーンを閉じる時に` 缺少完成的主体，宜说 `チュートリアル提示が完了したとき、またはシーンが閉じられたとき`。`画面` 应使用界面组件含义明确的 `UI`，`ヒントへの参照` 也应明确为教学提示对象。对象销毁宜用 `破棄する`。最后的 `クリア` 应为涵盖全部步骤的 `クリーンアップ`；`再びに` 多了 `に`，`それ` 的指代也不够清楚，应直接写对象名称。

> [!tip] 参考回答
> 一時的なチュートリアル提示オブジェクトを作成した後は、キャラクターの移動イベントとジャンプイベントを登録します。提示が完了したとき、またはシーンが閉じられたときは、チュートリアルマネージャーがまず二つのイベントの登録を解除し、次にUIが保持している提示オブジェクトへの参照をクリアし、最後にそのオブジェクトを破棄します。クリーンアップの完了後に発生した移動イベントやジャンプイベントが、そのオブジェクトへ再びアクセスしてはいけません。

---

### 练习 5

> [!question] 题目
> 玩家把鼠标停留在物品图标上时，提示框内容组件只提供物品名称、说明文字、图标和期望大小，提示框布局组件负责根据鼠标位置和画面边界决定最终显示位置；内容组件不能改变提示框的锚点，布局组件也不能修改物品数据。

> [!example] 自己的答案
>　プレイヤーがマウスをアイテムアイコンにホバーする時には、ヒントコンポーネントはアイテム名、説明文字、アイコンと要求されるサイズだけを提供します。ヒントレイアウトコンポーネントはマウスの位置と画面の端に基づいて最後の表示位置を決めます。コンテンツコンポーネントはポップアップのピボットを変更してはいけなく、レイアウトコンポーネントもアイテムのデータを変更してはいけません。

> [!warning] 指正
> 内容组件和布局组件的分工以及双方的禁止事项都表达出来了。第一处 `ヒントコンポーネント` 应与题目一致，明确为 `ツールチップ内容コンポーネント`。期望大小可说 `希望サイズ`，`要求されるサイズ` 更像外部强制要求的尺寸。`最後の表示位置` 表示上一次的位置，最终位置应为 `最終的な表示位置`。锚点是 `アンカー`，`ピボット` 是枢轴或中心点，概念不同。`変更してはいけなく` 不能这样连接，应结束为 `変更してはいけません`，再说明另一个组件的限制。

> [!tip] 参考回答
> プレイヤーがマウスカーソルをアイテムアイコンの上に置いたときは、ツールチップ内容コンポーネントは、アイテム名、説明文、アイコン、希望サイズだけを提供します。ツールチップレイアウトコンポーネントは、マウスの位置と<mark style="background: #FF5582A6;">画面領域の境界</mark>に基づいて、最終的な表示位置を決定します。内容コンポーネントはツールチップのアンカーを変更してはいけません。また、レイアウトコンポーネントもアイテムデータを変更してはいけません。

---

### 练习 6

> [!question] 题目
> 玩家切换游戏语言时，语音资源管理器应先载入新语言所需的全部语音文件，并确认每个文件都能正常播放；全部确认完成后才能把对话系统的引用切换到新资源，如果任何文件无法使用，则释放本次新载入的资源，继续使用原来的语言，并向界面说明语言资源不完整。

> [!example] 自己的答案
>　プレイやーがゲーム言語を切り替える時は、オーディオリソースマネージャーはまず要求された言語に使用するファイルをロードし、各ファイルを正常に再生することを確認します。確認が完了後にのみ対話システムは参照を新しいリソースに切り替えます。いずれか一つでも使用できない場合、今回ロード済みリソースを解放し、元の言語を使い続け、画面に言語リソースが不完備していることを説明します。

> [!warning] 指正
> 新资源全部确认后再切换、失败时释放并保留原语言的流程完整。`プレイやー` 应为 `プレイヤー`。`要求された言語に使用するファイル` 不自然，应为 `新しい言語に必要なすべての音声ファイル`。确认能够播放需要可能形式 `正常に再生できること`。`確認が完了後` 应为 `確認が完了した後`。题目中执行引用切换的主体是语音资源管理器，原答案在这一句改成了对话系统。`今回ロード済みリソース` 应为 `今回新しくロードしたリソース`，`不完備している` 则可改为 `不完全である` 或 `不足している`。

> [!tip] 参考回答
> プレイヤーがゲームの言語を切り替えるときは、音声リソースマネージャーはまず、新しい言語に必要なすべての音声ファイルをロードし、各ファイルが正常に再生できることを確認します。すべての確認が完了した後で、対話システムが保持する参照を新しいリソースへ切り替えます。いずれか一つでも使用できないファイルがある場合は、今回新しくロードしたリソースを解放し、元の言語を使い続け、言語リソースが不完全であることをUIへ通知します。

---

### 练习 7

> [!question] 题目
> 角色在奔跑过程中播放换弹动作时，动画系统应只在上半身使用换弹动画，并继续在下半身播放移动动画；角色中途卸下武器后，应停止换弹动画并逐渐减小上半身的混合权重，不能中断下半身的移动动作。

> [!example] 自己的答案
> キャラクターが走行中にリロードアニメーションを再生する際に、アニメーションシステムは上半身でリロードアニメーションを適用し、上半身で移動アニメーションを再生し続けます。キャラクターが途中で武器をしまった後に、リロードアニメーションを停止して上半身のブレンドウェイトを徐々に減らし、下半身の動きを中断してはいけません。

> [!warning] 指正
> 换弹动画停止、上半身混合权重下降以及不能中断下半身动作都表达正确，但第一句出现了关键对象错误。题目要求下半身继续播放移动动画，原答案却写成 `上半身で移動アニメーションを再生し続けます`，应改为 `下半身では移動アニメーションを再生し続けます`。上半身“使用”换弹动画可说 `上半身だけに適用する`。`武器をしまった` 可以表示收起武器；若强调从装备中卸下，也可说 `武器を外した`。

> [!tip] 参考回答
> キャラクターが走っている間にリロード動作を再生するときは、アニメーションシステムはリロードアニメーションを上半身だけに適用し、下半身では移動アニメーションを再生し続けます。キャラクターが途中で武器を外した後は、リロードアニメーションを停止して上半身のブレンドウェイトを徐々に下げます。その際、下半身の移動動作を中断してはいけません。

---

### 练习 8

> [!question] 题目
> 调查高速投射物穿过薄墙却没有产生碰撞结果的问题时，请记录投射物每帧移动的距离、墙壁厚度、投射物碰撞形状的大小，以及启用连续碰撞检测前后的命中结果；连续碰撞检测是沿物体移动路径检查碰撞，从而减少高速物体越过薄小障碍物的技术。

> [!example] 自己的答案
>　高速Projectileが薄い壁をすり抜けてもコリジョンが発生しない問題を調査する時には、Projetileが毎フレームでの移動距離、壁の厚さ、Projectileの形状のサイズ、連続検出を有効化した前後の命中結果を記録してください。連続コリジョン検出は移動経路に沿って検出し続けることで高速オブジェクトが薄く小さい障害物をすり抜け問題を軽減する技術です。

> [!warning] 指正
> 四项记录内容和连续碰撞检测的作用基本表达出来了。`Projectile` 的拼写前后不一致，第二处写成了 `Projetile`。`毎フレームでの移動距離` 宜改为 `一フレーム当たりの移動距離`。题目要求的是碰撞形状大小，应明确为 `コリジョン形状の大きさ`。`連続検出` 应保持完整技术名称 `連続衝突判定`，`有効化した前後` 应为 `有効にする前後`。定义句中的 `移動経路に沿って検出し続ける` 没有说明检测什么，应补出 `衝突を検出する`；`すり抜け問題` 也缺少 `の`。

> [!tip] 参考回答
> 高速のProjectileが薄い壁を通り抜け、衝突結果が発生しない問題を調査するときは、Projectileの<mark style="background: #FF5582A6;">一フレーム当たりの</mark>移動距離、壁の厚さ、Projectileのコリジョン形状の大きさ、連続衝突判定を有効にする前後のヒット結果を記録してください。連続衝突判定とは、オブジェクトの移動経路に沿って衝突を検出し、高速なオブジェクトが薄い<mark style="background: #FF5582A6;">障害物を通り抜ける</mark>問題を減らすための技術です。

---

### 练习 9

> [!question] 题目
> 人工智能角色寻找恢复生命值的位置时，环境查询系统（Environment Query System）应分别计算角色到候选位置的移动代价、从该位置能够看见角色的敌人数量，以及位置附近是否存在危险区域；人工智能控制组件只能从已经确认能够到达的候选位置中选择最终结果。

> [!example] 自己的答案
>　AIキャラクターが回復ポイントを探すときは、EQSはプレイヤーが候補ポイントへの移動コスト、該当位置から敵に見られるの数、該当位置の周りに危険なエリアが存在するかをそれぞれ計算します。AIコンポーネントは確定されて到達できる候補から最終の結果を選びます。

> [!warning] 指正
> 三个评分项目和只能从可到达候选中选择的限制都有涉及。第一句的评分主体是环境查询系统，而移动主体是人工智能角色；`EQSはプレイヤーが候補ポイントへの移動コスト` 混入了玩家，应改为 `AIキャラクターから候補地点までの移動コスト`。`敵に見られるの数` 语法不成立，也没有明确从候选位置能够看见角色，应为 `その位置からAIキャラクターを視認できる敵の数`。`確定されて到達できる候補` 应改为 `到達可能であることが確認された候補地点`，最终结果使用 `最終候補` 更自然。

> [!tip] 参考回答
> AIキャラクターが体力を回復できる場所を探すときは、環境クエリシステム（Environment Query System）は、AIキャラクターから候補地点までの移動コスト、その地点からAIキャラクターを<mark style="background: #FF5582A6;">視認できる</mark>敵の数、その地点の周囲に危険区域があるかどうかをそれぞれ評価します。AIコントローラーは、到達可能であることが確認された候補地点の中からだけ、最終候補を選択します。

---

### 练习 10

> [!question] 题目
> 同一个角色在短时间内连续产生多个脚步声时，音频并发管理器应把这些声音限制在设定数量以内；数量超过上限时，应停止最早开始播放的脚步声，但不能因此停止属于其他分类的角色语音或环境声音。

> [!example] 自己的答案
>　同一キャラクターが短時間内で連続して足音を出す時には、オーディオマネージャーはそれらを設定数量内に抑えるべきです。数が上限を上回った時に、最も早期に再生された足音から停止します。それにより他のキャラクターボイス又は環境音を停止してはいけません。

> [!warning] 指正
> 限制脚步声数量、超出后停止最早声音、不能影响其他声音分类的三项意思都保留了。`短時間内で` 应为 `短時間に` 或 `短時間のうちに`。题目指定音频并发管理器，宜使用 `オーディオ同時再生マネージャー` 或保留 `コンカレンシーマネージャー`。`設定数量内` 不自然，应为 `設定された上限数以内`。`最も早期に再生された` 宜改为 `最も早く再生を開始した`。最后应明确“其他分类”，使用 `別のカテゴリに属するキャラクターボイスや環境音`。

> [!tip] 参考回答
> 同じキャラクターが短時間に複数の足音を連続して発生させたときは、オーディオ同時再生マネージャーは、それらの音を設定された上限数以内に制限します。数が上限を超えた場合は、最も早く再生を開始した足音を停止します。ただし、別のカテゴリに属するキャラクターボイスや環境音を停止してはいけません。

---

### 练习 11

> [!question] 题目
> 保存玩家创建的地图标记时，存档组件应记录标记所属的场景标识、世界坐标和显示名称；读取存档后，应确认场景标识能够对应到当前已经载入的场景，并确认坐标位于允许范围内，如果标记数据不符合要求，则跳过该标记并报告它在存档中的编号，不能在地图外创建图标。

> [!example] 自己的答案
>　プレイヤーによる生成された地図マーカを保存する時は、セーブコンポーネントは所属シーンID、ワールド座標、表示名称を記録します。セーブデータを読み込んだ後、シーンIDがロード済みシーン内で対応するオブジェクトを確認できるべきで、座標は許容範囲内であることを確認します。

> [!warning] 指正
> 场景标识、世界坐标、显示名称以及读取后的两项确认都写到了，但句子在异常分支前结束，遗漏了“跳过标记、报告存档编号、不得在地图外创建图标”三个重要结果。`プレイヤーによる生成された` 的修饰形式错误，应为 `プレイヤーが作成した`。`地図マーカ` 应写作 `地図マーカー`。场景标识应对应当前载入的场景本身，不是场景中的对象，因此可说 `現在ロード済みのシーンに対応していること`。`確認できるべきで` 不自然，两个确认条件应使用并列结构。

> [!tip] 参考回答
> プレイヤーが作成した地図マーカーを保存するときは、セーブコンポーネントは、マーカーが属するシーンの識別子、ワールド座標、表示名を記録します。セーブデータを読み込んだ後は、シーン識別子が現在ロード済みのシーンに対応していることと、座標が許容範囲内にあることを確認します。マーカーデータが条件を満たしていない場合は、そのマーカーを読み飛ばしてセーブデータ内の番号を報告し、マップの外側にアイコンを作成してはいけません。

---

### 练习 12

> [!question] 题目
> 调查大量状态效果组件分别执行每帧更新而导致中央处理器耗时增加的问题时，请记录当前生效的状态效果数量、每个组件一次更新所需的时间、各组件的更新频率，以及改为由一个管理器集中更新前后的总耗时。

> [!example] 自己的答案
>　大量の効果コンポーネントは毎フレーム別々に更新されるによるCPU処理時間が増える問題を調査するときは、現在に効いている効果数、コンポーネントごとに更新にかかる時間、頻度、及び一つマネージャーに集約して一元的に更新するようになった前後かかった時間。

> [!warning] 指正
> 四项记录内容基本都能对应，但整句停在名词列举处，缺少 `記録します` 或 `記録してください` 这样的最终谓语。`更新されるによる` 应为 `更新されることで`。`現在に効いている効果数` 宜明确为 `現在有効なステータス効果の数`。`コンポーネントごとに更新にかかる時間` 可改为 `各コンポーネントの一回の更新にかかる時間`。`一つマネージャー` 缺少 `の`，最后的比较对象应明确为改用管理器集中更新前后的总处理时间。

> [!tip] 参考回答
> 多数のステータス効果コンポーネントがそれぞれ毎フレーム更新されることで、CPU処理時間が増える問題を調査するときは、現在有効なステータス効果の数、各コンポーネントの一回の更新にかかる時間、各コンポーネントの更新頻度、一つのマネージャーによる一括更新へ変更する前後の総処理時間を記録してください。

---

### 练习 13

> [!question] 题目
> 面试中，请说明你会如何设计一个可以暂停并恢复计时的限时状态效果系统。游戏暂停期间，效果的剩余时间必须保持不变；游戏恢复后，效果应从原来的剩余时间继续计时。请按照以下顺序回答：
>
> 1. 这个系统的核心职责应该属于哪个组件？
> 2. 正常情况下，效果的剩余时间应该如何更新？
> 3. 游戏暂停时，系统应该如何保存剩余时间？
> 4. 你会如何验证暂停前后的持续时间正确？

> [!example] 自己的答案
>  指摘:题目意思不清楚，应该是 有时间限制的效果系统，在游戏暂停，恢复后继续正常运行。
>   这系统核心职责是什么？
>  一般情况下如何更新 
>  游戏暂停时候 怎么处理
>  1. 時間制限効果管理システムはすべての効果の追加、削除、更新を一元的に担当します。
>  2. 通常状況では管理システムがエンジンの更新メソッドに依存して更新されます。毎フレーム更新する前にポーズフラグを確認します。ポーズフラグがtrueである場合、更新を続ける。Falseである場合は、更新しない。
>  3. ゲームをポーズしている場合、ブールファラグにfalseを代入することでシステムを停止します。
>  4. 検証には停止前後に残り時間を出力します。
>  

> [!warning] 指正
> 你先指出了题意不够直观，这条反馈合理。题目的本意是：系统管理具有剩余持续时间的临时状态效果；游戏暂停时倒计时冻结，恢复后从暂停前的剩余时间继续。按编号看：1）你明确把添加、删除和更新集中到管理系统中，核心职责回答完整。2）提到了逐帧更新和检查标志，但没有明确每帧应按经过时间减少剩余时间；而且名为 `ポーズフラグ` 的变量通常在暂停时为 `true`，原答案却写成 `true` 时继续更新，语义容易相反。3）把布尔标志设为 `false` 可以停止更新，但没有说明如何保证剩余时间保持原值；`ブールファラグ` 也应为 `ブールフラグ`。4）只输出暂停前后的剩余时间还不够，应在暂停期间等待固定时长，确认剩余时间不变，再恢复并确认效果经过原有剩余时间后结束。

> [!tip] 参考回答
> 1. 時間制限付きステータス効果の追加、削除、残り時間の管理は、ステータス効果管理コンポーネントが一元的に担当します。
> 2. 通常時は、各フレームの経過時間を残り時間から差し引き、残り時間がゼロになった効果を終了させます。
> 3. ゲームが一時停止したときは残り時間を変更せず、その値を保持します。ゲームが再開した後は、保持していた残り時間から更新を再開します。
> 4. テストでは、一時停止前の残り時間を記録し、一定時間停止しても値が変化しないことを確認します。その後ゲームを再開し、記録した残り時間が経過した時点で効果が終了することを確認します。

---

### 练习 14

> [!question] 题目
> 家人劝你选择更加稳定的生活方式，但你仍想坚持自己的选择。你对他说：我不想欺骗自己的心意，我想过一种将来不会后悔的人生。

> [!example] 自己的答案
>　もっと安定な生活を選ぶって家族に言われても、あなたが自分の選択を続けていく。自分の気持ちを騙したくない、後悔しない人生を送りたいって言いました。

> [!warning] 指正
> “不想违背自己的心意”和“想过不后悔的人生”已经表达出来，但又把场景说明翻译成了人物台词。题目中“家人劝你……”和“你仍想坚持……”用于说明背景，真正需要说出口的是冒号后的内容，因此不需要 `家族に言われても`、`あなたが` 和句尾的 `って言いました`。`安定な生活` 应为 `安定した生活`。日语中“欺骗自己的心意”自然地说 `自分の気持ちに嘘をつく`，不是 `自分の気持ちを騙す`。`後悔しない人生` 可以理解，原场景的 `後悔のない人生` 更自然。

> [!tip] 参考回答
> 自分の気持ちに嘘をつかず、後悔のない人生を生きたいよ。

---

### 练习 15

> [!question] 题目
> 团队成员的工作态度受到上司批评时，你作为负责人说明：对于我指导不到位的部分，我会认真改进。所以，能否请您再从长远的角度观察他们一段时间？

> [!example] 自己的答案
>　チームメンバが仕事態度におけて上司に怒られる時に、僕の指導が至らない部分について、今回真剣に改善していきます。だから、もっと長い目で彼らを見てくれませんか。

> [!warning] 指正
> 承认指导不足、表示改进以及请求继续观察三个核心意思都保留了。冒号前的“团队成员受到上司批评”是场景说明，不必作为对上司说出的第一句；如果保留，`仕事態度におけて` 应为 `仕事への姿勢について`，`怒られる` 也偏口语。`チームメンバ` 应为 `チームメンバー`。向上司说明时使用 `僕` 和 `見てくれませんか` 稍显随意，宜改为 `私` 与 `見守っていただけませんか`。`今回` 是原题没有的“这一次”，可以删去；`指導が至らない部分` 应使用已经发生的 `指導が至らなかった部分`。

> [!tip] 参考回答
> 私の指導が至らなかった部分は、今後きちんと改善していくつもりです。ですから、もう少し長い目で彼らを見守っていただけませんか。

## 2026-09-15

### 练习 1

> [!question] 题目
> 玩家在空中按下冲刺键时，角色不能进入冲刺状态，也不能消耗耐力；落地后必须重新按键。

> [!example] 自己的答案
>　プレイヤーが空中でダッシュボタンを押した時は、キャラクターがダッシュ状態へ遷移しなく、スタミナも消費しないようにします。着地後ボタンを押し直します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 2

> [!question] 题目
> 伤害计算组件负责生成最终伤害值，生命值组件只应用该结果，不能重新计算护甲减伤。

> [!example] 自己的答案
>　ダメージ計算コンポーネントは最終のダメージ数値の生成を担当し、HPコンポーネントは結果を適用するだけで、鎧によるダメージ軽減の計算を行ってはいけません。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 3

> [!question] 题目
> 调查粒子特效造成画面卡顿时，请比较关闭特效前后的图形处理器耗时和活动粒子数量。

> [!example] 自己的答案
>　パーティクルエフェクトによる画面のカクつきを調査する時は、エフェクトを無効化した前後のGPU処理時間とアクティブパーティクルの数を比較してください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 4

> [!question] 题目
> 异步载入的武器模型不再需要时，应取消载入请求；若文件已经载入完成，则立即释放对应资源。

> [!example] 自己的答案
>　非同期でロードした武器モデルが要らなくなるときは、ロードリクエストをキャンセルすべきです。モデルがロード済み場合なら、すぐ該当リソースを解放します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 5

> [!question] 题目
> 读取旧版本存档时，应先把数据转换成当前格式；转换失败时必须保留原文件。

> [!example] 自己的答案
>　旧セーブデータを読み込む時に、まず現在のフォーマットに変換します。変換に失敗した場合に元のファイルを維持すべきです。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 6

> [!question] 题目
> 换武器动画播放到指定帧时，才可以替换角色手中的模型；提前替换会使动作显得不自然。

> [!example] 自己的答案
>　武器の切り替えアニメーションを決定されたフレームまで進行した場合にのみ、キャラクター手元のモデルを差し替えます。早期に切り替えるならモーションが不自然に見えるようになります。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 7

> [!question] 题目
> 角色沿墙滑落时，移动组件应限制下降速度，但不能清除玩家水平方向的输入。

> [!example] 自己的答案
>　キャラクターが壁に沿って落ちるときに、移動コンポーネントでは下落速度を制限すべきで、でもプレイヤーの横入力をクリアしてはいけません。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 8

> [!question] 题目
> 面试中，请说明如何设计同时出现多个交互提示时的选择规则。
>
> 1. 这个系统的核心职责是什么？
> 2. 如何确定提示的显示优先级？
> 3. 如何验证目标消失后的结果？

> [!example] 自己的答案
>　インタラクションシステムは同時に複数のインタラクションオブジェクトが操作できる場合に、操作優先度、オブジェクトへの参照を保持し、削除などの責務を担当します。
>　キャラクターがオブジェクトのインタラクション許容範囲内に入った時に、システムがそのオブジェクトへの参照を保持するようにします。キャラクターが離れた時に、該当参照をクリアします。
>　同時に複数のインタラクションできるオブジェクトが出現する時に、キャラクターとの距離によって優先度を決めます。
>　シーン内に複数のオブジェクトを配置しておきます。キャラクターを各オブジェクト範囲内に移動させます。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 9

> [!question] 题目
> 洗衣机和冰箱你可以随便用。

> [!example] 自己的答案
>　洗濯機と冷蔵庫を好きに使って

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 10

> [!question] 题目
> 商品页面的样式调整进展如何？原定昨天完成，对吧？

> [!example] 自己的答案
>　商品のページ仕様の調整進捗はどうですか？昨日完成したはずですか

> [!warning] 指正
>

> [!tip] 参考回答
>

## 2026-09-16

### 练习 1

> [!question] 题目
> 攻击动作尚未结束时，新的攻击输入应保存到输入缓冲区；动作结束后再执行该输入。

> [!example] 自己的答案
>　攻撃アニメーションが終了していない時は、新しく来る入力をバファに入れるべきです。アニメーションが終了後にその入力を処理します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 2

> [!question] 题目
> 任务数据组件负责记录任务进度，任务界面只显示结果，不能直接修改完成状态。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 3

> [!question] 题目
> 调查纹理显示模糊的问题时，请记录系统需要的纹理层级、实际载入层级和纹理占用内存。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 4

> [!question] 题目
> 角色死亡后，锁定系统必须解除当前目标并隐藏锁定标记，避免继续访问已销毁的对象。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 5

> [!question] 题目
> 玩家取消装备强化时，强化材料不能减少，装备属性也必须保持原值。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 6

> [!question] 题目
> 角色停止攀爬后，动画系统应逐渐减小手部修正权重，不能让手臂突然返回基础姿势。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 7

> [!question] 题目
> 对象池没有可用的子弹对象时，管理器可以新建对象，但必须记录对象池达到的最大数量。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 8

> [!question] 题目
> 面试中，请说明如何设计技能冷却时间的显示流程。
>
> 1. 哪个组件保存冷却时间？
> 2. 界面如何取得剩余时间？
> 3. 如何验证界面不会修改数据？

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 9

> [!question] 题目
> 生活再规律一点怎么样？

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 10

> [!question] 题目
> 当前版本已经上传到测试环境。请告诉对方，页面动画效果还没有完成。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>
