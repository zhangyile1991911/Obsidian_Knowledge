## 2026-08-03

### 练习 1

> [!question] 题目
> 在 Unreal 中实现攀爬系统时，角色移动模式和动画状态机应该通过明确的状态字段保持一致。

> [!example] 自己的答案
>　Unrealでクライミングシステムを実装する時に、キャラクター移動モードとアニメーションステートマシンは明確なステートフィールドを通じて一致します。

> [!warning] 指正
> 意思基本清楚。`実装する時に` 可改为更自然的 `実装するときは`。`ステートフィールド` 虽能理解，但日语技术说明里更常说 `状態フィールド`。`一致します` 像描述事实，题目是“应该保持一致”，建议用 `一致させるべきです` 或 `整合性を保つべきです`。

> [!tip] 参考回答
> Unrealでクライミングシステムを実装するときは、キャラクターの移動モードとアニメーションステートマシンを、明確な状態フィールドを通じて一致させるべきです。

---

### 练习 2

> [!question] 题目
> Unity 的战斗日志最好记录技能编号、命中对象和最终伤害值，方便之后排查结算问题。

> [!example] 自己的答案
>　UnityのバトルログではスキルID、ヒットしたターゲットと計算ダメージを記録したほうがいいです。後から計算バグを特定しやすくなります。

> [!warning] 指正
> 大意正确。`ヒットしたターゲットと計算ダメージ` 并列时建议写成 `ヒットした対象、最終ダメージ値`。题目里的“结算问题”不是普通计算 bug，更自然是 `集計問題`、`計算ミス` 或 `ダメージ計算の問題`。`ほうがいいです` 可以用，但技术建议里 `記録しておくとよいです` 更自然。

> [!tip] 参考回答
> Unityのバトルログには、スキルID、ヒットした対象、最終ダメージ値を記録しておくと、後から<mark style="background: #FF5582A6;">集計問題</mark>を調査しやすくなります。

---

### 练习 3

> [!question] 题目
> 如果角色阴影在移动中闪烁，请先比较骨骼网格边界、光源角度和阴影偏移设置。

> [!example] 自己的答案
>　キャラクターのシャドウが移動中にちらついている場合、先にスケルトンの境界、光源の角度、シャドウオフセットを比較してください。

> [!warning] 指正
> 主要意思能表达。`スケルトンの境界` 容易理解成骨架本身的边界，题目是“骨骼网格边界”，建议说 `スケルタルメッシュの境界` 或 `バウンズ`。`比較してください` 可以，但这里是排查设置，也可以用 `確認してください`。

> [!tip] 参考回答
> キャラクターの影が移動中にちらつく場合は、まずスケルタルメッシュの<mark style="background: #FF5582A6;">バウンズ</mark>、光源の角度、シャドウオフセット設定を比較してください。

---

### 练习 4

> [!question] 题目
> 在设计道具拾取流程时，检测范围、提示 UI 和背包写入应该分成独立步骤。

> [!example] 自己的答案
>　アイテムを拾う時に、検出範囲、提示UI、インベントリーの書き込みを独立した別々の流れにするべきです。

> [!warning] 指正
> 题意基本完整。`提示UI` 是中文词直接放进日语，建议用 `ヒントUI` 或 `案内UI`。`独立した別々の流れ` 有重复感，可以简化为 `独立したステップ`。题目是“设计流程时”，建议补出 `フローを設計するときは`。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">アイテム拾得</mark>フローを設計するときは、検出範囲、ヒントUI、インベントリへの書き込みを独立したステップに分けるべきです。

---

### 练习 5

> [!question] 题目
> 为了减少角色蓝图的复杂度，可以把攻击判定参数移动到数据资产里管理。

> [!example] 自己的答案
>　キャラクターのBlueprintの複雑度を減らすため、攻撃判定パラメータをデータアセットに移して管理します。

> [!warning] 指正
> 表达自然，题意也完整。`Blueprint` 可以用，但在日语文章里常写 `ブループリント`。`複雑度` 能懂，不过更自然的是 `複雑さ`。整体只需稍微调整语气即可。

> [!tip] 参考回答
> キャラクターのブループリントの複雑さを減らすために、攻撃判定パラメータをデータアセットに移して管理できます。

---

### 练习 6

> [!question] 题目
> 当技能预览范围发生变化时，地面指示器应该立即更新半径和颜色。

> [!example] 自己的答案
>　スキルプレビュー範囲が変化した場合、インジケータがすぐに半径と色を更新するべきです。

> [!warning] 指正
> 意思清楚。`インジケータ` 建议写成长音 `インジケーター`。题目是“地面指示器”，最好明确为 `地面インジケーター`。`変化した場合` 正确，也可用更自然的 `変わったときは`。

> [!tip] 参考回答
> スキルのプレビュー範囲が変わったときは、地面インジケーターの半径と色をすぐに更新するべきです。

---

### 练习 7

> [!question] 题目
> 在优化加载流程时，请把必须立即显示的资源和可以稍后显示的资源分开整理。

> [!example] 自己的答案
>　読み込みを最適化する時に、すぐに表示するリソースと後から表示するリソースを分けてまとめます。

> [!warning] 指正
> 主要意思正确。`読み込みを最適化する時に` 可以改为 `ロード処理を最適化するときは`，更贴近技术语境。`すぐに表示するリソース` 和 `後から表示するリソース` 能表达题意；如果强调“必须”，可加 `必要がある`。

> [!tip] 参考回答
> ロード処理を最適化するときは、すぐに表示する必要があるリソースと、後から表示できるリソースを分けて整理してください。

---

### 练习 8

> [!question] 题目
> 如果技能图标的冷却遮罩没有正确变化，请检查计时数据和材质参数是否同时更新。

> [!example] 自己的答案
>　スキルアイコンのマスクが正しく変化しない場合、タイマーデータをマテリアルデータが同時に更新されたかを確認してください。

> [!warning] 指正
> 题意有一处连接错误。`タイマーデータをマテリアルデータが` 中的 `を` 应为 `と`，并且题目是“材质参数”，不是 `マテリアルデータ`。`スキルアイコンのマスク` 少了“冷却”，建议说 `クールダウンマスク`。

> [!tip] 参考回答
> スキルアイコンのクールダウンマスクが正しく変化しない場合は、タイマーデータとマテリアルパラメータが同時に更新されているか確認してください。

---

### 练习 9

> [!question] 题目
> 在实现连击窗口时，动画通知只负责标记时间点，具体输入判断应该放在 gameplay 代码里。

> [!example] 自己的答案
>　ウィンドウの連打を実装する時に、Anim Notifyはタイミングをつけるだけで、具体的入力判定をGameplayコードに実装するべきです。

> [!warning] 指正
> “连击窗口”误译了。`ウィンドウの連打` 会变成“窗口的连打”，应说 `コンボ受付時間` 或 `コンボウィンドウ`。`タイミングをつける` 不自然，动画通知“标记时间点”可说 `タイミングを示す`。`具体的入力判定` 前面建议加 `な` 或改为 `具体的な入力判定`。

> [!tip] 参考回答
> コンボ受付時間を実装するときは、Anim Notifyはタイミングを示すだけにし、具体的な入力判定はGameplayコード側に実装するべきです。

---

### 练习 10

> [!question] 题目
> Unity 的对象生成器应该从配置读取预制体和初始参数，避免在场景脚本里硬编码。

> [!example] 自己的答案
> Unityのオブジェクト生成は設定からプレハブと初期パラメータを読み込むするべきです。シーン内のスクリプトでハードコードしないようにします。

> [!warning] 指正
> 意思基本能传达，但 `読み込むするべきです` 是错误搭配，应为 `読み込むべきです`。`Unityのオブジェクト生成` 少了“生成器”，建议用 `オブジェクト生成器` 或 `スポナー`。第二句可以和第一句合并，让“避免硬编码”的逻辑更自然。

> [!tip] 参考回答
> Unityのオブジェクト生成器は、設定からプレハブと初期パラメータを読み込み、シーン内のスクリプトにハードコードしないようにするべきです。

---

### 练习 11

> [!question] 题目
> 如果帧时间偶尔出现尖峰，请把粒子数量、动画更新和垃圾回收分别记录到分析表里。

> [!example] 自己的答案
>　フレーム時間がたまにピーク値が出る場合、パーティクルの数、アニメーション更新、ガベージコレクションを分析テーブルにそれぞれ記録します

> [!warning] 指正
> 大意正确。`フレーム時間がたまにピーク値が出る` 不自然，建议说 `フレーム時間にたまにスパイクが出る`。`分析テーブル` 能懂，但性能分析里更自然是 `分析表` 或 `プロファイル用の表`。句末缺少句号。

> [!tip] 参考回答
> フレーム時間にたまにスパイクが出る場合は、パーティクル数、アニメーション更新、ガベージコレクションを分析表にそれぞれ記録してください。

---

### 练习 12

> [!question] 题目
> 在制作存档菜单时，自动保存槽和手动保存槽应该使用不同的显示标签。

> [!example] 自己的答案
>　セーブデータメニューを作成するときに、自動セーブスラットと修道セーブスラットに別々のラベルを使うべきです。

> [!warning] 指正
> 题意基本在，但有两个拼写问题。`セーブスラット` 应为 `セーブスロット`，`修道セーブ` 应为 `手動セーブ`。`セーブデータメニュー` 会偏向“存档数据菜单”，题目是“存档菜单”，可说 `セーブメニュー`。

> [!tip] 参考回答
> セーブメニューを作成するときは、自動セーブスロットと手動セーブスロットに別々の表示ラベルを使うべきです。

---

### 练习 13

> [!question] 题目
> 调试敌人警戒范围时，请同时确认视野角度、听觉半径和遮挡判断。

> [!example] 自己的答案
>　敵の警戒範囲をデバッグする時に、視野の角度、聴覚の半径、遮蔽判断を同時に確認してください。

> [!warning] 指正
> 意思完整。`聴覚の半径` 可以理解，但 AI 感知语境里更自然是 `聴覚範囲` 或 `聴覚半径`。`遮蔽判断` 能表达“遮挡判断”，也可以说 `遮蔽判定`。整体很接近自然技术表达。

> [!tip] 参考回答
> 敵の警戒範囲をデバッグするときは、視野角、聴覚半径、遮蔽判定を同時に確認してください。

---

### 练习 14

> [!question] 题目
> 今天如果你不太累，我们回家前顺路去超市买点水果吧。

> [!example] 自己的答案
>　まだ疲れていないなら、帰る前にスーパーで果物を買いましょう

> [!warning] 指正
> 日常意思能传达，但漏了“今天”和“回家前顺路”。`まだ疲れていないなら` 是“还没累的话”，题目是“不太累”，可说 `あまり疲れていなければ`。句末需要句号。

> [!tip] 参考回答
> 今日、あまり疲れていなければ、帰る前にスーパーに寄って果物を少し買いましょう。

---

### 练习 15

> [!question] 题目
> 我先整理这次修改的确认事项，下午会议上再和大家一起过一遍。

> [!example] 自己的答案
>　まず変更した確認事項を整理し、午後会議で皆さんと一緒に確認します。

> [!warning] 指正
> 职场意思基本清楚。`変更した確認事項` 听起来像“已经被修改的确认事项”，题目是“这次修改的确认事项”，建议说 `今回の変更に関する確認事項`。`午後会議で` 应为 `午後の会議で`。`過一遍` 用 `一緒に確認します` 可以表达，也可说 `一通り確認します`。

> [!tip] 参考回答
> まず今回の変更に関する確認事項を整理して、午後の会議で皆さんと一緒に一通り確認します。

## 2026-08-04

### 练习 1

> [!question] 题目
> 在 Unreal 中调整角色落地反馈时，摄像机震动、音效和粒子效果应该由同一个落地事件触发。

> [!example] 自己的答案
> Unrealでキャラクターの着地フィードバックを調整する時に、カメラシェーク、効果音、パーティクルは同じ着地イベント経由で発火するべきです。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 2

> [!question] 题目
> Unity 的血条组件最好只负责显示当前数值，不要直接修改角色的生命值数据。

> [!example] 自己的答案
>　UnityのHPバーコンポーネントでは現数値の表示だけを担当し、キャラクターのHPプロパーティーを直接変更するのではありません。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 3

> [!question] 题目
> 如果攻击特效和命中判定时间不一致，请先比较动画通知帧和特效生成时机。

> [!example] 自己的答案
>　攻撃エフェクトとヒット判定時間が不一致している場合、Anim Notifyとエフェクトの生成タイミングを比較してください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 4

> [!question] 题目
> 在设计技能状态机时，每个状态都应该明确进入条件、退出条件和可取消规则。

> [!example] 自己的答案
>　スキルのステートマシンを設計する時に、各ステートが入る条件、出る条件とキャンセルルールを明確にするべきです。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 5

> [!question] 题目
> 为了降低 UI 列表刷新成本，可以只重建发生变化的条目，而不是每次重建全部控件。

> [!example] 自己的答案
>　UIリストの更新リストを軽減するため、変更されたセルだけをリビルドし、毎フレームウィジェット全体を再生成するのではありません。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 6

> [!question] 题目
> 加载 Boss 战场景前，请提前准备必要的动画、音效和战斗数据。

> [!example] 自己的答案
>　ボスのシーンをロードする前に、必要になったアニメーション、効果音、バトルデータを事前に揃えてください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 7

> [!question] 题目
> 当玩家打开地图界面时，角色输入应该暂停，但菜单导航仍然需要保持可用。

> [!example] 自己的答案
>　プレイヤーがマップ画面を開くときに、キャラクターの入力を一時的に止め、メニューナビーを操作可能ままで保持します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 8

> [!question] 题目
> 在实现装备词条随机生成时，请把数值范围、权重和稀有度规则放在配置表里。

> [!example] 自己的答案
>　装備のラベルの乱数生成を実装する時に　数値範囲、重さ、レアルを設定テーブルに配置できるようにしてください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 9

> [!question] 题目
> 如果敌人受击后没有进入硬直状态，请检查伤害事件、状态标签和动画过渡条件。

> [!example] 自己的答案
>　敵がダメージを受けた後硬直状態に入らない場合は、ダメージイベント、状態タグ、アニメーショントランジション条件を確認してください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 10

> [!question] 题目
> Unity 的相机碰撞处理应该平滑调整距离，避免镜头突然穿过墙体或跳到角色面前。

> [!example] 自己的答案
>　Unityでのカメラコリジョン処理は滑らかに距離を調整し、カメラが突然に壁にめり込んだりキャラクター目の前にワープしたりするのを避けます。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 11

> [!question] 题目
> 在分析渲染瓶颈时，请把 Draw Call 数量、材质切换和透明物体排序分别记录下来。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 12

> [!question] 题目
> 保存快捷栏数据时，需要同时保存物品实例编号、格子位置和当前数量。

> [!example] 自己的答案
>　ショットキーデータを保存する時に、オブジェクトのインスタンスID、セル位置、アイテム数を同時に保存する必要があります。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 13

> [!question] 题目
> 调试手柄操作时，请确认焦点移动顺序、确认键映射和取消键映射是否符合预期。

> [!example] 自己的答案
>　ゲームパッドをデバッグする時に、フォーカス移動順、確認キーのマッピング、キャンセルマッピングが意図的に合っていないかを確認してください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 14

> [!question] 题目
> 今天晚上如果你想换换心情，我们可以出去散步一会儿。

> [!example] 自己的答案
>　今晩に気分転換したいなら、一緒に散歩しましょう。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 15

> [!question] 题目
> 我会先把测试结果整理成表格，明天上午再请大家确认优先级。

> [!example] 自己的答案
>　まずテスト結果をテーブルに整理し、明日午前皆さんに優先度を確認していただきます。

> [!warning] 指正
>

> [!tip] 参考回答
>
