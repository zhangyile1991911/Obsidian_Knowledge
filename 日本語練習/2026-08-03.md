## 2026-08-03

### 练习 1

> [!question] 题目
> 在 Unreal 中实现攀爬系统时，角色移动模式和动画状态机应该通过明确的状态字段保持一致。

> [!example] 自己的答案
>　Unrealでクライミングシステムを実装する時に、キャラクター移動モードとアニメーションステートマシンは明確なステートフィールドを通じて一致します。

> [!warning] 指正
> 意思基本清楚。`実装する時に` 可改为更自然的 `実装するときは`。`ステートフィールド` 虽能理解，但日语技术说明里更常说 `状態フィールド`。`一致します` 像描述事实，题目是“应该保持一致”，建议用 `一致させるべきです` 或 `整合性を保つべきです`。

> [!tip] 参考回答
> Unrealでクライミングシステムを実装するときは、キャラクターの移動モードとアニメーションステートマシンを、明確な状態フィールドを通じて一致させるべきです。

---

### 练习 2

> [!question] 题目
> Unity 的战斗日志最好记录技能编号、命中对象和最终伤害值，方便之后排查结算问题。

> [!example] 自己的答案
>　UnityのバトルログではスキルID、ヒットしたターゲットと計算ダメージを記録したほうがいいです。後から計算バグを特定しやすくなります。

> [!warning] 指正
> 大意正确。`ヒットしたターゲットと計算ダメージ` 并列时建议写成 `ヒットした対象、最終ダメージ値`。题目里的“结算问题”不是普通计算 bug，更自然是 `集計問題`、`計算ミス` 或 `ダメージ計算の問題`。`ほうがいいです` 可以用，但技术建议里 `記録しておくとよいです` 更自然。

> [!tip] 参考回答
> Unityのバトルログには、スキルID、ヒットした対象、最終ダメージ値を記録しておくと、後から<mark style="background: #FF5582A6;">集計問題</mark>を調査しやすくなります。

---

### 练习 3

> [!question] 题目
> 如果角色阴影在移动中闪烁，请先比较骨骼网格边界、光源角度和阴影偏移设置。

> [!example] 自己的答案
>　キャラクターのシャドウが移動中にちらついている場合、先にスケルトンの境界、光源の角度、シャドウオフセットを比較してください。

> [!warning] 指正
> 主要意思能表达。`スケルトンの境界` 容易理解成骨架本身的边界，题目是“骨骼网格边界”，建议说 `スケルタルメッシュの境界` 或 `バウンズ`。`比較してください` 可以，但这里是排查设置，也可以用 `確認してください`。

> [!tip] 参考回答
> キャラクターの影が移動中にちらつく場合は、まずスケルタルメッシュの<mark style="background: #FF5582A6;">バウンズ</mark>、光源の角度、シャドウオフセット設定を比較してください。

---

### 练习 4

> [!question] 题目
> 在设计道具拾取流程时，检测范围、提示 UI 和背包写入应该分成独立步骤。

> [!example] 自己的答案
>　アイテムを拾う時に、検出範囲、提示UI、インベントリーの書き込みを独立した別々の流れにするべきです。

> [!warning] 指正
> 题意基本完整。`提示UI` 是中文词直接放进日语，建议用 `ヒントUI` 或 `案内UI`。`独立した別々の流れ` 有重复感，可以简化为 `独立したステップ`。题目是“设计流程时”，建议补出 `フローを設計するときは`。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">アイテム拾得</mark>フローを設計するときは、検出範囲、ヒントUI、インベントリへの書き込みを独立したステップに分けるべきです。

---

### 练习 5

> [!question] 题目
> 为了减少角色蓝图的复杂度，可以把攻击判定参数移动到数据资产里管理。

> [!example] 自己的答案
>　キャラクターのBlueprintの複雑度を減らすため、攻撃判定パラメータをデータアセットに移して管理します。

> [!warning] 指正
> 表达自然，题意也完整。`Blueprint` 可以用，但在日语文章里常写 `ブループリント`。`複雑度` 能懂，不过更自然的是 `複雑さ`。整体只需稍微调整语气即可。

> [!tip] 参考回答
> キャラクターのブループリントの複雑さを減らすために、攻撃判定パラメータをデータアセットに移して管理できます。

---

### 练习 6

> [!question] 题目
> 当技能预览范围发生变化时，地面指示器应该立即更新半径和颜色。

> [!example] 自己的答案
>　スキルプレビュー範囲が変化した場合、インジケータがすぐに半径と色を更新するべきです。

> [!warning] 指正
> 意思清楚。`インジケータ` 建议写成长音 `インジケーター`。题目是“地面指示器”，最好明确为 `地面インジケーター`。`変化した場合` 正确，也可用更自然的 `変わったときは`。

> [!tip] 参考回答
> スキルのプレビュー範囲が変わったときは、地面インジケーターの半径と色をすぐに更新するべきです。

---

### 练习 7

> [!question] 题目
> 在优化加载流程时，请把必须立即显示的资源和可以稍后显示的资源分开整理。

> [!example] 自己的答案
>　読み込みを最適化する時に、すぐに表示するリソースと後から表示するリソースを分けてまとめます。

> [!warning] 指正
> 主要意思正确。`読み込みを最適化する時に` 可以改为 `ロード処理を最適化するときは`，更贴近技术语境。`すぐに表示するリソース` 和 `後から表示するリソース` 能表达题意；如果强调“必须”，可加 `必要がある`。

> [!tip] 参考回答
> ロード処理を最適化するときは、すぐに表示する必要があるリソースと、後から表示できるリソースを分けて整理してください。

---

### 练习 8

> [!question] 题目
> 如果技能图标的冷却遮罩没有正确变化，请检查计时数据和材质参数是否同时更新。

> [!example] 自己的答案
>　スキルアイコンのマスクが正しく変化しない場合、タイマーデータをマテリアルデータが同時に更新されたかを確認してください。

> [!warning] 指正
> 题意有一处连接错误。`タイマーデータをマテリアルデータが` 中的 `を` 应为 `と`，并且题目是“材质参数”，不是 `マテリアルデータ`。`スキルアイコンのマスク` 少了“冷却”，建议说 `クールダウンマスク`。

> [!tip] 参考回答
> スキルアイコンのクールダウンマスクが正しく変化しない場合は、タイマーデータとマテリアルパラメータが同時に更新されているか確認してください。

---

### 练习 9

> [!question] 题目
> 在实现连击窗口时，动画通知只负责标记时间点，具体输入判断应该放在 gameplay 代码里。

> [!example] 自己的答案
>　ウィンドウの連打を実装する時に、Anim Notifyはタイミングをつけるだけで、具体的入力判定をGameplayコードに実装するべきです。

> [!warning] 指正
> “连击窗口”误译了。`ウィンドウの連打` 会变成“窗口的连打”，应说 `コンボ受付時間` 或 `コンボウィンドウ`。`タイミングをつける` 不自然，动画通知“标记时间点”可说 `タイミングを示す`。`具体的入力判定` 前面建议加 `な` 或改为 `具体的な入力判定`。

> [!tip] 参考回答
> コンボ受付時間を実装するときは、Anim Notifyはタイミングを示すだけにし、具体的な入力判定はGameplayコード側に実装するべきです。

---

### 练习 10

> [!question] 题目
> Unity 的对象生成器应该从配置读取预制体和初始参数，避免在场景脚本里硬编码。

> [!example] 自己的答案
> Unityのオブジェクト生成は設定からプレハブと初期パラメータを読み込むするべきです。シーン内のスクリプトでハードコードしないようにします。

> [!warning] 指正
> 意思基本能传达，但 `読み込むするべきです` 是错误搭配，应为 `読み込むべきです`。`Unityのオブジェクト生成` 少了“生成器”，建议用 `オブジェクト生成器` 或 `スポナー`。第二句可以和第一句合并，让“避免硬编码”的逻辑更自然。

> [!tip] 参考回答
> Unityのオブジェクト生成器は、設定からプレハブと初期パラメータを読み込み、シーン内のスクリプトにハードコードしないようにするべきです。

---

### 练习 11

> [!question] 题目
> 如果帧时间偶尔出现尖峰，请把粒子数量、动画更新和垃圾回收分别记录到分析表里。

> [!example] 自己的答案
>　フレーム時間がたまにピーク値が出る場合、パーティクルの数、アニメーション更新、ガベージコレクションを分析テーブルにそれぞれ記録します

> [!warning] 指正
> 大意正确。`フレーム時間がたまにピーク値が出る` 不自然，建议说 `フレーム時間にたまにスパイクが出る`。`分析テーブル` 能懂，但性能分析里更自然是 `分析表` 或 `プロファイル用の表`。句末缺少句号。

> [!tip] 参考回答
> フレーム時間にたまにスパイクが出る場合は、パーティクル数、アニメーション更新、ガベージコレクションを分析表にそれぞれ記録してください。

---

### 练习 12

> [!question] 题目
> 在制作存档菜单时，自动保存槽和手动保存槽应该使用不同的显示标签。

> [!example] 自己的答案
>　セーブデータメニューを作成するときに、自動セーブスラットと修道セーブスラットに別々のラベルを使うべきです。

> [!warning] 指正
> 题意基本在，但有两个拼写问题。`セーブスラット` 应为 `セーブスロット`，`修道セーブ` 应为 `手動セーブ`。`セーブデータメニュー` 会偏向“存档数据菜单”，题目是“存档菜单”，可说 `セーブメニュー`。

> [!tip] 参考回答
> セーブメニューを作成するときは、自動セーブスロットと手動セーブスロットに別々の表示ラベルを使うべきです。

---

### 练习 13

> [!question] 题目
> 调试敌人警戒范围时，请同时确认视野角度、听觉半径和遮挡判断。

> [!example] 自己的答案
>　敵の警戒範囲をデバッグする時に、視野の角度、聴覚の半径、遮蔽判断を同時に確認してください。

> [!warning] 指正
> 意思完整。`聴覚の半径` 可以理解，但 AI 感知语境里更自然是 `聴覚範囲` 或 `聴覚半径`。`遮蔽判断` 能表达“遮挡判断”，也可以说 `遮蔽判定`。整体很接近自然技术表达。

> [!tip] 参考回答
> 敵の警戒範囲をデバッグするときは、視野角、聴覚半径、遮蔽判定を同時に確認してください。

---

### 练习 14

> [!question] 题目
> 今天如果你不太累，我们回家前顺路去超市买点水果吧。

> [!example] 自己的答案
>　まだ疲れていないなら、帰る前にスーパーで果物を買いましょう

> [!warning] 指正
> 日常意思能传达，但漏了“今天”和“回家前顺路”。`まだ疲れていないなら` 是“还没累的话”，题目是“不太累”，可说 `あまり疲れていなければ`。句末需要句号。

> [!tip] 参考回答
> 今日、あまり疲れていなければ、帰る前にスーパーに寄って果物を少し買いましょう。

---

### 练习 15

> [!question] 题目
> 我先整理这次修改的确认事项，下午会议上再和大家一起过一遍。

> [!example] 自己的答案
>　まず変更した確認事項を整理し、午後会議で皆さんと一緒に確認します。

> [!warning] 指正
> 职场意思基本清楚。`変更した確認事項` 听起来像“已经被修改的确认事项”，题目是“这次修改的确认事项”，建议说 `今回の変更に関する確認事項`。`午後会議で` 应为 `午後の会議で`。`過一遍` 用 `一緒に確認します` 可以表达，也可说 `一通り確認します`。

> [!tip] 参考回答
> まず今回の変更に関する確認事項を整理して、午後の会議で皆さんと一緒に一通り確認します。

## 2026-08-04

### 练习 1

> [!question] 题目
> 在 Unreal 中调整角色落地反馈时，摄像机震动、音效和粒子效果应该由同一个落地事件触发。

> [!example] 自己的答案
> Unrealでキャラクターの着地フィードバックを調整する時に、カメラシェーク、効果音、パーティクルは同じ着地イベント経由で発火するべきです。

> [!warning] 指正
> 意思完整。`調整する時に` 建议写成 `調整するときは`。`発火する` 在程序语境可用，但事件触发反馈时更自然是 `トリガーされる` 或 `発生させる`。`同じ着地イベント経由で` 可以理解，也可说 `同じ着地イベントから`。

> [!tip] 参考回答
> Unrealでキャラクターの着地フィードバックを調整するときは、カメラシェイク、効果音、パーティクルエフェクトを同じ着地イベントからトリガーするべきです。

---

### 练习 2

> [!question] 题目
> Unity 的血条组件最好只负责显示当前数值，不要直接修改角色的生命值数据。

> [!example] 自己的答案
>　UnityのHPバーコンポーネントでは現数値の表示だけを担当し、キャラクターのHPプロパーティーを直接変更するのではありません。

> [!warning] 指正
> 题意基本正确。`現数値` 不自然，建议用 `現在の値` 或 `現在値`。`HPプロパーティー` 中 `プロパーティー` 应为 `プロパティ`。后半 `変更するのではありません` 可用，但建议与前半合成更自然的对比句。

> [!tip] 参考回答
> UnityのHPバーコンポーネントは現在の値の表示だけを担当し、キャラクターのHPデータを直接変更するべきではありません。

---

### 练习 3

> [!question] 题目
> 如果攻击特效和命中判定时间不一致，请先比较动画通知帧和特效生成时机。

> [!example] 自己的答案
>　攻撃エフェクトとヒット判定時間が不一致している場合、Anim Notifyとエフェクトの生成タイミングを比較してください。

> [!warning] 指正
> 大意正确。`不一致している` 不太自然，通常说 `一致していない`。`ヒット判定時間` 建议说 `ヒット判定のタイミング`，因为题目强调时间点。`Anim Notify` 和 `エフェクトの生成タイミング` 用得合适。

> [!tip] 参考回答
> 攻撃エフェクトとヒット判定のタイミングが一致していない場合は、まずAnim Notifyのフレームとエフェクトの生成タイミングを比較してください。

---

### 练习 4

> [!question] 题目
> 在设计技能状态机时，每个状态都应该明确进入条件、退出条件和可取消规则。

> [!example] 自己的答案
>　スキルのステートマシンを設計する時に、各ステートが入る条件、出る条件とキャンセルルールを明確にするべきです。

> [!warning] 指正
> 意思清楚。`各ステートが入る条件、出る条件` 能懂，但技术说明里更自然是 `各ステートの開始条件、終了条件` 或 `遷移条件`。并列时 `出る条件とキャンセルルール` 建议改为顿号连接。`設計する時に` 可写成 `設計するときは`。

> [!tip] 参考回答
> スキルのステートマシンを設計するときは、各ステートの<mark style="background: #FF5582A6;">開始条件、終了条件、キャンセル</mark>可能なルールを明確にするべきです。

---

### 练习 5

> [!question] 题目
> 为了降低 UI 列表刷新成本，可以只重建发生变化的条目，而不是每次重建全部控件。

> [!example] 自己的答案
>　UIリストの更新リストを軽減するため、変更されたセルだけをリビルドし、毎フレームウィジェット全体を再生成するのではありません。

> [!warning] 指正
> 题意有几处偏差。`更新リストを軽減` 应为 `更新コストを下げる` 或 `軽減する`。题目说“发生变化的条目”，不是 `セル`，UI 列表里更自然是 `項目`。题目是“每次重建全部控件”，你的 `毎フレーム` 多加了“每帧”，应改为 `毎回`。

> [!tip] 参考回答
> UIリストの更新コストを下げるために、毎回すべてのウィジェットを再生成するのではなく、変更された項目だけを再構築できます。

---

### 练习 6

> [!question] 题目
> 加载 Boss 战场景前，请提前准备必要的动画、音效和战斗数据。

> [!example] 自己的答案
>　ボスのシーンをロードする前に、必要になったアニメーション、効果音、バトルデータを事前に揃えてください。

> [!warning] 指正
> 意思基本正确。`ボスのシーン` 稍泛，题目是“Boss 战场景”，更自然是 `ボス戦シーン`。`必要になった` 是“变得需要了”，这里应为 `必要な`。`揃えてください` 可以，但加载前准备资源也常说 `用意しておいてください`。

> [!tip] 参考回答
> ボス戦シーンをロードする前に、必要なアニメーション、効果音、戦闘データを事前に用意しておいてください。

---

### 练习 7

> [!question] 题目
> 当玩家打开地图界面时，角色输入应该暂停，但菜单导航仍然需要保持可用。

> [!example] 自己的答案
>　プレイヤーがマップ画面を開くときに、キャラクターの入力を一時的に止め、メニューナビーを操作可能ままで保持します。

> [!warning] 指正
> 主要意思能传达。`メニューナビー` 不自然，应说 `メニューのナビゲーション`。`操作可能ままで保持します` 语法不对，建议写成 `操作可能なままにしておく必要があります`。前半 `一時的に止め` 可以表达暂停。

> [!tip] 参考回答
> プレイヤーがマップ画面を開いたときは、キャラクター入力を一時停止しつつ、メニューのナビゲーションは操作可能なままにしておく必要があります。

---

### 练习 8

> [!question] 题目
> 在实现装备词条随机生成时，请把数值范围、权重和稀有度规则放在配置表里。

> [!example] 自己的答案
>　装備のラベルの乱数生成を実装する時に　数値範囲、重さ、レアルを設定テーブルに配置できるようにしてください。

> [!warning] 指正
> 这里有几个关键术语不准确。装备“词条”不是 `ラベル`，更自然是 `オプション`、`付与効果` 或 `プロパティ`。`乱数生成` 可理解，但“随机生成”通常说 `ランダム生成`。`重さ` 应为 `重み`，`レアル` 应为 `レア度`。句中 `時に　` 有多余空格。

> [!tip] 参考回答
> 装備オプションのランダム生成を実装するときは、数値範囲、重み、レア度のルールを設定テーブルに入れてください。

---

### 练习 9

> [!question] 题目
> 如果敌人受击后没有进入硬直状态，请检查伤害事件、状态标签和动画过渡条件。

> [!example] 自己的答案
>　敵がダメージを受けた後硬直状態に入らない場合は、ダメージイベント、状態タグ、アニメーショントランジション条件を確認してください。

> [!warning] 指正
> 表达自然，题意也完整。`ダメージを受けた後硬直状態に` 中间建议加 `に` 或改成 `ダメージを受けた後に`。`アニメーショントランジション条件` 可以用，若写日语资料也可说 `アニメーション遷移条件`。

> [!tip] 参考回答
> 敵がダメージを受けた後に硬直状態に入らない場合は、ダメージイベント、状態タグ、アニメーション遷移条件を確認してください。

---

### 练习 10

> [!question] 题目
> Unity 的相机碰撞处理应该平滑调整距离，避免镜头突然穿过墙体或跳到角色面前。

> [!example] 自己的答案
>　Unityでのカメラコリジョン処理は滑らかに距離を調整し、カメラが突然に壁にめり込んだりキャラクター目の前にワープしたりするのを避けます。

> [!warning] 指正
> 意思基本完整。`突然に` 应改为 `突然`。`壁にめり込む` 比“穿过墙体”更像“陷进墙里”，可接受；更贴题可说 `壁を突き抜ける`。`キャラクター目の前` 应为 `キャラクターの目の前`。整体建议把“平滑调整距离”放在句首主干。

> [!tip] 参考回答
> Unityのカメラコリジョン処理では、距離を滑らかに調整し、カメラが突然壁を突き抜けたり、キャラクターの目の前に飛んだりしないようにするべきです。

---

### 练习 11

> [!question] 题目
> 在分析渲染瓶颈时，请把 Draw Call 数量、材质切换和透明物体排序分别记录下来。

> [!example] 自己的答案
>　レンダリングボトルネックを分析する時に、ドロー数、マテリアル切り替え、透明オブジェクトの順番をそれぞれ記録してください。

> [!warning] 指正
> `ドロー回数` 能懂，但题目明确是 Draw Call 数量，面试/技术说明里建议直接说 `Draw Call数`，更准确。

> [!tip] 参考回答
> レンダリングボトルネックを分析する時に、Draw Call数、マテリアル切り替え、透明オブジェクトのソートをそれぞれ記録してください。

---

### 练习 12

> [!question] 题目
> 保存快捷栏数据时，需要同时保存物品实例编号、格子位置和当前数量。

> [!example] 自己的答案
>　ショットキーデータを保存する時に、オブジェクトのインスタンスID、セル位置、アイテム数を同時に保存する必要があります。

> [!warning] 指正
> 题意大致能懂，但 `ショットキー` 应为 `ショートカットキー`，如果指游戏快捷栏，更自然是 `ショートカットバー`。`オブジェクトのインスタンスID` 不如 `アイテムインスタンスID` 贴切。`アイテム数` 可以，但题目是“当前数量”，建议用 `現在の数量`。

> [!tip] 参考回答
> ショートカットバーのデータを保存するときは、アイテムインスタンスID、スロット位置、現在の数量を同時に保存する必要があります。

---

### 练习 13

> [!question] 题目
> 调试手柄操作时，请确认焦点移动顺序、确认键映射和取消键映射是否符合预期。

> [!example] 自己的答案
>　ゲームパッドをデバッグする時に、フォーカス移動順、確認キーのマッピング、キャンセルマッピングが意図的に合っていないかを確認してください。

> [!warning] 指正
> 主要意思有了，但后半逻辑反了。`意図的に合っていないか` 是“是否故意不匹配”，题目是“是否符合预期”，应说 `意図どおりになっているか`。`ゲームパッドをデバッグする` 可理解，但更自然是 `ゲームパッド操作をデバッグする`。`キャンセルマッピング` 建议补成 `キャンセルキーのマッピング`。

> [!tip] 参考回答
> ゲームパッド操作をデバッグするときは、フォーカスの移動順、決定キーのマッピング、キャンセルキーのマッピングが<mark style="background: #FF5582A6;">意図どおりになっているか</mark>確認してください。

---

### 练习 14

> [!question] 题目
> 今天晚上如果你想换换心情，我们可以出去散步一会儿。

> [!example] 自己的答案
>　今晩に気分転換したいなら、一緒に散歩しましょう。

> [!warning] 指正
> 日常意思基本清楚。`今晩に` 不自然，直接说 `今晩` 或 `今日の夜`。题目是“出去散步一会儿”，你的句子少了“出去”和“一会儿”的感觉，可以加 `少し外を散歩する`。`気分転換したいなら` 很自然。

> [!tip] 参考回答
> 今日の夜、気分転換したいなら、少し外を散歩しましょう。

---

### 练习 15

> [!question] 题目
> 我会先把测试结果整理成表格，明天上午再请大家确认优先级。

> [!example] 自己的答案
>　まずテスト結果をテーブルに整理し、明日午前皆さんに優先度を確認していただきます。

> [!warning] 指正
> 职场意思基本完整。`テーブルに整理` 能懂，但文档/汇报语境更自然是 `表にまとめる`。`明日午前` 应为 `明日の午前中に`。`優先度を確認していただきます` 稍像单方面安排，对同事更自然是 `確認していただければと思います` 或 `確認をお願いします`。

> [!tip] 参考回答
> まずテスト結果を表にまとめて、明日の午前中に皆さんに優先度を確認していただければと思います。

## 2026-08-05

### 练习 1

> [!question] 题目
> 在 Unreal 中实现翻滚闪避时，根运动、碰撞检测和无敌时间应该使用同一个动作状态管理。

> [!example] 自己的答案
> Unrealでローリング回避を実装する時に、ルートモーション、コリジョン検出と無効時間は同じアクション状態で管理します。

> [!warning] 指正
> 意思基本清楚。`無効時間` 会让人想到“无效时间”，游戏术语里“无敌时间”更自然是 `無敵時間`。`コリジョン検出と無効時間は` 并列时建议用顿号连接，最后用 `同じアクション状態で管理するべきです` 更贴近“应该”。

> [!tip] 参考回答
> Unrealでローリング回避を実装するときは、ルートモーション、コリジョン検出、無敵時間を同じアクション状態で管理するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的敌人生成点应该根据关卡阶段启用或停用，避免所有敌人一开始就出现。

> [!example] 自己的答案
>　Unityの敵の生成ポイントはステージフィーズによってオン、オフにし、最初に全ての敵が出現することを避けます。

> [!warning] 指正
> 题意能传达。`ステージフィーズ` 应为 `ステージフェーズ` 或 `レベルの進行段階`。`オン、オフにし` 可以用，但技术说明里 `有効化・無効化する` 更自然。`最初に全ての敵が出現することを避けます` 可改为 `最初からすべての敵が出現しないようにします`。

> [!tip] 参考回答
> Unityの敵生成ポイントは、レベルの進行段階に応じて有効化・無効化し、<mark style="background: #FF5582A6;">最初から</mark>すべての敵が出現しないようにするべきです。

---

### 练习 3

> [!question] 题目
> 如果角色材质在受击后没有变红，请检查动态材质实例和受击事件是否正确关联。

> [!example] 自己的答案
>　キャラクターのマテリアルは被弾した後に赤くなっていない場合、マテリアルインスタンスと被弾イベントは関連しているかを確認してください。

> [!warning] 指正
> 大意正确。开头 `キャラクターのマテリアルは...赤くなっていない場合` 主语和条件连接不自然，建议说 `キャラクターのマテリアルが...赤くならない場合`。题目是“动态材质实例”，应明确为 `動的マテリアルインスタンス`。`イベントは関連しているか` 应为 `イベントと関連付いているか`。

> [!tip] 参考回答
> キャラクターのマテリアルが被弾後に赤くならない場合は、動的マテリアルインスタンスと被弾イベントが正しく<mark style="background: #FF5582A6;">関連付いているか</mark>確認してください。

---

### 练习 4

> [!question] 题目
> 在设计任务系统时，任务条件、奖励内容和完成状态最好分别放在清晰的数据结构里。

> [!example] 自己的答案
>　クエストシステムを設計する時に、クエスト条件、報酬内容、完了状態を明確なデータ構造に入れるのが望ましいです。

> [!warning] 指正
> 表达自然，意思完整。`入れる` 能懂，但数据设计语境里 `格納する` 或 `分けて保持する` 更自然。`望ましいです` 用得合适。

> [!tip] 参考回答
> クエストシステムを設計するときは、クエスト条件、報酬内容、完了状態をそれぞれ明確な<mark style="background: #FF5582A6;">データ構造に格納する</mark>のが望ましいです。

---

### 练习 5

> [!question] 题目
> 为了减少 Tick 中的处理量，可以把不需要每帧判断的逻辑改成事件驱动。

> [!example] 自己的答案
>　Tick内の処理を減らすため、毎フレームで行う必要が無い判定ロジックをイベント通知に変えます。

> [!warning] 指正
> 主要意思正确。`毎フレームで行う必要が無い判定ロジック` 可以更自然地说成 `毎フレーム判定する必要のないロジック`。题目是“事件驱动”，`イベント通知` 有些窄，建议用 `イベント駆動`。`変えます` 可换成 `変更できます`，语气更像建议。

> [!tip] 参考回答
> Tick内の処理量を減らすために、<mark style="background: #FF5582A6;">毎フレーム判定する必要のない</mark>ロジックはイベント駆動に変更できます。

---

### 练习 6

> [!question] 题目
> 制作技能瞄准 UI 时，鼠标位置、地面命中点和范围预览需要保持一致。

> [!example] 自己的答案
>　スキル照準UIを作成する時に、マウス位置、地面ヒットポイント、範囲プレビューを一致にする必要があります。

> [!warning] 指正
> 意思能传达。`スキル照準UI` 可以理解，但更自然是 `スキル照準用UI` 或 `スキルエイムUI`。`範囲プレビューを一致にする` 不自然，应说 `一致させる`。并列的三个对象都是“需要保持一致”，可以用 `整合性を保つ必要があります`。

> [!tip] 参考回答
> スキル照準用UIを作成するときは、マウス位置、地面のヒットポイント、範囲プレビューの<mark style="background: #FF5582A6;">整合性を保つ</mark>必要があります。

---

### 练习 7

> [!question] 题目
> 当道具图标从背包拖到快捷栏时，请同时更新显示位置和绑定的物品实例编号。

> [!example] 自己的答案
>　アイテムアイコンをインベントリーからショットキー欄にドラッグする時に、表示位置と紐付けたオブジェクトインスタンスID
>　を同時に更新してください

> [!warning] 指正
> 题意基本在，但术语有几处需要调整。`ショットキー欄` 应为 `ショートカットバー` 或 `クイックスロット`。`オブジェクトインスタンスID` 不如 `アイテムインスタンスID` 准确。第二行单独的 `を` 会让句子断开，建议不要换行拆开助词。句末需要句号。

> [!tip] 参考回答
> アイテムアイコンをインベントリから<mark style="background: #FF5582A6;">ショートカットバー</mark>へドラッグしたときは、表示位置と紐付けられたアイテムインスタンスIDを同時に更新してください。

---

### 练习 8

> [!question] 题目
> 在优化移动端渲染时，请优先比较阴影质量、后处理强度和半透明 UI 的开销。

> [!example] 自己的答案
>　モバイル端末でレンダリングを最適化する時に、シャドウ品質、ポストプロセスと半透明UIのコストを優先的に比較してください。

> [!warning] 指正
> 意思清楚。`モバイル端末でレンダリングを最適化する時に` 可改为 `モバイル向けのレンダリングを最適化するときは`。`ポストプロセス` 可以用，但题目是“后处理强度”，建议说 `ポストプロセスの強度`。整体表达已经比较自然。

> [!tip] 参考回答
> モバイル向けのレンダリングを最適化するときは、シャドウ品質、ポストプロセスの強度、半透明UIのコストを優先的に比較してください。

---

### 练习 9

> [!question] 题目
> 如果存档读取后角色位置错误，请确认坐标系、关卡名称和出生点编号是否一致。

> [!example] 自己的答案
>　セーブデータを読み込んだ後にキャラクターの位置ずれた場合、座標、ステージ名とスパーンポイントIDが一致しているかを確認してください

> [!warning] 指正
> 大意正确。`キャラクターの位置ずれた場合` 缺少 `が`，应为 `位置がずれた場合`。`座標` 可以，但题目强调坐标系，更准确是 `座標系`。`スパーンポイントID` 拼写应为 `スポーンポイントID`。句末需要句号。

> [!tip] 参考回答
> セーブデータを読み込んだ後にキャラクターの位置がずれている場合は、<mark style="background: #FF5582A6;">座標系</mark>、レベル名、スポーンポイントIDが一致しているか確認してください。

---

### 练习 10

> [!question] 题目
> Unity 的动画控制器不应该直接处理伤害结算，只需要根据状态参数播放对应动画。

> [!example] 自己的答案
>　Unityのアニメーションコントローラはダメージ判定を直接するのではありません、ステートパラメータに応じてアニメーションを再生するだけです。

> [!warning] 指正
> 题意能表达，但 `ダメージ判定` 偏向“伤害判定”，题目是“伤害结算”，建议用 `ダメージ計算` 或 `ダメージ処理`。`直接するのではありません` 不自然，应说 `直接行うべきではありません`。逗号连接两个完整句子时，日语里建议用句号或 `、` 后接从句。

> [!tip] 参考回答
> Unityのアニメーションコントローラーはダメージ計算を直接行うべきではなく、ステートパラメータに応じて対応するアニメーションを再生するだけにするべきです。

---

### 练习 11

> [!question] 题目
> 在调试 Boss 技能阶段切换时，请记录当前血量比例、阶段编号和正在播放的蒙太奇。

> [!example] 自己的答案
>　ボスがスキルを切り替えることをデバッグする時に、現在のHPパーセント、フェーズ番号、再生中のMontageを記録してください。

> [!warning] 指正
> 意思基本清楚。`ボスがスキルを切り替えることをデバッグする` 不自然，题目是“阶段切换”，建议说 `ボスのスキルフェーズ切り替えをデバッグする`。`HPパーセント` 能懂，但更自然是 `HP割合`。`Montage` 在 Unreal 语境可用，也可以写 `モンタージュ`。

> [!tip] 参考回答
> ボスのスキルフェーズ切り替えをデバッグするときは、現在のHP割合、フェーズ番号、再生中のモンタージュを記録してください。

---

### 练习 12

> [!question] 题目
> 如果资源加载完成后 UI 仍显示等待状态，请检查完成标志和界面刷新时机。

> [!example] 自己的答案
>　リソースロードが完了した後もUIが待ち状態であり続いている場合には、完了フラグと画面更新タイミングを確認してください。

> [!warning] 指正
> 表达基本自然。`待ち状態であり続いている` 有些生硬，建议用 `待機状態のままになっている`。`リソースロード` 可以用，若想更自然可写 `リソースのロード`。`画面更新タイミング` 很贴近题意。

> [!tip] 参考回答
> リソースのロードが完了した後もUIが待機状態のままになっている場合は、完了フラグと画面更新タイミングを確認してください。

---

### 练习 13

> [!question] 题目
> 在实现可破坏物体时，外观变化、碰撞关闭和掉落奖励应该按固定顺序执行。

> [!example] 自己的答案
>　破壊可能オブジェクトを実装する時に、外見の変化、コリジョン無効化、ドロップ報酬を固定順番で実行します

> [!warning] 指正
> 主要意思正确。`固定順番` 不自然，应说 `固定の順序` 或 `決まった順序`。`ドロップ報酬` 可以表达“掉落奖励”。句末缺少句号。

> [!tip] 参考回答
> 破壊可能オブジェクトを実装するときは、外見の変化、コリジョンの無効化、ドロップ報酬を固定の順序で実行するべきです。

---

### 练习 14

> [!question] 题目
> 如果明天早上天气不错，我们早点出门，顺便在车站附近吃早餐吧。

> [!example] 自己的答案
>　明日朝は天気が良ければ、早めに出かけて、ついでに駅の近くで朝ごはんを食べましょう。

> [!warning] 指正
> 日常表达很自然，意思完整。`明日朝は` 可用，不过更口语的是 `明日の朝`。`ついでに駅の近くで朝ごはんを食べましょう` 很自然；如果强调“顺便”，也可以说 `駅の近くで朝ごはんも食べましょう`。

> [!tip] 参考回答
> 明日の朝、天気が良ければ、早めに出かけて、ついでに駅の近くで朝ごはんを食べましょう。

---

### 练习 15

> [!question] 题目
> 我先把今天讨论过的问题整理成清单，晚点发给大家确认有没有遗漏。

> [!example] 自己的答案
>　本日に検討済み問題をリストにまとめておき、後で皆さんに送って抜け漏れがないかを確認していただきます。

> [!warning] 指正
> 职场意思能传达。`本日に` 不自然，应说 `今日`。`検討済み問題` 像“已经研究完的问题”，题目是“今天讨论过的问题”，建议用 `今日話し合った問題` 或 `今日議論した課題`。`後で` 可以，但职场语气里 `後ほど` 更自然。整体敬语方向正确。

> [!tip] 参考回答
> まず今日話し合った問題をリストにまとめておき、後ほど皆さんに送って、抜け漏れがないか確認していただきます。

## 2026-08-06

### 练习 1

> [!question] 题目
> 在 Unreal 中实现角色锁定目标时，目标筛选、摄像机朝向和输入方向应该使用同一份锁定状态。

> [!example] 自己的答案
>　Unrealでキャラクターのターゲットロックを実装する時に、ターゲットフィルター、カメラ向き、入力方向は同じロック状態を使用するべきです。

> [!warning] 指正
> 意思基本清楚。`ターゲットフィルター` 更像“筛选器”，题目里的“目标筛选”可说 `ターゲットの絞り込み`。`同じロック状態を使用する` 能懂，但技术说明里 `同じロック状態を参照する` 更自然。

> [!tip] 参考回答
> Unrealでキャラクターのターゲットロックを実装するときは、<mark style="background: #FF5582A6;">ターゲットの絞り込み</mark>、カメラの向き、入力方向が同じロック状態を参照するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的拾取提示框应该根据玩家距离切换显示状态，而不是一直挂在场景物体上。

> [!example] 自己的答案
>　Unityの獲得提示はプレイヤーとの距離に応じて表示状態を切り替え、シーンオブジェクト上に表示しているままではありません。

> [!warning] 指正
> 题意大致能传达。`獲得提示` 不自然，拾取提示框更适合说 `拾得ヒント`、`ピックアップ表示` 或 `拾得用のヒントUI`。`シーンオブジェクト上に表示しているままではありません` 语法不自然，建议改成 `表示したままにするべきではありません`。

> [!tip] 参考回答
> Unityの<mark style="background: #FF5582A6;">拾得</mark>用ヒントUIは、プレイヤーとの距離に応じて表示状態を切り替え、シーン上のオブジェクトに表示したままにするべきではありません。

---

### 练习 3

> [!question] 题目
> 如果近战攻击经常打空，请先确认胶囊体尺寸、检测起点和动画播放速率。

> [!example] 自己的答案
>　近接攻撃はよく外れる場合、カプセルのサイズ、検出スタート、アニメーション再生速度を先に確認してください。

> [!warning] 指正
> 意思能理解。`近接攻撃はよく外れる場合` 应改为 `近接攻撃がよく外れる場合は`。`検出スタート` 不自然，题目是“检测起点”，建议说 `検出開始位置`。`先に` 可以用，但放在句首或 `まず` 更自然。

> [!tip] 参考回答
> 近接攻撃がよく外れる場合は、まずカプセルのサイズ、検出開始位置、アニメーションの再生速度を確認してください。

---

### 练习 4

> [!question] 题目
> 在设计技能冷却逻辑时，剩余时间、可用状态和 UI 显示文本应该从同一份数据计算。

> [!example] 自己的答案
>　スキルクールダウンロジックを設計する時に、残り時間、操作可能状態、UI表示テキストは同一データ計算から取得するべきです。

> [!warning] 指正
> 主要意思正确。`操作可能状態` 会偏向“可操作状态”，题目是技能“可用状态”，建议用 `使用可能状態`。`同一データ計算から取得する` 语序不自然，应说 `同じデータから計算する`。`設計する時に` 可改为 `設計するときは`。

> [!tip] 参考回答
> スキルのクールダウンロジックを設計するときは、残り時間、使用可能状態、UIの表示テキストを同じデータから計算するべきです。

---

### 练习 5

> [!question] 题目
> 为了减少关卡脚本的分支，可以把机关类型和触发条件整理到数据表里。

> [!example] 自己的答案
>　ステージスクリプト内の分岐を減らすため、ギミックタイプとトリガー条件をデータテーブルにまとめます。

> [!warning] 指正
> 表达自然，题意完整。`ステージスクリプト` 可以理解，但游戏关卡语境里 `レベルスクリプト` 也很常用。`ギミックタイプ`、`トリガー条件`、`データテーブル` 都贴合技术语境。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">レベルスクリプト</mark>内の分岐を減らすために、ギミックの種類とトリガー条件をデータテーブルにまとめることができます。

---

### 练习 6

> [!question] 题目
> 当角色进入瞄准状态时，移动速度、准星大小和武器扩散值都需要平滑变化。

> [!example] 自己的答案
>　キャラクターがエイム状態に入る時に、移動速度、エイムインジケータの大きさ、武器の拡散をスムーズに変更する必要があります。

> [!warning] 指正
> 意思基本完整。`入る時に` 建议写成 `入るときは`。`エイムインジケータ` 可以表达准星，但更常见是 `照準`、`クロスヘア` 或 `レティクル`。`武器の拡散` 还可以更明确为 `武器の拡散値`。

> [!tip] 参考回答
> キャラクターがエイム状態に入るときは、移動速度、<mark style="background: #FF5582A6;">クロスヘア</mark>の大きさ、武器の拡散値をすべて滑らかに変化させる必要があります。

---

### 练习 7

> [!question] 题目
> 在优化背包界面时，请优先检查拖拽层级、图标重建次数和鼠标悬停事件。

> [!example] 自己的答案
>　インベントリー画面を最適化する時に、ドラッグレイヤー、アイコン再構築の回数、マウスホバーイベントを優先的に確認してください。

> [!warning] 指正
> 题意清楚。`インベントリー` 可用，但同一文件里也常写 `インベントリ`。`ドラッグレイヤー` 能懂，若强调 UI 层级可说 `ドラッグ中の表示階層`。`最適化する時に` 建议写成 `最適化するときは`。

> [!tip] 参考回答
> インベントリ画面を最適化するときは、ドラッグ中の表示階層、アイコンの再構築回数、マウスホバーイベントを優先的に確認してください。

---

### 练习 8

> [!question] 题目
> Unity 的对象池在归还对象时，应该重置位置、粒子状态和脚本内部计时器。

> [!example] 自己的答案
>　Unityのオブジェクトプールではオブジェクトを返却するときに、位置、パーティクルの状態、スクリプト内のタイマーをリセットするべきです。

> [!warning] 指正
> 表达自然，意思完整。`オブジェクトを返却するときに` 用得合适。`スクリプト内のタイマー` 也能准确表达“脚本内部计时器”。整体只需把句式稍微整理得更顺。

> [!tip] 参考回答
> Unityのオブジェクトプールでは、オブジェクトを返却するときに、位置、パーティクルの状態、スクリプト内部のタイマーをリセットするべきです。

---

### 练习 9

> [!question] 题目
> 如果敌人巡逻路线出现停顿，请比较路径点顺序、等待时间和转向插值设置。

> [!example] 自己的答案
>　敵がパトロールする時に止まり、パスポイント順番、待ち時間、回転補完設定を比較してください。

> [!warning] 指正
> 主要意思有偏差。`敵がパトロールする時に止まり` 像“敌人在巡逻时停下”，题目是“巡逻路线出现停顿”，建议说 `巡回ルートで停止が発生する場合`。`パスポイント` 应为 `パスポイント` 也可理解，但更自然是 `経路ポイント` 或 `ウェイポイント`。`回転補完` 应为 `回転補間`。

> [!tip] 参考回答
> 敵の巡回ルートで停止が発生する場合は、ウェイポイントの順序、待機時間、回転補間の設定を比較してください。

---

### 练习 10

> [!question] 题目
> 在制作伤害数字时，暴击颜色、上浮速度和消失时间应该由样式配置控制。

> [!example] 自己的答案
>　ダメージ数値を作成する時に、カラー、浮かび上がり速度、消え時間は仕様で制御します。

> [!warning] 指正
> 大意能传达，但漏掉了“暴击”的信息。`カラー` 太泛，应说 `クリティカル時の色`。`消え時間` 不自然，建议用 `消えるまでの時間` 或 `フェードアウト時間`。题目是“样式配置”，`仕様` 会偏成“规格”，这里更适合 `スタイル設定`。

> [!tip] 参考回答
> ダメージ数値を作成するときは、<mark style="background: #FFB86CA6;">クリティカル時の色</mark>、浮き上がる速度、消えるまでの時間をスタイル設定で制御するべきです。

---

### 练习 11

> [!question] 题目
> 调试动画混合问题时，请分别记录输入速度、角色朝向和当前播放的状态名称。

> [!example] 自己的答案
>　アニメーションブレンド不具合をデバッグする時に、入力速度、キャラクターの向き、再生中ステート名をそれぞれ記録してください。

> [!warning] 指正
> 意思基本完整。`アニメーションブレンド不具合` 可以理解，不过 `アニメーションブレンドの問題` 更自然。`再生中ステート名` 应为 `再生中のステート名`。`デバッグする時に` 建议写成 `デバッグするときは`。

> [!tip] 参考回答
> アニメーションブレンドの問題をデバッグするときは、入力速度、キャラクターの向き、現在再生中のステート名をそれぞれ記録してください。

---

### 练习 12

> [!question] 题目
> 如果场景切换后音效仍然残留，请检查音源生命周期和场景卸载时的清理流程。

> [!example] 自己的答案
>　シーンを切り替えた後効果音がまた残っている場合、オーディオのライフサイクルとシーンアンロードのフローを確認してください。

> [!warning] 指正
> 题意基本在。`また残っている` 是“又残留”，这里应说 `まだ残っている`。`オーディオのライフサイクル` 可以，但题目是“音源生命周期”，更准确是 `音源のライフサイクル`。`シーンアンロードのフロー` 可改为 `シーンアンロード時のクリーンアップ処理`。

> [!tip] 参考回答
> シーンを切り替えた後も効果音がまだ残っている場合は、音源のライフサイクルと、シーンアンロード時のクリーンアップ処理を確認してください。

---

### 练习 13

> [!question] 题目
> 在实现暂停菜单时，游戏时间暂停和 UI 动画播放需要分开处理。

> [!example] 自己的答案
>　メニューを実装する時に、ゲーム時間の停止とUIアニメーションの再生を別々に切り分けて処理する必要があります。

> [!warning] 指正
> 意思能传达。`メニュー` 少了“暂停”，建议明确为 `ポーズメニュー`。`ゲーム時間の停止` 可以，但更贴近题目是 `ゲーム時間の一時停止`。`別々に切り分けて処理する` 有重复感，写成 `分けて処理する` 即可。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">ポーズメニュー</mark>を実装するときは、ゲーム時間の一時停止とUIアニメーションの再生を分けて処理する必要があります。

---

### 练习 14

> [!question] 题目
> 如果今晚雨停了，我们就去附近那家店吃点热的东西吧。

> [!example] 自己的答案
>　夜に雨が止まったら、近くの店で暖かいものを食べにいきましょん

> [!warning] 指正
> 日常意思基本清楚，但有拼写和自然度问题。`夜に` 少了“今晚”的明确感，建议说 `今夜`。`暖かいもの` 多用于气温或衣物，食物应写 `温かいもの`。句末 `いきましょん` 应为 `行きましょう`。

> [!tip] 参考回答
> 今夜、雨が止んだら、近くのあのお店で温かいものを食べに行きましょう。

---

### 练习 15

> [!question] 题目
> 我先确认一下负责范围，之后再把需要协助的部分发给你。

> [!example] 自己的答案
>　まず担当範囲を確認し、後ほど協力必要な部分を送ります。

> [!warning] 指正
> 职场意思能传达。`協力必要な部分` 语法不自然，应说 `協力が必要な部分` 或更自然的 `サポートしてほしい部分`。`送ります` 可以，但对同事委托时 `共有します` 或 `送りますね` 更柔和。`まず担当範囲を確認し` 很自然。

> [!tip] 参考回答
> まず担当範囲を確認して、後ほどサポートしてほしい部分を共有します。

## 2026-08-07

### 练习 1

> [!question] 题目
> 在 Unreal 中实现受击硬直时，伤害结算、动画过渡和输入锁定应该由同一个受击状态控制。

> [!example] 自己的答案
> Unrealで被弾硬直を実装する時に、ダメージ計算、アニメーショントランジション、入力ロックは同じ被弾状態で制御するべきです。

> [!warning] 指正
> 意思完整，技术对象也对。`アニメーショントランジション` 可用，但在日语技术说明里也常说 `アニメーション遷移`。`同じ被弾状態で制御する` 表达清楚。

> [!tip] 参考回答
> Unrealで被弾硬直を実装するときは、ダメージ計算、アニメーション遷移、入力ロックを同じ被弾状態で制御するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的技能范围预览应该根据目标位置实时调整贴花大小，而不是每次重新生成整个预览对象。

> [!example] 自己的答案
>　Unityのスキル範囲プレビューはターゲット位置に応じてデーカルのサイズをリアルタイムで調整し、毎回プレビューオブジェクトを再生成するべきではありません。

> [!warning] 指正
> 表达自然，题意完整。`デーカル` 拼写应为 `デカール`。`リアルタイムで調整し` 很贴近“实时调整”。后半 `再生成するべきではありません` 也准确。

> [!tip] 参考回答
> Unityのスキル範囲プレビューは、ターゲット位置に応じてデカールのサイズをリアルタイムで調整し、毎回プレビューオブジェクト全体を再生成するべきではありません。

---

### 练习 3

> [!question] 题目
> 如果角色转向动画显得生硬，请比较输入方向、旋转插值速度和动画混合参数。

> [!example] 自己的答案
>　キャラクターの回転が不自然に見える場合、入力方向、回転補間値速度、アニメーションブレンドパラメータを比較してください。

> [!warning] 指正
> 主要意思正确。题目是“转向动画”，你的 `キャラクターの回転` 稍微少了动画层面的信息，建议说 `旋回アニメーション`。`回転補間値速度` 不自然，应为 `回転補間速度`。`不自然に見える` 可以表达“生硬”。

> [!tip] 参考回答
> キャラクターの<mark style="background: #FF5582A6;">旋回アニメーション</mark>が不自然に見える場合は、入力方向、回転補間速度、アニメーションブレンドパラメータを比較してください。

---

### 练习 4

> [!question] 题目
> 在设计任务奖励流程时，经验值、道具发放和界面提示应该拆成可单独验证的步骤。

> [!example] 自己的答案
>　クエスト報酬フローを設計するときに、経験値、アイテム配り、画面提示ごとに独立した検証可能なステップに切り分けます。

> [!warning] 指正
> 题意基本完整。`アイテム配り` 不自然，任务奖励里的“道具发放”建议说 `アイテム付与`。`画面提示` 是中文式表达，可改为 `画面表示` 或 `UI通知`。`設計するときに` 更自然是 `設計するときは`。

> [!tip] 参考回答
> クエスト報酬フローを設計するときは、経験値、アイテム付与、UI通知を、それぞれ単独で検証できるステップに切り分けるべきです。

---

### 练习 5

> [!question] 题目
> 为了减少技能蓝图的分支，可以把命中类型、反馈效果和冷却时间整理到数据资产中。

> [!example] 自己的答案
>　スキルBlueprint内の分岐を減らすため、ヒットタイプ、フィードバック、クールダウンをデータアセットにまとめるようにします。

> [!warning] 指正
> 大意正确。`スキルBlueprint` 可以理解，但统一日语文章时可写 `スキルブループリント`。`フィードバック` 稍泛，题目是“反馈效果”，建议说 `フィードバック効果`。`クールダウン` 最好补成 `クールダウン時間`。

> [!tip] 参考回答
> スキルブループリント内の分岐を減らすために、ヒットタイプ、フィードバック効果、クールダウン時間をデータアセットにまとめることができます。

---

### 练习 6

> [!question] 题目
> 当玩家切换武器时，当前弹药数、装填状态和准星样式都需要根据新武器重新计算。

> [!example] 自己的答案
>　プレイヤーが武器を切り替える時に、弾薬数、リロード状態、クロスヘアスタイルは新しい武器に応じて計算し直します。

> [!warning] 指正
> 意思清楚。`切り替える時に` 建议写成 `切り替えるときは`。`弾薬数` 少了“当前”，可说 `現在の弾薬数`。

> [!tip] 参考回答
> プレイヤーが武器を切り替えるときは、現在の弾薬数、リロード状態、クロスヘアのスタイルを新しい武器に応じて再計算する必要があります。

---

### 练习 7

> [!question] 题目
> 在优化小地图绘制时，请优先检查图标数量、更新频率和相机捕捉范围。

> [!example] 自己的答案
>　ミニマップの描画を最適化するときに、アイコンの数、更新頻度、カメラのキャプチャー範囲を優先的に確認してください。

> [!warning] 指正
> 表达自然，题意完整。`最適化するときに` 可改为 `最適化するときは`。`キャプチャー範囲` 可以用，也可以写成 `キャプチャ範囲`。整体只需微调助词和语气。

> [!tip] 参考回答
> ミニマップの描画を最適化するときは、アイコンの数、更新頻度、カメラのキャプチャ範囲を優先的に確認してください。

---

### 练习 8

> [!question] 题目
> Unity 的敌人生成点应该从关卡配置读取波次编号、生成间隔和最大存活数量。

> [!example] 自己的答案
>　Unityの敵のスパンポイントはレベル設定アセットからウェブ番号、生成間隔、最大持続時間を読み込むべきです。

> [!warning] 指正
> 有几个关键术语误译。`スパンポイント` 应为 `スポーンポイント`。`ウェブ番号` 是“网页编号”，题目是“波次编号”，应说 `ウェーブ番号`。`最大持続時間` 是“最大持续时间”，题目是“最大存活数量”，应为 `最大生存数` 或 `最大同時存在数`。

> [!tip] 参考回答
> Unityの敵スポーンポイントは、レベル設定からウェーブ番号、生成間隔、最大同時存在数を読み込むべきです。

---

### 练习 9

> [!question] 题目
> 如果技能音效播放时间偏早，请先确认动画事件帧、音频延迟和角色攻击速度倍率。

> [!example] 自己的答案
>　スキル効果音の再生が早すぎる場合、Anim Notify、オーディオ遅延、キャラクターの攻撃倍速を先に確認してください。

> [!warning] 指正
> 意思基本清楚。`早すぎる` 可以表达“偏早”，也可用更中性的 `早めに再生される`。题目是“动画事件帧”，`Anim Notify` 可以，但最好补出 `フレーム`。`攻撃倍速` 不自然，建议说 `攻撃速度倍率`。

> [!tip] 参考回答
> スキル効果音の再生タイミングが早すぎる場合は、まずAnim Notifyのフレーム、オーディオ遅延、キャラクターの攻撃速度倍率を確認してください。

---

### 练习 10

> [!question] 题目
> 在实现关卡机关复位时，移动平台位置、触发器状态和特效残留都应该恢复到初始值。

> [!example] 自己的答案
>　レベルリセットを実装する時は、移動プラットフォーム位置、トリガー状態、エフェクトの残留を初期値に戻すべきです。

> [!warning] 指正
> 大意正确，但题目是“关卡机关复位”，`レベルリセット` 会变成“关卡重置”，范围稍大。建议用 `レベルギミックのリセット`。`エフェクトの残留を初期値に戻す` 不太自然，特效残留通常说 `残っているエフェクトを消す` 或 `初期状態に戻す`。

> [!tip] 参考回答
> レベルギミックのリセットを実装するときは、移動プラットフォームの位置、トリガー状態、残っているエフェクトを初期状態に戻すべきです。

---

### 练习 11

> [!question] 题目
> 调试材质闪烁问题时，请分别记录材质实例参数、渲染排序和可见性切换时机。

> [!example] 自己的答案
>　マテリアルのチラつきをデバッグする時に、マテリアルパラメータ、描画順番、切り替えタイミングをそれぞれ記録してください。

> [!warning] 指正
> 主要意思能传达。`チラつき` 可写作 `ちらつき`，两者都能理解。题目是“材质实例参数”，`マテリアルパラメータ` 稍泛，建议说 `マテリアルインスタンスのパラメータ`。`描画順番` 不自然，应为 `描画順` 或 `レンダリング順`。漏了“可见性”的对象，建议补 `表示/非表示`。

> [!tip] 参考回答
> マテリアルのちらつきをデバッグするときは、マテリアルインスタンスのパラメータ、描画順、<mark style="background: #FF5582A6;">表示/非表示の切り替え</mark>タイミングをそれぞれ記録してください。

---

### 练习 12

> [!question] 题目
> 如果存档版本升级后装备属性缺失，请检查字段迁移、默认值补齐和旧数据兼容处理。

> [!example] 自己的答案
> セーブデータがアップグレード後装備の属性が不足した場合は、フィールド移行、デフォルト値補完、旧データ互換性処理を確認してください。

> [!warning] 指正
> 题意基本正确。`セーブデータがアップグレード後装備の属性が不足した場合` 结构不自然，建议改为 `セーブデータのバージョンアップ後に、装備属性が欠けている場合は`。`フィールド移行`、`デフォルト値補完`、`旧データ互換性処理` 都很贴近题意。

> [!tip] 参考回答
> セーブデータのバージョンアップ後に装備属性が欠けている場合は、フィールド移行、デフォルト値の補完、旧データとの互換処理を確認してください。

---

### 练习 13

> [!question] 题目
> 在制作连锁爆炸效果时，伤害范围、视觉延迟和二次触发次数应该使用明确的上限。

> [!example] 自己的答案
> チャーン爆発効果を作成する時に、ダメージ範囲、視覚遅延、次回発火回数を明確に上限を設定するべきです。

> [!warning] 指正
> 这里有几处会影响理解。`チャーン爆発` 应为 `連鎖爆発` 或 `チェーン爆発`。`次回発火回数` 不是“二次触发次数”，建议说 `二次トリガー回数` 或 `追加発火回数`。`明確に上限を設定する` 前面主语并列后，建议写成 `明確な上限を設定する`。

> [!tip] 参考回答
> 連鎖爆発エフェクトを作成するときは、ダメージ範囲、視覚的な遅延、二次トリガー回数に明確な上限を設定するべきです。

---

### 练习 14

> [!question] 题目
> 如果周末人不多，我们去那家新开的咖啡店坐一会儿吧。

> [!example] 自己的答案
>　週末に人が少ないなら、家の近く新しくできた喫茶店に行ってみましょう

> [!warning] 指正
> 日常意思能传达，但漏了“那家”和“坐一会儿”的感觉。`人が少ないなら` 可以，但“人不多”更自然是 `人が多くなければ`。`家の近く新しくできた` 缺少 `に` 或 `の`，应为 `近くに新しくできた`。句末缺少句号。

> [!tip] 参考回答
> 週末、人が多くなければ、あの新しくできたカフェに行って少し座っていきましょう。

---

### 练习 15

> [!question] 题目
> 我先确认一下今天的修改范围，确认后再把测试结果发到群里。

> [!example] 自己的答案
>　まず本日の改修範囲を確認しておき、確認済み後にテスト結果をチャンネルで共有します。

> [!warning] 指正
> 职场意思基本清楚。`本日の改修範囲` 很自然。`確認済み後に` 不自然，应说 `確認できたら` 或 `確認後に`。`チャンネルで共有します` 可以表达“发到群里”，如果是公司沟通语境也可说 `グループに共有します`。

> [!tip] 参考回答
> まず本日の改修範囲を確認して、確認できたらテスト結果をグループに共有します。

## 2026-08-08

### 练习 1

> [!question] 题目
> 在 Unreal 中实现锁定目标系统时，候选目标筛选、优先级计算和镜头朝向应该分成三个阶段。

> [!example] 自己的答案
>　Unrealでターゲットロックシステムを実装するときは、ターゲットの絞り込み、優先度の計算、カメラの向きを3つのステップに切り分けます。

> [!warning] 指正
> 意思完整，结构也自然。`ターゲットの絞り込み` 可以表达“候选目标筛选”，如果想更贴近题目可说 `候補ターゲットの絞り込み`。`3つのステップに切り分けます` 很自然，技术说明里也可以用 `段階に分けます`。

> [!tip] 参考回答
> Unrealでターゲットロックシステムを実装するときは、候補ターゲットの絞り込み、優先度の計算、カメラの向きの制御を3つの段階に分けるべきです。

---

### 练习 2

> [!question] 题目
> Unity 的技能冷却队列应该只记录结束时间和技能标识，不要在每一帧创建新的临时对象。

> [!example] 自己的答案
>　Unityのクールダウン列は終了時間とスキルタグだけを記録し、毎フレーム一時的なオブジェクトを生成しないようにします。

> [!warning] 指正
> 大意正确。`クールダウン列` 不太自然，队列建议说 `クールダウンキュー`。题目是“技能标识”，`スキルタグ` 可以，但如果不是 Gameplay Tag 语境，`スキルID` 或 `スキル識別子` 更准确。`一時的なオブジェクト` 前面可加 `新しい`。

> [!tip] 参考回答
> Unityのスキルクールダウンキューは終了時間とスキル識別子だけを記録し、毎フレーム新しい一時オブジェクトを作らないようにするべきです。

---

### 练习 3

> [!question] 题目
> 如果透明特效遮住了角色轮廓，请先比较材质混合模式、渲染队列和深度测试设置。

> [!example] 自己的答案
>　透明エフェクトはキャラクターの輪郭を遮る場合、マテリアルのブレンドモード、描画列、深度テストを先に比較してください。

> [!warning] 指正
> 题意能传达。开头 `透明エフェクトは...場合` 助词不自然，应为 `透明エフェクトが...場合`。`描画列` 不太常用，渲染队列更自然是 `レンダーキュー` 或 `描画キュー`。`深度テスト` 用得准确。

> [!tip] 参考回答
> 透明エフェクトがキャラクターの輪郭を遮っている場合は、まずマテリアルのブレンドモード、レンダーキュー、深度テスト設定を比較してください。

---

### 练习 4

> [!question] 题目
> 在设计弹道预测时，瞄准输入、重力参数和碰撞采样应该使用同一份模拟结果。

> [!example] 自己的答案
>　弾道プレビューを設計する時に、照準入力、重力パラメータ、コリジョンサプリングは同じシミュレーション結果から参照するべきです。

> [!warning] 指正
> 意思基本正确。题目是“弹道预测”，`弾道プレビュー` 稍偏“预览显示”，更准确是 `弾道予測`。`コリジョンサプリング` 中 `サプリング` 应为 `サンプリング`。`同じシミュレーション結果から参照する` 能懂，但更自然是 `同じシミュレーション結果を使う`。

> [!tip] 参考回答
> 弾道予測を設計するときは、照準入力、重力パラメータ、コリジョンサンプリングで同じシミュレーション結果を使うべきです。

---

### 练习 5

> [!question] 题目
> 为了让道具栏排序更稳定，请把品质、类型和获得时间整理成明确的比较规则。

> [!example] 自己的答案
>　アイテムソーロを安定的に動作させるため、品質、タイプ、取得時間を明確な比較ルールを整理します。

> [!warning] 指正
> 这里有两个明显问题。`アイテムソーロ` 应为 `アイテムソート`，题目是“道具栏排序”，也可以说 `インベントリのソート`。`明確な比較ルールを整理します` 前面缺少助词，建议写成 `明確な比較ルールとして整理します`。

> [!tip] 参考回答
> インベントリのソートをより安定させるために、品質、種類、取得時間を明確な比較ルールとして整理してください。

---

### 练习 6

> [!question] 题目
> 当 Boss 进入第二阶段时，攻击表、动画蓝图参数和背景音乐状态都需要一次性切换。

> [!example] 自己的答案
>　ボスが第二フェーズに入るときは、攻撃テーブル、アニメーションブループリント、BGMの状態を一元的に切り替える必要があります。

> [!warning] 指正
> 意思清楚。`攻撃テーブル`、`アニメーションブループリント`、`BGMの状態` 都能表达题目内容。`一元的に切り替える` 偏“集中统一切换”，也可以；题目强调“一次性切换”，更贴近的是 `まとめて切り替える`。

> [!tip] 参考回答
> ボスが第二フェーズに入るときは、攻撃テーブル、アニメーションブループリントのパラメータ、BGMの状態をまとめて切り替える必要があります。

---

### 练习 7

> [!question] 题目
> Unreal 的 Gameplay Tag 判断应该集中在能力入口处，避免在多个蓝图节点里重复写条件。

> [!example] 自己的答案
>　UnrealのGameplay Tag判定はアビリティの入口に集約し、複数のブループリントノードに重複して実装しないようにします。

> [!warning] 指正
> 表达自然，题意完整。`アビリティの入口に集約し` 很贴近“集中在能力入口处”。后半 `複数のブループリントノードに重複して実装しない` 也准确，只是题目里的“写条件”可以更具体地说 `条件を書かない`。

> [!tip] 参考回答
> UnrealのGameplay Tag判定はアビリティの入口に集約し、複数のブループリントノードで同じ条件を繰り返し書かないようにするべきです。

---

### 练习 8

> [!question] 题目
> 在优化技能轨迹显示时，请优先减少线段数量、材质实例数量和不必要的颜色更新。

> [!example] 自己的答案
>　スキル軌跡の表示を最適化するときは、セグメントの数、マテリアル数と必要がない色更新を優先的に減らします。

> [!warning] 指正
> 大意正确。`セグメントの数` 可以表达“线段数量”。`マテリアル数` 少了“实例”，建议说 `マテリアルインスタンス数`。`必要がない色更新` 不自然，应为 `不要な色更新` 或 `不必要な色の更新`。

> [!tip] 参考回答
> スキル軌跡の表示を最適化するときは、線分数、マテリアルインスタンス数、不要な色更新を優先的に減らしてください。

---

### 练习 9

> [!question] 题目
> 如果玩家复活后仍保留死亡状态，请检查状态清除顺序、碰撞启用和输入恢复流程。

> [!example] 自己的答案
>　プレイヤーが蘇った後に死亡状態が残ったままになっている場合、状態のクリア順番、コリジョン有効化、入力フローのリセットを確認してください。

> [!warning] 指正
> 意思基本完整。`蘇った後` 可以用，但游戏复活语境里 `復活した後` 更常见。`状態のクリア順番` 建议改为 `状態をクリアする順序`。题目是“输入恢复流程”，`入力フローのリセット` 稍偏重置，建议说 `入力復帰フロー` 或 `入力を復帰させる流れ`。

> [!tip] 参考回答
> プレイヤーが復活した後も死亡状態が残っている場合は、状態をクリアする順序、コリジョンの有効化、入力復帰フローを確認してください。

---

### 练习 10

> [!question] 题目
> Unity 的相机跟随组件应该读取目标位置和平滑时间，而不是直接依赖角色脚本的内部变量。

> [!example] 自己的答案
>　Unityのカメラフォローコンポーネントはターゲット位置、スムーズ時間を読み込むべきで、キャラクターのスクリプト内の変数に直接に依存するのではありません。

> [!warning] 指正
> 题意能表达。`スムーズ時間` 不太自然，Unity 语境里可说 `平滑化時間` 或 `スムージング時間`。`直接に依存する` 应为 `直接依存する`。`読み込むべきで` 可以，但这里更自然是 `参照するべきで`。

> [!tip] 参考回答
> Unityのカメラフォローコンポーネントは、ターゲット位置とスムージング時間を参照するべきで、キャラクタースクリプト内の変数に直接依存するべきではありません。

---

### 练习 11

> [!question] 题目
> 在制作交互提示时，检测对象、按钮图标和提示文本应该在焦点变化时一起刷新。

> [!example] 自己的答案
>　インタラクション提示を作成するとは、検出オブジェクト、ボタンアイコン、提示テキストをフォーカスが変化する時に一元的に更新します。

> [!warning] 指正
> 主要意思在，但有几处语法和词汇问题。`作成するとは` 应为 `作成するときは`。`インタラクション提示` 是中日混合，建议用 `インタラクション表示` 或 `操作ヒント`。`提示テキスト` 建议改为 `表示テキスト`。`一元的に更新` 可以理解，但题目是“一起刷新”，`まとめて更新` 更自然。

> [!tip] 参考回答
> インタラクション表示を作成するときは、フォーカスが変わったタイミングで、検出対象、ボタンアイコン、表示テキストをまとめて更新するべきです。

---

### 练习 12

> [!question] 题目
> 如果存档读取后任务进度不正确，请比较任务编号、完成标记和奖励领取状态。

> [!example] 自己的答案
>　セーブデータを読み込んだ後クエスト進捗が誤った場合、クエスト番後、完了マーカー、報酬の受け取る状態を比較してください。

> [!warning] 指正
> 题意基本能看出，但有明显拼写和搭配问题。`クエスト番後` 应为 `クエスト番号`。`クエスト進捗が誤った場合` 不自然，建议说 `クエスト進捗が正しくない場合`。`報酬の受け取る状態` 应为 `報酬の受け取り状態`。

> [!tip] 参考回答
> セーブデータを読み込んだ後にクエスト進捗が正しくない場合は、クエスト番号、完了フラグ、報酬の受け取り状態を比較してください。

---

### 练习 13

> [!question] 题目
> 在实现敌人受击闪白效果时，材质参数、持续时间和恢复时机应该由同一个效果控制器管理。

> [!example] 自己的答案
> 敵の被弾効果を実装する時に、マテリアルパラメータ、持続時間、戻るタイミングは同じエフェクトコントローラで管理するべきです。

> [!warning] 指正
> 意思基本正确，但漏了“闪白”的具体效果。`敵の被弾効果` 太泛，建议说 `敵の被弾時の白点滅エフェクト` 或 `白く光る効果`。`戻るタイミング` 可以理解，不过更自然是 `元に戻すタイミング`。`コントローラ` 建议写成长音 `コントローラー`。

> [!tip] 参考回答
> 敵の被弾時の白点滅エフェクトを実装するときは、マテリアルパラメータ、持続時間、元に戻すタイミングを同じエフェクトコントローラーで管理するべきです。

---

### 练习 14

> [!question] 题目
> 如果明天早上你有时间，我们一起去车站附近吃早饭吧。

> [!example] 自己的答案
> 明日朝時間があれば、一緒に駅の近くに朝ご飯を食べに行きましょう。

> [!warning] 指正
> 日常意思完整。`明日朝` 可以理解，但更自然是 `明日の朝`。`駅の近くに朝ご飯を食べに行く` 助词稍不自然，地点建议用 `駅の近くへ` 或明确为 `駅の近くのお店へ`。整体只需微调。

> [!tip] 参考回答
> 明日の朝、時間があれば、一緒に駅の近くへ朝ご飯を食べに行きましょう。

---

### 练习 15

> [!question] 题目
> 我先把今天发现的问题整理成列表，之后再请你帮忙确认优先级。

> [!example] 自己的答案
>　まず本日二見つかった問題をリストにまとめ、後ほど優先度を確認していただきます。

> [!warning] 指正
> 职场意思大体能传达，但有两个问题。`本日二見つかった` 应为 `本日見つかった` 或 `今日見つかった`。`確認していただきます` 像在安排对方必须确认，题目是“请你帮忙确认”，更自然礼貌的是 `確認していただけますか` 或 `確認をお願いします`。

> [!tip] 参考回答
> まず今日見つかった問題をリストにまとめて、後ほど優先度の確認をお願いします。

## 2026-08-09

### 练习 1

> [!question] 题目
> 在 Unreal 中切换动画蒙太奇段落时，请先确认当前状态是否允许中断，而不是直接跳到下一个段落。

> [!example] 自己的答案
> UnrealでMontageのSegmentを切り替える時は、現在の状態が中断可能かを先に確認し、次の段落に直接飛び込むのではありません。

> [!warning] 指正
> 意思基本清楚。`MontageのSegment` 可以理解，但日语技术说明里更常写 `モンタージュのセクション`。`次の段落に直接飛び込む` 不自然，建议用 `次のセクションへ直接ジャンプする`。`のではありません` 偏说明事实，这里是建议，`べきではありません` 更合适。

> [!tip] 参考回答
> Unrealでアニメーションモンタージュのセクションを切り替えるときは、次のセクションへ直接ジャンプするのではなく、まず現在の状態が中断可能かを確認してください。

---

### 练习 2

> [!question] 题目
> Unity 的输入绑定最好按游戏状态启用或禁用，避免暂停菜单打开后角色仍然移动。

> [!example] 自己的答案
> Unityの入力バンディングはゲーム状態によってオン・オフにするのが望ましいです。ポーズメニューが開いた後キャラクターが動き続けることを避けます。

> [!warning] 指正
> 大意正确。`入力バンディング` 应为 `入力バインディング`。`ゲーム状態` 可以理解，但更自然是 `ゲームの状態` 或 `プレイ状態`。第二句的 `開いた後` 前建议加 `に`，并且“避免”可用 `防ぐ`，比 `避けます` 更贴近 bug 预防语境。

> [!tip] 参考回答
> Unityの入力バインディングは、ゲームの状態に応じて有効化・無効化するのが望ましいです。ポーズメニューを開いた後もキャラクターが動き続けることを防げます。

---

### 练习 3

> [!question] 题目
> 如果伤害数字出现位置不稳定，请比较世界坐标转换、相机朝向和 UI 锚点设置。

> [!example] 自己的答案
>　ダメージ数字の出現位置が不安定に動作する場合、ワード座標、カメラ向き、UIのピボットの設定を比較してください。

> [!warning] 指正
> 题意能传达，但有几个术语问题。`ダメージ数字` 可用，UI 里也常说 `ダメージ数値`。`ワード座標` 应为 `ワールド座標`。题目是“坐标转换”，建议写出 `変換`。`カメラ向き` 更自然是 `カメラの向き`，UI 锚点通常是 `アンカー`，不是 `ピボット`。

> [!tip] 参考回答
> ダメージ数値の表示位置が不安定な場合は、ワールド座標の変換、カメラの向き、UIアンカーの設定を比較してください。

---

### 练习 4

> [!question] 题目
> 在设计道具合成系统时，材料消耗、结果生成和失败回滚应该放在同一个事务流程里。

> [!example] 自己的答案
>　アイテムの合成システムを設定する際に、コスト、生成の結果、失敗したロールバックを同じトランジションに実装するべきです。

> [!warning] 指正
> 主要意思在，但关键术语需要调整。`設定する` 是“设置”，题目是“设计”，应为 `設計する`。`コスト` 过泛，材料消耗建议说 `素材の消費`。`トランジション` 是“转场/状态迁移”，这里的“事务流程”应为 `トランザクション処理`。`失敗したロールバック` 应改为 `失敗時のロールバック`。

> [!tip] 参考回答
> アイテム合成システムを設計するときは、素材の消費、結果の生成、失敗時のロールバックを同じトランザクション処理に入れるべきです。

---

### 练习 5

> [!question] 题目
> 为了减少 Tick 里的判断，请把需要持续检测的对象注册到专用管理器中统一更新。

> [!example] 自己的答案
>　Tick内の判定を減らすため、持続検出が必要なオブジェクトを専用のマネージャーで一元的に更新します。

> [!warning] 指正
> 表达整体自然。`持続検出が必要なオブジェクト` 能懂，但更自然是 `継続的な検出が必要なオブジェクト`。题目有“注册到管理器中”，建议补出 `登録して`。`一元的に更新します` 用得合适。

> [!tip] 参考回答
> Tick内の判定を減らすために、<mark style="background: #FF5582A6;">継続的な</mark>検出が必要なオブジェクトを専用マネージャーに登録し、まとめて更新します。

---

### 练习 6

> [!question] 题目
> 当连招被防御打断时，攻击状态、受击硬直和输入缓存都需要按固定顺序清理。

> [!example] 自己的答案
> コンボが防衛によって中断された場合、攻撃状態、被弾硬直、入力バッファを設定された順番でクリアする必要があります。

> [!warning] 指正
> 大意正确。`防衛` 更偏“防卫/防御体系”，游戏里“被防御打断”建议说 `ガードによって中断された` 或 `防御で中断された`。`設定された順番` 可以，但题目是“固定顺序”，更自然是 `決まった順序`。`入力バッファ` 用得准确。

> [!tip] 参考回答
> コンボが<mark style="background: #FF5582A6;">ガード</mark>によって中断された場合は、攻撃状態、被弾硬直、入力バッファを決まった順序でクリアする必要があります。

---

### 练习 7

> [!question] 题目
> Unreal 的 Enhanced Input 映射上下文应该根据当前玩法模式调整优先级，避免同一个按键触发多个行为。

> [!example] 自己的答案
> UnrealのEnhanced Input Mapping Contextは現在のゲームプレイモードに応じて優先度を調整し、同じボタンが複数のアクションを発動することを避けます。

> [!warning] 指正
> 意思完整。`現在のゲームプレイモードに応じて優先度を調整し` 很自然。`同じボタンが複数のアクションを発動する` 能表达题意，但输入语境里更常说 `同じボタンで複数のアクションが発動する`。`避けます` 可换成 `防ぐ`。

> [!tip] 参考回答
> UnrealのEnhanced Input Mapping Contextは、現在のゲームプレイモードに応じて優先度を調整し、<mark style="background: #FF5582A6;">同じボタンで複数のアクションが発動する</mark>ことを防ぐべきです。

---

### 练习 8

> [!question] 题目
> 在优化小地图图标时，请优先合并静态标记，并限制每帧更新的动态标记数量。

> [!example] 自己的答案
>　ミニマップのアイコンを最適化する際に、静的のマーカーを、毎フレーム動的のマーカーの更新数を制限します。

> [!warning] 指正
> 这句少了“优先合并”的谓语，导致结构不完整。`静的のマーカー`、`動的のマーカー` 应改为 `静的なマーカー`、`動的なマーカー`。`毎フレーム動的のマーカーの更新数` 前面需要补出“限制”的对象，可以说 `毎フレーム更新する動的マーカーの数`。

> [!tip] 参考回答
> ミニマップのアイコンを最適化するときは、静的なマーカーを優先的にまとめ、毎フレーム更新する動的マーカーの数を制限してください。

---

### 练习 9

> [!question] 题目
> 如果敌人的巡逻路线偶尔卡住，请记录当前路径点、移动状态和避障结果。

> [!example] 自己的答案
>　敵が巡回ルートで稀に引っかかる場合、現在の経路ポイント、移動状態、回避結果を記録してください。

> [!warning] 指正
> 意思基本清楚。`巡回ルート` 能懂，不过敌人巡逻路线更常说 `巡回ルート` 或 `パトロールルート`。`稀に` 正确但偏书面，日常技术说明里 `たまに` 也自然。`回避結果` 稍泛，题目是“避障结果”，建议说 `障害物回避の結果`。

> [!tip] 参考回答
> 敵がパトロールルート上でたまに引っかかる場合は、現在の経路ポイント、移動状態、障害物回避の結果を記録してください。

---

### 练习 10

> [!question] 题目
> Unity 的对象池回收子弹时，应该重置速度、碰撞状态和残留特效引用。

> [!example] 自己的答案
>　Unityのオブジェクトプールは弾を回収する時に、速度、コリジョン状態、残るエフェクトの参照をリセットするべきです。

> [!warning] 指正
> 题意完整。`Unityのオブジェクトプールは弾を回収する時に` 可以，但更自然是 `Unityのオブジェクトプールで弾を回収するときは`。`残るエフェクトの参照` 建议改为 `残っているエフェクト参照` 或 `残留エフェクトの参照`。

> [!tip] 参考回答
> Unityのオブジェクトプールで弾を回収するときは、速度、コリジョン状態、残留エフェクトの参照をリセットするべきです。

---

### 练习 11

> [!question] 题目
> 在制作锁链技能时，目标选择、距离限制和重复命中规则应该由技能逻辑统一判断。

> [!example] 自己的答案
>　チャーンスキルを作成する時に、目標選択、距離限定、重複ヒットルールを同じスキルロジックで一元的に判定するべきです。

> [!warning] 指正
> 有几个关键术语需要修正。`チャーンスキル` 应为 `チェーンスキル` 或 `連鎖スキル`。`目標選択` 能懂，但游戏目标选择更常说 `ターゲット選択`。`距離限定` 不自然，建议说 `距離制限`。`同じスキルロジックで一元的に判定` 表达方向正确。

> [!tip] 参考回答
> 連鎖スキルを作成するときは、ターゲット選択、<mark style="background: #FF5582A6;">距離制限</mark>、重複ヒットのルールをスキルロジック側でまとめて判定するべきです。

---

### 练习 12

> [!question] 题目
> 如果设置界面的滑块没有保存音量，请检查 UI 回调、配置写入和读取时的默认值。

> [!example] 自己的答案
>　設定画面のスライドが音量を保存しない場合、UIのコールバック、設定の書き込みと読み込み時のデフォルト値を確認してください。

> [!warning] 指正
> 大意正确。`スライド` 是“幻灯片/滑动”，UI 控件“滑块”应为 `スライダー`。`音量を保存しない` 能表达，但更自然是 `音量が保存されない`。`設定の書き込みと読み込み時のデフォルト値` 并列稍不清楚，建议拆成 `設定への書き込み` 和 `読み込み時のデフォルト値`。

> [!tip] 参考回答
> 設定画面のスライダーで音量が保存されない場合は、UIコールバック、設定への書き込み、読み込み時のデフォルト値を確認してください。

---

### 练习 13

> [!question] 题目
> 在拆分战斗组件职责时，请让属性计算、事件广播和表现效果分别依赖明确的数据接口。

> [!example] 自己的答案
>　バトルコンポーネント責務を分割する時に、属性計算、イベント送信、表現効果に別々の明確なインデータターフェースに依頼させます。

> [!warning] 指正
> 这里意思有些偏。`バトルコンポーネント責務` 前需要 `の`。`表現効果に別々の明確な...に依頼させます` 结构不自然，且 `インデータターフェース` 是拼写错误，应为 `データインターフェース`。题目是“让 A、B、C 分别依赖明确的数据接口”，可说 `それぞれ明確なデータインターフェースに依存させる`。

> [!tip] 参考回答
> バトルコンポーネントの責務を分割するときは、属性計算、<mark style="background: #FF5582A6;">イベント通知</mark>、演出効果をそれぞれ明確なデータインターフェースに依存させてください。

---

### 练习 14

> [!question] 题目
> 如果下午天气还不错，我们散步的时候顺便去书店看看吧。

> [!example] 自己的答案
>　午後天気が良ければ、一緒に散歩する時についでに本屋に立ち寄りましょう。

> [!warning] 指正
> 日常意思能传达。`午後天気が良ければ` 前面建议加 `の` 或逗号，写成 `午後、天気が良ければ` 更自然。`一緒に散歩する時に` 可用，但“散步的时候顺便”更自然是 `散歩のついでに`。`本屋に立ち寄りましょう` 很自然。

> [!tip] 参考回答
> 午後、天気がまだ良ければ、散歩のついでに本屋に立ち寄りましょう。

---

### 练习 15

> [!question] 题目
> 我先把这次讨论的决定事项整理出来，晚点发给大家确认。

> [!example] 自己的答案
>　まず今回で決定された事項を整理し、後ほど皆さんに送って確認していただきます。

> [!warning] 指正
> 职场表达基本自然。`今回で決定された事項` 中 `で` 不自然，应为 `今回決まった事項` 或 `今回の決定事項`。`確認していただきます` 有点像安排对方必须确认，题目是“发给大家确认”，可改为 `確認していただけるように送ります` 或 `確認をお願いします`。

> [!tip] 参考回答
> まず今回の決定事項を整理して、後ほど皆さんに送って確認していただきます。
