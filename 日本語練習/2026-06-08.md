## 2026-06-08

### 练习 1

> [!question] 题目
> 当摄像机快速移动时，请优先加载即将进入视野的高分辨率纹理，避免画面出现明显的模糊跳变。

> [!example] 自己的答案
> カメラが高速移動中に画面が明らかにブラーで崩れることを防ぐため、視野に入る高解像度のテクスチャを優先的にロードしてください

> [!warning] 指正
> 整体意思清楚。「視野に入る」表示已经进入视野，而“即将进入”应为「これから視野に入る」或「まもなく視野に入る」。「ブラーで崩れる」不自然，题目强调模糊程度突然变化，可说「ぼやけ方が急に変わる」或「目立つぼやけの切り替わりが起きる」。

> [!tip]- 参考回答
> カメラが高速移動している際に、画面のぼやけが<mark style="background: #FF5582A6;">目立つ</mark>ことを防ぐため、<mark style="background: #FF5582A6;">これから</mark>視野内に入る高解像度テクスチャを優先的にロードしてください。

---

### 练习 2

> [!question] 题目
> 为了防止客户端频繁发送远程调用，请为每种请求设置速率限制，并记录超过阈值的玩家。

> [!example] 自己的答案
>　クライアントが頻繁にリクエストを送信することを防ぐため、各リクエスト送信レートを制限し、れい値を超えたプレイヤーを記録してください

> [!warning] 指正
> “远程调用”漏译，可用「リモート呼び出し」或「RPC」。「各リクエスト送信レートを制限」搭配不自然，应说「リクエストの種類ごとにレート制限を設ける」。「れい値」通常写作「閾値（しきい値）」。

> [!tip]- 参考回答
> クライアントがリモート呼び出しを頻繁に送信するのを防ぐため、リクエストの種類ごとにレート<mark style="background: #FF5582A6;">制限を設け</mark>、<mark style="background: #FF5582A6;">しきい値を超えた</mark>プレイヤーを記録してください。

---

### 练习 3

> [!question] 题目
> 从对象池取出特效实例时，请重置上一次播放留下的计时器和粒子状态。

> [!example] 自己的答案
>　オブジェクトプールからエフェクト実例を取得する際に、前回から残っているタイマーとパーティクル状態をリセットしてください

> [!warning] 指正
> 「実例」是实际例子，不是程序中的实例；应使用「インスタンス」。「前回から残っている」可理解，但“上一次播放留下的”更准确地说是「前回の再生時の状態が残らないよう」或「前回の再生で使った」。粒子状态前加「の」更自然。

> [!tip]- 参考回答
> オブジェクトプールからエフェクトのインスタンスを<mark style="background: #FF5582A6;">取り出す</mark>際は、前回の再生で使ったタイマーとパーティクルの状態をリセットしてください。

---

### 练习 4

> [!question] 题目
> 本地化文本变长后，按钮标签可能会被截断，因此请让布局根据文字宽度自动调整。

> [!example] 自己的答案
>　ローカライズテキストが長く変更した場合、ボタンタグが折りたたみされるので文字の幅によってレイアウトが自動で調整できるようにします

> [!warning] 指正
> 「長く変更した」不自然，应为状态变化「長くなった場合」。「ボタンタグ」是标签元素，这里的按钮标签应为「ボタンのラベル」；“被截断”不是折叠，宜用「途中で切れる」或「見切れる」。题目是请求，因此句尾应为「調整されるようにしてください」，而不是「します」。

> [!tip]- 参考回答
> ローカライズテキストが長くなった場合、ボタンラベルが<mark style="background: #FF5582A6;">切り詰められる</mark>可能性があるため、文字幅<mark style="background: #FF5582A6;">に応じて</mark>レイアウトが自動調整されるようにしてください。

---

### 练习 5

> [!question] 题目
> 如果玩家在场景异步加载完成前返回标题画面，请取消剩余任务并释放已经分配的资源。

> [!example] 自己的答案
>　プレイヤーは非同期ロードが完了する前にタイトル画面に戻ったら、未完成タスクと割り当てたリソースを解放してください

> [!warning] 指正
> 条件句的主题应使用「プレイヤーが」，不是「プレイヤーは」。“取消剩余任务”和“释放资源”是两个不同动作；「未完成タスクを解放する」不准确，应分别写成「残りのタスクをキャンセルし」「割り当て済みのリソースを解放する」。另外应补出「シーンの」异步加载。

> [!tip]- 参考回答
> プレイヤーがシーンの非同期ロード完了前にタイトル画面へ戻った場合は、残りの<mark style="background: #FF5582A6;">タスクをキャンセルし</mark>、すでに割り当てたリソースを解放してください。

---

### 练习 6

> [!question] 题目
> 内存占用持续上升时，请比较多次快照，确认哪些对象在切换关卡后仍未被释放。

> [!example] 自己的答案
>　インメモリが一歩的に上昇する場合、各スナップショットを比較し、ステージが切り替わった後未解放オブジェクトを特定する

> [!warning] 指正
> 「インメモリ」表示内存中的处理方式，不是内存占用；应说「メモリ使用量」。「一歩的に」用词错误，持续上升可表达为「増え続ける」。「各スナップショット」不够准确，题目是比较多次快照，可说「複数のスナップショット」。后半应补助词并使用请求句式：「解放されていないオブジェクトを確認してください」。

> [!tip]- 参考回答
> <mark style="background: #FF5582A6;">メモリ使用量</mark>が増え続いている場合は、複数回取得したスナップショットを比較し、レベル切り替え後も解放されていないオブジェクトを特定してください。

---

### 练习 7

> [!question] 题目
> 为了提高构建缓存的命中率，请确保相同输入始终生成完全一致的中间文件。

> [!example] 自己的答案
>　キャッシュヒット率を高めるため、同一インプットで完全に同じのファイルが生成されることを確保してください

> [!warning] 指正
> 句首漏掉了构建缓存，应为「ビルドキャッシュのヒット率」。「完全に同じのファイル」中「の」多余，应说「完全に同一のファイル」。“中间文件”也漏译，应使用「中間ファイル」。「确保”在技术要求中用「必ず生成されるようにしてください」比直译「確保してください」自然。

> [!tip]- 参考回答
> ビルドキャッシュのヒット率を高めるため、同じ入力からは常に完全に同一の<mark style="background: #FF5582A6;">中間ファイル</mark>が生成されるようにしてください。

---

### 练习 8

> [!question] 题目
> 修改回放数据的序列化格式时，请保留版本号，以便旧客户端能够检测不兼容的数据。

> [!example] 自己的答案
>　リプレイデータをシリアライズする際に、バージョンを残して古いクライアントがデータの交換性を検出できるようにします

> [!warning] 指正
> 题目是修改“序列化格式”，不是执行序列化，因此应说「シリアライズ形式を変更する際」。「交換性」是可交换性，“兼容性”应为「互換性」；这里要检测的是不兼容数据，可说「互換性のないデータを検出する」。版本号应完整译为「バージョン番号」。请求句尾宜用「残してください」。

> [!tip]- 参考回答
> リプレイデータのシリアライズ<mark style="background: #FF5582A6;">形式</mark>を変更する際は、古いクライアントでも<mark style="background: #FF5582A6;">互換性</mark>のないデータを検出できるよう、バージョン番号を残してください。

---

### 练习 9

> [!question] 题目
> 我正好要去超市，你看看冰箱里还有没有需要补充的东西？

> [!example] 自己的答案
>　ちょっとスーパーに行くから、冷蔵庫に何か足りないものがあるか

> [!warning] 指正
> 「ちょっとスーパーに行く」是去一下超市，没有体现“正好”；可用「ちょうどスーパーに行くところ」。“你看看”对应「見てくれる？」。原句以「あるか」结束不完整，应补上请求或疑问句尾。

> [!tip]- 参考回答
> ちょうどスーパーに行くところなんだけど、冷蔵庫を見て、何か買い足すものがないか確認してくれる？

---

### 练习 10

> [!question] 题目
> 这个功能还在等待另一个团队提供接口，我会在依赖解决后立即更新进度。

> [!example] 自己的答案
>　この機能は他のチームが担当するインターフェースを待っているので、依頼性を解決する次第で進捗を直ちに更新する

> [!warning] 指正
> 「担当するインターフェース」变成另一个团队负责的接口，原意是等待其提供接口，应为「別チームからのインターフェース提供」。「依頼性」是错误词语，“依赖”在此为「依存関係」。「解決する次第で」不自然，表示条件一满足就行动可用「解消され次第」。职场汇报使用礼貌体更合适。

> [!tip] 参考回答
> この機能は現在、別チームからのインターフェース提供を待っている状態です。依存関係が解消され次第、すぐに進捗を更新します。

## 2026-06-09

### 练习 1

> [!question] 题目
> 为了减少不必要的绘制调用，请在提交渲染命令前剔除被大型物体完全遮挡的对象。

> [!example] 自己的答案
>　過剰なドローコールを減らすため、ドローコマンドをコミットする前に完全に被られたオブジェクトを排除してください

> [!warning] 指正
> 「過剰なドローコール」更像“过多的绘制调用”，原文“不必要的”宜用「不要な」。「ドローコマンドをコミットする」不够自然，渲染命令通常说「レンダリングコマンドを送信する」。「完全に被られた」搭配错误，应明确为「大きなオブジェクトに完全に隠された」。

> [!tip]- 参考回答
> <mark style="background: #FF5582A6;">不要な</mark>ドローコールを減らすため、レンダリングコマンドを送信する前に、大きなオブジェクトによって完全に<mark style="background: #FF5582A6;">遮蔽されている</mark>オブジェクトを<mark style="background: #FF5582A6;">カリング</mark>してください。

---

### 练习 2

> [!question] 题目
> 请根据玩家的位置和视野动态调整同步范围，不要向客户端发送无关区域内的实体状态。

> [!example] 自己的答案
> プレイヤーの位置及び視野に応じて同期範囲を動的に調整し、クライアントに無関係エリア内の実例状態を送信しない

> [!warning] 指正
> 「実例」是实际例子，不是游戏实体；这里应使用「エンティティ」。「無関係エリア内」缺少助词，可说「関係のないエリアにいる」或「同期対象外のエリアにある」。题目是请求，句尾应使用「送信しないでください」或「送信しないようにしてください」。

> [!tip]- 参考回答
> プレイヤーの位置と視野に応じて同期範囲を動的に調整し、関係のないエリアにいるエンティティの状態をクライアントへ送信しないようにしてください。

---

### 练习 3

> [!question] 题目
> 销毁界面时，请解除所有事件订阅，避免回调继续引用已经无效的组件。

> [!example] 自己的答案
>　画面を破壊する際に、コールバックにより無効なコンポーネントを参照されることを防ぐため、すべてサブスクリプションをキャンセルしてください

> [!warning] 指正
> 「コールバックにより～参照される」表示组件被回调引用，但题目强调回调“继续”引用，宜说「コールバックが～を参照し続ける」。「すべてサブスクリプション」应为「すべてのイベント購読」；解除订阅通常用「解除する」，不用「キャンセルする」。

> [!tip]- 参考回答
> 画面を破棄する際は、コールバックが<mark style="background: #FF5582A6;">無効になった</mark>コンポーネントを参照し続けないよう、すべてのイベント購読を解除してください。

---

### 练习 4

> [!question] 题目
> 如果资源包之间形成循环依赖，加载顺序就会变得不稳定，因此请在构建阶段检测并报告这种情况。

> [!example] 自己的答案
> アセットバンドル間に依頼関係のループとなってしまったら、ロード順が不安定になるので、ビルド段階に検出したら報告してください

> [!warning] 指正
> “依赖关系”应为「依存関係」，不是「依頼関係」；“循环依赖”常说「循環依存」。「ループとなってしまったら」不够简洁，可用「循環依存が発生すると」。「ビルド段階に」应为「ビルド時に」，并列动作应表达为「検出して報告してください」。

> [!tip]- 参考回答
> アセットバンドル間で<mark style="background: #FF5582A6;">循環依存</mark>が発生するとロード順が不安定になるため、ビルド時に検出して報告してください。

---

### 练习 5

> [!question] 题目
> 即使平均帧率足够高，帧时间波动过大也会让操作显得卡顿，请优先调查偶发的耗时峰值。

> [!example] 自己的答案
> 平均フレームレートが高ければ、フレーム間に差が大きくて操作にカクつきがある、稀にピーク値を優先的に調査してください

> [!warning] 指正
> 「高ければ」表示“如果高”，与“即使足够高”不同，应使用「十分に高くても」。「フレーム間に差」不如技术用语「フレームタイムの変動」准确。“偶发的耗时峰值”应译为「突発的な処理時間のスパイク」，不是「稀にピーク値」。

> [!tip]- 参考回答
> 平均フレームレートが十分に高くても、<mark style="background: #FF5582A6;">フレームタイムの変動</mark>が大きいと操作がカクついて感じられるため、<mark style="background: #FF5582A6;">突発的な処理時間のピーク</mark>を優先して調査してください。

---

### 练习 6

> [!question] 题目
> 使用手柄操作菜单时，请记住关闭窗口前的焦点位置，并在再次打开时恢复选择。

> [!example] 自己的答案
>　ハンドルでメニューを操作する際に再びに元の選択に戻れるため、ウェウドウズが閉じる前のフォカス位置を記録しておいてください。

> [!warning] 指正
> 游戏手柄应为「ゲームパッド」或「コントローラー」，「ハンドル」通常指方向盘或把手。「再びに」错误，应为「再び」；「ウェウドウズ」「フォカス」分别应为「ウィンドウ」「フォーカス」。“恢复选择”还需明确再次打开窗口时恢复原来的选中项。

> [!tip]- 参考回答
> <mark style="background: #FF5582A6;">ゲームパッド</mark>でメニューを操作する場合は、ウィンドウを閉じる前の<mark style="background: #FF5582A6;">フォーカス</mark>位置を記憶し、再度開いたときに元の選択状態を復元してください。

---

### 练习 7

> [!question] 题目
> 写入存档时，请先保存到临时文件，确认成功后再替换正式文件，以免中途失败导致数据损坏。

> [!example] 自己的答案
>　セーブデータを書き込む前に、セーブ中にデータが破損するのを避けるため、まず一時的ファイルを保存し、無事にセーブした後差し替える

> [!warning] 指正
> 原文是“写入存档时”，不是写入前，因此「書き込む前に」应改为「書き込む際は」。「一時的ファイル」不自然，应为「一時ファイル」。“确认成功后”宜说「書き込みが成功したことを確認してから」。句尾应使用请求形式「差し替えてください」。

> [!tip]- 参考回答
> セーブデータを書き込む際は、途中失敗によるデータ破損を防ぐため、まず一時ファイルに保存し、書き込みが成功したことを確認してから正式なファイル<mark style="background: #FF5582A6;">と差し替え</mark>てください。

---

### 练习 8

> [!question] 题目
> 请在编辑器工具中加入批量检查功能，以便在提交前找出缺少碰撞体或命名不规范的资源。

> [!example] 自己的答案
>　エディタツールにバッチ検出機能を追加し、コミット前に不足のコリラタとネーミングが不正性のリソースを検出できるようにする

> [!warning] 指正
> “批量检查”宜用「一括チェック」，「バッチ検出」意思略有偏差。「コリラタ」拼写错误，应为「コライダー」。「不足のコライダー」和「ネーミングが不正性」均不自然，可分别说「コライダーが設定されていない」「命名規則に違反している」。资源在游戏开发中通常用「アセット」。

> [!tip]- 参考回答
> エディターツールに<mark style="background: #FF5582A6;">一括チェック</mark>機能を追加し、コミット前にコライダーが設定されていないアセットや、命名規則に違反しているアセットを検出できるようにしてください。

---

### 练习 9

> [!question] 题目
> 外面好像要下雨，你出门的时候别忘了带伞。

> [!example] 自己的答案
> 外は雨が降りそうで、出かける時に傘を忘れないで

> [!warning] 指正
> 意思基本正确。「雨が降りそうで」中的「で」会让后句像原因或并列，口语中用「雨が降りそうだから」更连贯。“别忘了带伞”应明确是带上雨伞，可说「傘を持っていくのを忘れないで」。

> [!tip]- 参考回答
> 外は雨が降りそうだから、出かけるときは傘を持っていくのを忘れないでね。

---

### 练习 10

> [!question] 题目
> 为了赶上周五的评审，能否请你在明天下午之前确认一下修改后的设计方案？

> [!example] 自己的答案
>　金曜日のレビューに間に合うため、明日午後に修正した設計書を確認してもらえませんか

> [!warning] 指正
> “为了赶上”应为目的表达「間に合わせるために」，「間に合うため」不自然。「明日午後に」表示明天下午这个时间点，原文“之前”应为「明日の午後までに」。「修正した設計書」像是说话者修改的设计文档，题目的“修改后的设计方案”更自然地说「修正後の設計案」。

> [!tip]- 参考回答
> 金曜日のレビューに間に合わせるため、修正後の設計案を明日の午後までに確認していただけますか。

## 2026-06-10

### 练习 1

> [!question] 题目
> 动态分辨率发生变化后，请重置用于时间抗锯齿的历史缓冲区，避免上一帧的图像残留形成重影。

> [!example] 自己的答案
>　動的解像度が変更した後、前フレームから残る画像と重なることを防ぐため、時間による履歴バッファーをリセットしてください、

> [!warning] 指正
> 「動的解像度が変更した」中主语是分辨率，应使用自动词「変更された」或「変化した」。「時間による履歴バッファー」不能表达“用于时间抗锯齿的历史缓冲区”，应说「TAA（時間的アンチエイリアシング）の履歴バッファ」。“图像残留形成重影”宜表达为「前フレームの残像によるゴーストが発生する」。句末的顿号应改为句号。

> [!tip]- 参考回答
> 動的解像度が変化した後は、前フレームの<mark style="background: #FF5582A6;">残像によるゴースト</mark>を防ぐため、<mark style="background: #FF5582A6;">TAAの履歴バッファ</mark>をリセットしてください。

---

### 练习 2

> [!question] 题目
> 当多名玩家同时拾取同一个道具时，请以服务器的判定为准，并拒绝已经过期的请求。

> [!example] 自己的答案
>　複数プレイヤーが同時に同一のアイテムを拾う時、サーバーの判断を基準にし、時間切れたリクエストを拒否してください

> [!warning] 指正
> 「複数プレイヤー」也可理解，但「複数のプレイヤー」更自然。「拾う時」可改为表示事件发生条件的「拾おうとした場合」。“以服务器的判定为准”使用「サーバーの判定を優先する」更符合网络游戏语境。「時間切れた」缺少助词且只表示超时，“已经过期”宜用「すでに期限切れとなった」。

> [!tip]- 参考回答
> 複数のプレイヤーが同時に同じアイテムを拾おうとした場合は、<mark style="background: #FF5582A6;">サーバーの判定を優先し</mark>、すでに<mark style="background: #FF5582A6;">期限切れとなった</mark>リクエストを拒否してください。

---

### 练习 3

> [!question] 题目
> 请通过适配器隔离各平台的原生接口，以免平台相关代码扩散到游戏逻辑中。

> [!example] 自己的答案
>　プラットフォームに関するコードがゲームロジックに広がることを避けるため、アダプタでプラットフォームのインターフェースを分離してください

> [!warning] 指正
> 整体意思基本正确。“各平台的原生接口”没有完整表达出来；应明确为「各プラットフォームのネイティブAPI」。「アダプタで」可用，但表示手段时「アダプターを介して」更自然。「プラットフォームに関するコード」也可进一步准确表达为「プラットフォーム依存のコード」。

> [!tip]- 参考回答
> プラットフォーム依存のコードがゲームロジックに広がらないよう、アダプターを介して<mark style="background: #FF5582A6;">各プラットフォームのネイティブAPI</mark>を分離してください。

---

### 练习 4

> [!question] 题目
> 切换场景后，请卸载不再使用的长音频，并确认仍在播放的声音不会被错误释放。

> [!example] 自己的答案
>　シーンの切り替え後、未使用の長オーディオを解放し、使用中オーディオをミスで解放されることを確認してください

> [!warning] 指正
> 「未使用の長オーディオ」不自然；“不再使用的长音频”可说「使用しなくなった長尺の音声データ」。「解放」也可理解，但资源卸载通常用「アンロード」。“仍在播放的声音”应为「再生中の音声」。「ミスで解放されることを確認」变成确认它被错误释放，与原意相反，应说「誤って解放されないことを確認」。

> [!tip]- 参考回答
> シーンを切り替えた後は、使用しなくなった<mark style="background: #FF5582A6;">長尺の音声データをアンロードし</mark>、再生中の音声が<mark style="background: #FF5582A6;">誤って解放されない</mark>ことを確認してください。

---

### 练习 5

> [!question] 题目
> 为了避免导航网格更新阻塞主线程，请把大范围的重建任务分散到多个帧中执行。

> [!example] 自己的答案
>　ナビーメッシュの更新によってメインスレッドが塞ぐことを避けるため、大きな範囲の再構築タスクを複数フレームに分散してください

> [!warning] 指正
> 「ナビーメッシュ」应为「ナビメッシュ」。「メインスレッドが塞ぐ」中自动词和他动词混用；线程被阻塞应说「メインスレッドがブロックされる」。「大きな範囲」可理解，但技术表达通常使用「広範囲」。“分散到多个帧中执行”应补出执行动作，可说「複数のフレームに分散して実行する」。

> [!tip]- 参考回答
> ナビメッシュの更新によってメインスレッドが<mark style="background: #FF5582A6;">ブロックされる</mark>のを避けるため、広範囲の再構築タスクを複数のフレームに分散して実行してください。

---

### 练习 6

> [!question] 题目
> 在移动设备旋转屏幕后，请根据安全区域重新排列界面，确保重要按钮不会被刘海或圆角遮挡。

> [!example] 自己的答案
>　モバイルのモニターが回転後、重要なボタンがカメラによって遮蔽されないように、セーフエリアに応じてレイアウトし直してください

> [!warning] 指正
> 「モバイルのモニターが回転後」不自然，移动设备旋转屏幕应说「モバイル端末で画面を回転した後」。「カメラ」是相机，不是手机屏幕的刘海；“刘海或圆角”应译为「ノッチや画面の角丸部分」。后半的「セーフエリアに応じてレイアウトし直す」表达正确。

> [!tip]- 参考回答
> モバイル端末で画面を回転した後は、セーフエリアに応じてUIを再配置し、重要なボタンが<mark style="background: #FF5582A6;">ノッチや画面の角丸部分</mark>に隠れないようにしてください。

---

### 练习 7

> [!question] 题目
> 进行帧捕获时，请为每个渲染阶段添加清晰的调试标签，以便快速定位耗时的通道。

> [!example] 自己的答案
>　フレームを取得する際にレンダリング段階ごとにデバッグタグを設定し、時間がかかるチャンネルを特定しやすいようにする

> [!warning] 指正
> 「フレームを取得する」通常只是获取一帧，不能准确表达性能分析中的“帧捕获”；宜用「フレームキャプチャを行う」。「レンダリング段階」可改为更常用的「レンダリングステージ」。“耗时的通道”在渲染语境中应为「処理に時間のかかるパス」，不是「チャンネル」。题目是请求，句尾应改为「してください」。

> [!tip]- 参考回答
> フレームキャプチャを行う際は、各レンダリングステージに分かりやすいデバッグラベルを付け、負荷の高い<mark style="background: #FF5582A6;">パス</mark>をすばやく特定できるようにしてください。

---

### 练习 8

> [!question] 题目
> 发布构建版本时，请归档与该版本完全对应的符号文件，方便之后分析崩溃日志。

> [!example] 自己的答案
>　ビルドをリリースする際に今回リリースしたバージョンに対応するシンボルファイルをまとめ、今後クラッシュログを分析しやすいようにするため

> [!warning] 指正
> 「今回リリースしたバージョンに対応する」基本正确，但漏掉了“完全对应”，可用「完全に一致する」或「正確に対応する」。「まとめ」没有明确表达“归档”，应使用「アーカイブする」。句尾以「ため」结束，句子不完整；应补为请求句「アーカイブしてください」。

> [!tip]- 参考回答
> ビルドをリリースする際は、該当バージョンに<mark style="background: #FF5582A6;">完全に対応するシンボルファイルをアーカイブし</mark>、後からクラッシュログを解析できるようにしてください。

---

### 练习 9

> [!question] 题目
> 快递好像已经到了，你下楼的时候能顺便帮我拿一下吗？

> [!example] 自己的答案
>　宅配便が届いたみたい、下に降りるついでに持ってきてくれないか

> [!warning] 指正
> 整体表达自然。「届いたみたい」后面用逗号直接连接略显口语且句子边界不清，可加「なんだけど」承接请求。「下に降りる」是往下走，不一定明确表示下楼；可说「下へ行く」或「下に降りる」。快递通常需要“取”，使用「受け取ってきてくれる」比单纯的「持ってきてくれないか」更准确。

> [!tip]- 参考回答
> 宅配便がもう届いたみたいなんだけど、下へ行くついでに<mark style="background: #FF5582A6;">受け取ってきてくれる</mark>？

---

### 练习 10

> [!question] 题目
> 因为关键成员临时无法参加，能否把今天的会议推迟到明天上午？我会重新发送邀请。

> [!example] 自己的答案
>　重要なメンバーが出席できないので、今日の会議を明日午前に遅らせてもらえませんか？招待メールを再送信します

> [!warning] 指正
> 整体意思清楚，但“临时无法参加”中的“临时”漏译，可用「急に参加できなくなった」。「遅らせる」可理解为延后，但会议改期更常用「延期する」或「変更する」。“重新发送邀请”在日程语境中可明确为「会議の招待を送り直す」。使用「ため」说明理由比口语感较强的「ので」更适合职场表达。

> [!tip]- 参考回答
> 主要メンバーが<mark style="background: #FF5582A6;">急に参加できなくなった</mark>ため、本日の会議を明日の午前中に延期してもよろしいでしょうか。会議の招待は改めて送付します。

## 2026-06-11

### 练习 1

> [!question] 题目
> 为了缩短首次启动时间，请在打包时剔除当前平台不会使用的着色器变体。

> [!example] 自己的答案
> 初回起動時間を短縮するため、ビルド際に該当プラットフォームで未使用のシェーダーバリアントを排除してください

> [!warning] 指正
> 「ビルド際に」缺少助词，应为「ビルド時に」或「ビルドする際に」。「該当プラットフォームで未使用の」容易理解为目前没有使用，而题目强调该平台根本不会使用，宜说「対象プラットフォームでは使用されない」。“剔除”在构建和着色器语境中使用「除外する」比「排除する」自然。

> [!tip]- 参考回答
> 初回起動時間を短縮するため、<mark style="background: #FF5582A6;">ビルド時に</mark>、対象プラットフォームでは使用されないシェーダーバリアントを<mark style="background: #FF5582A6;">除外してください</mark>。

---

### 练习 2

> [!question] 题目
> 判定远程玩家是否命中时，请根据延迟补偿后的历史位置进行验证，而不是直接使用当前坐标。

> [!example] 自己的答案
>　長距離プレイヤーのヒットを判定する際に遅延修正によって履歴位置を検証し、直接当時点の位置を使わないようにする

> [!warning] 指正
> 「長距離プレイヤー」是距离很远的玩家，不是网络中的“远程玩家”；应使用「リモートプレイヤー」。「遅延修正によって履歴位置を検証」没有准确表达“根据延迟补偿后的历史位置”，宜说「ラグ補償を適用した過去の位置に基づいて検証する」。「当時点」不自然，而且原文是当前坐标，应为「現在の座標」。题目是请求，句尾应使用「してください」。

> [!tip]- 参考回答
> リモートプレイヤーの攻撃が命中したかを判定する際は、現在の座標を直接使用するのではなく、<mark style="background: #FF5582A6;">ラグ補償を適用した過去の位置</mark>に基づいて検証してください。

---

### 练习 3

> [!question] 题目
> 在遍历实体期间不要直接添加或删除组件，请把结构变更记录到命令缓冲区，稍后统一执行。

> [!example] 自己的答案
>　エンティティを走査中に直接コンポーネントを追加したり削除したりしないで、構造変更をコマンドバッファーに記録し、後で実行する

> [!warning] 指正
> 「エンティティを走査中に」应改为「エンティティの走査中に」或「エンティティを走査している間は」。「実装する」是实现功能，不能表示执行已经记录的变更；这里应使用「実行する」。“统一执行”还应体现为「まとめて実行する」。请求句末也需改为「してください」。

> [!tip]- 参考回答
> エンティティの走査中はコンポーネントを直接追加・削除せず、構造変更をコマンドバッファに記録し、後で<mark style="background: #FF5582A6;">まとめて実行してください</mark>。

---

### 练习 4

> [!question] 题目
> 当虚拟纹理缓存接近上限时，请优先淘汰长时间未被访问的页面，并避免反复加载同一块数据。

> [!example] 自己的答案
>　バーチュルテクスチャキャッシュが上限に近い際に、長時間未使用のページを優先的に廃棄し、同一データブロックを重複してロードすることを避ける

> [!warning] 指正
> 「バーチュルテクスチャ」拼写错误，应为「バーチャルテクスチャ」。“长时间未被访问”不等同于未使用，宜准确表达为「長時間アクセスされていない」。「重複してロードする」偏向重复加载，原文强调反复发生，可说「同じデータブロックを何度も再ロードする」。题目是请求，句尾应使用「避けてください」。

> [!tip]- 参考回答
> バーチャルテクスチャのキャッシュが上限に近づいた場合は、長時間<mark style="background: #FF5582A6;">アクセスされていないページ</mark>を優先的に破棄し、同じデータブロックを何度も再ロードしないようにしてください。

---

### 练习 5

> [!question] 题目
> 如果当前字体不包含某个字符，请按语言设置选择后备字体，避免界面上显示方框。

> [!example] 自己的答案
>　フォント内にとある文字が含まれない場合、言語設定に応じてフォールバックフォントを使用し、白枠が表示されることを防ぐ

> [!warning] 指正
> 「とある文字」常用于叙述某个特定但不明说的对象，这里表示任意字符，使用「ある文字」更合适。「フォールバックフォントを使用」基本正确，但原文强调按语言设置选择，应说「言語設定に応じてフォールバックフォントを選択する」。“方框”是缺字时显示的四方形占位符，不是「白枠」，可表达为「四角い代替記号」或技术用语「豆腐」。句尾应使用请求形式。

> [!tip]- 参考回答
> 現在のフォントにある文字が含まれていない場合は、言語設定に応じてフォールバックフォントを選択し、画面に<mark style="background: #FF5582A6;">四角い代替記号</mark>が表示されないようにしてください。

---

### 练习 6

> [!question] 题目
> 升级存档格式时，请让迁移过程具备事务性，任何一步失败都必须恢复原始数据。

> [!example] 自己的答案
>　セーブデータをアップグレードする際に、遷移過程に、どのステップに失敗しても元に戻れるようにします

> [!warning] 指正
> 「セーブデータをアップグレード」也可理解，但题目升级的是存档格式，宜明确为「セーブデータの形式を更新する」。「遷移過程」表示状态转移过程，数据迁移应使用「移行処理」。“具备事务性”漏译，应表达为「トランザクションとして実行する」或「トランザクション性を持たせる」。「どのステップに失敗」助词错误，应为「どのステップで失敗」。此外，“恢复原始数据”应明确为「元のデータにロールバックする」。

> [!tip]- 参考回答
> セーブデータの形式を更新する際は、移行処理を<mark style="background: #FF5582A6;">トランザクションとして実行し</mark>、どのステップで失敗しても元のデータに<mark style="background: #FF5582A6;">ロールバック</mark>されるようにしてください。

---

### 练习 7

> [!question] 题目
> 为了防止玩家连续点击确认按钮触发多次请求，请在响应返回前暂时禁用该按钮。

> [!example] 自己的答案
>　プレイヤーがボタン連打で過剰なリクエストを送信することを防ぐため、リスパンスがこない前にボタンを非活性化してください

> [!warning] 指正
> 「ボタン連打」可理解，但漏掉了具体的“确认按钮”，应为「確認ボタンを連打する」。「過剰なリクエスト」是过量请求，原文强调同一操作触发多次请求，宜说「複数回リクエストが送信される」。「リスパンス」拼写错误，应为「レスポンス」。「こない前に」不能表示“返回前”，应使用「返ってくるまで」。「非活性化」不如界面术语「無効化」自然。

> [!tip]- 参考回答
> プレイヤーが確認ボタンを連打して複数回リクエストを送信するのを防ぐため、<mark style="background: #FF5582A6;">レスポンスが返ってくるまで</mark>そのボタンを一時的に無効化してください。

---

### 练习 8

> [!question] 题目
> 自动测试发生崩溃时，请保存日志、随机种子和输入记录，以便在本地重现相同问题。

> [!example] 自己的答案
>　自動テストがクラッシュする際に、ログにランダムシードと入力履歴を出力し、ローカルで問題を再現できるようにする

> [!warning] 指正
> 「クラッシュする際」更像每当崩溃时，表示测试发生崩溃这一条件宜用「クラッシュした場合」。“保存日志、随机种子和输入记录”是三个并列对象，原答案的「ログに～を出力」改变为把种子和记录写进日志；应分别说「ログ、ランダムシード、入力履歴を保存する」。「問題を再現」可补上「同じ」，准确对应相同问题。句尾应使用请求形式。

> [!tip]- 参考回答
> 自動テストがクラッシュした場合は、ログ、ランダムシード、入力履歴を保存し、ローカル環境で<mark style="background: #FF5582A6;">同じ問題を再現</mark>できるようにしてください。

---

### 练习 9

> [!question] 题目
> 那家店周末总是很拥挤，我们要不要提前订个位子？

> [!example] 自己的答案
>　あの店は週末にいつも混んでいるみたいから、事前に予約しない

> [!warning] 指正
> 「混んでいるみたいから」语法错误；若表示观察到的固定情况，应直接说「いつも混んでいるから」，若保留推测则为「混んでいるみたいだから」。“要不要提前订个位子”是提议，而「予約しない」只是“不预约”或不完整的否定疑问；可说「予約しておかない？」。「席」也可补出，使预约内容更明确。

> [!tip]- 参考回答
> あの店は週末はいつも混んでいるから、<mark style="background: #FF5582A6;">事前に席を予約しておかない</mark>？

---

### 练习 10

> [!question] 题目
> 我已经处理完昨天的评审意见，方便的话能请你今天再看一遍吗？

> [!example] 自己的答案
>　昨日のレビューを対応しました、本日お手隙の際にこれをチェックしていただけませんか

> [!warning] 指正
> 「レビューを対応しました」助词和搭配均不正确；“处理评审意见”应说「レビューで指摘された点に対応しました」或「レビューコメントへの対応が終わりました」。两个独立句子不宜只用逗号连接，应使用句号。「これをチェック」略显含糊，原文的“再看一遍”可明确为「もう一度確認する」。使用「お手すきの際に」表达方便时很自然。

> [!tip]- 参考回答
> 昨日のレビューで指摘された点への対応が完了しました。<mark style="background: #FF5582A6;">本日、お手すきの際にもう一度確認していただけますか</mark>。

## 2026-06-12

### 练习 1

> [!question] 题目
> 为了减少显存占用，请让渲染图中生命周期不重叠的临时纹理复用同一块内存。

> [!example] 自己的答案
> GPUメモリ使用量を軽減するため、ライフサイクルが重ならないテクスチャに同一メモリを再利用させるようにしてください

> [!warning] 指正
> 「GPUメモリ使用量を軽減する」可以理解，但“减少占用”更自然地说「GPUメモリ使用量を削減する」。“渲染图”和“临时纹理”均有漏译，应分别为「レンダーグラフ」和「一時テクスチャ」。「テクスチャに同一メモリを再利用させる」像是让纹理主动执行复用，宜改为「同じメモリ領域を共有させる」或「同じメモリ領域を再利用する」。

> [!tip] 参考回答
> GPUメモリ使用量を削減するため、レンダーグラフ内でライフタイムが重ならない<mark style="background: #FF5582A6;">一時テクスチャ同士で、同じメモリ領域を再利用してください</mark>。

---

### 练习 2

> [!question] 题目
> 如果客户端时钟逐渐偏离服务器，请在插值快照时平滑修正时间差，避免角色突然跳动。

> [!example] 自己的答案
>　クライアントのタイマーがサーバーのと徐々にズレていくと、キャラクターが突然に変動するを避けるため、スナップショットで補間を行ってスムーズに時間を修正してください

> [!warning] 指正
> 「タイマー」是计时器，这里的客户端时钟应为「クライアントの時計」。「サーバーのと」缺少被比较的名词，应为「サーバーの時計から」。「突然に変動するを避ける」语法错误，角色突然跳动可说「キャラクターが急に飛ぶのを防ぐ」。“在插值快照时”应表达为「スナップショットを補間する際に」，修正的对象是时间差，因此应说「時間のずれを滑らかに補正する」。

> [!tip] 参考回答
> クライアントの時計がサーバーの時計から徐々にずれていく場合は、スナップショットを補間する際に<mark style="background: #FF5582A6;">時間差を滑らかに補正し</mark>、キャラクターが急に飛ばないようにしてください。

---

### 练习 3

> [!question] 题目
> 请通过依赖注入传入输入设备接口，以便在单元测试中替换成模拟实现。

> [!example] 自己的答案
>　依頼注入を用いて設備のインターフェースを渡し、テストユニット実行中にシミレーション実装に切り替えるようにする

> [!warning] 指正
> 「依頼注入」是错误词语，“依赖注入”应为「依存性注入」。「設備」通常指设施或大型设备，输入设备应为「入力デバイス」。“单元测试”不是「テストユニット」，应使用「単体テスト」。「シミレーション」拼写错误，而且“模拟实现”在程序设计中通常说「モック実装」或「模擬実装」。句尾应改为请求形式。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">依存性注入</mark>によって入力デバイスのインターフェースを渡し、単体テストではモック実装に置き換えられるようにしてください。

---

### 练习 4

> [!question] 题目
> 异步加载资源时，请正确管理引用计数，确保最后一个使用者释放后资源才会被卸载。

> [!example] 自己的答案
>　非同期リソースロードする際に、参照引数を正しく管理し、最後の使用者が解放したまでリソースをアンロードするように確保する

> [!warning] 指正
> 「非同期リソースロードする」缺少助词，应为「リソースを非同期でロードする」。「参照引数」是引用参数，不是引用计数；应使用「参照カウント」。「解放したまで」语法错误，应为「解放してから」。“才会被卸载”强调在最后一个使用者释放引用之前不能卸载，宜表达为「最後の利用者が参照を解放した後にのみアンロードされる」。「確保する」在这里不自然，技术要求常用「～ようにしてください」。

> [!tip] 参考回答
> リソースを非同期でロードする際は、<mark style="background: #FF5582A6;">参照カウント</mark>を正しく管理し、最後の利用者が参照を解放した後にのみリソースがアンロードされるようにしてください。

---

### 练习 5

> [!question] 题目
> 使用 GPU 实例化绘制大量物体时，请把每个实例不同的颜色和变换数据放入实例缓冲区。

> [!example] 自己的答案
>　GPUインスタンスを用いて大量のオブジェクトを描画する際に、各インスタンスの独自の色と差分データをインスタンスバッファーに保存する

> [!warning] 指正
> 「GPUインスタンス」是 GPU 实例，“GPU 实例化”应为「GPUインスタンシング」。「各インスタンスの独自の色」不够自然，可说「インスタンスごとに異なる色」。“变换数据”指位置、旋转、缩放等变换信息，应为「変換データ」或「トランスフォームデータ」，不是「差分データ」。题目是请求，句尾应使用「格納してください」。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">GPUインスタンシング</mark>で大量のオブジェクトを描画する際は、インスタンスごとに異なる色とトランスフォームデータをインスタンスバッファに格納してください。

---

### 练习 6

> [!question] 题目
> 当任务列表包含数千个项目时，请只生成可见区域内的界面元素，并在滚动时复用它们。

> [!example] 自己的答案
>　ミッションタスクに数千項目が含まれる場合、見える範囲にあるアイテムのみを生成し、ローリング中にアイテムを再利用してください

> [!warning] 指正
> 「ミッションタスク」变成了任务或关卡中的任务，原文是界面上的任务列表，应为「タスクリスト」。「数千項目」应加助词，写成「数千件の項目」更自然。“界面元素”不能只译为「アイテム」，宜使用「UI要素」。「ローリング」是滚动运动或压延，界面滚动应为「スクロール」。复用对象是已生成的界面元素，应明确为「UI要素を再利用する」。

> [!tip] 参考回答
> タスクリストに数千件の項目が含まれる場合は、表示領域内の<mark style="background: #FF5582A6;">UI要素だけを生成し</mark>、スクロール時にそれらを再利用してください。

---

### 练习 7

> [!question] 题目
> 检测到本地存档与云端存档发生冲突时，请向玩家显示双方的更新时间，不要自动覆盖较新的数据。

> [!example] 自己的答案
>　ローカルセーブデータがリモートデータとコンフレクションしてた際に、プレイヤーに更新時間をそれぞれに表示し、勝手に直近データで上書きしてはいけない

> [!warning] 指正
> 「リモートデータ」只是远程数据，云端存档应明确为「クラウドセーブデータ」。「コンフレクション」不是日语中的冲突表达，应使用「競合」。「コンフレクションしてた際に」语体也不适合技术要求，可改为「競合を検出した場合」。“双方的更新时间”应明确为「両方の更新日時」。「勝手に直近データで上書き」变成用最新数据覆盖，原文是不自动覆盖较新的那份数据，应说「新しい方のデータを自動的に上書きしない」。

> [!tip] 参考回答
> ローカルとクラウドのセーブデータの競合を検出した場合は、両方の<mark style="background: #FF5582A6;">更新日時</mark>をプレイヤーに表示し、新しい方のデータを自動的に上書きしないようにしてください。

---

### 练习 8

> [!question] 题目
> 为了定位偶尔出现的输入延迟，请记录事件进入队列、被处理以及画面响应的时间戳。

> [!example] 自己的答案
>　稀に発生した入力の遅延を特定するため、イベント入列、処理及び画面反映のタイムスタンプを記録してください

> [!warning] 指正
> 「稀に発生した」表示过去偶尔发生过，题目描述一般性问题，应为「まれに発生する」。「イベント入列」不是自然的日语技术表达，应说「イベントがキューに入った時点」。“被处理”应使用被动态「処理された」。“画面响应”不只是内容被反映，宜表达为「画面が応答した時点」。三个时间点均需记录时间戳。

> [!tip] 参考回答
> まれに発生する入力遅延を特定するため、イベントの受付時刻、 処理時刻、および画面反映時刻を記録する。

---

### 练习 9

> [!question] 题目
> 我把钥匙忘在家里了，你现在方便帮我开一下门吗？

> [!example] 自己的答案
>　鍵を家に忘れちゃったから、今大丈夫ですか、ドアーを開けてもらえませんか

> [!warning] 指正
> 「忘れちゃった」是随意口语，但后面的「大丈夫ですか」「もらえませんか」是礼貌体，语体不统一；可统一为较自然的口语请求。「今大丈夫ですか」是询问现在是否没事，没有完整表达“现在方便帮我”。「ドアー」通常写作「ドア」。“开一下门”可说「ドアを開けてもらえる？」。

> [!tip] 参考回答
> 鍵を家に忘れちゃったんだけど、今手が空いていたら<mark style="background: #FF5582A6;">ドアを開けてもらえる</mark>？

---

### 练习 10

> [!question] 题目
> 我还需要半天时间完成测试，能否把提交期限延长到明天中午？

> [!example] 自己的答案
>　まだ半日でテストが完了できるので、納品日を明日昼に延期していただけませんか

> [!warning] 指正
> 「まだ半日でテストが完了できるので」表示还能在半天内完成，并用「ので」引出原因，与“还需要半天时间”不同；应说「テストを完了するまで、あと半日必要です」。「納品日」是交货日期，提交期限应为「提出期限」或「締め切り」。“延长到”不是单纯延期，宜用「明日の正午まで延長する」。请求延期时使用「延長していただけないでしょうか」更自然。

> [!tip] 参考回答
> テストを完了するまで、あと半日ほど必要です。提出期限を<mark style="background: #FF5582A6;">明日の正午まで延長していただけないでしょうか</mark>。

## 2026-06-13

### 练习 1

> [!question] 题目
> 为了避免阴影贴图边缘闪烁，请根据摄像机距离调整级联范围，并让分割位置平滑变化。

> [!example] 自己的答案
> シャドウテクスチャ辺縁の変動を避けるため、カメラ距離に応じてカスケードシャドウ範囲を調整し、分割位置を滑らかに変化する

> [!warning] 指正
> 「辺縁の変動」不自然，阴影贴图边缘闪烁宜说「シャドウマップの境界のちらつき」。原文的“摄像机距离”是与摄像机的距离，应为「カメラからの距離」。「分割位置を滑らかに変化する」中助词错误，应为「分割位置が滑らかに変化するようにする」。题目是请求，句尾宜使用「してください」。

> [!tip] 参考回答
> シャドウマップの<mark style="background: #FF5582A6;">境界</mark>が<mark style="background: #FF5582A6;">ちらつく</mark>のを防ぐため、カメラからの距離に応じてカスケードの範囲を調整し、分割位置が滑らかに変化するようにしてください。

---

### 练习 2

> [!question] 题目
> 当网络包乱序到达时，请按照序列号重新排列，并丢弃已经处理过的重复数据包。

> [!example] 自己的答案
>　メッセージが無順で返って来る場合、番号でソートして処理済みメッセージを廃棄してください

> [!warning] 指正
> 「メッセージ」范围较广，网络包应明确为「ネットワークパケット」。「無順」不是自然表达，乱序到达可说「順不同で到着する」。「返って来る」表示返回，与“到达”不符。“序列号”应完整译为「シーケンス番号」；重复数据包也应体现「重複したパケット」。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">ネットワークパケット</mark>が順不同で到着した場合は、<mark style="background: #FF5582A6;">シーケンス番号</mark>に従って並べ直し、すでに処理済みの重複パケットを破棄してください。

---

### 练习 3

> [!question] 题目
> 请把战斗计算与画面表现分离，以便在不启动渲染系统的情况下运行服务器端测试。

> [!example] 自己的答案
>　ダメージ計算を画面表示と分離し、レンダリングがなくてもサーバーがダメージ計算を実行できるようにする

> [!warning] 指正
> 原文是较广义的“战斗计算”，「ダメージ計算」缩小成了伤害计算，宜用「戦闘ロジック」或「戦闘処理」。“画面表现”也不只是画面显示，可表达为「描画処理」或「画面上の演出」。后半漏掉“服务器端测试”，应明确为「サーバー側のテスト」。请求句尾应使用「してください」。

> [!tip] 参考回答
> 戦闘ロジックと描画処理を分離し、レンダリングシステムを起動しなくてもサーバー側のテストを実行できるようにしてください。

---

### 练习 4

> [!question] 题目
> 后台线程完成资源解压后，不要直接修改场景对象，而应将更新任务提交到主线程。

> [!example] 自己的答案
>　バックグラウンドスレッドがリソース解凍処理を処理した後、直接シーン内のオブジェクトを適用しなく、切り替えタスクをメインスレッドに渡すようにする

> [!warning] 指正
> 「解凍処理を処理した」重复，应改为「リソースの展開を完了した」。「オブジェクトを適用する」搭配错误，修改对象应为「シーンオブジェクトを変更する」。「適用しなく」也有活用错误，应为「変更せず」。“更新任务”不是切换任务，应使用「更新タスク」。句尾应改为请求形式。

> [!tip] 参考回答
> バックグラウンドスレッドでリソースの展開が完了した後は、シーンオブジェクトを直接変更せず、更新タスクを<mark style="background: #FF5582A6;">メインスレッドに投入</mark>してください。

---

### 练习 5

> [!question] 题目
> 为了缩短启动时间，请延迟初始化当前流程不会使用的系统，并记录各阶段的耗时。

> [!example] 自己的答案
>　初起動時間を短縮するため、現時点に使わないシステムを後回して初期化し、各段階にかかる時間をそれぞれに計測する

> [!warning] 指正
> 「初起動時間」不自然，启动时间通常说「起動時間」。“当前流程”漏译，可用「現在の処理フロー」。「後回して初期化」可理解，但延迟初始化的技术表达是「遅延初期化する」。「各段階にかかる時間をそれぞれに計測」中的「に」多余，可说「各段階の所要時間を記録する」。句尾应使用请求形式。

> [!tip] 参考回答
> 起動時間を短縮するため、現在の処理フローで使用しないシステムは<mark style="background: #FF5582A6;">遅延初期化</mark>し、各段階の所要時間を記録してください。

---

### 练习 6

> [!question] 题目
> 当多个窗口同时请求显示提示框时，请按优先级排队，避免重要消息被普通通知遮住。

> [!example] 自己的答案
>　複数ウィンドウが同時にポップアップの表示をリクエストする際に、優先順でキューに入れ、重要なメッセージが一般メッセージに隠れないようにする

> [!warning] 指正
> 整体意思准确。「複数ウィンドウ」应加助词写成「複数のウィンドウ」。「優先順」可以理解，但「優先度に応じてキューに入れる」更自然。“普通通知”应译为「通常の通知」，而不是较模糊的「一般メッセージ」。请求句尾宜使用「してください」。

> [!tip] 参考回答
> 複数のウィンドウが同時にポップアップの表示を<mark style="background: #FF5582A6;">要求</mark>した場合は、優先度付きキューに入れ、重要なメッセージが通常の通知に隠れないようにしてください。

---

### 练习 7

> [!question] 题目
> 请在修改运行时配置前验证数值范围，发现无效参数时保留上一份有效设置。

> [!example] 自己的答案
>　実行中に数値を上書きする前に有効範囲を確認し、無効パラメーターを検出したら直近の設定を保存する

> [!warning] 指正
> 「実行中に数値を上書きする」变成运行过程中覆盖数值，原文是修改运行时配置，应为「ランタイム設定を変更する」。“数值范围”应明确为「値の範囲」。「直近の設定を保存する」表示保存最近设置，不一定是上一份有效设置；应说「直前の有効な設定を維持する」。

> [!tip] 参考回答
> ランタイム設定を変更する前に値の範囲を検証し、無効なパラメータが見つかった場合は、直前の有効な設定を<mark style="background: #FF5582A6;">維持</mark>してください。

---

### 练习 8

> [!question] 题目
> 调试物理穿透问题时，请逐帧记录碰撞体的位置、速度以及接触法线。

> [!example] 自己的答案
>　物理すり抜けをデバッグする際に、フレーム単位でコライダー位置、スピード及び接触点の法線を記録してください

> [!warning] 指正
> 整体表达清楚。「物理すり抜け」口语上可理解，技术语境中「物理オブジェクトの貫通」更明确。「コライダー位置」应加「の」，写成「コライダーの位置」。「スピード」可用，但与物理数据搭配时「速度」更准确。原文是接触法线，不必添加“接触点”，宜用「接触法線」。

> [!tip] 参考回答
> 物理オブジェクトの貫通問題をデバッグする際は、コライダーの位置、速度、接触法線を<mark style="background: #FF5582A6;">フレームごとに</mark>記録してください。

---

### 练习 9

> [!question] 题目
> 我刚泡了咖啡，你也想喝一杯吗？

> [!example] 自己的答案
>　さっきコーヒーを入れたけど、お前も一杯はどうだか

> [!warning] 指正
> 「さっき」表示不久前，但“刚泡好”用「今コーヒーをいれたところ」更贴切。「お前」语气粗鲁，普通日常对话应省略或使用对方名字。「一杯はどうだか」语法错误，邀请对方喝一杯可说「一杯飲む？」或「一杯どう？」。

> [!tip] 参考回答
> 今コーヒーをいれたところなんだけど、あなたも一杯飲む？

---

### 练习 10

> [!question] 题目
> 我已经把调查结果整理到文档里了，请在下次会议前确认一下是否有遗漏。

> [!example] 自己的答案
>　調査結果をドキュメントにまとめましたが、次の会議前に拔け漏れをもう一回確認していただけませんか

> [!warning] 指正
> 「まとめましたが」中的「が」带有转折或铺垫感，此处直接用句号更清晰。「拔け漏れ」中的汉字错误，应写作「抜け漏れ」。“是否有遗漏”宜表达为「抜け漏れがないか」。「もう一回」较口语，职场请求使用「ご確認いただけますか」更自然。

> [!tip] 参考回答
> 調査結果をドキュメントにまとめました。次回の会議までに、抜け漏れがないかご確認いただけますか。

## 2026-06-14

### 练习 1

> [!question] 题目
> 为了减少首次进入场景时的卡顿，请提前创建本关可能使用的图形管线状态，并将结果保存到缓存中。

> [!example] 自己的答案
> 初回シーンに入るカクつきを軽減するため、ステージに関連するグラフィックパイプライン状態を事前に構築し、結果をキャッシュする

> [!warning] 指正
> 「初回シーンに入る」不自然，可说「初めてシーンに入る際」。“本关可能使用的”不是「ステージに関連する」，应表达为「このステージで使用する可能性のある」。题目是请求，句尾宜用「キャッシュしてください」。

> [!tip] 参考回答
> 初めてシーンに入る際のカクつきを軽減するため、このステージで<mark style="background: #FF5582A6;">使用する可能性の</mark>あるグラフィックスパイプラインステートを事前に作成し、その結果をキャッシュしてください。

---

### 练习 2

> [!question] 题目
> 客户端重新连接后，请先应用服务器发送的完整快照，再丢弃已经过期的本地输入。

> [!example] 自己的答案
>　クライアントが再接続した後、まずサーバーから最新のスナップショットを差し替え、期限切れたローカル入力を廃棄する

> [!warning] 指正
> “完整快照”被误译成了「最新のスナップショット」，应为「完全なスナップショット」。「スナップショットを差し替える」不适合“应用”，宜用「適用する」。「期限切れた」缺少助词，应为「期限切れになった」或「古くなった」。句尾应使用请求形式。

> [!tip] 参考回答
> クライアントが再接続した後は、まずサーバーから送信された完全なスナップショットを適用し、その後、古くなったローカル入力を破棄してください。

---

### 练习 3

> [!question] 题目
> 为了避免工作线程读取到修改一半的配置，请用不可变快照一次性替换当前设置。

> [!example] 自己的答案
>　ワークスレッドが変更中の設定を読み込むことを避けるため、現在のスナップショットを不可変スナップショットと置き換えます

> [!warning] 指正
> 「変更中の設定」基本表达了修改过程，但原文强调“修改到一半的不完整状态”，可说「変更途中の設定」。“用不可变快照一次性替换当前设置”应以当前设置为被替换对象：「現在の設定を不変スナップショットで一度に置き換える」。请求句尾应使用「してください」。

> [!tip] 参考回答
> ワーカースレッドが変更途中の設定を読み込まないように、現在の設定を不変スナップショットで一度に置き換えてください。

---

### 练习 4

> [!question] 题目
> 当纹理流送超过显存预算时，请优先移除远处物体使用的高分辨率 mip 层级。

> [!example] 自己的答案
>　テクスチャスティムがGPUメモリ閾値オーバーする際に遠いオブジェクトを除外して高い解像度ミップマップを使用してください

> [!warning] 指正
> 「テクスチャスティム」拼写和术语错误，应为「テクスチャストリーミング」。「GPUメモリ閾値オーバーする」不自然，可说「VRAMの予算を超えた場合」。“移除”的对象是远处物体使用的高分辨率 mip 层级，不是移除远处物体后继续使用高分辨率 mip；应使用「高解像度のMipレベルを優先的に削除する」。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">テクスチャストリーミング</mark>がVRAMの予算を超えた場合は、遠くのオブジェクトで使用されている高解像度のMipレベルを優先的に削除してください。

---

### 练习 5

> [!question] 题目
> 读取 GPU 计算结果时，请使用异步回读，避免主线程等待显卡完成当前帧。

> [!example] 自己的答案
>　GPU計算結果を取得する際に、GPU完了するまで所要時間によってメインスレッドがブロックされないように非同期を用いて取得してください

> [!warning] 指正
> “异步回读”应明确为「非同期リードバック」，只说「非同期を用いて取得」不够具体。「GPU完了するまで所要時間によって」语法不成立，可改为「GPUが現在のフレームの処理を完了するまで」。“等待”用「待機する」比「ブロックされる」更贴近原文。

> [!tip] 参考回答
> GPU計算結果を取得する際は、非同期リードバックを利用し、GPU処理完了待ちによるメインスレッドのブロックを防ぐ

---

### 练习 6

> [!question] 题目
> 使用键盘切换菜单焦点时，请让辅助功能朗读新选中项目的名称和当前状态。

> [!example] 自己的答案
>　キーボードでメニューフォーカスを切り替える際に、アクセシビリティで現在フォーカスされた項目と状態を朗読させてください

> [!warning] 指正
> 「アクセシビリティで」像是“通过辅助功能”，这里宜明确为「アクセシビリティ機能に」。“新选中项目的名称”不能只译成「項目」，应补出「項目名」。「現在フォーカスされた」偏向已经被聚焦，切换后的新项目可说「新しく選択された」。

> [!tip] 参考回答
> キーボードでメニューのフォーカスを切り替えた際は、アクセシビリティ機能に、新しく選択された項目の名前と現在の状態を読み上げさせてください。

---

### 练习 7

> [!question] 题目
> 写入大型存档时，请先记录事务日志，以便游戏崩溃后能够恢复到最后一次完整状态。

> [!example] 自己的答案
>　大きなセーブデータを書き込む際に、トランザクションログを出力し、ゲームがクラッシュした後直近の状態に復帰するのができるようにする

> [!warning] 指正
> “先记录”应体现顺序，可用「まずトランザクションログを記録する」。「直近の状態」不一定是“最后一次完整状态”，应为「最後の完全な状態」。「復帰するのができる」语法错误，应说「復元できる」。题目是请求，句尾宜使用「してください」。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">大容量</mark>のセーブデータを書き込む際は、まずトランザクションログを記録し、ゲームがクラッシュしても<mark style="background: #FF5582A6;">直前の</mark>完全な状態に復元できるようにしてください。

---

### 练习 8

> [!question] 题目
> 请为发布版本保留与构建编号对应的调试符号，以便把崩溃地址还原成函数名和代码行。

> [!example] 自己的答案
>　クラッシュアドレスをにメソッドとコード行に復元するように、リリースバージョンに応じるシンボルファイルとビルド番号を保持してください

> [!warning] 指正
> 「クラッシュアドレスをに」助词错误，应为「クラッシュアドレスを」。“还原成函数名和代码行”宜用「関数名とソースコードの行番号に変換する」。「リリースバージョンに応じる」没有表达调试符号与构建编号对应，应为「ビルド番号に対応するデバッグシンボル」。方法通常称「メソッド」，但原文是函数名，使用「関数名」更准确。

> [!tip] 参考回答
> リリースビルドごとに対応するデバッグシンボルを保持し、クラッシュアドレスを関数名およびソースコード行番号へ復元できるようにする。

---

### 练习 9

> [!question] 题目
> 房间里有点闷，你介意我把窗户打开一会儿吗？

> [!example] 自己的答案
>　部屋で空気がまずいだから、窓をあけてもいいですか

> [!warning] 指正
> 「空気がまずい」表示空气味道或品质不好，不是房间闷；可说「部屋が少し蒸し暑い」或「少し空気がこもっている」。「まずいだから」语法错误，名词或ナ形容词后应接「だから」，但这里应整体换词。“打开一会儿”可补为「少しの間、窓を開ける」。

> [!tip] 参考回答
> 部屋が少し蒸し暑いんだけど、しばらく窓を開けてもいい？

---

### 练习 10

> [!question] 题目
> 请在今天下班前把会议纪要发给大家，并标出仍需负责人确认的事项。

> [!example] 自己的答案
>　本日仕事が上がる前にミーティングの記事を送り、要確認項目に関連者をラベルで付けます

> [!warning] 指正
> 「仕事が上がる」不表示下班，应使用「退勤する」；“下班前”可说「今日の退勤前までに」。「ミーティングの記事」是会议文章，会议纪要应为「議事録」。“标出仍需负责人确认的事项”不是给相关人员贴标签，应表达为「担当者による確認が必要な項目が分かるようにする」。原文是请求，句尾应使用「してください」。

> [!tip] 参考回答
> 今日の退勤前までに議事録を全員に送付し、まだ担当者の確認が必要な項目が分かるように印を付けてください。
