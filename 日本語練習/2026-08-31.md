## 2026-08-31

### 练习 1

> [!question] 题目
> 角色准备更换装备时，装备组件应先确认新装备仍然属于该角色，并检查目标槽位是否可用；只有两项检查都通过后，才替换当前装备。

> [!example] 自己的答案
> キャラクターが装備を切り替えようとしている時は、装備コンポーネントはまず装備の所有権が該当キャラクターにあっているか、目標スロットが空いているかを確認します。二つチェックを通した後に装備を切り替えます。

> [!warning] 指正
> 主要检查项和处理顺序都表达出来了。`所有権が該当キャラクターにあっているか` 不自然，应说角色是否拥有该装备，例如 `そのキャラクターが所有しているか`。`目標スロットが空いている` 只表示槽位为空，题目是槽位“可用”；`二つチェックを通した後` 应改为 `両方の確認に成功した場合のみ`，以保留“只有两项都通过”的限制。

> [!tip] 参考回答
> キャラクターが装備を切り替えようとしたときは、装備コンポーネントがまず新しい装備をそのキャラクターが所有していることと、装備先のスロットが使用可能であることを確認します。両方の確認に成功した場合のみ、現在の装備を切り替えます。

---

### 练习 2

> [!question] 题目
> 任务追踪系统负责确定当前应该显示的目标，指南针界面只把已经确定的目标转换为画面上的方向标记，不能自行改变目标的优先级。

> [!example] 自己的答案
>　タスク進捗システムは現在の目標を表示し、コンパス画面は決まったターゲットを画面上の方向印に変換し、勝手に目標の優先度を変えてはいけません。

> [!warning] 指正
> UI 不能修改优先级这一限制表达正确，但前半把“确定显示目标”写成了直接“显示目标”。`タスク進捗システム` 是任务进度系统，题目的任务追踪系统宜说 `タスク追跡システム`。`方向印` 不自然，应使用 `方向マーカー`；技术说明中的“自行改变”用 `独自に変更する` 比 `勝手に` 更合适。

> [!tip] 参考回答
> タスク追跡システムが現在表示すべき目標を決定し、コンパスUIは<mark style="background: #FF5582A6;">確定した目標</mark>を画面上の方向マーカーに変換するだけで、目標の優先度を独自に変更してはいけません。

---

### 练习 3

> [!question] 题目
> 调查虚拟纹理在镜头快速移动时出现模糊的问题时，请记录纹理细节层级（Mip）的请求数量、页面加载等待时间以及图形处理器（GPU）的纹理采样耗时。

> [!example] 自己的答案
>　カメラが高速移動中にバチュルテクスチャがぼかし不具合を調査する際に、テクスチャのMipの要求数、ページロード待ち時間及びGPUサンプリングにかかる時間を記録してください。

> [!warning] 指正
> 三个记录项目基本都写到了。`バチュルテクスチャ` 是拼写错误，应为 `バーチャルテクスチャ`；`ぼかし不具合` 不自然，画面变模糊应说 `ぼやける問題`。`GPUサンプリング` 范围不够明确，应完整表达为 `GPUのテクスチャサンプリングにかかる時間`。

> [!tip] 参考回答
> カメラが高速で移動するときにバーチャルテクスチャがぼやける問題を調査する場合は、テクスチャのMipレベルの要求数、ページの読み込み待ち時間、GPUのテクスチャサンプリングにかかる時間を記録してください。

---

### 练习 4

> [!question] 题目
> 从对象池中取出敌人后，敌人管理组件必须重新设置生命值、人工智能状态和事件订阅；如果初始化失败，应将该敌人放回对象池，并且不能让它出现在画面中。

> [!example] 自己的答案
>　オブジェクトプールから敵オブジェクトを取り出した後、敵コンポーネントはHP、AI状態、イベントの購読を必ずにリセットします。初期化に失敗した場合、敵をプールに返却すべきです。該当敵が画面内で表示されてはいけません。

> [!warning] 指正
> 初始化失败后放回对象池并禁止显示的流程完整。`敵コンポーネント` 没有表达“敌人管理组件”，应为 `敵管理コンポーネント`；`必ずに` 应改为 `必ず`。事件订阅不是简单重置数值，宜说 `イベント購読を設定し直す`。最后一句用 `画面に表示されないようにします` 更自然。

> [!tip] 参考回答
> オブジェクトプールから敵を取り出した後は、敵管理コンポーネントがHP、AIの状態、イベント購読を必ず設定し直します。初期化に失敗した場合は、その敵をオブジェクトプールに戻し、画面に表示されないようにします。

---

### 练习 5

> [!question] 题目
> 读取旧版本存档时，存档系统应先把数据转换为当前格式，并在验证成功后再替换内存中的当前进度；如果转换失败，应保留原数据并显示具体的失败原因。

> [!example] 自己的答案
>　旧バージョンを読み込むときは、セーブシステムは該当データを現在のフォマットに変換し、検証に成功した後に進捗データを書き込みます。失敗した場合は、元のデータをそのままに保持して具体的原因を表示します。

> [!warning] 指正
> 转换、验证和失败保留的主干正确。`旧バージョンを読み込む` 缺少读取对象，应写成 `旧バージョンのセーブデータを読み込む`；`フォマット` 应为 `フォーマット`。`進捗データを書き込みます` 没有准确表达“替换内存中的当前进度”，还应明确失败的是格式转换，并使用 `具体的な失敗理由`。

> [!tip] 参考回答
> 旧バージョンのセーブデータを読み込むときは、セーブシステムがまずデータを現在のフォーマットに変換し、検証に成功した後で、メモリ上の現在の進行データを置き換えます。変換に失敗した場合は、元のデータを保持し、具体的な失敗理由を表示します。

---

### 练习 6

> [!question] 题目
> 伤害计算完成后，战斗系统只把最终伤害值和受击方向交给表现层；表现层可以选择受击动作和特效，但不能重新计算伤害。

> [!example] 自己的答案
>　ダメージ決定が終了後に、バトルシステムは最終のダメージ値と被弾ベクトルだけを表現層に渡します。表現層は被弾アニメーションとエフェクトを選択できるがダメージを再計算してはいけません。

> [!warning] 指正
> 职责边界和禁止重新计算伤害的要求都已表达。`ダメージ決定が終了後に` 语法不自然，应使用 `ダメージ計算が完了した後`。`最終のダメージ値` 宜说 `最終ダメージ値`；题目是受击方向，`被弾方向` 比 `被弾ベクトル` 更直接。后半应统一敬体，使用 `選択できますが`。

> [!tip] 参考回答
> ダメージ計算が完了した後、戦闘システムは最終ダメージ値と被弾方向だけを演出層に渡します。演出層は被弾モーションとエフェクトを選択できますが、ダメージを再計算してはいけません。

---

### 练习 7

> [!question] 题目
> 使用动作匹配（Motion Matching）选择转身动作时，应比较角色当前速度、期望方向和候选动作的起始姿势，并记录最终被选中的候选动作编号。

> [!example] 自己的答案
>　Motion Matchingを使って回転アニメーションを選ぶときは、キャラクターの速度、希望方向、候補アニメーション開始ポーズを比較して決定されたアニメーション番号を記録してください。

> [!warning] 指正
> 比较项目和记录动作基本完整。转身动作使用 `振り向きモーション` 比 `回転アニメーション` 更自然；`希望方向` 宜改为技术语境中的 `目標方向`。`候補アニメーション開始ポーズ` 缺少助词，应为 `候補モーションの開始姿勢`；最后记录的是候选动作编号，应明确为 `選択された候補モーションの番号`。

> [!tip] 参考回答
> Motion Matchingを使って<mark style="background: #FF5582A6;">振り向き</mark>モーションを選択するときは、キャラクターの現在速度、目標方向、候補モーションの開始姿勢を比較し、最終的に選択された候補モーションの番号を記録します。

---

### 练习 8

> [!question] 题目
> 为可破坏物体调整物理约束时，请分别测试持续施加的力和瞬间产生的冲量，并确认物体断裂后，残留的约束已经从物理场景中移除。

> [!example] 自己的答案
>　壊せるオブジェクトの物理制限を調整する時は、持続的な力と瞬間的な力積を別々にテストし、オブジェクトが別れた後に、制限が物理シーン内から取り除かれたことを確認します。

> [!warning] 指正
> 持续力、瞬间力积和断裂后清理都表达到了。物理术语“约束”是 `制約`，不是表示限制的 `制限`；可破坏物体宜说 `破壊可能なオブジェクト`。物体断裂是 `破断する`，不能使用表示分开的 `別れる`。句末应与题目的请求语气一致，使用 `確認してください`。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">破壊可能な</mark>オブジェクトの物理制約を調整するときは、持続的に加える力と瞬間的な力積を別々にテストし、オブジェクトが破断した後に、残っている制約が物理シーンから取り除かれていることを確認してください。

---

### 练习 9

> [!question] 题目
> 当人工智能对两个目标的评分非常接近时，应继续保留当前目标；只有新目标的分数超过设定差值并持续一段时间后，才切换到新目标，以避免目标在两者之间反复变化。

> [!example] 自己的答案
>　AIは二つスコアがとても近い評価に対する時に、現在のターゲットを保持すべきで、新しいターゲットのスコアが設定差分を一定時間に超えてた後、新しいターゲットに切り替えます。二つターゲット間で繰り返して変化することを防ぎます。

> [!warning] 指正
> 保留当前目标和延迟切换的方向正确，但前半 `二つスコアがとても近い評価に対する時に` 无法成立，应写成 `二つのターゲットに対する評価スコアが非常に近いとき`。`設定差分を一定時間に超えてた後` 也没有明确“新分数超过当前分数并持续一段时间”；结尾应说 `二つのターゲットの間で`。

> [!tip] 参考回答
> AIが二つのターゲットに付けた評価スコアが非常に近いときは、現在のターゲットを維持します。新しいターゲットのスコアが現在のターゲットを設定された差分以上上回る状態が<mark style="background: #FF5582A6;">一定時間続いた</mark>場合のみ、新しいターゲットに切り替え、二つのターゲットの間で選択が繰り返し変わるのを防ぎます。

---

### 练习 10

> [!question] 题目
> 玩家修改按键设置时，输入设置模块应先检查新按键是否与现有操作冲突；只有玩家确认替换后，才更新两个操作的按键绑定并保存配置。

> [!example] 自己的答案
>　プレイヤーがキー設定を変更するときは、入力設定モージュルはまず新しく設定するキーと既存の設定が衝突していないかを確認し、プレイヤーが確認済み後にのみ、二つキーバインディグを保存します。

> [!warning] 指正
> 冲突检查和玩家确认后的保存时机基本正确。`モージュル` 应为 `モジュール`，`キーバインディグ` 应为 `キーバインディング`。`プレイヤーが確認済み後` 不自然，应说 `プレイヤーが置き換えを確認した後`。答案只写了保存绑定，没有明确先更新两个操作各自的绑定再保存配置。

> [!tip] 参考回答
> プレイヤーがキー設定を変更するときは、入力設定モジュールがまず新しいキーと既存の操作との<mark style="background: #FF5582A6;">競合</mark>を確認します。プレイヤーが置き換えを確認した後でのみ、二つの操作のキーバインディングを更新し、設定を保存します。

---

### 练习 11

> [!question] 题目
> 场景从室内过渡到室外时，音频系统应根据过渡区域逐渐改变混响参数；如果区域数据缺失，应使用默认的环境设置，并记录发生数据缺失的位置。

> [!example] 自己的答案
>　レベルは室内から室外に遷移する時は、オーディオシステムは遷移先エリアによってブレンドパラメーターを徐々に変更します。エリアに対応するデータが欠落している場合、デフォルト値を使って欠落位置を記録してください。

> [!warning] 指正
> 过渡和缺失数据的分支表达出来了，但主体应是场景或环境从室内过渡到室外，而不是 `レベルは`。题目要求改变“混响参数”，应使用 `リバーブパラメータ`，不能替换成普通的混合参数 `ブレンドパラメーター`。缺失时应使用完整的 `デフォルトの環境設定`，并记录缺失数据对应的位置。

> [!tip] 参考回答
> シーンが屋内から屋外へ移り変わるときは、オーディオシステムが遷移エリアに応じてリバーブパラメータを徐々に変更します。エリアのデータが欠けている場合は、デフォルトの環境設定を使用し、データが欠けている場所を記録します。

---

### 练习 12

> [!question] 题目
> 调查对象数量增加后中央处理器（CPU）耗时上升的问题时，请分别记录参与更新的对象数量、每个更新阶段的耗时，以及这一帧实际执行的功能类别。

> [!example] 自己的答案
>　オブジェクト数が増えた後CPU使用率が上がる問題を調査する時に、更新されたオブジェク数、各段階かかった時間、該当フレームで実行された機能種類をそれぞれ記録してください。

> [!warning] 指正
> 三个记录项目都没有遗漏，但把“中央处理器耗时”改成了“中央处理器使用率”，两者不是同一个指标，应使用 `CPU処理時間`。`オブジェク数` 是拼写错误，应为 `オブジェクト数`；`各段階かかった時間` 应改为 `各更新段階にかかった時間`，`機能種類` 应加助词成为 `機能の種類`。

> [!tip] 参考回答
> オブジェクト数が増えた後にCPU処理時間が増加する問題を調査するときは、更新対象となったオブジェクト数、各更新段階にかかった時間、そのフレームで実際に実行された機能の種類をそれぞれ記録してください。

---

### 练习 13

> [!question] 题目
> 面试中设计可以叠加的状态效果系统时，请分别说明效果叠加次数、持续时间更新和提前移除的规则，并解释如何测试多个效果在同一帧结束时产生的处理成本。

> [!example] 自己的答案
> バフシステムを設計する時は、スタック、持続時間、早め削除ルールなどルールが明確する必要があります。バフがどのオブジェクトに所属するか、キャラクターがバフを直接持つのは一般的です。その上でキャラクターが毎回行動する前に持っているバフを決算します。キャラクターに複数のキューを持たせる。付与待ちバフキュー、動作中バフキュー、削除バフキューです。処理順はまず動作中バフキューをループして回数を更新して期限切れたバフを削除キューに入れます。その後削除バフキューをループして削除します。最後に付与待ちバフキューをループして追加します。

> [!warning] 指正
> 付与待ち、运行中和删除三个队列的设计能够避免遍历过程中直接修改集合，技术思路有价值。不过答案没有分别说明叠加次数的上限、重复添加时如何更新持续时间、哪些条件允许提前移除，也没有说明如何构造“多个效果在同一帧结束”的测试并测量处理成本。语言上 `ルールが明確する` 应为 `ルールを明確にする`，`バフを決算します` 中的 `決算` 是财务结算，应改为 `処理します`。

> [!tip] 参考回答
> 同じ種類の効果が付与された場合は、上限までスタック数を増やし、上限を超えた分は無視します。持続時間は、再付与時に更新するのか加算するのかを効果ごとに定義します。<mark style="background: #FF5582A6;">早期解除</mark>の要求は処理中に直接削除せず、削除待ちキューに入れて更新後にまとめて反映します。複数の効果が同じフレームで終了するケースを大量に作り、処理時間、メモリ割り当て、削除結果を測定してコストを検証します。

---

### 练习 14

> [!question] 题目
> 你今晚有空吗？我想再去一次上次你带我去的那家店喝一杯。如果方便的话，要不要一起去？

> [!example] 自己的答案
>　今晩都合がいいか、前回に連れて行った店にもう一回行って飲もうと思って、良ければ、一緒に行かない

> [!warning] 指正
> 邀请对方一起去喝一杯的意思可以理解。`今晩都合がいいか` 是嵌入句形式，直接询问应说 `今晩、時間ある？`。题目是对方上次带自己去，因此必须使用授受表达 `連れていってくれた店`，而不是 `連れて行った店`。`行って飲もうと思って` 不自然，可用 `また飲みに行きたいと思ってるんだけど`。

> [!tip] 参考回答
> 今晩、時間ある？ この前連れていってくれた店に、また飲みに行きたいと思ってるんだけど、よかったら一緒に行かない？

---

### 练习 15

> [!question] 题目
> 除了任务清单以外，我觉得最好再制作一份各页面的进度表。我有现成的模板，稍后发给你。

> [!example] 自己的答案
>　タスクリスト以外、各画面の進捗テーブルをもう一つ分を作成したほうがいいと思います。テンプレートがあるんだ、後で送ります。

> [!warning] 指正
> 制作进度表并发送模板的意思完整。`タスクリスト以外` 应改为表示另行制作的 `タスクリストとは別に`。题目是各页面，应使用 `各ページ`，不是 `各画面`；`もう一つ分` 多余。职场语境中 `テンプレートがあるんだ` 过于随意，应使用 `ひな形があるので` 与后句自然连接。

> [!tip] 参考回答
> タスクリストとは別に、各ページの進捗リストも作ったほうがいいと思います。ひな形があるので、後で送ります。

## 2026-09-01

### 练习 1

> [!question] 题目
> 角色进入冲刺状态之前，移动组件应确认耐力足够，并确认角色没有处于无法移动的状态；只有状态切换成功后才开始持续消耗耐力，如果切换失败则保持原来的移动速度。

> [!example] 自己的答案
>　キャラクターはダーシュ状態に入る前に、移動コンポーネントはまず体力が足りてるかを確認し、キャラクタが移動不可状態ではないことを確認します。状態を正常に切り替えた後にのみ体力を消費します。状態切り替えに失敗した場合、元の移動速度を維持します。

> [!warning] 指正
> `ダーシュ` 应为 `ダッシュ`，`体力` 表示体力或生命力，题目中的“耐力”应使用 `スタミナ`。`キャラクタ` 少了长音，应为 `キャラクター`；技术说明中宜写成 `足りているか`。另外，答案只写了切换后消耗耐力，遗漏了“开始持续消耗”，应明确写成 `継続消費を開始する`。

> [!tip] 参考回答
> キャラクターがダッシュ状態に入る前に、移動コンポーネントはスタミナが十分にあり、キャラクターが移動不能状態ではないことを確認します。状態の切り替えに成功した後でのみスタミナの継続消費を開始し、失敗した場合は元の移動速度を維持します。

---

### 练习 2

> [!question] 题目
> 任务奖励模块负责生成奖励清单，并确认每项奖励是否发放成功；界面只显示已经确认的结果，不能根据动画是否播放完毕来修改奖励数量。

> [!example] 自己的答案
>　クエストモージュルは報酬リストの生成を担当し、各項目が正常に配られるたことを確認します。画面は確定結果を表示するだけで、アニメーションの進行によって報酬数を変更してはいけません。

> [!warning] 指正
> `クエストモージュル` 应改为 `クエスト報酬モジュール`；`配られるたこと` 语法错误，应说 `付与されたこと`。后半的 `アニメーションの進行によって` 没有准确表达“动画是否播放完毕”，应改为 `アニメーションの再生が完了したかどうかによって`。`報酬数` 可以理解，但 `報酬の数量` 更完整。

> [!tip] 参考回答
> クエスト報酬モジュールは報酬リストを生成し、各報酬が正常に付与されたかどうかを確認します。UIは確認済みの結果だけを表示し、アニメーションの再生が完了したかどうかによって報酬の数量を変更してはいけません。

---

### 练习 3

> [!question] 题目
> 调查屏幕空间反射（SSR）在画面边缘突然消失的问题时，请记录相机视野角度、深度缓冲中的物体位置，以及反射射线允许检测的最大距离。

> [!example] 自己的答案
>　SSRによる画面の境界に消える問題を調査する時は、カメラの視野角、オブジェクトは深度バッファに位置、反射検出最大の距離を記録してください。

> [!warning] 指正
> `SSRによる画面の境界に消える問題` 缺少“反射”这一主语，而且助词不自然，应说 `SSRが画面の端で突然消える問題`。`オブジェクトは深度バッファに位置` 语法不成立，应改为 `深度バッファ上のオブジェクトの位置`。最后一项应明确是反射射线允许判定的最大距离。

> [!tip] 参考回答
> スクリーンスペースリフレクション（SSR）が画面の端で突然消える問題を調査するときは、カメラの視野角、深度バッファ上のオブジェクトの位置、反射レイが判定できる最大距離を記録してください。

---

### 练习 4

> [!question] 题目
> 切换角色外观时，外观系统应先载入全部必需资源，并在模型隐藏期间完成材质绑定；只有准备工作全部完成后才替换当前模型，如果准备失败则释放本次载入的新资源。

> [!example] 自己的答案
>　キャラクター外見を切り替える時は、外見システはまず必要になるリソースを全部読み込み、モデルを隠すうちにマテリアルのバインディングがを完成させます。全部が整った後にのみ現在のモデルを切り替えます。準備に失敗した場合今回の読み込んだリソースを解放します。

> [!warning] 指正
> `外見システ` 应为 `外見システム`；`モデルを隠すうちに` 应改为“模型处于隐藏期间”的 `モデルを非表示にしている間に`。`バインディングがを` 助词重复，应直接说 `バインディングを完了させます`。`今回の読み込んだリソース` 应为 `今回読み込んだ新しいリソース`，并应明确准备完成后是替换当前模型，而不是笼统地再次“切换”。

> [!tip] 参考回答
> キャラクターの外見を切り替えるときは、外見システムがまず必要なリソースをすべて読み込み、モデルを非表示にしている間にマテリアルのバインディングを完了させます。すべての準備が完了した後でのみ現在のモデルを置き換え、準備に失敗した場合は今回読み込んだ新しいリソースを解放します。

---

### 练习 5

> [!question] 题目
> 回放系统记录输入事件时，每个事件都应包含发生帧、事件类型和顺序编号；播放回放时如果发现编号缺失，应停止应用后续事件，并报告第一个缺失编号所在的位置。

> [!example] 自己的答案
>　リプレイシステムは入力イベントを記録する時は、各イベントに発生フレーム、イベントタイプとシーケンスを含めるべきです。リプレイ最中にシーケンスが欠落する場合、すぐに停止し、一番目欠落した番号をリポートします。

> [!warning] 指正
> `記録する時は` 前面已经有主题助词，整句宜改成 `記録するときは`。题目中的“顺序编号”应使用 `順序番号`，`シーケンス` 不够准确。`リプレイ最中` 应为 `リプレイの再生中`；发现缺失时不是笼统地“立即停止”，而是停止应用后续事件。`一番目欠落した番号` 也不自然，应表达为最初缺失编号所在的位置。

> [!tip] 参考回答
> リプレイシステムが入力イベントを記録するときは、各イベントに発生フレーム、イベントの種類、順序番号を含めます。リプレイの再生中に番号の欠落を検出した場合は、後続イベントの適用を停止し、最初に欠落した番号の位置を報告します。

---

### 练习 6

> [!question] 题目
> 交互系统完成开门条件的判断后，只把成功结果或具体失败原因交给门对象；门对象负责播放开门动作和音效，但不能重新检查玩家是否持有钥匙。

> [!example] 自己的答案
>　インタラクションシステムはドアーを開く条件を判断した後に、成功または失敗原因をドアーオブジェクトに渡すだけで、ドアーオブジェクトはドアーを開くアニメーションと効果音の再生を担当するだけで、プレイヤーが鍵を持っているかを再判定しないようにします。

> [!warning] 指正
> 日语中通常写作 `ドア`，不是 `ドアー`。`成功または失敗原因` 缺少修饰关系，应说 `成功結果または具体的な失敗理由`。整句连续使用多个 `だけで` 显得累赘，宜分成两句；最后应直接说明门对象“不得重新判断”，而不是较弱的 `再判定しないようにします`。

> [!tip] 参考回答
> インタラクションシステムはドアを開ける条件の判定後、成功結果または具体的な失敗理由だけをドアオブジェクトに渡します。ドアオブジェクトは開くアニメーションと効果音の再生を担当しますが、プレイヤーが鍵を持っているかどうかを再判定してはいけません。

---

### 练习 7

> [!question] 题目
> 攻击动作被提前中断时，动画系统必须结束仍处于活动状态的动画通知状态区间（Anim Notify State），并关闭由该区间打开的碰撞判定，防止下一段动作继续使用旧状态。

> [!example] 自己的答案
>　攻撃アニメーションが早期に中断された場合、アニメーションシステムはまだ動いているAnimNotifyStateを必ずに終わらせて、次のモーションが旧状態を使い続けることを避けるため、該当ステートによる衝突判定を閉じます。

> [!warning] 指正
> `必ずに` 应改为 `必ず`。仍处于活动状态并不是“还在运动”，应说 `まだ有効なAnim Notify State`。碰撞判定宜使用 `無効にする`，而不是直译成 `閉じる`；目的句中的 `旧状態を使い続ける` 可以改为更自然的 `古い状態を引き継ぐ`。

> [!tip] 参考回答
> 攻撃モーションが途中で中断された場合、アニメーションシステムはまだ有効なAnim Notify Stateを必ず終了させ、その区間によって有効化されたコリジョン判定を無効にします。これにより、次のモーションが古い状態を引き継ぐのを防ぎます。

---

### 练习 8

> [!question] 题目
> 角色从移动平台上跳下时，移动组件应分别计算平台传递的速度和角色自身的起跳速度，避免把平台速度重复加到角色身上，并记录角色离开平台后一帧的实际速度。

> [!example] 自己的答案
> キャラクターが移動プラットフォームから跳ね上がる時は、移動コンポーネントはプラットフォームから速度とキャラクター自身の跳ね上がる開始の速度を個別に計算して、プラットフォーム速度を二度とキャラクターに加算しないようにします。そひてキャラクターがプラットフォームから離れる際に最後の一フレーム実際の速度を記録してください。

> [!warning] 指正
> `跳ね上がる` 是向上弹起，题目中的“从平台上跳下”应为 `飛び降りる`。`プラットフォームから速度` 缺少谓语关系，应说 `プラットフォームから受け継ぐ速度`；`跳ね上がる開始の速度` 应改为 `自身の跳躍初速`。`二度と` 表示“再也不”，这里应使用“不要重复加算”的 `重複して加算しない`。另外，`そひて` 是拼写错误；“离开平台后一帧”是 `離れた次のフレーム`，不是“最后一帧”。

> [!tip] 参考回答
> キャラクターが移動プラットフォームから飛び降りるときは、移動コンポーネントがプラットフォームから受け継ぐ速度とキャラクター自身の跳躍初速を別々に計算し、プラットフォームの速度を重複して加算しないようにします。また、キャラクターがプラットフォームを離れた次のフレームの実際の速度を記録します。

---

### 练习 9

> [!question] 题目
> 人工智能使用的当前路径因为障碍发生变化而无法继续使用时，应先停止沿旧路径移动并重新计算路径；找到有效路径后再继续移动，如果没有找到路径则停止角色并记录具体失败原因。

> [!example] 自己的答案
> AIは現在の経路を使用する時に障害物の変化によって使えなくなる場合、旧経路を停止して新しい経路を計算し直します。有効な経路を見つけた後に移動し続け、見つけなかった場合キャラクターを停止して具体的な失敗原因を記録します。

> [!warning] 指正
> 开头的条件关系不够清楚，宜说 `AIが使用中の経路を障害物の変化によって利用できなくなった場合`。路径本身不能“停止”，`旧経路を停止する` 应改为停止沿旧路径移动。`見つけなかった場合` 是主动“没有寻找”，题目中的“没有找到”应使用自动词 `見つからなかった場合`。

> [!tip] 参考回答
> AIが使用中の経路を障害物の変化によって利用できなくなった場合は、まず古い経路に沿った移動を停止し、経路を再計算します。有効な経路が見つかった後で移動を再開し、見つからなかった場合はキャラクターを停止して具体的な失敗理由を記録します。

---

### 练习 10

> [!question] 题目
> 列表的筛选条件发生变化后，界面系统应根据稳定的项目标识查找原来选中的项目；如果该项目已经不再显示，应清除选择状态，并把第一个可用项目设置为新的焦点。

> [!example] 自己的答案
>　リストフィールド状態が変化した後に、画面システムは項目IDで元の項目を選択します。該当項目が存在しない場合、選択状態をクリアして、最初な有効項目を新しいフォーカスにします。

> [!warning] 指正
> `リストフィールド状態` 不是“列表的筛选条件”，应使用 `リストのフィルター条件`。题目要求根据稳定标识“查找”原来选中的项目，不能直接写成 `選択します`。项目不再显示不等于项目不存在，应说 `表示対象ではなくなっている場合`。`最初な` 应为 `最初の`，设置焦点宜表达为 `フォーカスを移します`。

> [!tip] 参考回答
> リストのフィルター条件が変わった後は、UIシステムが安定した項目IDを使って、以前選択されていた項目を検索します。その項目が表示対象ではなくなっている場合は、選択状態をクリアし、最初の利用可能な項目へフォーカスを移します。

---

### 练习 11

> [!question] 题目
> 调查脚步声音量忽大忽小的问题时，请记录听者与角色之间的距离、距离衰减曲线输出的数值，以及遮挡检测在同一帧得到的结果。

> [!example] 自己的答案
> 足音が不安定している不具合を調査する時は、聴取者がキャラクターとの距離、距離による減衰カーブの数値、及びオクルージョン検出結果を記録してください。

> [!warning] 指正
> `足音が不安定している` 不自然，也遗漏了不稳定的是音量，应说 `足音の音量が不安定になる`。`聴取者がキャラクターとの距離` 助词错误，应为 `リスナーとキャラクターの距離`。`距離による減衰カーブの数値` 应明确为曲线的输出值；答案还遗漏了遮挡检测结果必须来自同一帧这一条件。

> [!tip] 参考回答
> 足音の音量が不安定になる問題を調査するときは、リスナーとキャラクターの距離、距離減衰カーブの出力値、同じフレームで得られたオクルージョン判定の結果を記録してください。

---

### 练习 12

> [!question] 题目
> 调查关卡载入过程中出现停顿的问题时，请分别记录文件读取与解压缩的耗时、资源对象的创建耗时，以及渲染状态登记完成的时间点。

> [!example] 自己的答案
>　レベルを読み込む際にカクツキが発生する不具合を調査する時は、ファイルの読み込み、圧縮とリソースオブジェクトの生成にかかった時間、描画状態が完了した時刻をそれぞれ記録してください。

> [!warning] 指正
> `読み込む際に……調査する時は` 的时间表达重复。最重要的是，题目中的“解压缩”被写成了相反的 `圧縮`，应使用 `解凍` 或 `展開`。文件读取与解压缩的耗时、资源对象创建耗时应分别记录；`描画状態が完了した時刻` 还遗漏了“登记”，应写成 `描画状態の登録が完了した時点`。

> [!tip] 参考回答
> レベルの読み込み中にカクつきが発生する問題を調査するときは、ファイルの読み込みと解凍にかかった時間、リソースオブジェクトの生成時間、描画状態の登録が完了した時点をそれぞれ記録してください。

---

### 练习 13

> [!question] 题目
> 面试中设计游戏对象标识系统时，请说明如何保证每个标识保持唯一、对象销毁后如何防止旧标识指向新对象，以及如何测试大量创建和销毁对象时的查找成本。

> [!example] 自己的答案
>　唯一性を確保するために、エンティティIDを生成するシングルトンマネージャーを作成します。すべてのIDをそのマネージャーを通じて生成します。マネージャーはマチルスレッド場合でも唯一を担保します。
>　オブジェクトを廃棄した後にすぐ対する参照にヌルを代入することで廃棄されたオブジェクトを使い続けることを防ぎます。大量のオブジェクトを同時に廃棄したり生成したりすることをテストする時に、プロファイラ上で該当フレームで処理時間とGCコラムを確認します。

> [!warning] 指正
> 使用统一管理器生成标识的方向合理，但还需要说明并发生成时如何保证唯一性。`マチルスレッド` 应为 `マルチスレッド`，`唯一を担保します` 应改为 `一意性を保証します`。对象销毁后把引用设为空，只能防止通过该引用继续使用已销毁对象，不能防止旧标识指向后来复用同一位置的新对象；这里需要世代编号、版本号或永不复用的标识。测试部分也不能只看某一帧的处理时间和垃圾回收数据，还应测量大量创建、销毁时的查找成本，以及旧标识能否被可靠判定为无效。

> [!tip] 参考回答
> 各識別子は、オブジェクトの格納位置を示すインデックスと世代番号の組み合わせで生成します。同じインデックスを再利用するときは世代番号を増やし、古い識別子と一致しないようにします。生成処理はロックまたはアトミック操作で保護して一意性を保証します。大量のオブジェクトを生成・破棄しながら、有効な識別子と無効な識別子の検索時間、誤参照の有無、メモリ使用量を測定します。

---

### 练习 14

> [!question] 题目
> 家里还有开封后没有用完的挂面吗？我有点饿，想简单吃一点东西。

> [!example] 自己的答案
>　食べかけているうどんがあるか？腹が減ったから、ちょっと食べよう

> [!warning] 指正
> 原台词中的物品是 `使いかけのそうめん`，不是 `食べかけているうどん`。`食べかけ` 表示已经吃了一部分，`使いかけ` 才适合表示开封后用了一部分；`そうめん` 是挂面，不能换成乌冬面。`腹が減った` 比“有点饿”强烈，原台词使用的是 `小腹がすいた`；直接询问家人时，`あるか` 也显得较生硬。

> [!tip] 参考回答
> 使いかけのそうめん、あった？ ちょっと小腹がすいたから、何か簡単に食べたいな。

---

### 练习 15

> [!question] 题目
> 关于首页，我有一件事想和您商量。您稍后方便抽出一点时间吗？

> [!example] 自己的答案
> ホームページについて相談したいことがあるんです。少し時間をくれませんか

> [!warning] 指正
> 整体意思可以理解。这里的“首页”是网站中的顶部页面，应使用 `トップページ`；`ホームページ` 也可能被理解为整个网站。`少し時間をくれませんか` 更像直接要求对方“给我一点时间”，没有表达“稍后是否方便”。原台词中的职场说法 `後でお時間いいですか` 更自然。

> [!tip] 参考回答
> トップページの件で、ご相談したいことがあるんですけど、後でお時間いいですか。

## 2026-09-02

### 练习 1

> [!question] 题目
> 角色准备进入技能蓄力状态时，技能组件应先确认玩家仍然按住输入按键，并确认角色当前可以执行该动作；只有成功进入蓄力状态后才开始计算蓄力时间，如果状态切换失败则保持原来的动作状态。

> [!example] 自己的答案
> キャラクターが力積状態に入る時は、スキルコンポーネントはまずプレイヤーが押し付けているかを確認し、キャラクターがモーションを実行できるかを確認します。力積状態に入った後のみに時間を開始して、状態の切り替えに失敗した場合元のモーション状態を維持します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 2

> [!question] 题目
> 镜头震动系统负责根据受击强度计算震动幅度和持续时间，画面表现组件只使用已经确定的数值播放震动效果，不能修改角色受到的伤害数值或角色位置。

> [!example] 自己的答案
>　カメラシェークシステムは受け取った強度に応じて幅と持続時間を計算し、画面表現コンポーネントは決定した数値を使って振動効果を再生し、キャラクターが受け取ったダメージと位置を変更してはいけません。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 3

> [!question] 题目
> 调查时间抗锯齿（Temporal Anti-Aliasing，TAA）造成移动物体残影的问题时，请记录当前帧和上一帧的观察矩阵、问题像素的运动矢量，以及该像素使用历史画面数据时的混合权重。

> [!example] 自己的答案
>　TAAによる移動オブジェクトの残影の不具合を調査するときは、現在のフレームと前回のフレームの観察、モーションベクトル、不具合のピクソルが履歴画面データを参照する時のブレンドウィンドウを記録してください。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 4

> [!question] 题目
> 卸载采用分区方式载入的场景区域时，场景管理器应先阻止该区域继续生成新对象，再取消尚未完成的对象创建任务；确认区域中的对象都已销毁，并且其他对象不再保留对它们的引用后，才能释放该区域使用的资源。

> [!example] 自己的答案
>　区切りシーンをアンロードする時は、シーン管理マネージャーがまず新しいオブジェクトの生成を停止し、その後まだオブジェクト生成中のタスクをキャンセルします。エリア内のオブジェクトがすでに廃棄されたことを確認し、他のオブジェクトはこれの参照を保持しないことを確認した後に該当エリアに使われてたリソースを解放します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 5

> [!question] 题目
> 玩家确认删除存档后，存档管理器应再次确认目标存档槽位仍然存在，并保存用于恢复列表显示的存档信息；只有删除操作成功后才从界面列表中移除该项目，如果删除失败则恢复原来的显示并说明具体原因。

> [!example] 自己的答案
>　プレヤーがセーブデータの削除ことを確認した後に、セーブ管理マネージャーがスロットが存在しないかを再びに確認し、削除に成功した後にのみ画面リストから該当項目を外します。削除に失敗した場合データを元に戻して具体的な原因を説明します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 6

> [!question] 题目
> 玩家把物品拖到不可放置的位置时，物品栏组件应让物品回到原来的槽位，并将拖动期间创建的临时界面元素放回对象池；这次失败的拖动不能改变物品的实际数量和所属容器。

> [!example] 自己的答案
>　プレヤーがアイテムを位置にドローグする時に、バーコンポーネントはアイテムを元のスロットに戻し、ドラーグ中に生成された一時的なアイテムをオブジェクトプールに返却します。アイテム数量と所属コンテナを変更しません。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 7

> [!question] 题目
> 两个动作进行过渡时，动画系统应分别读取当前动作和目标动作中表示脚部是否固定在地面的曲线数据，并按照两个动作各自的过渡权重计算脚部位置；过渡结束后必须停止使用当前动作的曲线数据，只保留目标动作的计算结果。

> [!example] 自己的答案
>　２つアニメーション間でトランジションを行う場合、アニメーションシステムは現在のアニメーションと目標アニメーション内の足が地面にくっつくカーブを個別に読み込み、各ブレンドのウェイトを照らし合わせて計算します。ブレンドが終了後に現在のアニメーションのカーブデータ参照を停止し、目標アニメーションの計算結果を保持します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 8

> [!question] 题目
> 角色从布娃娃物理状态恢复正常动作之前，角色移动组件应确认骨盆速度已经低于设定值、身体接触地面，并且胶囊碰撞体放置在目标位置时不会与周围物体重叠；全部条件满足后才能停止物理模拟并对齐角色位置。

> [!example] 自己的答案
>　キャラクターがラグドールから通常の状態に復帰するときは、キャラクターの移動コンポーネントは骨盤の速度が設定値より低い、体が地面に接触し、カプセルが目的地の周りオブジェクトと重ならないことを確認するべきです。条件が全部満たされた場合のみ物理シミュレーションを停止します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 9

> [!question] 题目
> 人工智能控制的角色暂时看不见当前目标时，应在设定的宽限时间内保留该目标，并记录最后一次看见目标的位置；如果宽限时间结束前仍未再次发现目标，则清除目标并重新选择行动。

> [!example] 自己的答案
>　AIキャラクターは現在の視野内にいない場合、設定許与時間内でターゲットを保持し続け、最後の見かけた位置を記録します。許与期限が切れる前にターゲットを見つけない場合、ターゲットをクリアして行動を選び直します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 10

> [!question] 题目
> 材质实例缓存应使用基础材质、参数组合和画面质量等级，共同组成用于查找缓存条目的标识；返回已有实例之前，缓存管理器必须确认三个条件都完全一致，并把记录该实例当前使用方数量的引用计数增加一。

> [!example] 自己的答案
>　マテリアルインスタンキャッシュは基礎マテリアル、パラメータ組み合わせ、画面品質レベルから構成したキャッシュIDを使うべきです。既存のインスタンを返し、キャッシュ管理マネージャーは３つの条件が必ずに一致することを確認し、該当インスタンスの参照数を１増やします。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 11

> [!question] 题目
> 同时播放的声音数量超过限制时，音频系统应根据声音优先级、声音与听者之间的距离以及声音是否循环播放来选择停止对象，并记录被停止声音的名称和选择原因。

> [!example] 自己的答案
>　オディオ同時再生数が上限を超える時に、オディオシステムは優先度、聴取者との距離及びループ設定に基づいてオブジェクトを停止します。その後に停止されたオディオ名と原因を記録します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 12

> [!question] 题目
> 调查粒子效果数量增加后每秒显示的画面帧数下降的问题时，请分别记录这一帧新生成的粒子数量、仍在活动的粒子数量、因为不在视野内而不参与绘制的粒子数量，以及中央处理器和图形处理器各自花费的时间。

> [!example] 自己的答案
>　パーティクル数が増えた後フレームレートが低下した問題を調査する時には、このフレームで生成されたパーティクル数、動いているパーティクル数、視野外のパーティクル数、及びCPUとGPU別々にかかった時間をそれぞれ記録します。

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 13

> [!question] 题目
> 面试中设计支持撤销和重做的编辑操作历史时，请说明每条操作记录需要保存哪些数据、被编辑对象已经销毁时如何拒绝执行旧操作，以及如何在固定内存容量下淘汰最早的记录并测试查找成本。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 14

> [!question] 题目
> 果然在这里找到你了。之前听说你每次想让自己冷静下来时都会来这个地方，所以我就来这里看看。

> [!example] 自己的答案
>　やっぱりここでいるだ、自分を冷静させる時に必ずここに来るって聞いた、だから僕も来てみる

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 15

> [!question] 题目
> 这种情况如果不事先向我们说明，我们会很为难。您能和其他人交换一下各自负责校对的排版样稿吗？

> [!example] 自己的答案
>　この状況を事前に説明してくれないと、困るんです、他の人と校閲内容を交換してもらえませんか

> [!warning] 指正
>

> [!tip] 参考回答
>
