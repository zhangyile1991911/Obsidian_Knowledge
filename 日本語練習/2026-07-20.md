## 2026-07-20

### 练习 1

> [!question] 题目
> 请用 2 到 5 句回答：在 Unreal 的 Ability 设计中，为什么把输入判定、资源消耗和实际效果分成不同阶段更容易维护？

> [!example] 自己的答案
> 入力判定、コストと効果を全部一箇所に実装してしまうとクラスが肥大化しやすくなります。後からこの中一部を差し替える時に、テスト範囲が広まり、手間がかかります。

> [!warning] 指正
> 意思基本清楚。`この中一部` 不自然，应说 `その一部`；`テスト範囲が広まり` 更自然是 `テスト範囲が広がり`。面试回答里可以再补一句“責務が明確になる”来加强设计理由。

> [!tip] 参考回答
> 入力判定、コスト消費、実際の効果処理を一箇所にまとめると、クラスが肥大化しやすくなります。各段階を分けておくことで、仕様変更時に<mark style="background: #FF5582A6;">一部だけを差し替えやすくなり</mark>、テスト範囲も限定できます。

---

### 练习 2

> [!question] 题目
> 请用 2 到 5 句回答：当角色同时受到减速、眩晕和击退效果时，为什么需要明确状态优先级？

> [!example] 自己的答案
>　キャラクターが複数の状態を同時に受ける時に、どの状態を優先的に処理すべきですか、どの状態が上書きされるかなど状況に対応するため、状態を優先度をつける必要があります。

> [!warning] 指正
> 主要意思能表达，但句子结构有些乱。`状態を優先度をつける` 应改为 `状態に優先度をつける`。`どの状態を優先的に処理すべきですか` 这里不是提问，应用 `どの状態を優先的に処理すべきか`。

> [!tip] 参考回答
> キャラクターが減速、スタン、ノックバックなど複数の状態を同時に受ける場合、どの状態を優先するかを明確にしておく必要があります。優先度がないと、状態の上書き順が不安定になり、意図しない挙動が発生しやすくなります。

---

### 练习 3

> [!question] 题目
> 请用 2 到 5 句回答：在 Unity 中制作技能预览界面时，为什么预览用对象和正式战斗对象应该分开管理？

> [!example] 自己的答案
>　プレビュー用のオブジェクトに含まれるアニメーション、LOD、テクスチャなどリソースが違います。プレビューロード速度を向上させるため、プレビューオブジェクトが正式オブジェクトのリソースの一部しか持たせないようにします。

> [!warning] 指正
> 方向是对的，但 `プレビューロード速度` 应写成 `プレビューのロード速度`。`持たせないようにします` 搭配不太自然，这里更适合说 `必要なリソースだけを持たせます`。另外题目问“为什么分开管理”，可以补充避免影响正式战斗对象。

> [!tip] 参考回答
> プレビュー用オブジェクトと実際のバトル用オブジェクトでは、必要なアニメーション、LOD、テクスチャなどが異なる場合があります。分けて管理することで、プレビューに必要なリソースだけを読み込み、正式なバトル処理へ影響を与えないようにできます。

---

### 练习 4

> [!question] 题目
> 请用 2 到 5 句回答：如果一个 UI 按钮会根据玩家状态变为可用或不可用，你会怎样设计状态更新流程？

> [!example] 自己的答案
>　まず、ロジック処理とUI表現を二つに切り分けます。キャラクタークラス側で状態管理を行い、外部にこの状態の変化イベントを提供して状態が変化した時にイベントを通じて送信します。UI側では状態の変化イベントを登録して最新の状態に応じてボタンを変えます。

> [!warning] 指正
> 回答结构很好，意思也清楚。`状態が変化した時にイベントを通じて送信します` 稍硬，可以改成 `イベントで通知します`。`ボタンを変えます` 太笼统，建议明确为 `有効・無効状態を更新します`。

> [!tip] 参考回答
> まず、ロジック処理とUI表示を切り分けます。キャラクター側で状態を管理し、状態が変化したタイミングでイベントを通知します。UI側ではそのイベントを受け取り、最新の状態に応じてボタンの有効・無効を更新します。

---

### 练习 5

> [!question] 题目
> 请用 2 到 5 句回答：在 Unreal 中使用 Gameplay Tag 表示瞄准、装填和翻滚状态时，有什么好处？

> [!example] 自己的答案
>　検索、管理しやすくなります。

> [!warning] 指正
> 意思太短，作为面试回答不够。`検索、管理しやすくなります` 可以作为结论，但需要补出“为什么”：状态名统一、判断条件集中、UI 和 Ability 都能用同一套标签判断。

> [!tip] 参考回答
> Gameplay Tagを使うと、状態を文字列や複数のフラグでばらばらに管理せず、統一された形で扱えます。Ability、UI、移動処理などから同じタグを参照できるため、状態の検索や管理がしやすくなります。

---

### 练习 6

> [!question] 题目
> 这个技能的消耗、冷却时间和命中范围都从配置资源中读取。

> [!example] 自己的答案
>　このスキルのコスト、クールダウンと命中範囲の設定は設定アセットから読み込みます。

> [!warning] 指正
> 基本意思正确。`コスト、クールダウンと命中範囲` 这里并列更自然是 `コスト、クールダウン、命中範囲`。`設定は設定アセットから` 稍重复，可以改成 `各設定値は設定アセットから`。

> [!tip] 参考回答
> このスキルのコスト、クールダウン、命中範囲は、すべて設定アセットから読み込みます。

---

### 练习 7

> [!question] 题目
> Profiler 显示这次卡顿主要发生在脚本逻辑处理上。

> [!example] 自己的答案
>　Profilerでは今回カクつき原因はロジック処理に問題があることが分かりました。

> [!warning] 指正
> 意思能看懂，但语法需要整理。`今回カクつき原因` 应为 `今回のカクつきの原因`；`ロジック処理に問題があること` 比较绝对，Profiler 只是显示主要耗时位置，可以说 `ロジック処理にあること` 或 `ロジック処理で発生していること`。

> [!tip] 参考回答
> Profilerを見ると、今回のカクつきは主にスクリプトのロジック処理で発生していることが分かりました。

---

### 练习 8

> [!question] 题目
> 从对象池取出投射物后，发射前必须重置速度、命中记录和特效状态。

> [!example] 自己的答案
>　オブジェクトプールから取り出した飛び道具を発射する前に、必ず速度、ヒット記録とエフェクト状態をリセットします。

> [!warning] 指正
> 整体很自然。小问题是 `ヒット記録とエフェクト状態` 并列时更推荐 `ヒット記録、エフェクト状態`。`飛び道具` 可以用，技术语境里也可以说 `Projectile` 或 `投射物`。

> [!tip] 参考回答
> オブジェクトプールから投射物を取り出した後、発射前に速度、ヒット記録、エフェクト状態を必ずリセットします。

---

### 练习 9

> [!question] 题目
> 战斗日志应该记录最终伤害结果，而不是只保存最初的技能参数。

> [!example] 自己的答案
>　バトルログは最終のダメージ結果を記録すべきです。最初のスキルパラメータを保存するのではありません。

> [!warning] 指正
> 句子清楚。`最終のダメージ結果` 更自然是 `最終的なダメージ結果`。第二句 `保存するのではありません` 语气略生硬，翻译题里可写成 `保存するだけでは不十分です` 或合并为一句。

> [!tip] 参考回答
> バトルログには、最初のスキルパラメータだけでなく、最終的なダメージ結果を記録するべきです。

---

### 练习 10

> [!question] 题目
> 请确认攻击判定窗口是否和动画中的实际出招时机一致。

> [!example] 自己的答案
>　攻撃判定ウィンドウはアニメーション内の動作が一致していないかを確認してください

> [!warning] 指正
> 题意没有完全表达出来。原句是确认“攻击判定窗口是否和实际出招时机一致”，你的句子变成了“攻击判定窗口是否和动画内动作不一致”。`一致していないか` 会把意思反过来。句末也需要句号。

> [!tip] 参考回答
> 攻撃判定ウィンドウが、アニメーション内の実際の攻撃タイミングと一致しているか確認してください。

---

### 练习 11

> [!question] 题目
> 关卡机关的触发条件、当前状态和表现效果需要分开管理。

> [!example] 自己的答案
>　レベルにあるギミックの発火条件、ステートとエフェクトを別々に管理すべきです。

> [!warning] 指正
> 大意正确。`レベルにあるギミック` 可以理解，但游戏开发语境里 `レベルギミック` 更自然。`発火条件` 在程序语境可以用，不过对机关更常说 `発動条件` 或 `トリガー条件`。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">レベルギミック</mark>の<mark style="background: #FF5582A6;">トリガー条件</mark>、現在の状態、演出効果は分けて管理するべきです。

---

### 练习 12

> [!question] 题目
> 玩家连续切换装备时，需要先清理旧装备留下的效果和引用。

> [!example] 自己的答案
>  プレイヤが装備を連続して切り替える際に、まず旧装備由来効果と参照をクリアします


> [!warning] 指正
> 意思基本正确。`プレイヤ` 建议写成 `プレイヤー`。`旧装備由来効果` 不自然，应说 `旧装備由来の効果`。

> [!tip] 参考回答
> プレイヤーが装備を連続して切り替える際は、まず旧装備由来の効果や参照をクリアする必要があります。

---

### 练习 13

> [!question] 题目
> 存档版本升级后，道具结构发生了变化，所以需要追加迁移处理。

> [!example] 自己的答案
>　セーブデータがアップグレードした後、アイテムのデータ構造が変化した場合、移行処理を追加する必要があります。

> [!warning] 指正
> 意思清楚。`セーブデータがアップグレードした後` 不太自然，因为升级的是存档版本，不是存档本身主动升级；建议说 `セーブデータのバージョンアップ後`。`アイテムのデータ構造` 很自然。

> [!tip] 参考回答
> セーブデータのバージョンアップ後にアイテムのデータ構造が変わったため、移行処理を追加する必要があります。

---

### 练习 14

> [!question] 题目
> 今天下班后如果不太累的话，要不要一起去吃点热的东西？

> [!example] 自己的答案
>　業務が上がった後に疲れらないなら、一緒に暖かいものを食べに行きませんか

> [!warning] 指正
> `業務が上がった後` 不自然，应该说 `仕事が終わった後` 或 `退勤後`。`疲れらない` 是错误形式，应为 `疲れていなければ`。`暖かいもの` 多用于天气、衣服等，食物更自然是 `温かいもの`。句末建议加 `？`。

> [!tip] 参考回答
> 今日、仕事が終わった後、あまり疲れていなければ、一緒に温かいものを食べに行きませんか？

---

### 练习 15

> [!question] 题目
> 我把今天的进度整理好了，稍后会发到项目频道里，请大家确认一下有没有遗漏。

> [!example] 自己的答案
>　本日の進捗を整理し、後でプロジェクトチャンネルで共有します。皆さんに抜漏れを確認していただきます。

> [!warning] 指正
> 基本意思能传达。`抜漏れ` 应写成 `抜け漏れ`。`皆さんに確認していただきます` 听起来像单方面安排，职场请求更自然是 `ご確認ください` 或 `ご確認いただければと思います`。

> [!tip] 参考回答
> 本日の進捗を整理しましたので、後ほどプロジェクトチャンネルで共有します。抜け漏れがないか、ご確認いただければと思います。

---

## 2026-07-21

### 练习 1

> [!question] 题目
> 请用 2 到 5 句回答：在 Unity 的 UI 系统中，为什么把数据变化、显示刷新和点击处理分开会更容易维护？

> [!example] 自己的答案
> ロジック処理と表示処理を切り分けることで後から仕様変更時に一部を差し替えしやすくなります。
> 三つの部分間でインターフェースを通じて通知してどの部分が変化しても他の部分に影響しません。

> [!warning] 指正
> 意思基本清楚。`後から仕様変更時に` 更自然是 `後から仕様変更があった時に`。`一部を差し替えしやすく` 应改为 `一部を差し替えやすく`。第二句的 `三つの部分間で` 稍硬，建议说 `各部分はインターフェースを通じて連携する`。

> [!tip] 参考回答
> ロジック処理、表示更新、クリック処理を切り分けることで、後から仕様変更があった時に一部だけを差し替えやすくなります。<mark style="background: #FF5582A6;">各部分はインターフェースを通じて連携する</mark>ため、どこか一つを変更しても他の部分への影響を抑えられます。

---

### 练习 2

> [!question] 题目
> 请用 2 到 5 句回答：在 Unreal 的近战攻击中，为什么不能只依赖动画最后一帧来结束攻击状态？

> [!example] 自己的答案
>　近接攻撃では最後の１フレームに依存するとBlend Outされたり中断されたりしたら、最後の1フレームにあるAnim Notifyが発火しない可能性があります。そのため、最後の1フレームに依存しないほうがいいと考えています。

> [!warning] 指正
> 技术点抓得很好，`Blend Out` 和中断时 `Anim Notify` 不发火的风险表达清楚。语法上 `最後の１フレームに依存するとBlend Outされたり中断されたりしたら` 条件重复，建议整理为 `依存してしまうと、Blend Outや中断によって〜可能性があります`。

> [!tip] 参考回答
> 近接攻撃では、最後の1フレームだけに依存して攻撃状態を終了させるべきではありません。Blend Outや中断が発生した場合、そのフレームに配置したAnim Notifyが発火しない可能性があります。そのため、どの終了経路でも共通の終了処理を実行できるようにします。

---

### 练习 3

> [!question] 题目
> 请用 2 到 5 句回答：当技能有多个命中阶段时，如何设计数据结构才能方便策划调整？

> [!example] 自己的答案
>　スキルには複数のヒットフェーズが存在している場合はAnim Notifyを配置することで対応します。Anim NotifyにフェーズIDを持たせる必要があります。

> [!warning] 指正
> 方向有一部分正确，但题目重点是“方便策划调整的数据结构”，不只是用 Anim Notify。`スキルには複数のヒットフェーズが存在している場合は` 结构不自然，应为 `スキルに複数のヒットフェーズがある場合は`。建议补出每个阶段的数据表字段，例如倍率、范围、时机、特效等。

> [!tip] 参考回答
> スキルに複数のヒットフェーズがある場合は、フェーズごとにダメージ倍率、判定範囲、バフIDなどを設定できるデータ構造にします。Anim Notify側ではフェーズIDだけを渡し、具体的な数値は設定データから参照するようにすると、プランナーが調整しやすくなります。

---

### 练习 4

> [!question] 题目
> 请用 2 到 5 句回答：如果角色在翻滚中不能释放技能，你会把这个限制放在哪一层判断？

> [!example] 自己的答案
> まず、ローリングをアビリティーとして設計します。アビリティー側に条件判断を実装します。それにより、責務を明確します。アビリティーが自分の発動条件、終了条件などを持つべきです。

> [!warning] 指正
> 回答方向需要调整。题目是“翻滚中不能释放技能”的限制，通常应放在技能发动条件或状态检查层，而不是把“翻滚本身设计成 Ability”作为主要结论。`責務を明確します` 应为 `責務を明確にします`。`アビリティー` 也可写作 `Ability` 或 `アビリティ`。

> [!tip] 参考回答
> この制限は、スキルの発動条件を判定する層でチェックします。たとえばキャラクターがローリング中であることをGameplay Tagや状態フラグで管理し、Ability発動前にその状態を確認します。そうすることで、入力処理とスキルごとの発動条件を分けられ、責務も明確になります。

---

### 练习 5

> [!question] 题目
> 角色进入瞄准状态时，移动速度会降低，镜头灵敏度也会切换到专用配置。

> [!example] 自己的答案
>　キャラクターが照準状態に入る際に、移動速度が下がり、カメラが専用設定に切り替わります。

> [!warning] 指正
> 基本意思能表达。`照準状態` 可以理解，但游戏里“瞄准状态”更常说 `エイム状態`。原句还有“镜头灵敏度切换到专用配置”，你的 `カメラが専用設定に切り替わります` 少了“灵敏度”，建议补成 `カメラ感度`。

> [!tip] 参考回答
> キャラクターがエイム状態に入ると、移動速度が下がり、カメラ感度も専用の設定に切り替わります。

---

### 练习 6

> [!question] 题目
> 这个 Buff 只影响伤害计算，不应该直接控制受击特效的播放。

> [!example] 自己的答案
>　このバフはダメージ計算に影響するだけ、ヒットエフェクトを直接制御するのではありません。

> [!warning] 指正
> 意思能看懂，但 `影響するだけ` 后面需要接续，建议用 `影響するだけで`。`ヒットエフェクト` 容易理解，不过“受击特效”更明确可说 `被弾エフェクト`。句子最后 `制御するのではありません` 可以更自然地写成 `直接制御するべきではありません`。

> [!tip] 参考回答
> このBuffはダメージ計算にだけ影響するもので、被弾エフェクトの再生を直接制御するべきではありません。

---

### 练习 7

> [!question] 题目
> 切换关卡前，请先释放临时加载的预览模型和材质引用。

> [!example] 自己的答案
>　レベルを切り替える前に、一時的にロードしたプレビューモデルとマテリアルへの参照を解放してください。

> [!warning] 指正
> 句子很自然，意思也完整。`プレビューモデルとマテリアルへの参照` 表达准确。这里几乎没有问题，如果想更像工作指示，可以把 `解放` 换成 `解放しておいてください` 或 `クリアしてください`。

> [!tip] 参考回答
> レベルを切り替える前に、一時的にロードしたプレビューモデルやマテリアルへの参照を解放しておいてください。

---

### 练习 8

> [!question] 题目
> 如果同一个 Widget 每帧都重新计算布局，低端设备上可能会出现明显卡顿。

> [!example] 自己的答案
>　同じWdigetではマイフレームレイアウトの計算を行うと、ローエンド端末でカクつきが明らかに発生します。

> [!warning] 指正
> 主要意思能表达，但有拼写和搭配问题。`Wdiget` 应为 `Widget`；`マイフレーム` 应为 `毎フレーム`。`明らかに発生します` 语气偏绝对，题目是“可能会出现”，应说 `発生する可能性があります`。

> [!tip] 参考回答
> 同じWidgetで毎フレームレイアウト計算を行うと、ローエンド端末では明らかなカクつきが発生する可能性があります。

---

### 练习 9

> [!question] 题目
> 存档读取失败时，应该保留错误信息，并回退到安全的默认数据。

> [!example] 自己的答案
>　セーブデータの読み込みに失敗した時に、エラーメッセージを保存し、また安全なデフォルト値に戻します

> [!warning] 指正
> 意思基本正确。`エラーメッセージ` 可以，但题目里的“错误信息”更宽泛，`エラー情報` 更自然。`また安全なデフォルト値に戻します` 的 `また` 不太需要；`戻します` 可以，但 `フォールバックします` 更贴近“回退”。句末缺句号。

> [!tip] 参考回答
> セーブデータの読み込みに失敗した場合は、エラー情報を保持し、安全なデフォルトデータに<mark style="background: #FF5582A6;">フォールバック</mark>します。

---

### 练习 10

> [!question] 题目
> 设计卡牌效果时，需要区分立即生效的效果和回合结束时结算的效果。

> [!example] 自己的答案
>　カードの効果を設計する時に、すぐ発生する効果とターン終了時に計算効果を切り分ける必要があります。

> [!warning] 指正
> 意思大致清楚，但 `すぐ発生する効果` 更自然是 `即時に発動する効果`。`ターン終了時に計算効果` 少了连接，应为 `ターン終了時に計算される効果` 或 `解決される効果`。

> [!tip] 参考回答
> カード効果を設計する時は、<mark style="background: #FF5582A6;">即時に</mark>発動する効果と、ターン終了時に<mark style="background: #FF5582A6;">解決される効果</mark>を切り分ける必要があります。

---

### 练习 11

> [!question] 题目
> 调试伤害异常时，先确认基础数值、倍率和抗性是否都按预期参与计算。

> [!example] 自己的答案
>　ダメージ不具合をデバッグする際に、まず基礎数値、倍率と抗性は予定通りに計算に入れているかを確認します。

> [!warning] 指正
> 意思能表达。`倍率と抗性` 并列时更自然是 `倍率、耐性`。`予定通りに計算に入れているか` 不自然，应说 `想定通りに計算に反映されているか`。`ダメージ不具合` 可以，但 `ダメージ異常` 更贴近题目。

> [!tip] 参考回答
> ダメージ異常をデバッグする際は、まず基礎値、倍率、耐性が想定通りに<mark style="background: #FF5582A6;">計算へ反映されているか</mark>確認します。

---

### 练习 12

> [!question] 题目
> 敌人的警戒范围应该由配置控制，而不是写死在行为树节点里。

> [!example] 自己的答案
>　敵の警戒範囲は設定を通じて制御し、Behaviour Tree内にハードコードするのではありません。

> [!warning] 指正
> 意思很清楚。`設定を通じて制御し` 能懂，但更自然是 `設定で制御し`。`Behaviour Tree` 拼写可以，不过 Unreal 日语语境里常写 `Behavior Tree`。`ハードコードするのではありません` 可以换成更自然的 `ハードコードするべきではありません`。

> [!tip] 参考回答
> 敵の警戒範囲は設定で制御し、Behavior Treeノード内にハードコードするべきではありません。

---

### 练习 13

> [!question] 题目
> 当多个系统都需要玩家等级时，最好通过统一的数据接口读取。

> [!example] 自己的答案
>　複数のシステムはプレイヤーのレベルを取得する場合、統一インターフェースで取得したほうがいいです。

> [!warning] 指正
> 意思正确。`複数のシステムはプレイヤーのレベルを取得する場合` 主语标记不自然，应为 `複数のシステムがプレイヤーのレベルを必要とする場合`。`統一インターフェース` 前面最好加 `された`，即 `統一されたインターフェース`。

> [!tip] 参考回答
> 複数のシステムがプレイヤーのレベルを必要とする場合は、統一されたデータインターフェースを通じて取得するのが望ましいです。

---

### 练习 14

> [!question] 题目
> 周末如果天气不错，我们可以去附近的公园散步，顺便买杯咖啡。

> [!example] 自己的答案
>　週末　天気が良ければ、近くの公園に散歩して　ついでにコーヒーを買います。

> [!warning] 指正
> 大意能表达，但自然度需要调整。`週末　天気が良ければ` 中间要用逗号。`公園に散歩して` 应为 `公園を散歩して` 或 `公園に散歩に行って`。`買います` 听起来像单方面决定，原句是提议，应用 `買いましょう` 或 `買ってもいいですね`。

> [!tip] 参考回答
> 週末、天気が良ければ、近くの公園を散歩して、ついでにコーヒーを買いましょう。

---

### 练习 15

> [!question] 题目
> 这个任务我今天会先完成核心部分，剩下的细节明天上午继续处理。

> [!example] 自己的答案
>　本日は　まずこのタスクの核心を完全させて、残りは明日午前にやり続けます。

> [!warning] 指正
> `核心を完全させて` 是主要错误，应说 `核心部分を完成させて`。`やり続けます` 表示持续做，和“继续处理剩下细节”不完全一致；更自然是 `対応します` 或 `進めます`。`本日は まず` 中间不需要空格。

> [!tip] 参考回答
> 本日はまずこのタスクの核心部分を完成させて、残りの細かい部分は明日の午前中に引き続き対応します。

---

## 2026-07-22

### 练习 1

> [!question] 题目
> 在 Unreal 的角色状态机里，攻击结束前不要切回待机状态，否则连击输入的判定时机可能会错乱。

> [!example] 自己的答案
>　このUnrealのキャラクターステートマシンでは、攻撃終了まで待機状態に遷移しないようにし、連打入力を判定するタイミングが間違います

> [!warning] 指正
> 大意能表达，但后半句逻辑需要补全。`連打入力` 更偏“连打输入”，题目是“连击输入”，可用 `コンボ入力`。`判定するタイミングが間違います` 缺少“可能会错乱”的语气，建议改成 `ずれる可能性があります`。句末也需要句号。

> [!tip] 参考回答
> Unrealのキャラクターのステートマシンでは、攻撃が終了する前に待機状態へ戻さないようにします。そうしないと、コンボ入力を判定するタイミングがずれる可能性があります。

---

### 练习 2

> [!question] 题目
> 在 Unity 的 UI 列表中，只刷新发生变化的格子，可以减少不必要的布局计算。

> [!example] 自己的答案
>　UnityのUIリストでは、変更されたセルだけを更新することでレイアウトの計算を減らせます。

> [!warning] 指正
> 表达自然，意思也完整。`セル` 可以表达 UI 列表的格子，`更新することで` 和 `減らせます` 搭配也正确。更贴近“只刷新”的语气时，可以说 `変更があったセルだけを更新することで`。

> [!tip] 参考回答
> UnityのUIリストでは、変更があったセルだけを更新することで、不要なレイアウト計算を減らせます。

---

### 练习 3

> [!question] 题目
> 技能冷却时间最好由数据层管理，UI 只负责显示剩余时间和按钮状态。

> [!example] 自己的答案
>　スキルクールダウンはデータ階層で管理するべきです。UIは残り時間とボタン状態を表示するだけです。

> [!warning] 指正
> 意思清楚。`データ階層` 可以理解，但日语开发语境中 `データ層` 或 `データ側` 更自然。`ボタン状態` 前面加 `ボタンの状態` 更顺。

> [!tip] 参考回答
> スキルのクールダウン時間はデータ側で管理し、UIは残り時間とボタンの状態を表示するだけにするべきです。

---

### 练习 4

> [!question] 题目
> 如果对象池里的特效没有重置颜色和播放位置，下一次播放时可能会出现残留效果。

> [!example] 自己的答案
>　オブジェクトプール内のエフェクトが色と進行状況をリセットしないと次再生時に残留効果が出現するかもしれません。

> [!warning] 指正
> 大意正确，但有几处不自然。`進行状況` 不太适合特效播放位置，建议用 `再生位置`。`次再生時に` 应为 `次に再生するときに`。`残留効果` 比较生硬，`前回の見た目が残る` 或 `残ったエフェクトが表示される` 更自然。

> [!tip] 参考回答
> オブジェクトプール内のエフェクトで色や再生位置をリセットしていないと、次に再生するときに前回の見た目が残ってしまう可能性があります。

---

### 练习 5

> [!question] 题目
> 保存数据版本升级时，需要把旧字段转换成新结构，并为缺失的字段补上默认值。

> [!example] 自己的答案
>　セーブデータがアップグレード時に、旧フィールドを新しい構造に変換する場合、足りてないフィールドをデフォルト値で補完します。

> [!warning] 指正
> 意思基本正确，但句子结构有问题。`セーブデータがアップグレード時に` 应改为 `セーブデータのバージョンを上げる際に` 或 `アップグレード時には`。`足りてない` 口语偏强，技术说明中建议用 `不足している`。这里不是“在转换旧字段的情况下”，而是并列的两个处理。

> [!tip] 参考回答
> セーブデータのバージョンを上げる際には、旧フィールドを新しい構造に変換し、不足しているフィールドにはデフォルト値を補完します。

---

### 练习 6

> [!question] 题目
> 在 Unreal 的 UMG 中，血条数值变化时只更新进度条，不要重新创建整个界面。

> [!example] 自己的答案
>　UnrealのUMGでは、HPバーが変化している時にプログレスだけを更新し、画面全体を再生成しないようにしてください

> [!warning] 指正
> 大意能表达。`HPバーが変化している時` 容易理解成血条对象本身在变化，题目是数值变化，建议说 `HPの値が変わったとき`。`プログレスだけ` 可更明确为 `プログレスバーだけ`。句末缺少句号。

> [!tip] 参考回答
> UnrealのUMGでは、HPの値が変わったときにプログレスバーだけを更新し、画面全体を作り直さないようにします。

---

### 练习 7

> [!question] 题目
> 角色受到击退时，移动输入应该暂时失效，直到击退动画和位移都结束。

> [!example] 自己的答案
>　キャラクターがキックバックする際に、キックバックと移動が終了するまでは移動入力を一時的に無効化にします。

> [!warning] 指正
> 主要意思能看懂，但 `キックバック` 不适合“击退”，游戏语境中更常用 `ノックバック`。`キックバックと移動が終了するまでは` 应明确为 `ノックバックのアニメーションと移動が終わるまでは`。`無効化にします` 应为 `無効にします`。

> [!tip] 参考回答
> キャラクターがノックバックを受けたときは、ノックバックのアニメーションと移動が終わるまで、移動入力を一時的に無効にします。

---

### 练习 8

> [!question] 题目
> 使用 ScriptableObject 管理敌人参数时，运行时修改的数值不要直接写回配置资源。

> [!example] 自己的答案
>　ScriptableObjectを使って敵のパラメータを管理する時に、実行時に数値の変更を直接に設定アセットに上書きしないようにします

> [!warning] 指正
> 意思清楚，但自然度可调整。`実行時に数値の変更` 应为 `実行時に変更した数値`。`直接に` 在这里不自然，直接用 `直接`。`設定アセットに上書きする` 可以表达写回配置资源。

> [!tip] 参考回答
> ScriptableObjectを使って敵のパラメータを管理する場合、実行時に変更した値を設定アセットへ直接上書きしないようにします。

---

### 练习 9

> [!question] 题目
> 在性能调查中，先确认瓶颈来自逻辑处理、绘制处理还是资源加载，再决定优化方向。

> [!example] 自己的答案
>　パフォーマンス調査で、まずボトルネック原因はロジック処理のか、描画処理のか、またリソースロードを特定してから最適化方向を決めます。

> [!warning] 指正
> 方向正确，但句型需要整理。`ボトルネック原因は...のか` 不自然，应说 `ボトルネックが...にあるのか`。`リソースロードを特定して` 少了“来自资源加载”的结构，建议用 `リソース読み込みにあるのかを確認してから`。`最適化方向` 更自然是 `最適化の方針`。

> [!tip] 参考回答
> パフォーマンス調査では、まずボトルネックがロジック処理、描画処理、リソース読み込みのどこにあるのかを確認してから、最適化の方針を決めます。

---

### 练习 10

> [!question] 题目
> 当装备被卸下时，要清理它持有的特效引用和临时状态，避免后续装备受到影响。

> [!example] 自己的答案
>　装備を下げる時に、保持しているエフェクト参照と一時的なステートをクリアし、後からその装備に影響されないようにします。

> [!warning] 指正
> 大意能表达，但 `装備を下げる` 不适合“卸下装备”，应说 `装備を外す`。`後からその装備に影響されない` 指向不清，题目是避免影响后续装备，建议说 `後続の装備に影響しないように`。`ステート` 可用，但 `状態` 更自然。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">装備を外す</mark>ときは、その装備が保持しているエフェクト参照や一時的な状態をクリアし、後続の装備に影響しないようにします。

---

### 练习 11

> [!question] 题目
> 在 Unreal 的 Data Table 中，如果道具 ID 重复，加载阶段就应该输出明确的错误日志。

> [!example] 自己的答案
>　UnrealのData Tableでは、アイテムIDが重複する場合、ロードフェーズではエラーを明確に出力するべきです

> [!warning] 指正
> 意思基本正确。`ロードフェーズでは` 的 `では` 有点像对比，`ロード段階で` 更自然。`エラーを明確に出力する` 可以理解，但通常会说 `明確なエラーログを出力する`。句末缺少句号。

> [!tip] 参考回答
> UnrealのData TableでアイテムIDが重複している場合は、ロード段階で明確なエラーログを出力するべきです。

---

### 练习 12

> [!question] 题目
> Unity 的 Render Feature 适合把描边效果放到渲染流程的后半段统一处理。

> [!example] 自己的答案
>　UnityのRender Featureではアウトライン描画をレンダリング後半にまとめて処理するに適します

> [!warning] 指正
> 技术意思能看懂，但语法需要修正。`Render Featureでは` 容易变成“在 Render Feature 中”，这里应说 `Render Featureは...に適しています`。`処理するに適します` 是错误搭配，应为 `処理するのに適しています`。`レンダリング後半` 建议加 `の段階`。

> [!tip] 参考回答
> UnityのRender Featureは、アウトライン描画をレンダリングフローの後半段階でまとめて処理するのに適しています。

---

### 练习 13

> [!question] 题目
> 伤害计算和命中特效分开处理，可以让战斗逻辑更容易测试，也方便以后替换表现效果。

> [!example] 自己的答案
>　ダメージ計算とヒットエフェクトを切り分けます。こうすることでバトルロジックがテストしやすくなり、後から表現効果を切り替えやすくなります。

> [!warning] 指正
> 表达清楚，结构也自然。`表現効果` 能理解，但游戏开发语境中更常说 `演出` 或 `見た目の表現`。`切り替えやすく` 也可以，若强调“替换”可用 `差し替えやすく`。

> [!tip] 参考回答
> ダメージ計算とヒットエフェクトを分けて処理することで、バトルロジックをテストしやすくなり、後から演出も差し替えやすくなります。

---

### 练习 14

> [!question] 题目
> 晚饭后如果还有时间，我们去便利店买点饮料吧。

> [!example] 自己的答案
>　晩飯が終わった後に時間があれば、コンビニに飲み物を買いに行きましょう

> [!warning] 指正
> 意思正确。`晩飯` 偏随意，日常自然对话里可以用，但更中性自然的是 `夕飯`。`コンビニに飲み物を買いに行く` 更标准应为 `コンビニへ飲み物を買いに行く` 或 `コンビニで飲み物を買う`。句末缺少句号。

> [!tip] 参考回答
> 夕飯のあと、まだ時間があれば、コンビニへ飲み物を買いに行きましょう。

---

### 练习 15

> [!question] 题目
> 我先整理一下今天的修改内容，十分钟后在会议上向大家说明。

> [!example] 自己的答案
>　まず本日の改修内容を整理し、十分後に会議で皆さんに説明します

> [!warning] 指正
> 意思清楚，表达也比较自然。`十分後` 容易被看成“足够之后”，时间长度建议写作 `10分後`。`改修内容` 偏“代码修改/修复内容”，如果想泛指今天的修改，用 `変更内容` 更稳。句末缺少句号。

> [!tip] 参考回答
> まず本日の変更内容を整理して、10分後の会議で皆さんに説明します。

---

## 2026-07-23

### 练习 1

> [!question] 题目
> 请用 2 到 5 句回答：在 Unreal 的近战连击里，为什么要把输入缓存、状态切换和伤害判定分开处理？

> [!example] 自己的答案
>　一箇所に実装してしまうと、テスト手間がかかります。それだけではなく、コードの保守性、拡張性も低くなります。例えば　後からダメージ判定を差し替える要求があったら、他の部分に影響を与える可能性が高いです。そのため、この三つ部分を切り分けたほうが適切だと考えています。

> [!warning] 指正
> 结构和意思基本成立，能说清“分开处理是为了保守性和扩展性”。`テスト手間がかかります` 应改为 `テストに手間がかかります`。`例えば　後から` 中间不需要空格。`この三つ部分` 应为 `この三つの部分`，更自然也可以说 `この三つの処理`。

> [!tip] 参考回答
> 一箇所にまとめて実装してしまうと、テストに手間がかかるだけでなく、コードの保守性や拡張性も下がります。例えば、後からダメージ判定だけを差し替えたい場合でも、入力や状態遷移に影響が出る可能性があります。そのため、入力バッファ、状態切り替え、ダメージ判定は別々に処理したほうが適切だと考えています。

---

### 练习 2

> [!question] 题目
> 请用 2 到 5 句回答：在 Unity 的背包界面中，为什么物品数据、排序规则和格子显示不应该写在同一个类里？

> [!example] 自己的答案
> 同じクラス内に実装してしまうと、このクラスが肥大化しやすくなるだけではなく後から一部だけを置き換えにくくなります。そのため、アイテムデータ、並び替えルール、セル表示をそれぞれにしてインターフェース経由で呼び出すようにします。

> [!warning] 指正
> 大意很好，职责分离的理由也明确。`それぞれにして` 不够完整，建议说 `それぞれ別の責務として分けて`。`インターフェース経由で呼び出す` 可以表达设计方式，但如果只是说明 UI 架构，也可以说 `必要に応じて参照する`，避免听起来所有处理都必须通过接口调用。

> [!tip] 参考回答
> 同じクラス内に実装してしまうと、そのクラスが肥大化しやすくなるだけでなく、後から一部だけを差し替えにくくなります。そのため、アイテムデータ、並び替えルール、セル表示はそれぞれ別の責務として分け、必要に応じてインターフェース経由で参照できるようにします。

---

### 练习 3

> [!question] 题目
> 请用 2 到 5 句回答：当角色同时处于装填和翻滚状态时，为什么需要先定义清楚哪个状态拥有更高优先级？

> [!example] 自己的答案
>　例えば、プレイヤーが同時にリロードとローリングを押した場合は、どの要求を答える、どの要求を無視するか選択しますか。そのため、動作に優先度をつける必要があります。

> [!warning] 指正
> 意图能看懂，但第一句有几处需要修正。`要求を答える` 应为 `要求に応える`，这里更自然是 `どちらの入力を優先するか`。`選択しますか` 在陈述句中不合适，可以改成 `決める必要があります`。`動作に優先度をつける` 表达正确。

> [!tip] 参考回答
> 例えば、プレイヤーがリロードとローリングを同時に入力した場合、どちらの入力を優先するかを決める必要があります。優先度が決まっていないと、毎回違う挙動になったり、意図しない状態遷移が発生したりする可能性があります。そのため、各動作に明確な優先度をつけておく必要があります。

---

### 练习 4

> [!question] 题目
> 请用 2 到 5 句回答：在技能编辑工具里，为什么预览效果最好使用独立的临时对象，而不是直接修改正式资源？

> [!example] 自己的答案
>

> [!warning] 指正
> 意思基本清楚。`メリットは...高いです` 搭配不自然，应说 `メリットは...高められることです` 或拆成两句。`コード保守性` 前面加 `コードの` 更自然。第二句可以补出“实际效果处理不受影响”，让测试边界更明确。

> [!tip] 参考回答
> 条件判断と実際の効果処理を分けることで、不具合の原因を特定しやすくなります。CanActivate側だけをテストすれば発動条件を確認でき、効果処理側への影響も抑えられます。そのため、コードの保守性や拡張性も高めやすくなります。

---

### 练习 5

> [!question] 题目
> 请用 2 到 5 句回答：如果一个 Boss 技能由多个阶段组成，你会怎样拆分阶段数据、动画表现和命中判定？

> [!example] 自己的答案
> ボースの複数フェーズはステトーマシンで管理します。各フェーズごとに発生条件、アビリティ、ビヘイビアツリーを持たせます。フェーズに入る時に、このフェーズに対応するアビリティを付与します。フェーズから出る時に、入る時に付与したアビリテを外します。アビリティにアニメーション、発動条件、ヒット判定などを持たせる。

> [!warning] 指正
> `ボース` は `ボス`、`ステトーマシン` は `ステートマシン`、`アビリテ` は `アビリティ` です。回答内容は少し题目からずれており、题目要求的是“阶段数据、动画表现、命中判定”的拆分方式，而你的答案偏向 Boss 状态和 Ability 付与流程。`持たせる。` 句尾也要统一为 `持たせます。`。


> [!tip] 参考回答
> ボススキルが複数のフェーズで構成される場合、各フェーズの開始条件、継続時間、倍率などはフェーズデータとして管理します。アニメーションやエフェクトなどの演出は、そのフェーズデータを参照して再生します。命中判定は演出に直接依存させず、フェーズごとの判定タイミングと範囲を別データとして持たせると、後から調整しやすくなります。

---

### 练习 6

> [!question] 题目
> 在 Unity 中加载角色头像时，应该复用已经缓存的纹理，避免同一资源被反复读取。

> [!example] 自己的答案
> Unityで顔アイコンをロードするときに、キャッシュしたテクスチャを再利用するべきです。同一リソースを重複して読み込まないようにしてください

> [!warning] 指正
> 意思正确。`顔アイコン` 可以理解，但游戏 UI 中“角色头像”更自然是 `キャラクターアイコン` 或 `顔アイコン`，两者都可以。`ロードするときに` 没问题。句末缺少句号。

> [!tip] 参考回答
> Unityでキャラクターアイコンを読み込むときは、すでにキャッシュしているテクスチャを再利用するべきです。同じリソースを重複して読み込まないようにします。

---

### 练习 7

> [!question] 题目
> Unreal 的相机抖动应该只表现受击反馈，不应该影响角色真实朝向和移动方向。

> [!example] 自己的答案
>　Unrealのカメラシェークは被弾を反映するだけです。キャラクター実際の向きと移動方向に影響するべきではありません。

> [!warning] 指正
> 大意正确。`カメラシェーク` 更常见写法是 `カメラシェイク`。`被弾を反映するだけです` 能理解，但更自然是 `被弾フィードバックを表現するためのものです`。`キャラクター実際の向き` 应为 `キャラクターの実際の向き`。

> [!tip] 参考回答
> Unrealのカメラシェイクは、被弾フィードバックを表現するためのものです。キャラクターの実際の向きや移動方向には影響させるべきではありません。

---

### 练习 8

> [!question] 题目
> 当技能按钮进入冷却时，需要同时更新倒计时文本、遮罩进度和可点击状态。

> [!example] 自己的答案
>　スキルボタンがクールダウンに入る時に、残り時間テキスト、進捗マスクとアクティブ状態を同期する必要があります。

> [!warning] 指正
> 意思基本正确，但 `同期する` 会让人以为只是同步数值，题目是“更新”。`進捗マスク` 可以表达遮罩进度，但 `マスクの進捗` 或 `マスクの表示割合` 更自然。`アクティブ状態` 不一定等于“可点击状态”，建议用 `クリック可能かどうか` 或 `押下可能状態`。

> [!tip] 参考回答
> スキルボタンがクールダウンに入ったときは、残り時間のテキスト、マスクの進捗、クリック可能かどうかの状態を同時に更新する必要があります。

---

### 练习 9

> [!question] 题目
> 如果敌人生成点没有正确释放引用，离开关卡后仍可能残留无用对象。

> [!example] 自己的答案
>　敵の生成時に参照を正しく解放していないとステージを出た後も無効なオブジェクトが残り続けてしまう可能性があります。

> [!warning] 指正
> 主要意思能传达，但开头对象说错了。题目是“敌人生成点”，不是“敌人生成时”，应说 `敵の生成地点` 或 `敵スポナー`。`無効なオブジェクト` 更像“无效对象”，题目是“无用对象”，可用 `不要なオブジェクト`。`ステージを出た後` 可更自然为 `ステージを離れた後`。

> [!tip] 参考回答
>  敵の生成地点が参照を正しく解放していないと、ステージを離れた後も不要なオブジェクトが残り続けてしまう可能性があります。

---

### 练习 10

> [!question] 题目
> 在调查掉帧问题时，先记录发生时机和场景条件，再使用工具确认主要耗时位置。

> [!example] 自己的答案
>　カクつきを調査する時に、発生したタイミングとフィールド条件を記録しておき、後でツールでかかった時間

> [!warning] 指正
> 句子没有完成，`後でツールでかかった時間` 后面缺少“确认主要耗时位置”。`フィールド条件` 在这里不太自然，游戏调试语境中建议用 `シーン条件` 或 `発生した状況`。`カクつき` 很自然，可以继续使用。

> [!tip] 参考回答
> カクつきを調査するときは、まず発生したタイミングとシーン条件を記録しておきます。その後、Profilerなどのツールを使って、主にどの処理に時間がかかっているのかを確認します。

---

### 练习 11

> [!question] 题目
> 读取旧存档时，如果任务目标数量缺失，就用当前配置中的默认数量进行补全。

> [!example] 自己的答案
>  旧セーブデータを読み込むときに、タスクにターゲット数量が不足している場合、現在設定に存在するデフォルト値で補完してください。

> [!warning] 指正
> 大意正确。`タスクにターゲット数量` 稍微生硬，任务系统里可说 `クエスト目標の数`。`現在設定に存在する` 应为 `現在の設定にある`。`補完してください` 是命令语气，如果写技术说明可用 `補完します`。

> [!tip] 参考回答
>  旧セーブデータを読み込むときにクエスト目標の数が不足している場合は、現在の設定にあるデフォルト値で補完します。

---

### 练习 12

> [!question] 题目
> 角色死亡后，应该停止新的输入处理，并等待死亡演出结束后再显示结果界面。

> [!example] 自己的答案
>　キャラクターが死亡後に入力処理を停止し、死亡演出が終了してからリザルト画面を表示します。

> [!warning] 指正
> 意思正确，但 `キャラクターが死亡後に` 应改为 `キャラクターが死亡した後は`。`入力処理を停止し` 可以表达，但题目强调“新的输入处理”，可说 `新しい入力処理を受け付けないようにし`。整体很接近自然表达。

> [!tip] 参考回答
> キャラクターが死亡した後は、新しい入力処理を受け付けないようにし、死亡演出が終了してからリザルト画面を表示します。

---

### 练习 13

> [!question] 题目
> 配置表中的技能 ID、图标路径和说明文本都需要在启动阶段进行检查。

> [!example] 自己的答案
>　起動時に設定タブーるにあるスキルID、アイコンパースと説明テキストをチェックする必要があります。

> [!warning] 指正
> `設定タブーる` 应为 `設定テーブル`。`アイコンパース` 应为 `アイコンパス`。`チェックする` 可以用，但技术说明中更自然的是 `検証する`。整体语序可以改成 `設定テーブルにある〜を起動時に検証する必要があります`。

> [!tip] 参考回答
>  設定テーブルにあるスキルID、アイコンパス、説明テキストは、起動時に検証する必要があります。

---

### 练习 14

> [!question] 题目
> 如果你晚上不方便出门，我们可以改成明天中午再见面。

> [!example] 自己的答案
>　今晩都合が悪いなら、明日昼に会うにかえてもいいです

> [!warning] 指正
> 意思能看懂，但自然度需要调整。`都合が悪いなら` 稍直接，日常对话里可说 `都合が悪ければ` 或 `出かけるのが難しければ`。`明日昼に会うにかえてもいいです` 语法不对，应为 `明日の昼に会う形に変えても大丈夫です`。

> [!tip] 参考回答
>  今晩出かけるのが難しければ、明日の昼に会う形に変えても大丈夫です。

---

### 练习 15

> [!question] 题目
> 我已经确认了今天的修改范围，如果没有问题，下午就开始处理。

> [!example] 自己的答案
>　本日の改修範囲をすでに確認しました。問題がなければ、午後から着手します。

> [!warning] 指正
> 表达自然，意思也完整。`改修範囲` 适合代码修改或修复范围；如果只是一般修改，也可以用 `変更範囲`。`午後から着手します` 很自然。

> [!tip] 参考回答
> 本日の変更範囲はすでに確認しました。問題がなければ、午後から着手します。

---

## 2026-07-24

### 练习 1

> [!question] 题目
> 请用 2 到 5 句回答：在 Unreal 的 Gameplay Ability 中，为什么把 CanActivate 的条件判断和实际效果处理分开更容易测试？

> [!example] 自己的答案
> 　条件判断と実際の効果を別々に処理するメリットは不具合を特定しやすく、コード保守性と拡張性が高いです。後から条件判断を置き換えても他の部分に影響しません。

> [!warning] 指正
> 意思基本清楚。`メリットは...高いです` 搭配不自然，应说 `メリットは...高められることです` 或拆成两句。`コード保守性` 前面加 `コードの` 更自然。第二句可以补出“实际效果处理不受影响”，让测试边界更明确。

> [!tip] 参考回答
> 条件判断と実際の効果処理を分けることで、不具合の原因を特定しやすくなります。CanActivate側だけをテストすれば発動条件を確認でき、効果処理側への影響も抑えられます。そのため、コードの保守性や拡張性も高めやすくなります。

---

### 练习 2

> [!question] 题目
> 请用 2 到 5 句回答：在 Unity 的背包系统里，为什么物品实例数据不应该直接修改共享的道具配置？

> [!example] 自己的答案
>　アイテムインスタンス側では共有している設定を変更したら、同一設定を使っているアイテムに影響します。そのため、実行中に変更するデータは個別にインスタンス化しておきます。

> [!warning] 指正
> 回答方向很好。`共有している設定を変更したら` 可以理解，但条件句更自然是 `変更すると`。`同一設定を使っているアイテム` 建议改成 `同じ設定を参照している他のアイテム`，指向更清楚。`個別にインスタンス化` 也可以，但这里更准确是 `インスタンス側に持たせる`。

> [!tip] 参考回答
> アイテムインスタンス側で共有設定を直接変更すると、<mark style="background: #FF5582A6;">同じ設定を参照している他のアイテム</mark>にも影響してしまいます。そのため、耐久度や現在数など実行中に変化するデータは、設定データではなく各インスタンス側に持たせます。

---

### 练习 3

> [!question] 题目
> 请用 2 到 5 句回答：如果一个技能既有前摇、命中窗口和收尾动作，你会怎样设计状态切换，避免角色卡在攻击状态？

> [!example] 自己的答案
>　スキルは命中ウィンドウ、終了を三つに分けます。どのステップも中断される可能性があるので、同じ終了処理を呼ぶように保証します。そのため、MontageのBlend Out、Cancel、Completedイベントを全部登録します。各段階に応じるフラグを立てて、アニメーションが始まる時にフラグにfalseを代入して完全に終了した後にフラグにTrueを置き換えます。終了処理で各フラグを判断し、ダメージの計算を行い、正常状態に遷移します。

> [!warning] 指正
> 技术点很多，`Blend Out`、`Cancel`、`Completed` 都考虑到了，这很好。第一句漏了“前摇”，应写成 `前隙、命中ウィンドウ、終了`。`各段階に応じるフラグ` 应为 `各段階に応じたフラグ`。`フラグにfalseを代入` 和 `Trueを置き換えます` 不自然，可说 `フラグをfalseに戻す`、`trueにします`。另外结束处理里不一定要做伤害计算，伤害通常应在命中窗口处理。

> [!tip] 参考回答
> スキルを<mark style="background: #FF5582A6;">前隙、命中ウィンドウ、終了処理</mark>の三つの状態に分けて管理します。どの段階でも中断される可能性があるため、Completed、Cancel、Blend Outなどの終了経路から共通の終了処理を呼べるようにします。命中判定は命中ウィンドウ中だけ実行し、終了時には攻撃フラグを戻して通常状態へ遷移させます。

---

### 练习 4

> [!question] 题目
> 请用 2 到 5 句回答：在 Unity 中制作长列表 UI 时，为什么滚动过程中只复用可见格子比重新生成全部项目更合适？

> [!example] 自己的答案
>　スクロール中に毎回新しいインスタンスを生成するとメモリ使用量が増えてGCが発生してゲームが重くなります。そのため、スクロール中で見えなくなるセルを再利用することで、メモリ使用量を一定範囲内に抑えます。

> [!warning] 指正
> 意思清楚，性能理由也正确。`スクロール中で見えなくなるセル` 应为 `スクロール中に見えなくなったセル`。`メモリ使用量を一定範囲内に抑えます` 很自然。可以再补 `生成コスト` 或 `GC負荷`，让答案更完整。

> [!tip] 参考回答
> スクロールのたびにリスト全体を生成し直すと、生成コストやGC負荷が増えて、ゲームが重くなる可能性があります。そのため、画面外に出たセルを再利用し、表示中の項目だけを更新する形にします。これにより、メモリ使用量と処理負荷を抑えられます。

---

### 练习 5

> [!question] 题目
> 请用 2 到 5 句回答：在 Unreal 的 Data Asset 中配置武器参数时，哪些数据应该保留在运行时实例侧？

> [!example] 自己的答案
>　攻撃力、防衛力などバフ・デバフ・アイテムに影響されるパラメータはインスタンス側で管理します。もし、実行中に直接変更してしまう、他のインスタンスに影響します。

> [!warning] 指正
> `防衛力` 更像“防卫力”，角色参数通常说 `防御力`。题目问“武器参数中哪些保留在实例侧”，除了受 Buff 影响的攻击力，也可以提到当前弹药数、耐久度、临时强化状态等运行时变化数据。`変更してしまう、他のインスタンスに影響します` 需要接续，应为 `変更してしまうと、他のインスタンスにも影響します`。

> [!tip] 参考回答
> Data Assetには武器の基本攻撃力、射程、初期設定など共有できる値を置きます。一方で、現在の弾数、耐久度、強化状態、BuffやDebuffで変化した値などは、実行時のインスタンス側で管理します。共有データを直接変更すると、他の武器インスタンスにも影響してしまうためです。

---

### 练习 6

> [!question] 题目
> 当玩家快速连续点击技能按钮时，需要先判断当前状态和冷却时间，再决定是否进入释放流程。

> [!example] 自己的答案
>　プレイヤーがボタンを素早く連続して押した場合、まず現在のステートとクールダウンを判断してから発動フローに入るかを決めます。

> [!warning] 指正
> 翻译基本准确。`ボタンを押した場合` 没问题，但“技能按钮”最好明确为 `スキルボタン`。`ステート` 可用，日语技术说明中也可以说 `状態`。`発動フローに入るかを決めます` 自然。

> [!tip] 参考回答
> プレイヤーがスキルボタンを素早く連続して押した場合は、まず現在の状態とクールダウン時間を確認します。そのうえで、スキルの発動フローに入るかどうかを判断します。

---

### 练习 7

> [!question] 题目
> 特效对象从对象池取出后，显示前要重置播放时间、颜色参数和上一次绑定的目标引用。

> [!example] 自己的答案
>　オブジェクトプールからエフェクトオブジェクトを取り出した後、表示する前に、進行状況、色パラメータと前回参照をリセットする必要があります。

> [!warning] 指正
> 意思能表达，但 `進行状況` 对特效播放时间略泛，建议说 `再生時間`。`色パラメータと前回参照` 并列不自然，应为 `色パラメータ、前回バインドした対象への参照`。题目中的“显示前”可译为 `表示前に`。

> [!tip] 参考回答
> オブジェクトプールからエフェクトオブジェクトを取り出した後は、表示前に再生時間、色パラメータ、前回バインドした対象への参照をリセットする必要があります。

---

### 练习 8

> [!question] 题目
> 如果碰撞通道的规则分散在多个蓝图里，调试命中问题时会很难确认是哪一处设置覆盖了结果。

> [!example] 自己的答案
>　コリジョンが複数のBlueprintに分散すると、命中不具合をデバッグする時に結果がどこかに上書きされるかを特定しにくくなります。

> [!warning] 指正
> 大意正确。`コリジョンが複数のBlueprintに分散すると` 少了“规则”，建议说 `コリジョンチャンネルのルールが複数のBlueprintに分散していると`。`どこかに上書きされるか` 应为 `どこで上書きされたのか`。`命中不具合` 可以理解，更自然是 `命中問題`。

> [!tip] 参考回答
> コリジョンチャンネルのルールが複数のBlueprintに分散していると、命中問題をデバッグするときに、<mark style="background: #FF5582A6;">どこで</mark>設定が上書きされたのかを確認しにくくなります。そのため、ルールはできるだけ一箇所で管理したほうが調査しやすくなります。

---

### 练习 9

> [!question] 题目
> 存档版本升级后，如果新增了装备耐久度字段，读取旧数据时需要用默认值补齐。

> [!example] 自己的答案
>　セーブデータをアップグレードした後、装備耐久性フィールドを追加した場合、旧セーブデータを読み込む際にデフォルト値で補完する必要があります。

> [!warning] 指正
> 意思基本正确。`セーブデータをアップグレードした後` 不太自然，因为升级的是版本，建议说 `セーブデータのバージョンアップ後`。`装備耐久性フィールド` 更自然是 `装備の耐久度フィールド`。`デフォルト値で補完する` 用得很好。

> [!tip] 参考回答
> セーブデータのバージョンアップ後に装備の耐久度フィールドを追加した場合、旧セーブデータを読み込む際はデフォルト値で補完する必要があります。

---

### 练习 10

> [!question] 题目
> Profiler 显示 UI 布局重建占用了较多 CPU 时间，所以我们需要减少每帧触发的尺寸变化。

> [!example] 自己的答案
>　Profiler上でUIのレイアウト再構築がCPUの処理時間を大きく占めていることがわかりました。そのため毎フレームサイズ変化を減らすようにします。

> [!warning] 指正
> 表达清楚。`CPUの処理時間を大きく占めている` 很自然。第二句 `毎フレームサイズ変化を減らす` 少了助词，应为 `毎フレーム発生するサイズ変化を減らす`。也可以补 `レイアウト再構築の回数を減らす` 来贴近题意。

> [!tip] 参考回答
> Profiler上でUIのレイアウト再構築がCPU時間を大きく占めていることが分かりました。そのため、毎フレーム発生するサイズ変更を減らし、レイアウト再構築の回数を抑える必要があります。

---

### 练习 11

> [!question] 题目
> 请确认动画 Montage 的结束处理在正常播放完毕、被打断和 Blend Out 时都会执行。

> [!example] 自己的答案
>　Montageの終了処理は通常処理、中断とBlend Outどの場合でも呼ばれるように確認してください。

> [!warning] 指正
> 意思能传达。`通常処理` 容易被理解为普通逻辑处理，题目是“正常播放完毕”，建议说 `正常に再生が完了した場合`。`中断とBlend Outどの場合でも` 应为 `中断やBlend Outの場合でも`。`呼ばれるように確認してください` 可以更自然地说 `実行されることを確認してください`。

> [!tip] 参考回答
> Montageの終了処理が、正常に再生が完了した場合、中断された場合、Blend Outした場合のいずれでも実行されることを確認してください。

---

### 练习 12

> [!question] 题目
> 关卡机关的当前开关状态应该保存在实例侧，而触发条件和表现资源应该来自配置数据。

> [!example] 自己的答案
>　レベルギミックの状態はインスタンス側で管理すべきです。トリガーと演出リソースは設定データで管理します。

> [!warning] 指正
> 简洁准确。`レベルギミックの状態` 可以再具体一点说 `現在のオン・オフ状態`。`トリガー` 和 `演出リソース` 都自然。第二句可合并成“配置数据から参照します”，更像技术说明。

> [!tip] 参考回答
> レベルギミックの現在のオン・オフ状態は、インスタンス側で管理するべきです。一方で、トリガー条件や演出リソースは設定データから参照します。

---

### 练习 13

> [!question] 题目
> 在调试角色朝向异常时，先记录输入方向、相机方向和移动组件最终计算出的旋转结果。

> [!example] 自己的答案
>　キャラクター向きをデバッグする時に、まず入力方向、カメラ方向を記録し、そして移動コンポーネントの回転結果を記録します。

> [!warning] 指正
> 意思正确。`キャラクター向き` 应为 `キャラクターの向き`。`デバッグする時に` 更自然是 `デバッグするときは`。`そして` 在这里略硬，可以改成 `そのうえで`。题目里的“最终计算出的旋转结果”建议说 `最終的に計算された回転結果`。

> [!tip] 参考回答
> キャラクターの向きをデバッグするときは、まず入力方向とカメラ方向を記録します。そのうえで、移動コンポーネントによって最終的に計算された回転結果も記録します。

---

### 练习 14

> [!question] 题目
> 如果周末天气不错，我们去附近的公园走一走，然后找家店吃午饭吧。

> [!example] 自己的答案
>　週末天気が良ければ、近くの公園に散歩し、後でレストランで昼ご飯を食べましょう


> [!warning] 指正
> 意思能看懂，但日常表达需要调整。`週末天気が良ければ` 应为 `週末、天気が良ければ`。`公園に散歩し` 不自然，应说 `公園を散歩して` 或 `公園に散歩に行って`。`後でレストランで` 可以，但更自然是 `その後、どこかのお店で`。

> [!tip] 参考回答
> 週末、天気が良ければ、近くの公園を散歩して、その後どこかのお店で昼ご飯を食べましょう。

---

### 练习 15

> [!question] 题目
> 我会先确认今天的构建结果，午饭后再把剩下的问题整理成列表发给大家。

> [!example] 自己的答案
>　本日は　まずビルド結果を確認し、昼ご飯が終わった後残っている問題をリストに整理して皆さんに共有します。

> [!warning] 指正
> 意思完整。`本日は　まず` 中间不需要空格。`昼ご飯が終わった後` 能懂，但职场表达里 `昼休みの後` 或 `昼食後` 更自然。`皆さんに共有します` 很自然，也可以用 `共有いたします` 增加礼貌度。

> [!tip] 参考回答
> 本日はまずビルド結果を確認し、昼食後に残っている問題をリストに整理して、皆さんに共有します。

---

## 2026-07-25

### 练习 1

> [!question] 题目
> 请用 2 到 5 句回答：在 Unreal 的角色移动系统中，为什么把输入读取、移动意图和最终移动结果分开记录，有助于调试异常位移？

> [!example] 自己的答案
> 最終結果がズレている場合は、入力ベクトルと移動意図ベクトルをそれぞれに確認できます。問題を特定しやすくなります。


> [!warning] 指正
> 意思基本清楚，能说明分开记录有助于定位问题。`それぞれに確認できます` 稍不自然，建议改为 `それぞれ確認できます`。另外题目里有“输入读取、移动意图、最终移动结果”三层，你的答案少提了“最終的な移動結果”，可以补出来让调试链路更完整。

> [!tip] 参考回答
> 入力値、移動意図、最終的な移動結果を分けて記録しておくと、異常な移動が発生したときに、どの段階で値がずれたのかを確認しやすくなります。たとえば入力は正しいのに移動結果だけがおかしい場合、移動計算側に問題があると切り分けられます。

---

### 练习 2

> [!question] 题目
> 请用 2 到 5 句回答：在 Unity 中制作技能瞄准指示器时，为什么要把范围计算和显示效果分开实现？

> [!example] 自己的答案
>　ロジック処理と表示処理を別々にすることで、不具合を調整しやすくなります。UI上で現在の拡散度を読み込んで表示するだけで、問題が起きた時にロジック処理だけを調査すれば解決できます。

> [!warning] 指正
> 回答方向是对的，能表达“逻辑和显示分离”。不过题目是“技能瞄准指示器的范围计算和显示效果”，你的答案写成了 `現在の拡散度`，意思偏到了准星扩散。`不具合を調整しやすく` 也不自然，应说 `不具合を特定しやすく` 或 `修正しやすく`。

> [!tip] 参考回答
> 範囲計算と表示処理を分けることで、当たり判定や有効範囲のロジックをUI演出から独立して確認できます。表示側は計算済みの範囲データを受け取って描画するだけにすると、見た目を変更してもスキルの判定ロジックに影響しにくくなります。

---

### 练习 3

> [!question] 题目
> 请用 2 到 5 句回答：当多个 UI 面板都需要显示玩家资源数量时，为什么不应该让每个面板各自读取并修改资源数据？

> [!example] 自己的答案
>　UIパネルは表示処理だけを担当します。データ変更はロジック処理側だけを担当します。そうすることで責務が明確になります。UIパネル側で変更イベントを登録し、変更イベントを受け取ります。もしUI側もデータを変更すれば問題を特定しくくなります。

> [!warning] 指正
> 意思清楚，职责分离的方向很好。`問題を特定しくくなります` 应为 `問題を特定しにくくなります`。`変更イベントを登録し、変更イベントを受け取ります` 有点重复，可以说 `変更イベントを購読して、通知を受け取ります`。还可以补一句“数据的唯一来源”，更贴近题目。

> [!tip] 参考回答
> 複数のUIパネルがそれぞれプレイヤーリソースを読み書きすると、どの画面が値を変更したのか分かりにくくなります。リソースの変更はロジック側に集約し、UI側は変更通知を受け取って表示だけを更新する形にすると、責務が明確になります。

---

### 练习 4

> [!question] 题目
> 请用 2 到 5 句回答：在战斗系统中，为什么伤害公式、暴击判定和受击表现最好保持职责分离？

> [!example] 自己的答案
>　各ロジック計算と被弾演出を別々に実装することで、責務が明確になります。後から置き換えしやすくなり、デバッグもやりやすくなります。

> [!warning] 指正
> 大意正确，`責務が明確になります` 很合适。`各ロジック計算` 略泛，题目中的“伤害公式、暴击判定”可以直接说 `ダメージ計算`、`クリティカル判定`。`置き換えしやすく` 应改为 `置き換えやすく`。

> [!tip] 参考回答
> ダメージ計算、クリティカル判定、被弾演出を分けておくことで、それぞれの責務が明確になります。計算式を調整するときに演出側へ影響を出しにくくなり、逆に被弾エフェクトを差し替える場合もダメージロジックを変更せずに済みます。

---

### 练习 5

> [!question] 题目
> 请用 2 到 5 句回答：如果一个 Boss 技能有预警、蓄力、命中和后摇阶段，你会怎样设计阶段数据，方便策划调整节奏？

> [!example] 自己的答案
>　四つのフェーズにわけます。警告フェーズでは範囲、継続時間が配置できる。力溜めフェーズでは継続時間と中断されるかどうかが配置できる。ヒット判定フェーズではタイミングが配置できる。

> [!warning] 指正
> 设计思路基本对，四个阶段也抓到了。`わけます` 建议写成 `分けます`；`配置できる` 在这里不自然，策划可调整数据更常说 `設定できる`。`力溜め` 一般说 `溜め` 或 `チャージ`。题目还有“后摇阶段”，你的答案没有说明收尾阶段的数据。

> [!tip] 参考回答
> Bossスキルを予兆、チャージ、命中、後隙の四つのフェーズに分け、各フェーズごとに継続時間、判定範囲、エフェクト、キャンセル可否などを設定できるデータにします。そうすることで、プランナーがコードを変更せずにスキル全体のテンポを調整しやすくなります。

---

### 练习 6

> [!question] 题目
> 渲染调试时，请先确认透明材质的排序、深度写入设置和后处理影响范围。

> [!example] 自己的答案
>　描画をデバッグする際に、まず半透明の描画順、深度の書き込み、ポストプロセスの影響範囲を確認しておきます

> [!warning] 指正
> 翻译基本准确。`半透明の描画順`、`深度の書き込み`、`ポストプロセスの影響範囲` 都能表达题意。小问题是句末缺少句号；另外“透明材质”可更具体说 `透明マテリアル` 或 `半透明マテリアル`。

> [!tip] 参考回答
> 描画をデバッグする際は、まず透明マテリアルの描画順、深度書き込みの設定、ポストプロセスの影響範囲を確認してください。

---

### 练习 7

> [!question] 题目
> 这个角色状态机在进入受击状态前，需要先清除上一段攻击留下的连击标记。

> [!example] 自己的答案
>　キャラクターが被弾ステートに入る前に、前コンボのマークをクリアする必要があります。

> [!warning] 指正
> 意思能传达。`被弾ステート` 可以用，也可以说 `被弾状態`。`前コンボのマーク` 不太自然，题目里的“连击标记”更适合说 `前回の攻撃で残ったコンボフラグ` 或 `コンボ用のフラグ`。

> [!tip] 参考回答
> このキャラクターのステートマシンでは、被弾状態に入る前に、前回の攻撃で残ったコンボフラグを先にクリアする必要があります。

---

### 练习 8

> [!question] 题目
> 加载战斗场景前，请分阶段读取角色模型、动画资源和技能配置，避免首帧出现明显卡顿。

> [!example] 自己的答案
>　バトルシーンを読み込む前に、キャラクターモデル、アニメーションリソースとスキリ設定を各フェーズに分散して読み込ます。そうすることで明らかなカクつきを避けられます。

> [!warning] 指正
> 主要意思能看懂，但有几个词需要修正。`スキリ設定` 应为 `スキル設定`。`各フェーズに分散して読み込ます` 不自然，应说 `段階的に読み込みます`。`明らかなカクつき` 可以用，不过这里更自然是 `目立つカクつき`。

> [!tip] 参考回答
> バトルシーンを読み込む前に、キャラクターモデル、アニメーションリソース、スキル設定を段階的に読み込みます。そうすることで、最初のフレームで目立つカクつきが発生するのを避けられます。

---

### 练习 9

> [!question] 题目
> 如果道具图标缺失，背包界面应该显示默认图标，并在日志中记录对应的配置 ID。

> [!example] 自己的答案
>　アイテムアイコンが不足している場合は、インベントリ画面ではデフォルトアイコンで補完し、そのアイテムIDをログに出力してください。

> [!warning] 指正
> 意思基本正确。`アイテムアイコンが不足している` 可以理解，但“缺失”更自然是 `見つからない` 或 `設定されていない`。题目要求记录“对应的配置 ID”，你的 `そのアイテムID` 稍微偏了，可以改成 `対応する設定ID`。

> [!tip] 参考回答
> アイテムアイコンが見つからない場合、インベントリ画面ではデフォルトアイコンを表示し、ログには対応する設定IDを記録します。

---

### 练习 10

> [!question] 题目
> 这个技能的命中盒只在有效帧内开启，动画结束后必须立即关闭。

> [!example] 自己的答案
>　ヒットボックスは有効フレーム間でオンにし、アニメーションが終了下後に必ずすぐに閉じます。

> [!warning] 指正
> 题意基本表达出来了。`有効フレーム間で` 应为 `有効フレームの間だけ`。`終了下後` 是错误形式，应为 `終了した後`。`閉じます` 能懂，但命中盒通常说 `オフにします` 或 `無効化します` 更自然。

> [!tip] 参考回答
> このスキルのヒットボックスは有効フレームの間だけオンにし、アニメーションが終了した後は必ずすぐにオフにします。

---

### 练习 11

> [!question] 题目
> 调查掉帧问题时，请分别记录游戏线程、渲染线程和资源加载的耗时。

> [!example] 自己的答案
>　カクつきの原因を調査する時に、ゲームスレッド、描画スレッドとリソースロードにかかっている時間をそれぞれに記録してください。

> [!warning] 指正
> 翻译准确，技术词汇也没问题。`ゲームスレッド、描画スレッドとリソースロード` 并列时更自然是 `ゲームスレッド、描画スレッド、リソースロード`。`それぞれに記録` 应改为 `それぞれ記録`。

> [!tip] 参考回答
> カクつきの原因を調査するときは、ゲームスレッド、描画スレッド、リソースロードにかかっている時間をそれぞれ記録してください。

---

### 练习 12

> [!question] 题目
> 存档读取失败时，不要直接覆盖旧文件，而是先保存错误原因和备份路径。

> [!example] 自己的答案
>　セーブデータを読み込むに失敗した場合は、旧ファイルを直接上書きしないで、先にエラー原因とバックアップパスを保存しておきます。

> [!warning] 指正
> 意思清楚，但 `セーブデータを読み込むに失敗した場合` 语法不对，应为 `セーブデータの読み込みに失敗した場合` 或 `セーブデータを読み込めなかった場合`。`上書きしないで` 可以用，技术说明里也可写成 `上書きせずに`。

> [!tip] 参考回答
> セーブデータの読み込みに失敗した場合は、旧ファイルを直接上書きせずに、先にエラー原因とバックアップパスを保存しておきます。

---

### 练习 13

> [!question] 题目
> 手柄操作下，菜单焦点需要在打开界面时设置到第一个可选按钮上。

> [!example] 自己的答案
>　ゲームパットを利用する場合、メニューを開いた時にフォーカスを最寄りの選択可能ボタンに移します。

> [!warning] 指正
> 题意有一部分偏差。`ゲームパット` 应为 `ゲームパッド`。原句是“第一个可选按钮”，你的 `最寄りの選択可能ボタン` 变成了“最近的可选按钮”，意思不同。应说 `最初の選択可能なボタン`。

> [!tip] 参考回答
> ゲームパッド操作の場合、メニューを開いたときに、フォーカスを最初の選択可能なボタンに設定する必要があります。

---

### 练习 14

> [!question] 题目
> 如果明天早上雨还很大，我们就晚一点出门，先在家吃早饭吧。

> [!example] 自己的答案
>　明日朝　雨が大きく続いていだったら、。自宅で朝ご飯を食べておいてちょっと遅くて出かけます。

> [!warning] 指正
> 这句意思能猜到，但语法和自然度问题比较多。`雨が大きく` 不自然，雨势大应说 `雨が強い`。`続いていだったら` 应为 `続いていたら`。中间的 `、。` 需要删除。`ちょっと遅くて出かけます` 应改为 `少し遅く出かけましょう`。

> [!tip] 参考回答
> 明日の朝も雨が強かったら、少し遅く出かけて、先に家で朝ご飯を食べましょう。

---

### 练习 15

> [!question] 题目
> 我先把会议上决定的事项整理成文档，下午再请大家确认优先级是否正确。

> [!example] 自己的答案
>　まず会議で決まった事項をドキュメントに整理し、午後皆さんに優先度を確認していただきます。

> [!warning] 指正
> 意思基本完整，职场语气也可以。`午後皆さんに` 中间需要加助词，建议写成 `午後に皆さんに`。题目是“确认优先级是否正确”，可以更明确说 `優先度が正しいかどうか`。如果是请大家确认，`確認していただきます` 稍像安排别人做事，`確認していただきます` 可以改为更柔和的 `確認していただければと思います`。

> [!tip] 参考回答
> まず会議で決まった事項をドキュメントに整理し、午後に優先度が正しいかどうかを皆さんに確認していただければと思います。

---

## 2026-07-26

### 练习 1

> [!question] 题目
> 在 Unreal 中调整角色转身时，应该先确认输入方向、目标朝向和插值后的最终旋转是否一致。

> [!example] 自己的答案
> Unrealではキャラクターが回転する時、まず入力方向を確認し、ターゲット方向と補間後の結果が一致しています。

> [!warning] 指正
> 意思大致能传达，但最后的 `結果が一致しています` 变成了陈述“结果已经一致”，没有表达“是否一致”。`ターゲット方向` 可以用，不过这里更自然是 `目標方向` 或 `目標の向き`。句子应改成确认动作：`一致しているかを確認します`。

> [!tip] 参考回答
> Unrealでキャラクターの旋回を調整するときは、まず入力方向、目標の向き、補間後の最終的な回転が一致しているかを確認します。

---

### 练习 2

> [!question] 题目
> Unity 的技能按钮进入冷却后，UI 只负责显示剩余时间，不应该直接修改技能状态。

> [!example] 自己的答案
>　Unityのスキルボタンがクールダウンに入った後、UIは残り時間の表示だけを担当し、スキル状態を直接変更するべきではありません。

> [!warning] 指正
> 翻译很自然，题意也完整。`スキル状態` 可以理解，技术说明里更常写成 `スキルの状態`。`変更するべきではありません` 的语气准确。

> [!tip] 参考回答
> Unityのスキルボタンがクールダウンに入った後、UIは残り時間の表示だけを担当し、スキルの状態を直接変更するべきではありません。

---

### 练习 3

> [!question] 题目
> 如果攻击命中盒的位置偏移了，请先检查骨骼 Socket、动画姿势和碰撞通道设置。

> [!example] 自己的答案
>　ヒットボックスがズレている場合は、まずボンのSocket、アニメーションポーズとコリジョンチェンネルを確認します。

> [!warning] 指正
> 主要意思能看懂，但有几个词需要修正。`ボン` 应为 `ボーン`。`コリジョンチェンネル` 应为 `コリジョンチャンネル`。并列时 `アニメーションポーズとコリジョン...` 更自然是 `アニメーションポーズ、コリジョン...`。另外“位置偏移”可以明确说 `位置がずれている`。

> [!tip] 参考回答
> 攻撃のヒットボックスの位置がずれている場合は、まずボーンのSocket、アニメーションポーズ、コリジョンチャンネルの設定を確認してください。

---

### 练习 4

> [!question] 题目
> 敌人进入警戒状态时，需要保存发现玩家的原因、当前位置和下一步行动计划。

> [!example] 自己的答案
>　敵が警戒状態に入る時に、プレイヤーを検知した原因、現在の位置と次の動作を保存しておく必要があります。

> [!warning] 指正
> `現在の位置と次の動作` 能懂，但“下一步行动计划”更自然是 `次の行動予定`。

> [!tip] 参考回答
> 敵が警戒状態に入るときは、プレイヤーを発見した理由、現在位置、次の行動予定を保存しておく必要があります。

---

### 练习 5

> [!question] 题目
> 这个弹道预览效果只用于瞄准阶段，发射后应该立即隐藏并释放临时引用。

> [!example] 自己的答案
>　弾道プレビュー効果が照準フェーズだけに適応し、発射後にすぐに隠して一時的な参照を解放します。

> [!warning] 指正
> 意思大体正确。`適応し` 应改为 `適用され`，或者更自然地说 `使われます`。`弾道プレビュー効果` 可以理解，但 `弾道プレビューエフェクト` 更贴近游戏开发表达。`隠して` 可以用，技术说明里也可说 `非表示にします`。

> [!tip] 参考回答
> この弾道プレビューエフェクトはエイム中だけに使われるため、発射後はすぐに非表示にし、一時的な参照を解放します。

---

### 练习 6

> [!question] 题目
> 资源加载完成前，背包格子可以先显示占位图标，避免界面突然跳动。

> [!example] 自己的答案
>　リソースロードが完了する前にインベントリーセルをプレースホルダーで表示しておき、画面がちらつくことを避けます。

> [!warning] 指正
> 基本意思正确。`インベントリーセルをプレースホルダーで表示` 稍微少了“图标”，建议说 `プレースホルダーアイコンを表示`。`画面がちらつく` 是“闪烁”，题目是“突然跳动”，这里更自然是 `画面が急に跳ねる` 或 `レイアウトが急に変わる`。

> [!tip] 参考回答
> リソースのロードが完了する前は、インベントリのセルにプレースホルダーアイコンを表示しておき、画面が急に跳ねないようにします。

---

### 练习 7

> [!question] 题目
> 在设计连击系统时，每一段攻击都应该有独立的输入窗口、伤害倍率和结束条件。

> [!example] 自己的答案
>　コンボシステムを設計する時に、フェーズごとに独立した入力ウィンドウ、ダメージ倍率と終了条件を配置します。

> [!warning] 指正
> 意思能表达。`フェーズごとに` 可以用，但“每一段攻击”更具体是 `攻撃段階ごとに` 或 `各攻撃ごとに`。`配置します` 在这里不自然，数据或条件应说 `持たせます` 或 `設定します`。并列时 `ダメージ倍率と終了条件` 建议写成 `ダメージ倍率、終了条件`。

> [!tip] 参考回答
> コンボシステムを設計するときは、<mark style="background: #FF5582A6;">各攻撃段階に</mark>独立した入力受付時間、ダメージ倍率、終了条件を持たせます。

---

### 练习 8

> [!question] 题目
> 渲染角色残影时，请限制生成频率，并在效果结束后回收使用过的材质实例。

> [!example] 自己的答案
>　キャラクターの残影を描画する時、生成レートを制限し、その上で効果が終了後に使い済みマテリアルを回収してください

> [!warning] 指正
> 题意基本清楚。`生成レート` 可以用，不过 `生成頻度` 更直接。`効果が終了後に` 应为 `効果が終了した後に`。`使い済みマテリアル` 不自然，应说 `使用済みのマテリアルインスタンス`。句末缺少句号。

> [!tip] 参考回答
> キャラクターの残影を描画するときは、<mark style="background: #FF5582A6;">生成頻度</mark>を制限し、効果が終了した後に使用済みのマテリアルインスタンスを回収してください。

---

### 练习 9

> [!question] 题目
> 如果自动寻路失败，角色应该停止移动，并在调试日志里记录失败原因。

> [!example] 自己的答案
>　自動経路探索が失敗した場合、キャラクターが直ちに止まり、失敗原因をログに出力します。

> [!warning] 指正
> 翻译基本准确。`キャラクターが直ちに止まり` 可以理解，但技术说明里更自然是 `キャラクターを停止させ`。`失敗原因をログに出力します` 很自然。题目里的“调试日志”可以更明确写成 `デバッグログ`。

> [!tip] 参考回答
> 自動経路探索に失敗した場合は、キャラクターを停止させ、デバッグログに失敗原因を記録します。

---

### 练习 10

> [!question] 题目
> 这个交互物的高亮效果应该由可交互状态控制，而不是由鼠标悬停事件单独决定。

> [!example] 自己的答案
> 操作可能なオブジェクトの効果は状態制御で管理するべき、マウスのホバーできめるのではありません。

> [!warning] 指正
> 意思有一部分表达出来了，但 `効果は状態制御で管理するべき` 语法不完整，应为 `状態で制御するべきです`。`操作可能なオブジェクト` 容易理解，但“交互物”更自然是 `インタラクト可能なオブジェクト`。`マウスのホバーできめる` 应为 `マウスホバーだけで決める`。

> [!tip] 参考回答
> このインタラクト可能なオブジェクトのハイライト効果は、インタラクト可能な状態で制御するべきであり、マウスホバーイベントだけで決めるべきではありません。

---

### 练习 11

> [!question] 题目
> 在性能分析时，请分别比较动画更新、粒子效果和 UI 刷新的耗时。

> [!example] 自己的答案
>　パフォーマンス時に、アニメーション更新、パーティクルとUI更新をそれぞれに計測します

> [!warning] 指正
> 主要意思能看懂，但 `パフォーマンス時に` 不自然，应说 `パフォーマンス分析時に`。`パーティクル` 后面少了“效果”或“更新耗时”，建议写成 `パーティクルエフェクト`。`それぞれに計測します` 应改为 `それぞれ計測します`。

> [!tip] 参考回答
> パフォーマンス分析時には、アニメーション更新、パーティクルエフェクト、UI更新にかかる時間をそれぞれ比較してください。

---

### 练习 12

> [!question] 题目
> 读取旧版本存档时，如果字段不存在，就使用默认值并记录一次迁移日志。

> [!example] 自己的答案
>　旧バージョンのセーブデータを読み込む時に、フィールドが存在しない場合、デフォルト値で補完して移行ログに記録します。

> [!warning] 指正
> 意思清楚，整体自然。`読み込む時に` 可以更自然写作 `読み込むときに`。题目是“记录一次迁移日志”，你的 `移行ログに記録します` 可以表达，但如果强调“一次”，可说 `移行ログを一度記録します`。

> [!tip] 参考回答
> 旧バージョンのセーブデータを読み込むときに、フィールドが存在しない場合は、デフォルト値で補完し、移行ログを一度記録します。

---

### 练习 13

> [!question] 题目
> 关卡机关播放完开启动画后，才应该把碰撞体切换到可通过状态。

> [!example] 自己的答案
>　ステージギミックがアニメーションを再生した後にコリジョン状態を通過可能に切り替えるべきです。

> [!warning] 指正
> 意思基本正确。`ステージギミック` 能懂，游戏关卡语境里也常说 `レベルギミック`。`アニメーションを再生した後に` 少了“播放完”，应改为 `再生し終わった後に`。`コリジョン状態を通過可能に` 很自然。

> [!tip] 参考回答
> レベルギミックの開くアニメーションが再生し終わった後で、コリジョンを通過可能な状態に切り替えるべきです。

---

### 练习 14

> [!question] 题目
> 今天有点热，我们买点喝的，然后找个凉快的地方休息一下吧。

> [!example] 自己的答案
>　今日はちょっと暑いから、飲み物を買ってから涼しい場所で休憩しましょう

> [!warning] 指正
> 日常表达很自然，意思完整。`涼しい場所で休憩しましょう` 可以用；如果更口语一点，可以说 `涼しいところで少し休みましょう`。句末建议加句号。

> [!tip] 参考回答
> 今日はちょっと暑いので、飲み物を買ってから、涼しいところで少し休みましょう。

---

### 练习 15

> [!question] 题目
> 我上午先确认剩下的问题，下午把需要大家配合的事项整理出来。

> [!example] 自己的答案
>　午前はまず残っている問題を確認し、午後に皆さんの協力事項を整理します。

> [!warning] 指正
> 基本意思能传达，但 `皆さんの協力事項` 听起来像“大家的协作事项”，不太像“需要大家配合的事项”。更自然是 `皆さんにご協力いただきたい事項`。`午前はまず` 可以用，职场汇报里也可以说 `午前中にまず`。

> [!tip] 参考回答
> 午前中にまず残っている問題を確認し、午後に皆さんにご協力いただきたい事項を整理します。
