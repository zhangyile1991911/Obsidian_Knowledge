
### 06-22

> [!question] 题目  
> 为了避免第一次播放技能特效时卡顿，请在进入战斗前预热该角色会使用的 Shader Variant。

> [!example] 我的回答  
> 初めてスキルエフェクトを再生する時のかくつきを避けるため、戦闘開始前にキャラクターに使用されるシェーダーバリアントを事前にウォーミングアップしてください

> [!warning] 指正  
> 整体意思对，但「初めてスキルエフェクトを再生する時のかくつき」稍微啰嗦，技术文档中更自然的是「初回再生時のカクつき」。  
> 「キャラクターに使用される」不自然。这里是“该角色会使用的”，推荐用「そのキャラクターが使用する」或「対象キャラクターで使用される」。  
> 「ウォーミングアップ」可以理解，但 Shader Variant 语境里更常用「ウォームアップ」。

> [!tips] 推荐回答  
> スキルエフェクトの初回再生時のカクつきを避けるため、戦闘開始前に、そのキャラクターが使用するシェーダーバリアントを事前にウォームアップしてください。

---

> [!question] 题目  
> 如果 Sprite 的材质在运行时被动态替换，请确认新的材质仍然支持当前使用的 Stencil 设置。

> [!example] 我的回答  
> 実行中にSpriteのマテリアルを動的に差し替える場合は　新しいマテリアルがstencil可能を確認してください

> [!warning] 指正  
> 「実行中」可以理解，但游戏运行时更常用「実行時」或「ランタイム中」。  
> 「マテリアルを動的に差し替える」很好。  
> 「stencil可能」不成立。这里应该说「Stencil設定に対応している」「現在使用しているStencil設定をサポートしている」。  
> 另外，「確認してください」前面需要接「〜かを」，例如「対応しているかを確認してください」。

> [!tips] 推荐回答  
> ランタイム中にSpriteのマテリアルを動的に差し替える場合は、新しいマテリアルが現在使用している<mark style="background: #FF5582A6;">Stencil設定に対応している</mark>かを確認してください。

---

> [!question] 题目  
> 关闭角色详情界面时，请释放该界面中动态加载的头像和立绘资源，避免引用残留导致内存无法回收。

> [!example] 我的回答  
> キャラクター詳細画面を閉じる際に、この画面では動的にロード済みアイコンと立ち絵リソースを解放してください。参照が残るとメモリが正しく回収されないようになる。

> [!warning] 指正  
> 「この画面では動的にロード済み」不自然。应该说「この画面で動的にロードした」或「その画面内で動的にロードした」。  
> 「アイコン」可以，但中文是“头像”，游戏 UI 里也可以用「顔アイコン」「アバター画像」。  
> 「メモリが正しく回収されないようになる」不自然。推荐「メモリが回収されなくなる」或「メモリが正しく解放されなくなる」。

> [!tips] 推荐回答  
> キャラクター詳細画面を閉じる際は、その画面で動的にロードした顔アイコンと立ち絵リソースを解放してください。参照が残ると、メモリが回収されなくなります。

---

> [!question] 题目  
> 从对象池取出特效对象后，请在播放结束时自动归还，避免只借不还导致池内对象耗尽。

> [!example] 我的回答  
> オブジェクトプールからエフェクトを取り出した後、再生後に自動的に返却してください。そうしないとプール内のオブジェクトがなくなってしまいます

> [!warning] 指正  
> 整体不错。  
> 「エフェクト」最好明确成「エフェクトオブジェクト」。  
> 「再生後」能懂，但更准确的是「再生終了時」或「再生が終了した時」。  
> “只借不还”可以说「返却漏れ」或「借りっぱなし」。  
> “对象池耗尽”推荐用「プール内のオブジェクトが枯渇する」。「枯渇する」很适合表示池中对象耗尽。

> [!tips] 推荐回答  
> オブジェクトプールからエフェクトオブジェクトを取り出した後は、再生終了時に自動で返却してください。<mark style="background: #FF5582A6;">返却漏れ</mark>が続くと、プール内のオブジェクトが<mark style="background: #FF5582A6;">枯渇</mark>してしまいます。

---

> [!question] 题目  
> 如果 Montage 播放结束后角色仍然停留在最后一帧，请确认 Slot 节点是否仍然保持了上一次的动画结果。

> [!example] 我的回答  
> Montage再生後にキャラクターが最後フレームに止まっている場合、Slotノードには前回アニメーションが残っているかを確認してください。

> [!warning] 指正  
> 「最後フレーム」应为「最後のフレーム」。  
> 「止まっている」可以理解，但动画表现中更自然的是「停止している」「固まっている」。  
> 「Slotノードには」这里助词不自然。推荐「Slotノードが前回のアニメーション結果を保持していないか」或「Slotノードで前回のアニメーション結果が残っていないか」。  
> 原文是“是否仍然保持了上一次的动画结果”，所以用「保持していないか」更准确。

> [!tips] 推荐回答  
> Montage再生終了後もキャラクターが<mark style="background: #FF5582A6;">最後のフレームで</mark>停止している場合は、Slotノードが前回のアニメーション結果を保持していないか確認してください。

---

> [!question] 题目  
> 两个带有 Root Motion 的动画混合时，如果根骨骼轨迹没有对齐，角色位置可能会在过渡过程中被拉回。

> [!example] 我的回答  
> 二つのRoot Motion付きアニメーションがブレンドする際にルートボーンが揃わないとキャラクターが遷移中に位置が戻るかもしれません

> [!warning] 指正  
> 「アニメーションがブレンドする」不自然。动画是“被混合”，或者说“混合动画”，应写成「アニメーションをブレンドする」或「アニメーションがブレンドされる」。  
> 「ルートボーンが揃わない」不够准确。题目是“根骨骼轨迹没有对齐”，应该说「ルートボーンの軌跡が揃っていない」「ルートモーションの軌跡が揃っていない」。  
> 「キャラクターが遷移中に位置が戻る」结构不自然。推荐「遷移中にキャラクター位置が引き戻される」或「遷移中にキャラクターの位置が戻ってしまう」。

> [!tips] 推荐回答  
> 2つのRoot Motion付きアニメーションをブレンドする際に、ルートボーンの軌跡が揃っていないと、遷移中にキャラクター位置が<mark style="background: #FF5582A6;">引き戻される</mark>可能性があります。

---

> [!question] 题目  
> 进入死亡演出子流程后，主状态机应该暂停当前流程，直到死亡演出结束后再切换到结果状态。

> [!example] 我的回答  
> 死亡演出サブフローに入った後、メインステートマシンが一時停止し、死亡演出が終了後にリザルト状態に切り替える

> [!warning] 指正  
> 「メインステートマシンが一時停止し」不是完全错，但需求文档里更自然的是「メインステートマシンを一時停止し」，因为这里是“让主状态机暂停”。  
> 「死亡演出が終了後に」不自然，应为「死亡演出が終了した後に」或「死亡演出終了後に」。  
> 「切り替える」可以，但状态机语境里更自然的是「遷移する」。

> [!tips] 推荐回答  
> 死亡演出サブフローに入った後は、メインステートマシンの現在フローを一時停止し、死亡演出終了後にリザルト状態へ遷移してください。

---

> [!question] 题目  
> 如果玩家在攻击后摇期间输入了翻滚，请缓存该输入，并在角色恢复可操作后立即执行。

> [!example] 我的回答  
> 攻撃後の硬直時にプレイヤーがローリングを入力した場合、入力をバッファリングし、キャラクターが操作可能に復帰してから直ちにキャッシュした入力を実行する

> [!warning] 指正  
> 整体不错。  
> 「攻撃後の硬直時」可以，但题目是“期间”，更自然的是「攻撃後硬直中に」或「攻撃後の硬直中に」。  
> 「ローリング」可以，不过动作游戏里「回避」「ローリング回避」也常见。  
> 「操作可能に復帰」可以理解，但更自然的是「操作可能になった後」或「操作可能状態に戻った後」。  
> 「入力をバッファリング」和「キャッシュした入力」有点重复，统一成一种说法即可。

> [!tips] 推荐回答  
> 攻撃後硬直中にプレイヤーがローリングを入力した場合は、その入力をバッファリングし、キャラクターが操作可能になった後すぐに実行してください。

---

> [!question] 题目  
> 请不要在 UI 按钮事件里直接修改装备数据，而是通过 Gameplay 层提供的接口请求装备切换。

> [!example] 我的回答  
> UIボタンイベント内で装備データを直接変更せず、Gameplay層が提供しているインターフェース経由で装備切り替えをリクエストします

> [!warning] 指正  
> 这句很好，基本自然。  
> 「UIボタンイベント内で」可以，但更自然的是「UIボタンのイベント内で」。  
> 「提供している」可以，文档里也可以简洁写成「提供する」。  
> 如果是需求句，结尾用「してください」更符合题目语气。

> [!tips] 推荐回答  
> UIボタンのイベント内で装備データを直接変更せず、Gameplay層が提供するインターフェース経由で装備切り替えをリクエストしてください。

---

> [!question] 题目  
> Profiler 显示，每次刷新背包列表时都会生成大量临时字符串，所以需要减少字符串拼接并复用 UI Item。

> [!example] 我的回答  
> プロファイラーにより、インベントリが更新する旅に大量の一時文字列が生成されることが判明しましたため、文字列連結を減らしてUI Itemを再利用してください

> [!warning] 指正  
> 「プロファイラーにより」不自然。推荐「Profilerを見ると」或「Profiler上では」。  
> 「インベントリが更新する」有点像“背包自己更新”。原文是“刷新背包列表”，应写成「インベントリリストを更新する」或「インベントリ一覧を更新する」。  
> 「旅」是错字，应为「たび」。  
> 「判明しましたため」非常硬，而且接续不自然。推荐直接说「生成されているため」或「生成されていることが分かっているため」。

> [!tips] 推荐回答  
> Profiler上では、インベントリリストを更新するたびに大量の一時文字列が生成されているため、文字列連結を減らし、UI Itemを再利用してください。


### 06-23

> [!question] 题目  
> 角色 HP 变化时，不要让 Gameplay 逻辑直接刷新 UI，而是通过观察者模式通知 UI 层。

> [!example] 我的回答  
> キャラクターのHPが変化した場合は、GameplayがUIを更新せず、オブザーバーパターン経由でUI層へ通知すべきです

> [!warning] 指正  
> 意思基本正确。  
> 「GameplayがUIを更新せず」少了「直接」这个重点。  
> 另外，「Gameplay」本身有点泛，推荐写成「Gameplayロジック」或「Gameplay側の処理」。  
> 「通知すべきです」可以，但需求文档里更自然的是「通知してください」或「通知するようにしてください」。

> [!tips] 推荐回答  
> キャラクターのHPが変化した場合は、GameplayロジックからUIを直接更新せず、オブザーバーパターンを使ってUI層へ通知してください。

---

> [!question] 题目  
> 不同类型的敌人需要使用不同的 Prefab，请把生成逻辑封装到工厂类中。

> [!example] 我的回答  
> 異なる敵が専用プレハブを使用するため、生成ロジックをファクトリーに集約してください

> [!warning] 指正  
> 「異なる敵」能懂，但原文是“不同类型的敌人”，推荐写成「敵の種類ごとに」。  
> 「専用プレハブ」不是错，但更接近原文的是「異なるPrefab」或「それぞれ異なるPrefab」。  
> 「ファクトリーに集約」可以理解，但“封装到工厂类中”更自然的是「ファクトリークラスにカプセル化する」「ファクトリークラスにまとめる」「ファクトリークラスへ切り出す」。

> [!tips] 推荐回答  
> 敵の種類ごとに使用するPrefabが異なるため、生成ロジックをファクトリークラスにまとめてください。

---

> [!question] 题目  
> 为了兼容旧输入系统和新输入系统，请添加一个 Adapter 层，把输入结果转换成统一的 Gameplay 指令。

> [!example] 我的回答  
> 古い入力システムと新しい入力システムの互換性を保持するため、アダプター層を追加し、入力結果を同じGameplayコマンドに変換してください

> [!warning] 指正  
> 整体不错。  
> 「互換性を保持する」可以，但更自然的是「互換性を保つ」或「両方に対応する」。  
> 「同じGameplayコマンド」能懂，但“统一的 Gameplay 指令”更自然的是「共通のGameplayコマンド」或「統一されたGameplayコマンド」。

> [!tips] 推荐回答  
> 古い入力システムと新しい入力システムの互換性を保つため、アダプター層を追加し、入力結果を共通のGameplayコマンドに変換してください。

---

> [!question] 题目  
> 请不要在 Update 里用巨大的 switch 判断角色状态，而是把移动、攻击、受击等状态拆成独立的 State 类。

> [!example] 我的回答  
> Updateメソッド内で肥大化したswitchでキャラクターステートを判断しないように、移動、攻撃、被弾など状態を独立のStateクラスに切り出してください

> [!warning] 指正  
> 这句很接近自然表达。  
> 「しないように、〜してください」不是最稳。这里推荐用「判断するのではなく、〜してください」。  
> 「switch」在文档里更常写成「switch文」。  
> 「移動、攻撃、被弾など状態」少了「の」，应为「移動、攻撃、被弾などの状態」。  
> 「独立のStateクラス」应为「独立したStateクラス」。

> [!tips] 推荐回答  
> Updateメソッド内で肥大化したswitch文を使ってキャラクター状態を判断するのではなく、移動、攻撃、被弾などの状態を独立したStateクラスに切り出してください。

---

> [!question] 题目  
> 为了让装备计算逻辑更容易测试，请不要在类内部直接创建依赖对象，而是通过构造函数注入。

> [!example] 我的回答  
> 装備計算ロジックをテストしやすいため、クラス内で依存するオブジェクトを直接生成しないで依存性注入によって

> [!warning] 指正  
> 这句没有写完。  
> 「テストしやすいため」应改为「テストしやすくするため」。  
> 「依存するオブジェクト」可以理解，但更自然的是「依存オブジェクト」或「依存先オブジェクト」。  
> “通过构造函数注入”应说「コンストラクタ経由で注入する」或「コンストラクタインジェクションを使う」。

> [!tips] 推荐回答  
> 装備計算ロジックをテストしやすくするため、クラス内で依存オブジェクトを直接生成せず、<mark style="background: #FF5582A6;">コンストラクタ</mark>経由で注入してください。

---

> [!question] 题目  
> 技能系统只应该依赖伤害计算接口，不应该直接依赖具体的角色属性类。

> [!example] 我的回答  
> スキルシステムがダメージ計算インターフェースに依頼すべき、具体的キャラクター属性に依頼するのではありません

> [!warning] 指正  
> 这里主要问题是「依頼」用错了。  
> 编程里的“依赖”是「依存する」，不是「依頼する」。  
> 「依頼する」是“委托、拜托别人做事”。  
> 「具体的キャラクター属性」不自然，推荐「具体的なキャラクター属性クラス」或「具体的なキャラクターステータスクラス」。

> [!tips] 推荐回答  
> スキルシステムはダメージ計算インターフェースのみに依存し、具体的なキャラクター属性クラスには直接依存しないようにしてください。

---

> [!question] 题目  
> 为了支持编辑器里的撤销功能，请把每次修改地图对象的操作封装成 Command。

> [!example] 我的回答  
> エディタ内のキャンセルに対応するため、毎回マップに対する操作をコマンドにラップしてください

> [!warning] 指正  
> 「撤销功能」不是「キャンセル」，而是「Undo機能」或「取り消し機能」。  
> 「毎回マップに対する操作」不自然。原文是“每次修改地图对象的操作”，推荐「マップオブジェクトを変更する各操作」或「マップオブジェクトへの変更操作」。  
> 「ラップする」能懂，但设计模式语境里更自然的是「Commandとしてカプセル化する」。

> [!tips] 推荐回答  
> エディタ上のUndo機能に対応するため、マップオブジェクトへの各変更操作をCommandとしてカプセル化してください。

---

> [!question] 题目  
> 多个 UI 页面里都有相同的按钮冷却逻辑，请把这部分共通处理抽到基类或工具类中。

> [!example] 我的回答  
> 複数のUI画面には共通のボタンクールダウンロジックがあるため、この共通の処理を切り出して基底クラスに移行してください

> [!warning] 指正  
> 意思基本正确。  
> 「複数のUI画面には」可以，但更自然的是「複数のUI画面で」。  
> 「基底クラスに移行」有点怪。逻辑不是“迁移”，而是“抽到基类/工具类中”，推荐「基底クラスに切り出す」或「基底クラスまたはユーティリティクラスに切り出す」。  
> 题目中有“基类或工具类”，你的翻译漏了「ユーティリティクラス」。

> [!tips] 推荐回答  
> 複数のUI画面で同じボタンクールダウンロジックを使用しているため、この共通処理を基底クラスに切り出してください。

---

> [!question] 题目  
> 任务系统和成就系统不应该互相直接引用，可以通过事件总线传递“任务完成”事件。

> [!example] 我的回答  
> クエストシステムと実績システムの間で互い参照しべきではない、イベントバスでクエスト完成を通知します

> [!warning] 指正  
> 「互い参照しべき」语法错误。应该说「互いに直接参照すべきではありません」或「相互に直接参照しないようにしてください」。  
> 「クエスト完成」可以理解，但更自然的是「クエスト完了イベント」或「『クエスト完了』イベント」。  
> 「イベントバスで通知します」可以，但“通过事件总线传递事件”更自然是「イベントバス経由で通知する」「イベントバス経由で発行する」。

> [!tips] 推荐回答  
> クエストシステムと実績システムは互いに直接参照せず、イベントバス経由で「クエスト完了」イベントを通知してください。

### 06-24

> [!question] 题目  
> 远程武器攻击时，我会从武器上的发射点生成子弹对象，并根据角色朝向决定发射方向。

> [!example] 我的回答  
> 遠距離武器が攻撃する際に、まず武器の発射ポイント上で弾を生成し、そして現在キャラクター方向に向かって飛ばす

> [!warning] 指正  
> 「遠距離武器が攻撃する」不太自然，听起来像“武器自己攻击”。面试口语里更自然的是「遠距離武器で攻撃する際は」或「遠距離武器の攻撃時は」。  
> 「発射ポイント上で」不自然。子弹是“从发射点生成”，应说「発射ポイントから弾を生成する」。  
> 「現在キャラクター方向」不自然。推荐「キャラクターの向きに合わせて」或「キャラクターの向いている方向に」。

> [!tips] 推荐回答  
> 遠距離武器で攻撃する際は、武器の発射ポイントから弾オブジェクトを生成し、キャラクターの向きに合わせて発射方向を決めるようにしました。

---

> [!question] 题目  
> 子弹命中敌人后会触发伤害处理，并播放命中特效；如果碰到墙壁，就直接回收到对象池。

> [!example] 我的回答  
> 弾が敵に命中した場合はダメージ計算を行い、ヒットエフェクトを再生する。壁にヒットした場合は、直接オブジェクトプールに返却する

> [!warning] 指正  
> 这句整体很好，基本自然。  
> 「ダメージ計算」可以，但“伤害处理”更广一点，推荐「ダメージ処理」。  
> 「壁にヒットした場合」能懂，不过更自然的是「壁に当たった場合」。  
> 「直接」可以，但这里用「そのまま」也很自然。

> [!tips] 推荐回答  
> 弾が敵に命中した場合はダメージ処理を行い、ヒットエフェクトを再生します。壁に当たった場合は、そのままオブジェクトプールに返却するようにしました。

---

> [!question] 题目  
> 为了避免频繁生成和销毁 Projectile，我给子弹设置了生存时间，超时后自动归还到对象池。

> [!example] 我的回答  
> 飛び道具を頻繁の生成と破壊を避けるため、飛び道具に有効時間を設定し、時間切れになったら自動的にオブジェクトプールに返却する

> [!warning] 指正  
> 「頻繁の生成」错误，应为「頻繁な生成」。  
> 「破壊」能懂，但对象生命周期里更常用「破棄」。  
> 「有効時間」可以，但 Projectile 的生存时间更常说「生存時間」或「ライフタイム」。  
> 「飛び道具」可以，但如果面试里讲程序实现，用「Projectile」或「弾」也很自然。

> [!tips] 推荐回答  
> Projectileの頻繁な生成と破棄を避けるため、弾にライフタイムを設定し、時間切れになったら自動的にオブジェクトプールへ返却するようにしました。

---

> [!question] 题目  
> UI 按钮不会直接修改角色状态，而是通过 Gameplay 层公开的接口请求状态切换。

> [!example] 我的回答  
> UIボタンからキャラクターの状態を直接変更せず、Gameplay層で公開しているインターフェース経由で状態切り替えをリクエストする

> [!warning] 指正  
> 这句整体很好。  
> 「Gameplay層で公開している」可以，但更自然的是「Gameplay層が公開している」。  
> 面试口语里，如果是说明自己做过的实现，结尾可以用「〜するようにしました」。

> [!tips] 推荐回答  
> UIボタンからキャラクターの状態を直接変更せず、Gameplay層が公開しているインターフェース経由で状態切り替えをリクエストするようにしました。

---

> [!question] 题目  
> 角色 HP 变化时，Gameplay 逻辑只负责发送通知，UI 层接收到通知后再刷新血条。

> [!example] 我的回答  
> キャラクタのHPが変化した際に、Gameplayロジックがイベントを通知するだけ、UI層がイベントを受け取ってからHPバーの表示を更新する

> [!warning] 指正  
> 「キャラクタ」应为「キャラクター」。  
> 「通知するだけ、」这个连接不自然。推荐「通知するだけにして」或「通知だけを行い」。  
> 「受け取ってから」可以，但这里更自然的是「受け取った後に」。  
> 「HPバーの表示を更新する」可以，简单说「HPバーを更新する」也自然。

> [!tips] 推荐回答  
> キャラクターのHPが変化した際は、Gameplayロジック側ではイベント通知だけを行い、UI層がその通知を受け取った後にHPバーを更新するようにしました。

---

> [!question] 题目  
> 近战武器和远程武器的攻击方式不同，所以我把攻击逻辑拆成不同的 Strategy 类。

> [!example] 我的回答  
> 近接武器と遠距離武器の方式が異なるので、攻撃ロジックを異なるストラテジークラスに切り出す

> [!warning] 指正  
> 「方式」最好具体说成「攻撃方式」或「攻撃方法」。  
> 「異なるストラテジークラス」可以，但更自然的是「別々のStrategyクラス」或「武器種別ごとのStrategyクラス」。  
> 面试中说明自己做过的实现时，结尾推荐用「切り出しました」。

> [!tips] 推荐回答  
> 近接武器と遠距離武器では攻撃方式が異なるので、攻撃ロジックを武器種別ごとのStrategyクラスに切り出しました。

---

> [!question] 题目  
> 为了支持编辑器里的 Undo 功能，我把每一次地图对象的修改操作封装成 Command。

> [!example] 我的回答  
> エディタが取り消しに対応できるように、毎回マップオブジェクトへの操作をコマンドとしてカプセイル化した

> [!warning] 指正  
> 「取り消し」可以，但这里更推荐「Undo機能」。  
> 「毎回マップオブジェクトへの操作」不自然。应说「マップオブジェクトへの各変更操作」。  
> 「カプセイル化」拼写错误，应为「カプセル化」。  
> 设计模式里“封装成 Command”可以说「Commandとしてカプセル化する」。

> [!tips] 推荐回答  
> エディタのUndo機能に対応できるように、マップオブジェクトへの各変更操作をCommandとしてカプセル化しました。

---

> [!question] 题目  
> 角色详情界面关闭时，我会释放该界面中动态加载的头像和立绘资源，避免引用残留导致内存无法回收。

> [!example] 我的回答  
> キャラクター詳細画面を閉じた際に、画面内で動的にロードした顔アイコンと立ち絵リソースを解放する。参照が残るによるインメモリが正しく回収されないことを避ける

> [!warning] 指正  
> 第一句基本自然。面试口语里也可以说「閉じるタイミングで」。  
> 「参照が残るによる」错误。应说「参照が残ると」或「参照が残ることで」。  
> 「インメモリ」不自然，这里直接说「メモリ」即可。  
> 面试口语里可以用「〜ので、それを避けるためです」来解释原因。

> [!tips] 推荐回答  
> キャラクター詳細画面を閉じるタイミングで、画面内で動的にロードした顔アイコンと立ち絵リソースを解放するようにしました。参照が残るとメモリが回収されなくなるので、それを避けるためです。

---

> [!question] 题目  
> 角色进入受击状态后，会中断移动和攻击状态，直到受击动画结束前都不会重新进入这些状态。

> [!example] 我的回答  
> キャラクターが被弾状態に入った後、移動状態と攻撃状態を中断し、被弾アニメーションが終了するまでこれらの状態に復帰しない

> [!warning] 指正  
> 这句很好。  
> 状态机语境中，「入った後」可以换成「遷移した後」。  
> 「これらの状態に復帰しない」可以。更状态机一点可以说「これらの状態へ再遷移しない」。  
> 面试中说明实现时，结尾推荐「〜ようにしました」。

> [!tips] 推荐回答  
> キャラクターが被弾状態に遷移した後は、移動状態と攻撃状態を中断し、被弾アニメーションが終了するまでこれらの状態へ再遷移しないようにしました。

---

> [!question] 题目  
> Profiler 显示，刷新背包列表时会生成大量临时字符串，所以我减少了字符串拼接，并复用了 UI Item。

> [!example] 我的回答  
> プロファイラーっ場で、インベントリを更新するたびに大量の一時文字列が生成されているため、文字列連結を減らしてUI Itemを再利用する

> [!warning] 指正  
> 「プロファイラーっ場」错误。应说「Profiler上では」或「Profilerを見ると」。  
> 「インベントリを更新」可以，但“背包列表”更清楚的是「インベントリリストを更新する」。  
> 「〜ため」没错，但面试口语里可以用「〜ので」更自然。  
> 面试中说明自己优化过的内容时，推荐用过去形「減らしました」「再利用するようにしました」。

> [!tips] 推荐回答  
> Profilerを見ると、インベントリリストを更新するたびに大量の一時文字列が生成されていたので、文字列連結を減らしてUI Itemを再利用するようにしました。


## 06-27

## 1. Root Motion / 位移修正

> [!question] 题目  
> 如果攻击动画带有 Root Motion，请不要在同一帧里再次手动修改角色位置，否则可能导致角色位移被重复应用。

> [!example] 我的回答  
> 攻撃アニメーションに Root Motion付く場合には、同一フレーム内で手動でキャラクター位置を修正するとキャラクターの位置が重複して適用されてしまいます。

> [!warning] 指正  
> 「Root Motion付く」应改成「Root Motionが付いている」。  
> 「位置が重複して適用される」意思能懂，但技术上更准确的是「移動量が二重に適用される」。

> [!tips] 推荐回答  
> 攻撃アニメーションにRoot Motionが付いている場合、同一フレーム内で手動でキャラクター位置を変更すると、移動量が二重に適用されてしまう可能性があります。

---

## 2. Ground Snap / 贴地处理

> [!question] 题目  
> 当角色从台阶边缘走下来时，请在一定距离内向下检测地面，并将角色位置吸附到可站立表面上。

> [!example] 我的回答  
> キャラクターが階段の境界から降りる際に、一定範囲内で下向き検出し、キャラクターを立ち入れる表面にスナップしてください。

> [!warning] 指正  
> 「階段の境界」不太自然，推荐「段差の端」。  
> 「下向き検出」应改成「下方向に地面を検出」。  
> 「立ち入れる表面」不是“可站立表面”，推荐「歩行可能な面」或「立てる面」。

> [!tips] 推荐回答  
> キャラクターが段差の端から降りる際は、一定距離内で下方向に地面を検出し、キャラクター位置を歩行可能な面にスナップしてください。

---

## 3. Hit Stop / 命中暂停

> [!question] 题目  
> 为了增强攻击命中的手感，我在命中瞬间短暂暂停角色和敌人的动画，但不暂停 UI 动画和输入缓冲计时。

> [!example] 我的回答  
> 命中した手応えを向上させるため、命中した瞬間にキャラクターと敵のアニメーションを一時的に停止するが、UIアニメーションと入力バッファが動き続けるようにしました。

> [!warning] 指正  
> 「命中した手応え」可以理解，但更自然的是「攻撃命中時の手応え」。  
> 「入力バッファが動き続ける」不太准确。输入缓冲不是“动”，而是计时继续，所以推荐「入力バッファのタイマーは止めない」。

> [!tips] 推荐回答  
> 攻撃命中時の手応えを高めるため、命中した瞬間にキャラクターと敵のアニメーションを一時的に停止します。  
> ただし、UIアニメーションと入力バッファのタイマーは止めずに動かし続けるようにしています。

---

## 4. Lock-on / 目标切换

> [!question] 题目  
> 锁定目标时，如果当前目标死亡或离开视野范围，请自动切换到距离玩家最近的有效敌人。

> [!example] 我的回答  
> ターゲットをロックした際に、ターゲットが死亡及び視野内から離れた場合、自動的に直近の敵に移してください

> [!warning] 指正  
> 「ターゲットをロックした際に」容易理解成“锁定那一瞬间”，这里更推荐「ロックオン中に」。  
> 「死亡及び」偏书面，口语里用「死亡した場合、または〜」更自然。  
> 「直近の敵」可以理解，但游戏语境里「最も近い有効な敵」更明确。  
> 「移す」建议改成「切り替える」。

> [!tips] 推荐回答  
> ロックオン中に現在のターゲットが死亡した場合、または視野範囲外に出た場合は、自動的にプレイヤーに最も近い有効な敵へターゲットを切り替えてください。

---

## 5. Save Data / 版本兼容

> [!question] 题目  
> 读取旧版本存档时，如果缺少新字段，请使用默认值补全，避免因为反序列化失败导致游戏无法启动。

> [!example] 我的回答  
> 古いセーブデータを読み込む際に、新しく追加されたフィールドが存在しない場、ディフォルト値で補完してください。逆シリアル化の失敗によるゲームが起動できないことを避ける

> [!warning] 指正  
> 「存在しない場」应为「存在しない場合」。  
> 「ディフォルト」应为「デフォルト」。  
> 「逆シリアル化」不自然，反序列化推荐「デシリアライズ」。  
> 「失敗によるゲームが起動できない」接法不自然，应改成「失敗してゲームが起動できなくなる」。

> [!tips] 推荐回答  
> 古いセーブデータを読み込む際に、新しく追加されたフィールドが存在しない場合は、デフォルト値で補完してください。  
> デシリアライズに失敗してゲームが起動できなくなることを避けるためです。

---

## 6. Shader Variant / 预热

> [!question] 题目  
> 为了避免技能第一次播放时卡顿，请在战斗开始前预热该角色可能使用的 Shader Variant。

> [!example] 我的回答  
> スキルの初回再生のカクつきを避けるため、ボトル開始時に該当キャラクターが使用可能のシェーダーバリアントをWarmingUpしてください

> [!warning] 指正  
> 「ボトル」应为「バトル」。  
> 「使用可能の」不自然，推荐「使用する可能性がある」。  
> 「WarmingUpしてください」应改成「ウォームアップしてください」。

> [!tips] 推荐回答  
> スキルの初回再生時のカクつきを避けるため、バトル開始前に、そのキャラクターが使用する可能性があるシェーダーバリアントをウォームアップしてください。

---

## 7. Cutscene / 跳过演出

> [!question] 题目  
> 当玩家跳过剧情演出时，请立即停止 Timeline，并把角色、相机和 UI 状态恢复到演出结束后的状态。

> [!example] 我的回答  
> プレイやがカットシーンをスキップする際に、Timlineを直ちに停止し、キャラクター、カメラとUI状態を演出後に復帰してください

> [!warning] 指正  
> 「プレイや」应为「プレイヤー」。  
> 「Timline」应为「Timeline」。  
> 「カメラとUI状態」应为「カメラとUIの状態」。  
> 「演出後に復帰」不够明确，推荐「演出終了後の状態に戻す」。

> [!tips] 推荐回答  
> プレイヤーがカットシーンをスキップした場合は、Timelineを直ちに停止し、キャラクター、カメラ、UIの状態を演出終了後の状態に戻してください。

---

## 8. Input Buffer / 硬直结束

> [!question] 题目  
> 如果玩家在攻击后硬直结束前输入回避，请缓存这个输入，并在角色恢复可操作状态后立即执行。

> [!example] 我的回答  
> キャラクターが攻撃後の硬直状態中に回避を入力した場合、その入力をバッファリングし、キャラクターが操作可能に復帰してからすぐに実行してください

> [!warning] 指正  
> 这句意思明确，基本没问题。  
> 「攻撃後の硬直状態中」可以简化为「攻撃後の硬直中」。  
> 「操作可能に復帰してから」可以改成「操作可能状態に戻った後」更自然。

> [!tips] 推荐回答  
> 攻撃後の硬直中にプレイヤーが回避を入力した場合は、その入力をバッファリングし、キャラクターが操作可能状態に戻った後、すぐに実行してください。

---

## 9. NavMesh / 动态障碍物

> [!question] 题目  
> 当大型敌人倒下并阻挡通路时，请更新 NavMesh 障碍物状态，避免其他敌人继续尝试穿过该区域。

> [!example] 我的回答  
> 大方敵が倒れたによる通路がブロックされた場合は、NavMeshの障害状態を更新し、敵がそのエリアを引き続き走り抜けてみることを避ける

> [!warning] 指正  
> 「大方敵」应为「大型の敵」。  
> 「倒れたによる」接法错误，推荐「倒れて」。  
> 「障害状態」不够准确，推荐「障害物の状態」。  
> 「走り抜けてみる」不自然，推荐「通過しようとする」。

> [!tips] 推荐回答  
> 大型の敵が倒れて通路を塞いだ場合は、NavMesh上の障害物の状態を更新し、他の敵がそのエリアを通過しようとし続けないようにしてください。

---

## 10. Patch / 热更新资源

> [!question] 题目  
> 下载热更新资源前，请先校验版本号和文件哈希，避免加载到不匹配或损坏的资源包。

> [!example] 我的回答  
> ホットフィックスリソースをダウンロードする前に、先に該当バージョン番号とファイルのハッシュ値を検出し、破損しているやバージョンズレアセットバンドルをロードしないようにします。

> [!warning] 指正  
> 「ホットフィックスリソース」可以理解，但游戏资源热更新里也可以说「パッチリソース」或「更新リソース」。  
> 「検出」这里不准确，版本和哈希应该是「検証」。  
> 「破損しているや」应为「破損している、または〜」。  
> 「バージョンズレアセットバンドル」不成立，应改成「バージョンが一致しないAssetBundle」。

> [!tips] 推荐回答  
> パッチリソースをダウンロードする前に、まずバージョン番号とファイルのハッシュ値を検証してください。  
> これにより、バージョンが一致しない、または破損しているAssetBundleをロードしないようにします。

---
