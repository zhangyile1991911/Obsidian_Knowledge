
## 06-29
## 1. Weapon / リロード中の武器切り替え

> [!question] 题目  
> 如果玩家在换弹过程中切换武器，请取消当前换弹动画，并保存已经消耗的弹药状态。

> [!example] 我的回答  
> リロード中にプレイヤーが武器を切り替えた場合、リロードアニメーションを中断し、残弾数を保存してください

> [!warning] 指正  
> 这句整体意思明确。  
> 但原文是“保存已经消耗的弹药状态”，不只是保存「残弾数」。如果想更准确，可以说「消費済みの弾薬状態」或「リロード中に消費された弾薬状態」。  
> 另外，「現在の」可以补上，让对象更明确。

> [!tips] 推荐回答  
> リロード中にプレイヤーが武器を切り替えた場合は、現在のリロードアニメーションを中断し、すでに消費された<mark style="background: #FF5582A6;">弾薬状態</mark>を保存してください。

---

## 2. Line Trace / 命中优先级

> [!question] 题目  
> 射击时如果 Line Trace 同时命中角色和场景物体，请优先处理距离相机最近的命中结果。

> [!example] 我的回答  
> 射撃時にLineTraceがキャラクターとシーンオブジェクトを同時に命中した場合、カメラから距離が最も近い結果を優先的に処理する

> [!warning] 指正  
> 「LineTrace」建议写成「Line Trace」。  
> 「同時に命中した」可以理解，但 Line Trace 语境里更自然是「複数のヒット結果がある」。  
> 「距離が最も近い結果」最好改成「カメラに最も近いヒット結果」。

> [!tips] 推荐回答  
> 射撃時にLine Traceがキャラクターとシーンオブジェクト<mark style="background: #FF5582A6;">の両方</mark>にヒットした場合は、<mark style="background: #FF5582A6;">カメラに最も近い</mark>ヒット結果を優先的に処理してください。

---

## 3. Recoil / 后坐力恢复

> [!question] 题目  
> 射击后请逐渐恢复相机角度，不要在一帧内直接把视角拉回原来的位置。

> [!example] 我的回答  
> 射撃後にカメラの角度を徐々に復元し、一つフレーム内で視野を元の位置に戻さないようにしてください

> [!warning] 指正  
> 「一つフレーム」应改成「1フレーム」。  
> 「視野」是 field of view / 视野范围，不是玩家视角。这里应使用「視点」或「カメラ角度」。  
> 「復元する」可以理解，但相机角度更自然是「戻す」。  
> 「直接拉回」可以表达为「一気に戻す」。

> [!tips] 推荐回答  
> 射撃後はカメラ角度を徐々に元に戻し、1フレーム内で視点を<mark style="background: #FF5582A6;">一気に</mark>元の位置へ戻さないようにしてください。

---

## 4. Animation Montage / Anim Notify

> [!question] 题目  
> 伤害判定应该在 Anim Notify 触发时开启，并在攻击结束通知触发时关闭。

> [!example] 我的回答  
> ダメージ判定は　Anim Notifyが有効時期のうちにオンにし、攻撃の終了時にオフイベントを通知する

> [!warning] 指正  
> 这里需要大改。  
> 「Anim Notifyが有効時期のうちに」不自然。应说「Anim Notifyが発火したタイミングで」。  
> 「オフイベントを通知する」意思偏了。原文是关闭伤害判定，不是通知关闭事件。  
> 推荐使用「有効化する / 無効化する」。

> [!tips] 推荐回答  
> ダメージ判定は、Anim Notifyが発火したタイミングで有効化し、攻撃終了Notifyが発火したタイミングで無効化するようにします。

---

## 5. Aim Offset / 瞄准方向

> [!question] 题目  
> 角色瞄准时，请根据相机方向更新 Aim Offset，避免上半身朝向和准星方向不一致。

> [!example] 我的回答  
> キャラクターが照準する時に、カメラ方向に合わせてAimOffsetを更新し、上半身向きと照準向きを一致する

> [!warning] 指正  
> 「照準する」可以理解，但更常用「エイムする」或「照準時」。  
> 「AimOffset」建议写成「Aim Offset」。  
> 「上半身向き」应为「上半身の向き」。  
> 「一致する」这里要用使役或否定目标：「一致させる」或「不一致にならないようにする」。

> [!tips] 推荐回答  
> キャラクターがエイムする時は、カメラ方向に合わせてAim Offsetを更新し、上半身の向きと照準方向がずれないようにしてください。

---

## 6. Cover AI / 掩体选择

> [!question] 题目  
> 敌人寻找掩体时，请排除已经被其他敌人占用的掩体点，避免多个敌人挤在同一个位置。

> [!example] 我的回答  
> 敵が遮蔽物を探す時、まず他の敵に使われている遮蔽物を排除し、複数の敵が同一位置に隠れないようにする

> [!warning] 指正  
> 这句意思能懂。  
> 不过原文是“掩体点”，不是遮蔽物本身，推荐「カバー位置」或「遮蔽物ポイント」。  
> 「使われている」可以，但 AI 语境里更自然是「占有されている」。  
> 「挤在同一个位置」可以说「同じ位置に集中する」。

> [!tips] 推荐回答  
> 敵が<mark style="background: #FF5582A6;">カバー位置</mark>を探す時は、すでに他の敵に<mark style="background: #FF5582A6;">占有され</mark>ているカバー位置を除外し、複数の敵が<mark style="background: #FF5582A6;">同じ位置に集中しない</mark>ようにします。

---

## 7. Camera Collision / 墙体穿透

> [!question] 题目  
> 如果相机检测距离太短，角色靠近墙壁时相机可能会穿进墙体。

> [!example] 我的回答  
> カメラの判定距離が短すぎると　キャラクターが壁に近つく時にカメラは壁にめり込んでしまいます

> [!warning] 指正  
> 「近つく」应为「近づく」。  
> 「カメラは」可以，但这里用「カメラが」更自然。  
> 「判定距離」能懂，不过相机碰撞语境里也可以说「検出距離」。

> [!tips] 推荐回答  
> カメラの<mark style="background: #FF5582A6;">検出距離</mark>が短すぎると、キャラクターが壁に近づいた時に、カメラが壁にめり込んでしまう可能性があります。

---

## 8. Network / 武器切换同步

> [!question] 题目  
> 客户端切换武器后，请等待服务器确认，再播放正式的换武器完成表现。

> [!example] 我的回答  
> クライアント側では武器を切り替えた後、まずサーバーの認定を待ち、その後に武器を切り替えるアニメーションを再生する

> [!warning] 指正  
> 「サーバーの認定」不自然。这里应说「サーバーの承認」或「サーバーからの確認」。  
> 原文是“换武器完成表现”，不一定只是动画，可能包括 UI、音效、特效等，所以推荐「完了演出」或「完了表現」。  
> 「武器を切り替えるアニメーション」容易被理解成“开始换武器动画”，而不是“完成表现”。

> [!tips] 推荐回答  
> クライアント側で武器を切り替えた後は、サーバーからの<mark style="background: #FF5582A6;">確認</mark>を待ってから、正式な武器切り替え完了演出を再生してください。

---

## 9. UI / 弹药显示

> [!question] 题目  
> 弹药数量变化时，请只刷新弹药显示 UI，不要重新创建整个武器 HUD。

> [!example] 我的回答  
> 残弾数の変化時に、残弾数のUIを更新するだけで武器のHUD全体を生成するのではありません

> [!warning] 指正  
> 「更新するだけで」后面接「〜するのではありません」不自然。应使用「〜だけにして、〜しないようにする」。  
> 「生成する」这里最好加上「再」，即「再生成する」。  
> 「残弾数のUI」可以，但更自然是「残弾数表示UI」。

> [!tips] 推荐回答  
> 残弾数が変化した時は、残弾数表示UIだけを更新し、武器HUD全体を再生成しないようにしてください。

---

## 10. Debug / 命中问题调查

> [!question] 题目  
> 调试命中问题时，请记录射击起点、Line Trace 方向、命中 Actor 和命中距离。

> [!example] 我的回答  
> ヒット不具合をデバッグする際に、射撃ポイント、Line Trace方向、ヒットしたActorとヒット距離をそれぞれに記録してください

> [!warning] 指正  
> 「射撃ポイント」可以理解，但“射击起点”更准确是「射撃開始位置」或「射撃起点」。  
> 「それぞれに記録」不自然，直接「記録してください」即可。  
> 「Line Trace方向」中间加「の」更自然。

> [!tips] 推荐回答  
> ヒット不具合をデバッグする際は、<mark style="background: #FF5582A6;">射撃開始位置</mark>、Line Traceの方向、ヒットしたActor、ヒット距離を記録してください。

---

## 06-30

# UE5 第三人称射击：翻訳練習まとめ

## 1. Profiler / Shader Variant

> [!question] 题目  
> Profiler 结果显示，战斗开始时的卡顿主要来自 Shader Variant 的首次编译，所以需要在进入战斗前预热。

> [!example] 我的回答  
> Profilerの結果から、戦闘開始時のカクつきはシェーダーバリアントの初回コンパイルが主な原因なので戦闘に入る前に事前にWarmUpする必要があります

> [!warning] 指正  
> 意思明确。  
> 「戦闘に入る前に」和「事前に」语义重复，保留一个即可。  
> 「WarmUpする」可以理解，但日语开发现场更常写「ウォームアップする」。

> [!tips] 推荐回答  
> Profilerの結果から、戦闘開始時のカクつきはシェーダーバリアントの初回コンパイルが主な原因だと分かったため、戦闘に入る前にウォームアップする必要があります。

---

## 2. Weapon / 切り替え前の武器情報

> [!question] 题目  
> 玩家开枪后立刻切换武器时，请保留切换前的武器信息，避免子弹命中时无法读取伤害数据。

> [!example] 我的回答  
> プレイヤーが射撃した後にすぐ武器を切り替える時に、元の武器の情報を保持し、ヒットした際にダメージデータを読み込まならないことを避ける

> [!warning] 指正  
> 「読み込まならない」是错误，应改成「読み込めない」。  
> 不过这里比起「読み込む」，更自然的是「参照する」。  
> 「すぐ武器を切り替える時に」建议改成「射撃した直後に武器を切り替えた場合は」。  
> 「元の武器」可以理解，但「切り替え前の武器情報」更明确。

> [!tips] 推荐回答  
> プレイヤーが射撃した直後に武器を切り替えた場合は、切り替え前の武器情報を保持し、弾がヒットした際にダメージデータを参照できなくなることを避けます。

---

## 3. Reload / リロードキャンセル

> [!question] 题目  
> 如果玩家在换弹过程中翻滚，请取消当前 Reload Montage，并根据已经完成的装填进度更新弹药数量。

> [!example] 我的回答  
> プレイヤーがリロード中にローリングする場合は、再生中のリーロドMontageをキャンセイルし、すでにリロード済み進捗に応じて弾薬数を更新してください

> [!warning] 指正  
> 「リーロド」应改成「リロード」。  
> 「キャンセイル」应改成「キャンセル」。  
> 「ローリングする」可以理解，但 UE 动作里更自然的是「回避ロールを行う」。  
> 「リロード済み進捗」不自然，推荐「完了済みのリロード進捗」。

> [!tips] 推荐回答  
> プレイヤーがリロード中に回避ロールを行った場合は、再生中のReload Montageをキャンセルし、完了済みのリロード進捗に応じて弾薬数を更新してください。

---

## 4. Line Trace / ヒット情報記録

> [!question] 题目  
> 调试射击命中问题时，请记录 Line Trace 的起点、方向、命中 Actor、命中位置和命中距离。

> [!example] 我的回答  
> ヒット不具合をデバッグする時に、Line　Traceの開始位置、方向、ヒットしたActor、命中位置と命中距離を記録してください

> [!warning] 指正  
> 整体很好。  
> 「デバッグする時に」建议改成「デバッグする際は」。  
> 「命中位置」可以理解，但 UE / 游戏开发现场更常说「ヒット位置」。  
> 并列列举时，用「、」连接更自然。

> [!tips] 推荐回答  
> ヒット不具合をデバッグする際は、Line Traceの開始位置、方向、ヒットしたActor、ヒット位置、ヒット距離を記録してください。

---

## 5. Aim Offset / 上半身の向き

> [!question] 题目  
> 角色在 ADS 状态下移动时，请持续更新 Aim Offset，避免上半身朝向和准星方向产生偏差。

> [!example] 我的回答  
> プレイヤーがADS状態で移動する場合、上半身と照準方向がズレないように、AimOffsetの更新を続けてください

> [!warning] 指正  
> 主语是角色，建议用「キャラクター」。  
> 「上半身と照準方向」不够准确，应为「上半身の向きと照準方向」。  
> 「AimOffset」建议写成「Aim Offset」。  
> 「更新を続ける」可以理解，更技术自然的是「継続的に更新する」。

> [!tips] 推荐回答  
> キャラクターがADS状態で移動する場合は、上半身の向きと照準方向がずれないように、Aim Offsetを<mark style="background: #FF5582A6;">継続的に</mark>更新してください。

---

## 6. Camera / Recoil回復

> [!question] 题目  
> 射击后相机角度应该逐渐恢复，不要在 1 帧内把视点强行拉回原来的方向。

> [!example] 我的回答  
> 射撃後にカメラの角度を徐々に戻すべき、１フレーム内で視点を元の方向に引っ張って戻さないようにする

> [!warning] 指正  
> 「戻すべき、」不能这样接，应拆成句子或改成连用形式。  
> 「引っ張って戻す」口语感较强，技术说明里推荐「一気に戻す」。  
> 「１フレーム」建议写成半角「1フレーム」。

> [!tips] 推荐回答  
> 射撃後はカメラ角度を徐々に元に戻し、1フレーム内で視点を<mark style="background: #FF5582A6;">一気に</mark>元の方向へ戻さないようにしてください。

---

## 7. Network / 武器切り替え確認

> [!question] 题目  
> 客户端可以先播放临时的切换表现，但正式的武器切换完成状态必须等服务器确认后再应用。

> [!example] 我的回答  
> クライアントが先に一時的な切り替え演出を再生して、サーバーの返信が戻ってきてから武器を正式に適用する

> [!warning] 指正  
> 「サーバーの返信が戻ってきてから」可以理解，但更自然的是「サーバーから確認が返ってきた後」。  
> 原文是“正式的武器切换完成状态”，所以只说「武器を正式に適用する」略微不够完整。  
> 推荐明确说「正式な武器切り替え完了状態」。

> [!tips] 推荐回答  
> クライアント側では先に一時的な切り替え演出を再生できますが、正式な武器切り替え完了状態は、サーバーから確認が返ってきた後に適用してください。

---

## 8. UI / Weapon HUD

> [!question] 题目  
> 残弹数变化时，请只刷新弹药显示 UI，不要重新生成整个 Weapon HUD，避免不必要的 GC Allocation。

> [!example] 我的回答  
> 残弾数が変化した場合、弾薬数UIだけを更新し、余計なGC アロケーションを避けるため、Weapon HUD全体を再生成しないようにしてください

> [!warning] 指正  
> 这句很好，意思明确。  
> 「弾薬数UI」可以理解，但和前面的「残弾数」统一的话，推荐「残弾数表示UI」。  
> 「GC アロケーション」中间不需要空格，写成「GCアロケーション」。

> [!tips] 推荐回答  
> 残弾数が変化した場合は、残弾数表示UIだけを更新し、余計なGCアロケーションを避けるため、Weapon HUD全体を再生成しないようにしてください。

---

## 9. AI / Cover Point占有

> [!question] 题目  
> 敌人选择掩体时，请排除已经被其他 AI 占用的 Cover Point，避免多个敌人移动到同一个位置。

> [!example] 我的回答  
> 敵がカバーを選択する際に、他のAIが占有されているカバーポイントを排除し、複数の敵が同一カバーに集中しないようにしてください。

> [!warning] 指正  
> 「他のAIが占有されている」意思会变成“其他 AI 被占用”。  
> 正确应为「他のAIに占有されている」。  
> 「カバー」可以理解，但题目是 Cover Point，建议统一写「Cover Point」。  
> 「排除」可以，筛选条件里更自然也可以用「除外」。

> [!tips] 推荐回答  
> 敵がCover Pointを選択する際は、すでに他のAIに占有されているCover Pointを除外し、複数の敵が同じ位置に集中しないようにしてください。

---

## 10. Design Explanation / 結論ファースト

> [!question] 题目  
> 结论优先地说，伤害计算和命中特效应该分成两个步骤处理，这样更容易区分是逻辑问题还是表现问题。

> [!example] 我的回答  
> 結論ファーストから言うと、ダメージ計算とヒットエフェクトを別々の処理ステップに切り分けるべきです。そうすると、ロジック問題なのか、表現の問題なのかを切り分けやすくなります。

> [!warning] 指正  
> 「結論ファーストから言うと」不自然。应改成「結論ファーストで言うと」或「まず結論から言うと」。  
> 「ロジック問題」可以理解，但更自然是「ロジック側の問題」。  
> 游戏开发里「表现问题」更常说「演出側の問題」或「表示側の問題」。这里用「演出側」更合适。  
> 「そうすると」可以，但说明设计理由时「そうすることで」更自然。

> [!tips] 推荐回答  
> 結論ファーストで言うと、ダメージ計算とヒットエフェクトは別々の処理ステップに切り分けるべきです。  
> そうすることで、ロジック側の問題なのか、演出側の問題なのかを切り分けやすくなります。

---

# 07-02

 ##  1.代码结构 / 生命周期
 
> [!question] 题目 1
> 在 Unity 中，如果在 `OnDestroy` 中访问已被销毁的对象，可能会导致空引用异常。

> [!example] 自己的答案
> Unity上では　OnDestroy関数内ですでに破棄されたオブジェクトを参照するとヌルでクラッシュする可能性があります

> [!warning] 指正
> 意思基本传达，但“空引用异常”在面试或正式技术沟通中应明确说成 **NullReferenceException** 或 **ヌル参照例外**，“ヌルでクラッシュ”偏口语且容易和真 Crash 混淆。另外“Unity上では”稍硬，说“Unityでは”即可。

> [!tip] 参考回答
> Unityでは、OnDestroy内ですでに破棄されたオブジェクトを参照すると、NullReferenceExceptionが発生する可能性があります。

---
## 2. 设计模式 / 单例

> [!question] 题目 2
> 这个 Manager 类使用了线程安全的懒汉式单例模式，确保在任何场景切换后依然只有一个实例。

> [!example] 自己的答案
> このManagerクラスではスレッドセー遅延フシングルトンパターンを使用しているのでどのシーンに切り替えた後にもインスタンスが唯一ことを確保できる

> [!warning] 指正
> 术语有拼写错误：**スレッドセー** → **スレッドセーフ**、**フシングルトン** → **シングルトン**。语法上“インスタンスが唯一ことを”应为“インスタンスが唯一であることを”。整体语法修正后，面试时就能清晰地表达设计模式。

> [!tip] 参考回答
> このManagerクラスは<mark style="background: #FF5582A6;">スレッドセーフな遅延シングルトン</mark>パターンを採用しており、シーンが切り替わっても常にインスタンスが一つだけであることを保証します。

---
## 3. 渲染 / 性能

> [!question] 题目 3
> 在移动端开启 HDR 会导致较高的带宽开销，因此我们在低端机型上使用了 LDR 渲染路径。

> [!example] 自己的答案
> モビール端末でHDRをオンにするとコストが上がるのでロー端末でLDPレンダリングパスを使用する

> [!warning] 指正
> 有重要拼写错误：**LDP** 应为 **LDR**。**モビール端末** 通常说 **モバイル端末**，**ロー端末** 最好说 **ローエンド端末** 或 **低スペック端末**。另外“コストが上がる”在语境中能懂，但面试时更建议说 **帯域幅の負荷が高くなる** 以体现专业性。

> [!tip] 参考回答
> モバイル端末でHDRを有効にすると<mark style="background: #FF5582A6;">帯域幅</mark>の負荷が高くなるため、低スペック端末ではLDRレンダリングパスを使用しています。

---
## 4. 代码结构 / 解耦
> [!question] 题目 4
> 通过事件总线解耦技能系统和 UI 层，可以避免硬引用造成的循环依赖。

> [!example] 自己的答案
> イベントバスを通じてスキルシステムとUI層の結合度を下げることでハード参照による循環依存を避けられる

> [!warning] 指正
> 翻译非常自然，意思完全准确。若想让表达更贴近技术文档，可以把“結合度を下げる”替换为 **疎結合にする**，但口语面试直接用原句也完全没问题。

> [!tip] 参考回答
> イベントバスによってスキルシステムとUI層を疎結合にすることで、ハード参照による循環依存を回避できます。

---
## 5. 设计模式 / 对象池
> [!question] 题目 5
> 弹丸频繁生成和销毁会产生大量 GC，因此我们使用对象池来复用实例，减少内存分配。

> [!example] 自己的答案
> 弾の生成と破棄によって大量のGCが発生するので　オブジェクトプールでインスタンスを再利用したり、メモリの割り当てを減らしたりする

> [!warning] 指正
> “～たり～たりする” 的并列结构在这里不太合适，会弱化“复用实例是为了减少分配”的因果关系。建议改为 **再利用することで～を減らす** 的目的表达。另外“弾”可用，但更精确的是 **弾丸** 或 **発射物**。

> [!tip] 参考回答
> 弾丸の頻繁な生成と破棄は大量のGCを引き起こすため、オブジェクトプールでインスタンスを再利用し、メモリ割り当てを削減しています。

---
## 6. Unreal / 网络同步
> [!question] 题目 6
> 该属性标记为 Replicated，在服务器上修改后会自动同步到所有客户端，无需手动 RPC 调用。

> [!example] 自己的答案
> Replicated付き属性ではサーバー側が変更した後に自動的にクライアントへ同期するので、手動でRPCを呼び出す必要がありません

> [!warning] 指正
> 技术含义很准确。“Replicated付き属性”略生硬，面试时可以说 **Replicated指定されたプロパティ**。其余部分很自然，口语中这样说完全没问题。

> [!tip] 参考回答
> Replicated<mark style="background: #FF5582A6;">指定されたプロパティ</mark>は、サーバー上で変更されると自動的に全クライアントに同期されるため、手動でRPCを呼び出す必要はありません。

---
## 7. 渲染 / Shader
> [!question] 题目 7
> 这个自定义 Shader 使用了多 Pass 渲染，先绘制不透明物体，再通过混合模式叠加透明特效。

> [!example] 自己的答案
> カスタマイズShaderでは複数のPassを使用している、先に不透明なオブジェクトを描画し、その後にブレンドモードで透明エフェクトをブレンドする

> [!warning] 指正
> “カスタマイズShader” 通常说 **カスタムShader**。句子用顿号断开偏口语，可以用 **使用しており、** 连接。整体技术描述清楚，面试中经过这一微调会更流畅。

> [!tip] 参考回答
> このカスタムShaderは複数のPassを使用しており、最初に不透明オブジェクトを描画し、その後ブレンドモードで透明エフェクトを重ねます。

---
## 8. 设计模式 / 命令模式
> [!question] 题目 8
> 我们将玩家的输入封装成命令对象，这样既能支持回放系统，也方便实现撤销操作。

> [!example] 自己的答案
> プレイヤーの入力をコマンドとしてカプセイル化ししました。こうするとリプレーシステムが実装可能だけではなく、取り消しも実現しやすくなります

> [!warning] 指正
> **カプセイル化** 是拼写错误，应为 **カプセル化**。**化ししました** 语法重复，应为 **化しました**。意思完全可懂，修正后就是很地道的面试表达。

> [!tip] 参考回答
> プレイヤーの入力をコマンドオブジェクトとしてカプセル化しました。これにより、リプレイ機能を実装できるだけでなく、Undo処理も容易になります。

---
## 9. 渲染 / 管线
> [!question] 题目 9
> 使用前向渲染时，每个物体受动态光源数量限制；切换到延迟渲染后，可以支持大量实时光源。

> [!example] 自己的答案
> フロントレンダリング使用時に、オブジェクトごと動的な光源数の受け取り数が制限され、遅延レンダリングに切り替えた後。大量のランタイム光源を受け取るようになります

> [!warning] 指正
> **フロントレンダリング** 是常见误译，标准说法是 **フォワードレンダリング**。此外“動的な光源数の受け取り数”稍啰嗦，直接说 **動的光源数に制限がある** 更清晰。修正术语后就是很好的回答。

> [!tip] 参考回答
> フォワードレンダリング使用時は、オブジェクトごとに動的<mark style="background: #FF5582A6;">光源数</mark>に制限がありますが、<mark style="background: #FF5582A6;">ディファードレンダリング</mark>に切り替えると、多数のリアルタイム光源を扱えるようになります。

---
## 10. 代码结构 / 组件化设计
> [!question] 题目 10
> 这个能力系统以组件方式组织，角色可以动态添加或移除 Ability 组件，而不会影响到核心移动逻辑。

> [!example] 自己的答案
> このアビリティーシステムはコンポーネントとして管理され、キャラクターがアビリティーコンポーネントを動的に追加・削除できる。そして核心の移動ロジックも影響されない。

> [!warning] 指正
> “コンポーネントとして管理され” 会让人感觉是“被当作组件来管理”，更贴近原意的说法是 **コンポーネント指向で構成され**。“核心の移動ロジック”不自然，说 **コアとなる移動ロジック** 更地道。修正后作为面试回答非常清晰。

> [!tip] 参考回答
> このAbilityシステムはコンポーネント指向で構成されており、キャラクターはAbilityコンポーネントを動的に追加・削除できます。その際も、コアとなる移動ロジックには影響を与えません。

---
# 07-03

> [!question] 题目 1
> 如果在协程中持有大型资源的引用，协程未结束时资源无法被垃圾回收，可能导致内存泄漏。

> [!example] 自己的答案
> コルーチンでは大容量リソース参照を保持する場合、コルーチンが終了するまでリソースが回収されない、メモリリークになる可能性があります

> [!warning] 指正
> 意思可懂，但“大容量リソース参照”稍生硬，“回収されない、メモリリークになる”语气断开。面试时更流畅的说法是“大きなリソースへの参照”和“回収されず、メモリリークが発生する”。

> [!tip] 参考回答
> コルーチン内で大きなリソースへの参照を保持している場合、コルーチンが終了するまでそのリソースはガベージコレクションされず、<mark style="background: #FF5582A6;">メモリリークが発生する</mark>可能性があります。

---

> [!question] 题目 2
> 我们使用条件复制来确保只有当角色的生命值发生变化时才同步给客户端，以减少带宽占用。

> [!example] 自己的答案
> 条件付きReplicatedでキャラクターのHPが変化した際のみにクライアントへ同期することを保証する。こうすることで、帯域幅負荷を下げる

> [!warning] 指正
> 关键词“条件付きReplicated”可优化为“条件付きレプリケーション”，“変化した際のみに”略硬，用“変化したときだけ”更自然。整体已经很清晰。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">条件付きレプリケーション</mark>を使用して、キャラクターのHPが変化したときだけクライアントに同期するようにしています。これによって帯域幅の負荷を減らしています。

---

> [!question] 题目 3
> 通过观察者模式，成就系统只需监听敌人的死亡事件，无需直接依赖战斗系统，从而降低耦合度。

> [!example] 自己的答案
> オブザーバーパターンを適用し、実績システムがバトルシステムに直接依存せず、敵の倒れたイベントを登録するだけにします。こうすることで結合度を下げる

> [!warning] 指正
> “敵の倒れたイベント”语序不当，应为“敵が倒されたイベント”。“登録するだけにします”改成“監視するだけで済む”更能体现观察者模式的优势。

> [!tip] 参考回答
> オブザーバーパターンを適用することで、実績システムはバトルシステムに直接依存せず、敵の死亡イベントを監視するだけで済みます。これにより結合度を下げられます。

---

> [!question] 题目 4
> 动态批处理无法合并使用不同材质的网格，因此我们需要尽量合并材质来减少 Draw Call。

> [!example] 自己的答案
> 動的バッチ処理では異なるマテリアルを使用しているメッシュを纏められないのでマテリアルをできるだけまとめることでドロー数を減らす

> [!warning] 指正
> 非常自然准确，无需修正。唯一可微调的是将“ドロー数”替换为更正式的“ドローコール”，但口语中完全没有问题。

> [!tip] 参考回答
> 動的バッチ処理では、異なるマテリアルを使用しているメッシュをまとめられないため、マテリアルをできるだけ<mark style="background: #FF5582A6;">統合し</mark>てドローコールを減らす必要があります。

---

> [!question] 题目 5
> 我创建了一个 Transform 的扩展方法，用于在不产生新向量的情况下重置本地位置和旋转，以减少 GC 分配。

> [!example] 自己的答案
> Transformの拡張方法を作成しました。ベクトルを生成せずにローカル位置と回転をリセットできるようにしています。こうすることで、GCアロケーションを減らします

> [!warning] 指正
> 术语小错误：“拡張方法”应为“拡張メソッド”。其余表达都很地道，GCアロケーション的说法也完全正确。

> [!tip] 参考回答
> Transformの拡張メソッドを作成し、新しいベクトルを生成せずにローカルの位置と回転をリセットできるようにしました。これでGCアロケーションを削減しています。

---

> [!question] 题目 6
> 在这个技能里，我们用 AbilityTask 异步等待输入释放来实现蓄力攻击，而不是每帧轮询。

> [!example] 自己的答案
> このスキルでは、AbilityTask非同期入力待ちを通じて力貯め攻撃を実装する。フレームごとにポーリングするのではありません

> [!warning] 指正
> “力貯め攻撃”应为“チャージ攻撃”。“AbilityTask非同期入力待ち”需拆解为“AbilityTaskを使って非同期で入力の解放を待つ”。意思能懂，调整后更专业。

> [!tip] 参考回答
> このスキルでは、AbilityTaskを使って非同期で入力の解放を待つことでチャージ攻撃を実装しており、毎フレームのポーリングは行っていません。

---

> [!question] 题目 7
> 碰撞检测中，将大量物体的碰撞体设为触发器比使用物理碰撞更省性能，因为不需要物理求解。

> [!example] 自己的答案
> コリジョン判定中に大量のオリジョンをトリガーに設定すると物理衝突の負荷が減る。物理演算が必要に無くなる

> [!warning] 指正
> “オリジョン”很可能是“オブジェクト”的拼写错误。“必要に無くなる”应为“不要になる”。修正这些并调整语序后会更清晰。

> [!tip] 参考回答
> 衝突判定において、大量のオブジェクトのコライダーをトリガーに設定すると、物理衝突を使用するよりもパフォーマンスが向上します。<mark style="background: #FF5582A6;">物理演算が不要になる</mark>からです。

---

> [!question] 题目 8
> 排行榜列表中的条目使用对象池复用，滑出视野时条目被回收并重新设置数据，而不是销毁重建。

> [!example] 自己的答案
> ランキングリスト内のアイテムをオブジェクトプールで再利用するように、表示領域外にスクロールした場合ではアイテムを破棄するのではなく、データをリセットしてオブジェクトプールに返却する。

> [!warning] 指正
> 结构稍微绕口，“再利用するように、…場合では”可简化。面试时直接说明“オブジェクトプールで管理し、画面外に出たら破棄せずにデータを再設定してプールに戻す”更清爽。

> [!tip] 参考回答
> ランキングリストのアイテムはオブジェクトプールで再利用しています。画面外にスクロールしたアイテムは破棄せず、データをリセットしてプールに戻すようにしています。

---

> [!question] 题目 9
> 加载场景时使用异步加载避免主线程卡顿，并在回调中初始化游戏逻辑。
> （注：你认为该中文表述奇怪，建议改为“为了避免主线程卡顿，使用异步加载方法来加载场景，并在回调中初始化游戏逻辑”。此处采纳你的修改进行翻译。）

> [!example] 自己的答案
> メインスレッドのカクつきを避けるため、非同期ロードでシーンをロードするようにします。コールバックによってゲームロジックを初期化する


> [!warning] 指正
> 原中文确实有些拗口。面试中直接说明“非同期読み込み”来避免主线程阻塞会更专业，“回调”常译作“コールバック”。

> [!tip] 参考回答
> メインスレッドのカクつきを避けるため、シーンを非同期で読み込み、コールバック内でゲームロジックを初期化しています。

---

> [!question] 题目 10
> 在攻击动画的最后一帧添加了动画事件来触发攻击判定结束，这样比用时间耦合更可靠。
> （你的观点：如果动画有 Blend Out 时间，最后一帧事件可能被跳过，并不比时间耦合更可靠。）

> [!example] 自己的答案
> 攻撃アニメーションの最後１フレームにイベントを追加することで終了を判定するようにします。時間判定より信頼度が高いです


> [!warning] 指正
> 这个设计观点本身存在技术争议。翻译时若保留原句会传递不严谨的信息。建议在面试中主动提出该方案的局限性，展示工程思维。

> [!tip] 参考回答（含技术点评）
> 攻撃アニメーションの最終フレームにイベントを置いて攻撃終了を通知する方法は、時間による判定より信頼性が高いと考えられます。ただし、Blend Out が設定されていると最終フレームのイベントがスキップされる可能性があるため、実際の実装では時間判定と併用するのが安全です。