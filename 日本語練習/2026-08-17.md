## 2026-08-17

### 练习 1

> [!question] 题目
> 在 Unreal 中管理可叠加的状态效果时，应由效果组件统一判断层数上限、持续时间刷新和移除条件。

> [!example] 自己的答案
> Unrealでは重なり状態エフェクトを管理する時に、エフェクトコンポーネントで重なり上限、持続時間の更新、削除条件を一元的に判断するべきです。

> [!warning] 指正
> 意思基本清楚。`重なり状態エフェクト` 不自然，可改为 `スタック可能なステータス効果`；“层数上限”宜说 `スタック上限`。说明通用规则时，`管理する時に` 改为 `管理するときは` 更自然。

> [!tip] 参考回答
> Unrealで<mark style="background: #FF5582A6;">スタック可能</mark>なステータス効果を管理するときは、エフェクトコンポーネントがスタック上限、持続時間の更新、削除条件を一元的に判断するべきです。

---

### 练习 2

> [!question] 题目
> 菜单输入应先由输入层转换成操作事件，再由当前打开的 UI 决定如何处理该事件。

> [!example] 自己的答案
>　メニューの入力はまず入力階層で入力を操作イベントに変換し、その後、処理を開かれているUIで決めます。

> [!warning] 指正
> 前半意思正确，但 `入力階層` 更自然地说 `入力層`。后半 `処理を開かれているUIで決めます` 语序和助词不自然，应明确为“当前打开的 UI 决定如何处理该事件”。

> [!tip] 参考回答
> メニュー入力は、まず入力層で操作イベントに変換し、その後、現在開いているUIがそのイベントをどのように処理するかを決定します。

---

### 练习 3

> [!question] 题目
> 释放蓄力技能之前，请先确认资源消耗和目标条件，全部满足后再生成攻击效果。

> [!example] 自己的答案
>　力溜めスキルを発動する前に、まずリソース消費とターゲット条件を確認し、全て条件を満たしてから攻撃エフェクトを生成します。

> [!warning] 指正
> 处理顺序表达得很清楚。`力溜めスキル` 可以理解，但游戏语境中 `チャージスキル` 更常见；`全て条件` 缺少助词，应为 `全ての条件`。题目的“全部满足”也可明确为 `両方の条件を満たした後で`。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">チャージスキル</mark>を発動する前に、まずリソース消費とターゲット条件を確認し、両方の条件を満たした後で攻撃エフェクトを生成します。

---

### 练习 4

> [!question] 题目
> 如果 Unreal 场景中的纹理突然变模糊，请记录纹理流送池的使用量、所需显存和问题出现的时间点。

> [!example] 自己的答案
>　Unrealシーンでテクスチャが突然にボヤしする場合、テクスチャストリーミングの使用量、GPUメモリ、問題が出るタイミングを記録してください。

> [!warning] 指正
> 调试方向基本传达出来了。`突然にボヤしする` 不自然，应使用自动词 `突然ぼやける`。`テクスチャストリーミングの使用量` 应明确为流送池的使用量；`GPUメモリ` 还遗漏了“所需”的含义。

> [!tip] 参考回答
> Unrealのシーンでテクスチャが<mark style="background: #FFB86CA6;">突然ぼやける</mark>場合は、テクスチャストリーミングプールの使用量、必要なGPUメモリ、問題が発生した時点を記録してください。

---

### 练习 5

> [!question] 题目
> Unity 的异步资源加载被取消时，也要清理等待中的任务并释放已经取得的资源。

> [!example] 自己的答案
>　Unityの非同期のリソースロードがキャンセルされた時に、待ちタスクをクリアしてロード済みリソースを解放します。

> [!warning] 指正
> 题意表达完整。`非同期のリソースロード` 可简化为 `非同期リソース読み込み`，`待ちタスク` 更自然地说 `待機中のタスク`。说明取消时必须执行的规则，用 `キャンセルされたときは` 更合适。

> [!tip] 参考回答
> Unityの非同期リソース読み込みがキャンセルされたときは、待機中のタスクをクリーンアップし、すでに取得したリソースも解放する必要があります。

---

### 练习 6

> [!question] 题目
> 读取跨越多个版本的存档时，应按版本顺序执行迁移，并在每一步保存验证结果。

> [!example] 自己的答案
>　複数のバージョンに渡ってセーブデータを読み込む時に、バージョン順に移行を行い、ステップごとに検証結果を保存します。

> [!warning] 指正
> 迁移顺序和保存验证结果都表达出来了。`複数のバージョンに渡ってセーブデータを読み込む` 略生硬，建议说 `複数のバージョンをまたぐセーブデータを読み込む`。通用规则仍宜使用 `〜するときは`。

> [!tip] 参考回答
> 複数のバージョンをまたぐセーブデータを読み込むときは、バージョン順に移行処理を実行し、各ステップで検証結果を保存します。

---

### 练习 7

> [!question] 题目
> 如果攻击判定比动画提前出现，请比较动画通知的触发帧、判定框的启用时间和播放速率。

> [!example] 自己的答案
>　攻撃判定がアニメーションより早めに発生する場合、Anim Notifyの発火、判定有効タイミング、再生倍速を比較してください。

> [!warning] 指正
> 排查对象基本正确。`早めに発生する` 是“较早发生”，此处应说 `アニメーションより先に発生する`。`Anim Notifyの発火` 遗漏了“触发帧”，`判定有効タイミング` 也应明确是判定框的启用时间；`再生速度` 比 `再生倍速` 更自然。

> [!tip] 参考回答
> 攻撃判定がアニメーションより先に発生する場合は、Anim Notifyの発火フレーム、判定ボックスが有効になるタイミング、アニメーションの再生速度を比較してください。

---

### 练习 8

> [!question] 题目
> 角色从布娃娃状态恢复时，需要先停止物理模拟，再校正角色位置，最后切换到起身动画。

> [!example] 自己的答案
>　キャラクターがRogDollから復元する時に、まず物理演算を停止し、その後キャラクターの位置を修正し、最終に立ち上がるアニメーションを切り替えます。

> [!warning] 指正
> 三个步骤都已表达，但术语和搭配需要调整。`RogDoll` 应为 `Ragdoll`，从该状态恢复宜说 `復帰する`；`最終に` 应改为 `最後に`。切换到某动画要用 `起き上がりアニメーションに切り替える`。

> [!tip] 参考回答
> キャラクターがRagdoll状態から復帰するときは、まず物理シミュレーションを停止し、次にキャラクターの位置を補正して、最後に起き上がりアニメーションへ切り替えます。

---

### 练习 9

> [!question] 题目
> 为了减少战斗中的内存分配，可以复用查询结果容器，并避免在每帧循环里创建临时列表。

> [!example] 自己的答案
>　戦闘中にメモリの割り当てを減らすために、絞り込み結果を再利用できるように、毎フレーム一時的リストを繰り返して生成することを防ぎます。

> [!warning] 指正
> 减少临时列表的方向正确，但 `絞り込み結果` 没有准确表达“查询结果容器”，建议使用 `クエリ結果用のコンテナ`。`生成することを防ぐ` 较生硬，这里用 `生成を避ける` 更自然。

> [!tip] 参考回答
> 戦闘中のメモリ割り当てを減らすために、クエリ結果用のコンテナを再利用し、毎フレームのループ内で一時リストを生成するのを避けます。

---

### 练习 10

> [!question] 题目
> 显示道具提示框时，应根据屏幕边缘调整位置，同时保持手柄焦点不被提示框夺走。

> [!example] 自己的答案
>　アイテムのヒントを表示するときに、モニターの端に応じて位置を調整し、ゲームパッドのフォーカスがヒントに遷移しないようにします。

> [!warning] 指正
> 意思基本准确。UI 语境中的“提示框”通常说 `ツールチップ`，`モニターの端` 应改为 `画面の端`。题目强调焦点“不被夺走”，用 `フォーカスを奪われないようにする` 比 `遷移しない` 更贴切。

> [!tip] 参考回答
> アイテムのツールチップを表示するときは、画面の端に応じて位置を調整し、ゲームパッドのフォーカスがツールチップに奪われないようにします。

---

### 练习 11

> [!question] 题目
> 使用命令模式实现撤销功能时，每个操作都要保存恢复上一状态所需的最小数据。

> [!example] 自己的答案
>　コマンドパターンでキャンセル機能を実装する時に、操作ごとに前回の状態に戻せる最小限データを保存します。

> [!warning] 指正
> 设计意图基本清楚，但“撤销”不是 `キャンセル`，应使用 `元に戻す機能` 或 `取り消し機能`。`最小限データ` 缺少助词 `の`；恢复的是操作前的状态，用 `直前の状態` 更准确。

> [!tip] 参考回答
> コマンドパターンで元に戻す機能を実装するときは、各操作で直前の状態を復元するために必要な最小限のデータを保存します。

---

### 练习 12

> [!question] 题目
> 测试敌人的决策逻辑时，请固定初始状态和随机数种子，并比较每一步选择是否一致。

> [!example] 自己的答案
>　敵の決定ロジックをテストする時に、初期状態と乱数のシードを固定してください。各ステップの選択が一致しているかを比較してください。

> [!warning] 指正
> 测试条件与比较方法都表达完整。`決定ロジック` 可以理解，但表示 AI 的“决策逻辑”时，`意思決定ロジック` 更自然。两句也可以连接起来，使测试流程更紧凑。

> [!tip] 参考回答
> 敵の意思決定ロジックをテストするときは、初期状態と乱数シードを固定し、各ステップでの選択が一致しているか比較してください。

---

### 练习 13

> [!question] 题目
> 战斗日志只负责记录结构化事件，调试界面再根据筛选条件决定显示哪些内容。

> [!example] 自己的答案
>　バトルログはイベントだけを記録し、デバッグ画面は絞り込み条件に応じて内容を表示します。

> [!warning] 指正
> Gameplay 与调试界面的责任边界表达正确，但 `イベントだけ` 遗漏了“结构化”的含义，应说 `構造化されたイベントだけ`。后半可补出“决定显示哪些内容”，使职责描述更完整。

> [!tip] 参考回答
> バトルログは構造化されたイベントの記録だけを担当し、デバッグ画面はフィルター条件に基づいて、どの内容を表示するかを決定します。

---

### 练习 14

> [!question] 题目
> 这家店周末总是排很长的队，我们早点出门怎么样？

> [!example] 自己的答案
>　この店は週末にいつも長く並んでいる、早めに出かけるどうですか。

> [!warning] 指正
> 意图能理解，但 `この店は〜並んでいる` 变成了“店在排队”，应说 `この店はいつも長い行列ができる`。两个分句不能只用逗号连接；`出かけるどうですか` 应为 `出かけるのはどうですか`。

> [!tip] 参考回答
> この店は週末になるといつも長い行列ができるから、少し早めに出かけるのはどう？

---

### 练习 15

> [!question] 题目
> 目前的修改可能影响测试进度，我整理完风险后，想请大家一起确认优先级。

> [!example] 自己的答案
>　今回の修正が進捗に影響するので、リスクをまとめてから皆さんに優先度を確認していただきます。

> [!warning] 指正
> 职场语气基本成立，但遗漏了“测试进度”，应明确为 `テストの進捗`。`影響するので` 表示确定会影响，而题目是“可能影响”，应使用 `影響する可能性があります`。`確認していただきます` 也没有表达“一起确认”的请求。

> [!tip] 参考回答
> 現在の修正がテストの進捗に影響する可能性があります。リスクを整理した後で、優先順位を皆さんと一緒に確認したいです。

## 2026-08-18

### 练习 1

> [!question] 题目
> 敌人进入处决状态时，应由战斗状态机锁定移动和技能入口，并在处决结束后统一恢复。

> [!example] 自己的答案
> 敵が処決状態に入る時に、バトルステートマシンで移動とスキル入口をロックするべきで、処決が終了後に一元で復元します。

> [!warning] 指正
> 处理流程能够理解，但 `処決` 不是自然的日语游戏术语，可用 `処刑` 或 `フィニッシュムーブ`。责任主体应写成 `バトルステートマシンが`；`スキル入口` 宜改为 `スキルの発動`，`一元で復元する` 则可自然地表达为 `まとめてロックを解除する`。

> [!tip] 参考回答
> 敵が処刑状態に入ったときは、バトルステートマシンが移動とスキルの発動をロックし、処刑が終了した後にまとめてロックを解除します。

---

### 练习 2

> [!question] 题目
> 应用画面设置之前，请先保存当前值；如果玩家在倒计时内没有确认，就恢复原来的设置。

> [!example] 自己的答案
>　画面設定を適用する前に、まず現在値を保存します。プレイヤーがカウントダウン終了前に確認していない場合は、元の設定に戻ります。

> [!warning] 指正
> 顺序和条件表达清楚。`現在値` 最好明确为 `現在の設定値`；玩家没有确认时，是系统“恢复设置”，因此后半应使用他动词 `元の設定に戻します`，而不是自动词 `戻ります`。

> [!tip] 参考回答
> 画面設定を適用する前に、まず現在の設定値を保存します。プレイヤーがカウントダウン中に確認しなかった場合は、元の設定に戻します。

---

### 练习 3

> [!question] 题目
> 如果 Unreal 的 Aim Offset 在斜坡上发生抖动，请比较角色朝向、地面法线和动画更新的时间点。

> [!example] 自己的答案
>　Unrealの Aim Offsetは斜め坂でガタつく場合、キャラクターの向き、地面の法線、アニメーション進行を比較してください。

> [!warning] 指正
> 调试对象基本正确。`斜め坂` 不自然，通常说 `斜面`；此处 Aim Offset 是发生抖动的主体，应使用 `Aim Offsetが`。`アニメーション進行` 没有准确表达“动画更新的时间点”，宜改为 `アニメーションが更新されるタイミング`。

> [!tip] 参考回答
> UnrealのAim Offsetが斜面でガタつく場合は、キャラクターの向き、地面の法線、アニメーションが更新されるタイミングを比較してください。

---

### 练习 4

> [!question] 题目
> 动态光源数量突然增加时，应分别测量阴影生成、光照计算和后处理的 GPU 成本。

> [!example] 自己的答案
> 動的な光源数が突然に増える場合、シャドウの生成、ライトニング計算、ポストプロセスのGPU負荷をそれぞれ計測するべきです。

> [!warning] 指正
> 测量对象都已覆盖。`突然に増える` 可改为更自然的 `急に増える`；`ライトニング` 表示“闪电”，光照计算应使用 `ライティング計算`。技术语境中也可以直接说 `GPUコスト`。

> [!tip] 参考回答
> 動的ライトの数が急に増えたときは、シャドウ生成、ライティング計算、ポストプロセスのGPUコストをそれぞれ計測するべきです。

---

### 练习 5

> [!question] 题目
> 从对象池取出特效实例时，需要重置材质参数、计时器和附着对象，避免继承上一次的状态。

> [!example] 自己的答案
>　オブジェクトプールからエフェクトのインスタンスを取り出す時に、マテリアルパラメータ、タイマー、アタッチされたオブジェクトをリセットする必要があります。前回の状態が残らないようにします。

> [!warning] 指正
> 题意表达完整。说明通用规则时，`取り出す時に` 改为 `取り出すときは` 更自然；“附着对象”在这里指附着目标，宜说 `アタッチ先`。后两句也可连接起来，使目的关系更清楚。

> [!tip] 参考回答
> オブジェクトプールからエフェクトのインスタンスを取り出すときは、マテリアルパラメータ、タイマー、アタッチ先をリセットし、<mark style="background: #FF5582A6;">前回の状態を引き継がない</mark>ようにします。

---

### 练习 6

> [!question] 题目
> 保存按键配置之前，应先检查新的组合是否与现有操作冲突，并明确提示被替换的功能。

> [!example] 自己的答案
>　キーバインディングを保存する前に、新しく追加された組み合わせが現在の設定と重なっていないかを確認しておくべきで、差し替えた機能を明確に提示します。

> [!warning] 指正
> 整体意思能够理解。`新しく追加された組み合わせ` 更像“新加入的组合”，这里用 `新しいキーの組み合わせ` 更准确；操作之间的冲突应说 `競合していないか`。`差し替えた機能` 是已经替换后的功能，题目所指的是将被替换的功能，应为 `置き換えられる機能`。

> [!tip] 参考回答
> キーバインドを保存する前に、新しいキーの組み合わせが既存の操作と<mark style="background: #FF5582A6;">競合していない</mark>か確認し、置き換えられる機能を明確に表示するべきです。

---

### 练习 7

> [!question] 题目
> 存档校验失败时，不要直接覆盖损坏的文件，应先保留它，再尝试读取最近的备份。

> [!example] 自己的答案
>　セーブデータの検証に失敗した場合、破損したファイルを直接上書きしないでまず保存してから直近のバックアップを読み込んでみます。

> [!warning] 指正
> 处理顺序表达正确，但 `まず保存する` 容易被理解为再次保存损坏内容。“保留损坏文件”可明确说 `破損したファイルを退避する`。`読み込んでみます` 偏口语，技术说明中使用 `読み込みを試みます` 更合适。

> [!tip] 参考回答
> セーブデータの検証に失敗した場合は、破損したファイルを直接上書きせず、まずそのファイルを退避してから、直近のバックアップの読み込みを試みます。

---

### 练习 8

> [!question] 题目
> 物理结果随帧率变化时，请确认固定时间步长、最大子步数和角色受力的执行时机。

> [!example] 自己的答案
>　物理演算はフレームレートと変化する時に、固定ステップ時間、最大のステップ回数とキャラクターが力を受け取るタイミングを確認してください。

> [!warning] 指正
> 三个确认项基本齐全。开头的助词和主语关系不正确，应说 `物理演算の結果がフレームレートによって変わる場合`。标准术语是 `固定タイムステップ` 和 `最大サブステップ数`；角色受力是系统向角色施力，因此用 `力を加えるタイミング`。

> [!tip] 参考回答
> 物理演算の結果がフレームレートによって変わる場合は、<mark style="background: #FF5582A6;">固定タイムステップ</mark>、最大サブステップ数、キャラクターに<mark style="background: #FF5582A6;">力を加える</mark>タイミングを確認してください。

---

### 练习 9

> [!question] 题目
> 背包重新排序后，应使用稳定的物品标识恢复选中项，而不是依赖列表中的位置。

> [!example] 自己的答案
>　インベントリーが再並び替えた後、安定なアイテムマーカーで選択中アイテムに復帰し、リストの位置に依存しないようにします。

> [!warning] 指正
> 设计意图可以理解，但 `インベントリーが再並び替えた` 的自动词、他动词关系不正确，应为 `インベントリを並べ替えた後`。`安定なアイテムマーカー` 不自然，“稳定的物品标识”宜使用 `変化しないアイテムID`；选中项应说 `選択中のアイテムを復元する`。

> [!tip] 参考回答
> インベントリを並べ替えた後は、リスト内の位置に依存するのではなく、変化しないアイテムIDを使って選択中のアイテムを復元します。

---

### 练习 10

> [!question] 题目
> 为了排查伤害修正器的顺序问题，请记录每个修正器接收到的数值和处理后的结果。

> [!example] 自己的答案
>　ダメージ修正順番を特定するため、修正メソッドでは処理前の数値と処理後の結果をそれぞれ記録してください。

> [!warning] 指正
> 记录处理前后数值的方向正确。`ダメージ修正順番` 缺少名词间的连接，宜说 `ダメージ補正の適用順`；题目要求记录每个修正器的输入和输出，不是泛指 `修正メソッド`，可使用 `各補正処理が受け取った値`。

> [!tip] 参考回答
> ダメージ補正の適用順に関する問題を調べるために、各補正処理が受け取った値と、処理後の結果を記録してください。

---

### 练习 11

> [!question] 题目
> UI 页面被停用时，应停止仍在运行的动画，并解除只属于该页面的事件订阅。

> [!example] 自己的答案
>　UI画面が中断された時に、再生中のアニメーションを停止するべきで、該当画面のイベントバインディングを解除します。

> [!warning] 指正
> 两项清理工作都已表达。UI 页面的“停用”应说 `無効化される`，`中断される` 更像处理被中途打断。`停止するべきで、〜解除します` 的连接略生硬，可统一为并列动作；事件订阅可说 `イベント購読`。

> [!tip] 参考回答
> UI画面が無効化されたときは、実行中のアニメーションを停止し、その画面だけで使用するイベント購読を解除します。

---

### 练习 12

> [!question] 题目
> Unreal 启动关卡之前，应验证数据表中的资源引用、数值范围和必填字段。

> [!example] 自己的答案
>　Unrealではレベルが起動する前に、データテーブル内のリソース参照、数値範囲、必須なフィールドを検証すべきです。

> [!warning] 指正
> 验证项目完整，意思清楚。`レベルが起動する` 不符合这里的动作关系，应使用 `レベルを開始する`；`必須なフィールド` 应改为名词连接形式 `必須フィールド` 或 `必須のフィールド`。

> [!tip] 参考回答
> Unrealでレベルを開始する前に、データテーブル内のリソース参照、数値の範囲、必須フィールドを検証するべきです。

---

### 练习 13

> [!question] 题目
> 测试检查点恢复功能时，请比较恢复前后的任务阶段、玩家属性和已取得的道具。

> [!example] 自己的答案
>　チェックポイントの復元機能をテストするときに、復元前後のクエストのフェーズ、プレイヤーの属性、獲得済みアイテムを比較してください。

> [!warning] 指正
> 比较对象全部表达出来了，整体准确。说明测试规则时，`テストするときに` 改为 `テストするときは` 更自然；Gameplay 语境中的“玩家属性”通常使用 `プレイヤーのステータス`。

> [!tip] 参考回答
> チェックポイントの復元機能をテストするときは、復元前後のクエストの段階、プレイヤーのステータス、取得済みのアイテムを比較してください。

---

### 练习 14

> [!question] 题目
> 我本来想买这家店的限定甜点，可惜刚到就卖完了。

> [!example] 自己的答案
>　この店限定デザートを買うつもりだった、残念ですが店に到着したところに売れ切った

> [!warning] 指正
> 基本意图能够理解，但 `この店限定デザート` 缺少 `の`；两个分句不能只用逗号连接。`到着したところに` 的助词不正确，应为 `着いたときには`；`売れ切った` 应改为 `売り切れていた`。日常会话中用 `限定スイーツ` 更自然。

> [!tip] 参考回答
> この店限定のスイーツを買おうと思っていたのに、着いたときにはもう売り切れていて残念だった。

---

### 练习 15

> [!question] 题目
> 评审资料还缺少性能测量结果，可以请你在下午的会议前帮我确认图表吗？

> [!example] 自己的答案
>　審査資料にはパフォーマンス結果が足りない、午後の会議前にテーブルを確認してくれませんか。

> [!warning] 指正
> 请求内容可以理解，但开发评审通常说 `レビュー資料`，不是 `審査資料`；题目的“性能测量结果”应为 `性能測定の結果`，`テーブル` 是表格而不是图表，应使用 `グラフ`。对同事提出礼貌请求时，`確認していただけますか` 更合适。

> [!tip] 参考回答
> レビュー資料には、まだ性能測定の結果が不足しています。午後の会議までに、グラフを確認していただけますか。

## 2026-08-19

### 练习 1

> [!question] 题目
> 角色进入无法操作的状态时，应由角色状态组件统一限制移动和技能，UI 只负责显示原因。

> [!example] 自己的答案
> キャラクターが操作不可能状態に入る時に、キャラクターのステートコンポーネント経由で移動とスキルを一元的に制限し、UIは原因だけを表示します。

> [!warning] 指正
> 责任划分表达清楚。`操作不可能状態` 更自然地说 `操作不能状態`；说明通用规则时，`入る時に` 应改为 `入ったときは`。`コンポーネント経由で` 表示“经由组件”，题目强调组件是责任主体，因此使用 `コンポーネントが` 更准确。

> [!tip] 参考回答
> キャラクターが操作不能状態に入ったときは、キャラクターの状態コンポーネントが移動とスキルの使用を一元的に制限し、UIはその理由だけを表示します。

---

### 练习 2

> [!question] 题目
> 处理攻击输入缓冲时，请先记录输入时刻，并在角色恢复行动后判断该输入是否仍在有效期内。

> [!example] 自己的答案
>　攻撃入力バッファを処理する時に、先に入力タイムスタンプを記録してください。キャラクターが復元した後その入力の有効時間を判断します。

> [!warning] 指正
> 记录输入时刻的步骤正确。角色“恢复行动”不能说 `復元した`，宜用 `再び行動できるようになった`。`入力の有効時間を判断する` 是判断有效时长，题目要求判断是否仍在有效期内，应说 `有効期間内かどうかを判断する`。

> [!tip] 参考回答
> 攻撃入力のバッファを処理するときは、まず入力時刻を記録し、キャラクターが再び行動できるようになった後で、その入力がまだ有効期間内かどうかを判断します。

---

### 练习 3

> [!question] 题目
> 如果 Unreal 的镜头震动没有按时结束，请检查震动实例的生命周期、所属来源和移除日志。

> [!example] 自己的答案
>　Unrealのカメラシェークが想定通りに終了しない場合、インスタンスのライフサイクル、由来、削除を確認してください。

> [!warning] 指正
> 现象和生命周期都已表达。实例的“所属来源”在这里应说 `発生元`，`由来` 偏向事物的起源。题目要求检查“移除日志”，答案中的 `削除` 缺少 `ログ`，需要明确为 `削除ログ`。

> [!tip] 参考回答
> Unrealのカメラシェイクが<mark style="background: #FF5582A6;">想定した時刻に</mark>終了しない場合は、シェイクインスタンスのライフサイクル、<mark style="background: #FF5582A6;">発生元</mark>、削除ログを確認してください。

---

### 练习 4

> [!question] 题目
> 优化半透明特效时，应分别测量材质指令数、屏幕覆盖面积和对应的 GPU 时间。

> [!example] 自己的答案
>　半透明エフェクトを最適化する時に、マテリアルパラメータ、スクリーン所有面積、GPU時間をそれぞれ計測するべきです。

> [!warning] 指正
> 三类性能数据的结构基本正确，但两个术语有误。“材质指令数”是 `マテリアルの命令数`，不是材质参数；“屏幕覆盖面积”应说 `画面占有面積`，不能用 `所有`。通用规则中 `最適化する時に` 改为 `最適化するときは`。

> [!tip] 参考回答
> 半透明エフェクトを最適化するときは、マテリアルの命令数、画面占有面積、それに対応するGPU時間をそれぞれ計測するべきです。

---

### 练习 5

> [!question] 题目
> Unity 创建预制体实例失败时，也要释放已经加载的资源句柄，避免引用一直残留。

> [!example] 自己的答案
>　Unityではプレハブの生成に失敗してもロード済みリソースハンドルを解放します。参照が残らないようにします。

> [!warning] 指正
> 题意完整，失败时释放句柄和避免残留都表达正确。`プレハブの生成に失敗しても` 可以使用；若要让条件更明确，可写成 `生成に失敗した場合でも`。两句连接起来会更自然。

> [!tip] 参考回答
> Unityでプレハブのインスタンス生成に失敗した場合でも、参照が残り続けないように、読み込み済みのリソースハンドルを解放する必要があります。

---

### 练习 6

> [!question] 题目
> 重新计算装备属性时，应先应用基础值和词条，再计算套装加成，最后只发布一次结果事件。

> [!example] 自己的答案
>　装備の属性を再計算する時に、まず基本値とタグを適用してからセットの加算を行い、最後に結果イベントを通知します。

> [!warning] 指正
> 计算顺序基本清楚，但 Gameplay 中的“装备属性”通常说 `装備のステータス`。“词条”不是 `タグ`，可用 `オプション効果`；“套装加成”宜说 `セットボーナス`。还需要明确结果事件“只发布一次”。

> [!tip] 参考回答
> 装備のステータスを再計算するときは、まず基礎値と<mark style="background: #FF5582A6;">オプション効果</mark>を適用し、次にセットボーナスを計算して、最後に結果イベントを一度だけ発行します。

---

### 练习 7

> [!question] 题目
> 为了降低远处 NPC 的更新成本，可以减少普通逻辑的执行频率，但必须保留重要计时器的准确性。

> [!example] 自己的答案
>　遠くにあるNPC更新コストを軽減するために、通常ロジックの実行頻度をさげても、タイマーの精度を確保する必要があります。

> [!warning] 指正
> 优化措施和保留计时准确性的条件都表达出来了。`遠くにあるNPC` 应改为有生命对象使用的 `遠くにいるNPC`；`NPC更新コスト` 需要补助词 `の`。题目中的“重要计时器”也应明确为 `重要なタイマー`。

> [!tip] 参考回答
> 遠くにいるNPCの更新コストを下げるために、通常ロジックの実行頻度を減らすことはできますが、重要なタイマーの正確性は維持する必要があります。

---

### 练习 8

> [!question] 题目
> 如果脚部 IK 在不平地面上发生穿插，请比较射线命中位置、骨盆偏移和骨骼求解的时间点。

> [!example] 自己的答案
>　足IKが地面にめり込んだ場合、ヒット位置、pelvisオフセット、ボーンResolverのタイムスタンプを比較してください。

> [!warning] 指正
> 排查方向可以理解，但 `pelvis` 通常写成日语技术词 `骨盤`。`ボーンResolver` 不是自然的常用表达，可说 `ボーンの計算` 或 `IKの解決処理`；题目要求比较“时间点”，应使用 `タイミング`，而不是表示时间记录值的 `タイムスタンプ`。

> [!tip] 参考回答
> 足のIKが凹凸のある地面でめり込む場合は、レイのヒット位置、骨盤のオフセット、ボーンが計算されるタイミングを比較してください。

---

### 练习 9

> [!question] 题目
> 拖放道具被数据层拒绝后，界面应把道具恢复到原来的格子，而不是自行修改占用状态。

> [!example] 自己的答案
>　アイテムのドラッグがデータ層に拒否された後に、画面はアイテムを元の位置に戻すべきです。勝手に占有状態を変更するのではありません。

> [!warning] 指正
> 数据层与 UI 的责任边界表达正确。这里是完整的“拖放”，宜说 `ドラッグ＆ドロップ`；题目的“原来的格子”应明确为 `元のスロット`，而非泛指位置。技术规则中，`変更するのではありません` 改为 `変更するべきではありません` 更自然。

> [!tip] 参考回答
> アイテムのドラッグ＆ドロップがデータ層に拒否された場合、UIはアイテムを元のスロットに戻し、占有状態を独自に変更するべきではありません。

---

### 练习 10

> [!question] 题目
> 如果低优先级命令长时间没有执行，请记录入队、开始和完成的时间，并比较各自的等待时长。

> [!example] 自己的答案
>　低優先度タスクが長時間に実行されない場合、入り、開始、終了の時間を記録してから、各待ち時間を比較してください。

> [!warning] 指正
> 要记录并比较时间的方向正确，但题目是低优先级“命令”，应使用 `コマンド`，不是 `タスク`。`長時間に実行されない` 中的 `に` 多余；“入队时间”应说 `キューに追加された時刻`，不能只写 `入り`。

> [!tip] 参考回答
> 低優先度のコマンドが長時間実行されない場合は、キューに追加された時刻、実行開始時刻、完了時刻を記録し、それぞれの待ち時間を比較してください。

---

### 练习 11

> [!question] 题目
> 存档选择界面只读取角色名、游玩时间等概要信息，不应为显示列表而加载完整存档。

> [!example] 自己的答案
>　セーブデータの選択画面はキャラクター名、プレイー時間など概要情報を読み込むだけで、リストを表示するためデータ全体を読み込むのではありません。

> [!warning] 指正
> UI 只读取概要信息的意思正确。`プレイー時間` 应为 `プレイ時間`；`など概要情報` 缺少助词，应为 `などの概要情報`。`表示するため` 后需加 `に`，并应明确“不应加载完整存档”。

> [!tip] 参考回答
> セーブデータの選択画面は、キャラクター名やプレイ時間などの概要情報だけを読み込み、一覧を表示するためにセーブデータ全体を読み込むべきではありません。

---

### 练习 12

> [!question] 题目
> 播放重要语音时，可以按优先级降低环境音量，并在语音结束后沿曲线平滑恢复。

> [!example] 自己的答案
>　重要なボイスを再生する時に、優先度に応じてBGMの音量を下げ、ボイスが終了した後、カーブに沿ってスムーズに復元します。

> [!warning] 指正
> 降低音量并平滑恢复的流程已表达，但题目是“环境音量”，`BGM` 变成了背景音乐，应使用 `環境音の音量`。音量恢复宜说 `元に戻す`，`復元する` 更常用于数据或状态恢复。通用规则中仍建议使用 `再生するときは`。

> [!tip] 参考回答
> 重要なボイスを再生するときは、優先度に応じて環境音の音量を下げ、ボイスの終了後にカーブに沿って滑らかに元の音量へ戻します。

---

### 练习 13

> [!question] 题目
> 测试合成逻辑时，应分别验证材料不足、配方无效和成功生成道具后的背包变化。

> [!example] 自己的答案
>　合成ロジックをテストする時に、リソースの数量、レシピの有効性、成功したインベントリーの変化をそれぞれ検証するべきです。

> [!warning] 指正
> 三种验证方向可以看出，但含义不够准确。`リソースの数量` 只是资源数量，没有明确“材料不足”；`レシピの有効性` 是一般有效性，而题目要求验证无效配方；`成功したインベントリーの変化` 的修饰关系错误，应说“合成成功后的背包变化”。

> [!tip] 参考回答
> 合成ロジックをテストするときは、素材が不足している場合、レシピが無効な場合、アイテムの生成に成功した後のインベントリの変化をそれぞれ検証するべきです。

---

### 练习 14

> [!question] 题目
> 我已经订好电影票了，开场前在车站旁边的咖啡店见面吧。

> [!example] 自己的答案
>　ムービーチケットをすでに予約した、オープンニング前に駅の近く珈琲屋で会いましょう。

> [!warning] 指正
> 基本意图能理解，但“电影票”应说 `映画のチケット`，`ムービーチケット` 不自然。`オープンニング` 拼写和含义都不合适，“开场前”应为 `上映前`。`駅の近く珈琲屋` 缺少助词，应为 `駅の近くのカフェ`；两个分句也不能只用逗号连接。

> [!tip] 参考回答
> 映画のチケットはもう取ってあるよ。上映前に、駅の近くのカフェで会おう。

---

### 练习 15

> [!question] 题目
> 任务范围发生了变化，我想先重新评估工期，再和大家确认新的计划。

> [!example] 自己的答案
>　タスク範囲が変化した後、まず見積もりを見直してから皆さんに新しい計画を確認していただきます。

> [!warning] 指正
> 重新评估后确认计划的顺序基本正确。`タスク範囲` 应补助词，写成 `タスクの範囲`；`変化した後` 不自然，可用 `変更になったので`。题目是“和大家确认”，应为 `皆さんと確認する`，不是请大家确认的 `確認していただく`。

> [!tip] 参考回答
> タスクの範囲が変更になったので、まず工期を見積もり直してから、新しい計画を皆さんと確認したいです。

## 2026-08-20

### 练习 1

> [!question] 题目
> 切换锁定目标时，请先排除已死亡和视野外的对象，再按角度与距离评分，最后只更新一次当前目标。

> [!example] 自己的答案
> ロックターゲットを切り替える時に、まず死亡と視野外オブジェクトを排除してから角度、距離を評価します。最後に現在ターゲットを一度だけ更新します。

> [!warning] 指正
> 处理顺序基本清楚。`ロックターゲット` 可以理解，但更常说 `ロックオン対象`。`死亡と視野外オブジェクト` 的修饰关系不自然，应为 `死亡した対象と視野外の対象`；评分依据宜用 `角度と距離に基づいて`，`現在ターゲット` 还缺少助词 `の`。

> [!tip] 参考回答
> ロックオン対象を切り替えるときは、まず死亡した対象と視野外の対象を除外し、次に角度と距離<mark style="background: #FF5582A6;">に基づいて評価し</mark>て、最後に現在の対象を一度だけ更新します。

---

### 练习 2

> [!question] 题目
> 开始过场演出时，应保存当前输入模式并隐藏 HUD，演出结束后再按原来的顺序恢复。

> [!example] 自己的答案
>　オープンニングが始まる時に、現在の入力モードを保存してHUDを非表示すべきです。演出が終了後に元の順番に復元します。

> [!warning] 指正
> 保存输入模式和隐藏 HUD 的步骤已经表达。`オープンニング` 不是“过场演出”，应使用 `カットシーン`。`演出が終了後に` 应改为 `演出が終了した後に`；“按原来的顺序恢复”可自然表达为 `元の順序で復元する`。

> [!tip] 参考回答
> カットシーンを開始するときは、現在の入力モードを保存してHUDを非表示にし、カットシーンが終了した後で<mark style="background: #FF5582A6;">元の順序に従って復元します</mark>。

---

### 练习 3

> [!question] 题目
> 如果 Unreal 打包后出现材质缺失，请比较烘焙日志、资源标签和软引用路径。

> [!example] 自己的答案
>　Unrealでビルド後マテリアルが欠落した場合、ベイクログ、アセットタグとソフト参照パスを比較してください。

> [!warning] 指正
> 三个检查对象都表达出来了。Unreal 语境中的“打包后”更准确地说 `パッケージ化した後`；这里的烘焙日志通常使用 `クックログ`，而 `ベイクログ` 容易被理解为光照或贴图烘焙日志。`ビルド後` 后也需要停顿或助词。

> [!tip] 参考回答
> Unrealでパッケージ化した後にマテリアルが欠落する場合は、<mark style="background: #FF5582A6;">クックログ</mark>、アセットラベル、ソフト参照のパスを比較してください。

---

### 练习 4

> [!question] 题目
> 优化 GPU 粒子时，应分别测量生成数量、透明区域重叠和碰撞计算的成本，再决定减少哪一项。

> [!example] 自己的答案
>　GPUパーティクルを最適化するときは、生成数、透明エリア重なり、コリジョン計算コストを別々に計測してからどの部分を減らすことを決めます。

> [!warning] 指正
> 测量后再决定的逻辑正确。`透明エリア重なり` 缺少助词，应为 `半透明領域の重なり`。`どの部分を減らすことを決めます` 不自然，应改为间接疑问结构 `どの項目を削減するか決めます`。

> [!tip] 参考回答
> GPUパーティクルを最適化するときは、生成数、<mark style="background: #FF5582A6;">半透明領域の重なり</mark>、コリジョン計算のコストをそれぞれ計測してから、<mark style="background: #FF5582A6;">どの項目を削減するか</mark>決めます。

---

### 练习 5

> [!question] 题目
> 卸载区域时，请先停止仍在等待播放的音频，并在没有使用者后释放对应的音频资源包。

> [!example] 自己的答案
>　エリアをアンロードするときには、まず再生待ち中のオーディオを停止し、使用者がいない場合に対応するオーディオを解放します。

> [!warning] 指正
> 停止等待播放内容和确认使用者的顺序清楚。题目要求释放“音频资源包”，答案只写了 `オーディオ`，对象不够明确；可使用 `オーディオバンク`。`再生待ち中` 可简化为更自然的 `再生待ち`。

> [!tip] 参考回答
> エリアをアンロードするときは、まず再生待ちのオーディオを停止し、利用者がいなくなった後で対応するオーディオバンクを解放します。

---

### 练习 6

> [!question] 题目
> 技能快捷栏重新绑定后，应先更新数据模型，再只刷新发生变化的格子。

> [!example] 自己的答案
>　スキルのショートカットキーがバンディングし直した後には、データモージュルを先に更新してから変更されたセルを更新します。

> [!warning] 指正
> 更新顺序基本正确，但有两处拼写错误：重新绑定是 `再バインド`，不是 `バンディング`；数据模型是 `データモデル`，不是 `データモージュル`。题目强调只刷新变化的格子，还需补上限定词 `だけ`。

> [!tip] 参考回答
> スキルのショートカットバーを再バインドした後は、まずデータモデルを更新し、その後で変更されたセルだけを更新します。

---

### 练习 7

> [!question] 题目
> 范围预览可以复用已经生成的几何数据，只有半径或地形发生变化时才重新构建。

> [!example] 自己的答案
>　範囲プレビーは既存のジオメトリーデータを再利用できる。半径または地面が変化する時だけに再構築します。

> [!warning] 指正
> 复用几何数据和按条件重建的意思正确。`プレビー` 应为 `プレビュー`；题目中的“地形”更适合说 `地形`，而不是泛指地面的 `地面`。`変化する時だけに` 中的 `に` 多余，也可用 `変化した場合にのみ` 强调限定条件。

> [!tip] 参考回答
> 範囲プレビューでは生成済みのジオメトリデータを再利用し、半径または地形が変化した場合にのみ再構築します。

---

### 练习 8

> [!question] 题目
> 攻击蒙太奇被中断时，必须关闭判定框、清除根运动修正，并让角色返回可行动状态。

> [!example] 自己的答案
>　攻撃Montageが中断された時に、判定ウィンドを必ずに閉じ、ルートモーション補正をクリアし、キャラクターを通常状態に戻します。

> [!warning] 指正
> 中断后的三个处理基本表达出来了。`判定ウィンド` 是“判定窗口”，题目是判定框，应使用 `判定ボックス`。`必ずに` 的 `に` 多余；恢复到“可行动状态”应明确为 `行動可能な状態`，不能只写含义较宽的 `通常状態`。

> [!tip] 参考回答
> 攻撃モンタージュが中断されたときは、判定ボックスを必ず無効にし、ルートモーションの補正をクリアして、キャラクターを行動可能な状態に戻します。

---

### 练习 9

> [!question] 题目
> 角色开始奔跑时，应沿曲线平滑改变镜头视野角，避免画面在一帧内突然跳变。

> [!example] 自己的答案
>　キャラクターが走る時に、カーブに沿ってカメラの視野を滑らかに変えるべきです。1フレーム内に急に跳ねないようにします。

> [!warning] 指正
> 平滑变化和避免突然跳变的目的都已表达。题目是“开始奔跑”，应使用 `走り始める`；镜头视野角更准确地说 `カメラの視野角`。`1フレーム内に` 应改为 `1フレームで`，并明确发生跳变的是画面。

> [!tip] 参考回答
> キャラクターが走り始めるときは、画面が<mark style="background: #FF5582A6;">1フレームで</mark>急に変化しないように、カーブに沿ってカメラの視野角を滑らかに変更するべきです。

---

### 练习 10

> [!question] 题目
> 如果 AI 偶尔无法生成，请记录导航可达性、碰撞重叠和生成规则拒绝该位置的原因。

> [!example] 自己的答案
>　AIがたまに生成できない場合、到達可能性、コリジョンの重なり、生成ルールによる該当の位置が拒否された原因を記録してください。

> [!warning] 指正
> 三类排查信息基本齐全。题目中的“导航可达性”应明确为 `ナビゲーション上の到達可能性`。`生成ルールによる該当の位置が拒否された原因` 结构错误，应让生成规则作主语，写成 `生成ルールがその位置を拒否した理由`。

> [!tip] 参考回答
> AIがまれに生成されない場合は、ナビゲーション上の到達可能性、コリジョンの重なり、生成ルールがその位置を拒否した理由を記録してください。

---

### 练习 11

> [!question] 题目
> 短时间内收到多个自动保存请求时，可以合并请求并避免同时写入多个文件。

> [!example] 自己的答案
>　短時間内に複数の自動保存要求を受け取った場合、リクエストをマージして複数のファイルを同時に書き込むことを防ぎます。

> [!warning] 指正
> 题意表达完整。`自動保存要求` 可以理解，但开发语境中 `自動保存リクエスト` 更自然。`書き込むことを防ぐ` 略显生硬，这里用 `同時に書き込むのを避ける` 更符合技术说明。

> [!tip] 参考回答
> 短時間に複数の自動保存リクエストを受け取った場合は、リクエストをまとめ、複数のファイルへ同時に書き込むのを避けます。

---

### 练习 12

> [!question] 题目
> 读取掉落表之前，应验证所有权重都不是负数、总和大于零，并准备无有效结果时的备用项。

> [!example] 自己的答案
>　ドロップテーブルを読み込む前に、全ての重みはマイナスではないとトータルはゼロより大きいを確認するべきです。無効な結果場合にバックアップオプションを用意しておきます。

> [!warning] 指正
> 验证目标能够看出，但前半语法需要调整。两个条件都应名词化为 `〜こと`：`すべての重みが負の値ではないこと` 和 `合計が0より大きいこと`。`無効な結果場合` 缺少 `の`，而题意是“没有有效结果”，应说 `有効な結果がない場合`。

> [!tip] 参考回答
> ドロップテーブルを読み込む前に、すべての重みが負の値ではないことと、合計が0より大きいことを確認し、有効な結果がない場合に備えて代替項目を用意しておくべきです。

---

### 练习 13

> [!question] 题目
> 测试技能树重置功能时，请确认点数已经返还、前置条件重新计算，并禁用不再满足条件的节点。

> [!example] 自己的答案
>　スキルツリーのリセットをテストする時に、ポイントの返却、前提条件の計算を確認してください。そして条件が達成しないノードを禁止します。

> [!warning] 指正
> 三个确认方向基本齐全。`ポイントの返却、前提条件の計算を確認` 应明确两项处理都已完成。`条件が達成しない` 的搭配错误，应说 `条件を満たさない`；“禁用节点”应使用 `ノードを無効にする`，不是表示禁止行为的 `禁止する`。

> [!tip] 参考回答
> スキルツリーのリセット機能をテストするときは、ポイントが返却され、前提条件が再計算されていることを確認し、条件を満たさなくなったノードを無効にしてください。

---

### 练习 14

> [!question] 题目
> 快递今天送到，但我晚上不在家，可以请他们改到明天上午吗？

> [!example] 自己的答案
>　宅配便が今日届くけど、僕は夜に留守するから、届き時間を明日午前に変更してもらえますか

> [!warning] 指正
> 请求改期的意思可以理解。`夜に留守する` 不自然，应说 `夜は家にいない`；`届き時間` 应改为快递语境中的 `配達時間`。`明日午前` 通常说 `明日の午前中`，句末还需要问号或句号。

> [!tip] 参考回答
> 宅配便が今日届くんだけど、夜は家にいないから、配達を明日の午前中に変更してもらえる？

---

### 练习 15

> [!question] 题目
> 这项工作还在等待前置资料，如果今天下午仍未收到，我会更新进度并请相关同事确认。

> [!example] 自己的答案
>　この仕事には前提資料を待っているから、本日午後までまだ届かない場合、進捗を更新して関係者に確認していただきます

> [!warning] 指正
> 后续处理方向正确，但 `この仕事には前提資料を待っている` 的主语和助词错误，应说 `この作業は前提資料を待っている状態です`。`本日午後までまだ届かない場合` 不自然，可改为 `今日の午後になっても届かない場合`。最后应明确是“请相关同事确认”。

> [!tip] 参考回答
> この作業はまだ前提資料を待っている状態です。今日の午後になっても届かない場合は、進捗を更新し、関係する同僚に確認を依頼します。

## 2026-08-21

### 练习 1

> [!question] 题目
> 解除多个状态效果时，应先跳过不可驱散的效果，再按优先级移除其他效果，最后只通知界面一次。

> [!example] 自己的答案
> 複数の状態効果を解除する時には、まず外せない効果をスキップし、優先度に残り効果を外して、最後に画面に通知します。

> [!warning] 指正
> 处理顺序基本正确。`外せない効果` 可以理解，但技术语境中 `解除できない効果` 更准确。`優先度に残り効果を外す` 缺少助词，应为 `優先度に従って残りの効果を解除する`；最后还需明确“只通知一次”，即 `一度だけ通知する`。

> [!tip] 参考回答
> 複数の状態効果を解除するときは、まず解除できない効果を除外し、<mark style="background: #FF5582A6;">優先度に従って</mark>残りの効果を解除して、最後にUIへ一度だけ通知します。

---

### 练习 2

> [!question] 题目
> 领取任务奖励之前，请先确认奖励尚未发放，成功添加道具后再保存领取状态。

> [!example] 自己的答案
>　クエストの報酬を受け取る前に、報酬が届いたかを確認し、アイテムの追加が成功してから状態を保存します。

> [!warning] 指正
> 添加道具后再保存状态的顺序正确，但前半意思反了。`報酬が届いたかを確認する` 是确认奖励是否已经发放，题目要求确认“尚未发放”，应说 `報酬がまだ付与されていないことを確認する`。最后也应明确保存的是领取状态。

> [!tip] 参考回答
> クエスト報酬を受け取る前に、まず報酬がまだ付与されていないことを確認し、アイテムの追加に成功してから受取済みの状態を保存します。

---

### 练习 3

> [!question] 题目
> 如果 Unreal 中的模型切换 LOD 时明显跳变，请比较屏幕尺寸阈值、包围盒和镜头视野角。

> [!example] 自己的答案
>　UnrealでLODを切り替える時に急に変化した場合、画面サイズの閾値、バンディんボックス、カメラ視野角を比較してください。

> [!warning] 指正
> 三个比较对象基本齐全。发生跳变的是模型，因此 `LODを切り替える時に急に変化した場合` 可改为 `LODが切り替わるときにモデルが大きく変化する場合`。`バンディんボックス` 是拼写错误，应为 `バウンディングボックス`；`カメラ視野角` 需补 `の`。

> [!tip] 参考回答
> UnrealでLODが切り替わるときにモデルが大きく変化する場合は、画面サイズの閾値、バウンディングボックス、カメラの視野角を比較してください。

---

### 练习 4

> [!question] 题目
> 首次打开战斗界面发生卡顿时，请记录着色器编译数量、帧耗时和首次使用的资源。

> [!example] 自己的答案
>　バトル画面を初めて開く時のカクつきが発生する時に、シェーダーコンパイルの数、フレームコスト、初めて使ったリソースを記録してください。

> [!warning] 指正
> 要记录的三项信息都已表达，但 `開く時のカクつきが発生する時に` 重复且结构不自然，应简化为 `初めて開いたときにカクつく場合`。帧耗时宜说 `フレーム時間`，首次使用的资源可说 `初回使用されたアセット`。

> [!tip] 参考回答
> バトル画面を初めて開いたときにカクつく場合は、シェーダーのコンパイル数、フレーム時間、<mark style="background: #FF5582A6;">初回使用されたアセット</mark>を記録してください。

---

### 练习 5

> [!question] 题目
> 关闭角色预览界面时，需要销毁临时角色并释放渲染纹理，避免预览资源继续占用内存。

> [!example] 自己的答案
>　キャラクタープレビュー画面を閉じる時には、プレビューリソースがメモリに残らないように、一時的なキャラクターを廃棄してテクスチャを解放する必要があります。

> [!warning] 指正
> 目的和资源清理流程基本正确。对象生命周期中“销毁”通常说 `破棄する`，不使用偏废弃含义的 `廃棄する`。题目是释放“渲染纹理”，答案只写 `テクスチャ`，应明确为 `レンダーテクスチャ`。

> [!tip] 参考回答
> キャラクタープレビュー画面を閉じるときは、プレビュー用リソースがメモリを占有し続けないように、一時的なキャラクターを破棄し、レンダーテクスチャを解放する必要があります。

---

### 练习 6

> [!question] 题目
> 玩家调整字体大小后，界面应重新计算布局，并确保当前选中项仍在可见范围内。

> [!example] 自己的答案
>　プレイヤーがフォントサイズを調整したご、画面がレイアウトを再計算するべきです。その上で現在選択中のオプションを表示範囲に保ちます。

> [!warning] 指正
> 两个后续处理都表达出来了。`調整したご` 是拼写错误，应为 `調整した後`；UI 作为主体时用 `画面が` 也能理解，但 `UIは` 更直接。题目的“当前选中项”应为 `現在選択中の項目`，不是范围更窄的 `オプション`。

> [!tip] 参考回答
> プレイヤーがフォントサイズを調整した後は、UIがレイアウトを再計算し、現在選択中の項目が表示範囲内に残るようにします。

---

### 练习 7

> [!question] 题目
> 战斗事件只传递已经确定的结果，接收方不应修改原始伤害数据或重新进行结算。

> [!example] 自己的答案
>　バトルイベントは決まった結果だけを通知し、受け側は元のダメージデータを変更したり再計算したりしないようにします。

> [!warning] 指正
> 责任边界和禁止的处理都表达得很清楚。`決まった結果` 容易理解为“预先决定的结果”，这里应使用 `確定した結果`；`受け側` 较口语，技术说明中更自然的是 `受信側`。其余结构正确。

> [!tip] 参考回答
> バトルイベントは<mark style="background: #FF5582A6;">確定した結果</mark>だけを伝え、<mark style="background: #FF5582A6;">受信側</mark>は元のダメージデータを変更したり、再計算したりするべきではありません。

---

### 练习 8

> [!question] 题目
> 射击结束后，上半身的后坐力叠加层应平滑回到零，不能继续影响移动动画。

> [!example] 自己的答案
>　が終了後に、上半身のRecoil加算層が滑らかに0に戻し、移動アニメーションに影響しないようにします。

> [!warning] 指正
> 句首缺少“射击”，`が終了後に` 没有主语且语法错误，应为 `射撃が終了した後は`。`Recoil加算層` 不自然，可说 `加算リコイルレイヤー`。这里图层自身回到零，应使用自动词 `0に戻り`，不是 `0に戻し`。

> [!tip] 参考回答
> 射撃が終了した後は、<mark style="background: #FF5582A6;">上半身の加算リコイルレイヤー</mark>が滑らかに0へ戻り、移動アニメーションに影響を与え続けないようにします。

---

### 练习 9

> [!question] 题目
> 大量破碎物同时产生时，可以限制活动刚体数量，并让静止碎片提前进入休眠状态。

> [!example] 自己的答案
>　断片が大量に生成される時に、動的な剛体数を制限する、そして静的な断片を早めに寝かせます。

> [!warning] 指正
> 限制刚体和让碎片休眠的方向正确。`断片` 可理解，但破碎物通常说 `破片`。两个动作不能用逗号后直接接 `そして`；可用 `〜を制限し、〜させます`。`静的な断片` 是静态碎片，题目是已静止的碎片，应说 `静止した破片`。

> [!tip] 参考回答
> 大量の<mark style="background: #FF5582A6;">破片</mark>が同時に生成されるときは、アクティブな剛体の数を制限し、静止した破片を早めにスリープ状態へ移行させます。

---

### 练习 10

> [!question] 题目
> AI 选择目标时，应让威胁值随时间下降，并且只有新目标超过阈值后才切换。

> [!example] 自己的答案
>　AIがターゲットを絞り込む時に、恨みを時間と共に下げるべきです。その上で新しいオブジェクトが閾値を超えた場合にのみ切り替えます。

> [!warning] 指正
> 随时间降低数值和按阈值切换的结构基本正确。目标选择应说 `ターゲットを選ぶ`，`絞り込む` 偏向筛选。Gameplay 中的“威胁值”是 `脅威値`，不能用表示个人怨恨的 `恨み`；新目标也应写为 `新しいターゲット`，而不是泛指对象。

> [!tip] 参考回答
> AIがターゲットを選ぶときは、<mark style="background: #FF5582A6;">脅威値</mark>が時間とともに低下するようにし、新しいターゲットの脅威値が閾値を超えた場合にのみ切り替えます。

---

### 练习 11

> [!question] 题目
> 本地设置应与游戏进度分开保存，写入失败时只恢复本次修改的设置值。

> [!example] 自己的答案
> ローカル設定とゲーム進行を分けて保存するべきで、書き込みに失敗する時に今回の変更された設定だけを復元します。

> [!warning] 指正
> 分开保存和失败时恢复的意思正确。`分けて保存する` 可以使用，`別々に保存する` 更简洁。已经发生写入失败时，应使用 `書き込みに失敗した場合`，不是表示一般时机的 `失敗する時に`；`今回の変更された設定` 应改为 `今回変更した設定値`。

> [!tip] 参考回答
> ローカル設定はゲームの<mark style="background: #FF5582A6;">進行状況</mark>とは<mark style="background: #FF5582A6;">別に保存し</mark>、書き込みに失敗した場合は、今回変更した設定値だけを元に戻します。

---

### 练习 12

> [!question] 题目
> 测试暂停功能时，请确认普通计时器停止，而使用独立时间的界面动画仍能播放。

> [!example] 自己的答案
>　ポーズ機能をテストする時に、通常のタイマーが止まったか確認し、独自時間での画面のアニメーションが再生し続けることを確認してください。

> [!warning] 指正
> 两类时间行为都已表达。说明测试规则时，`テストする時に` 改为 `テストするときは` 更自然。`止まったか` 是确认一次过去状态，此处用 `停止すること` 更合适；`独自時間での画面のアニメーション` 应自然表达为 `独立した時間を使うUIアニメーション`。

> [!tip] 参考回答
> ポーズ機能をテストするときは、通常のタイマーが停止する一方で、<mark style="background: #FF5582A6;">独立した時間を使う</mark>UIアニメーションは再生を続けられることを確認してください。

---

### 练习 13

> [!question] 题目
> 构建游戏之前，应检查道具标识是否重复、必需图标是否缺失，并把错误资源列入报告。

> [!example] 自己的答案
>　ゲームをパッケージ化する前に、アイテムタグが重複しているか、必須なアイコンが欠落しているかを確認すべきです。その後誤ったリソースをレポートします。

> [!warning] 指正
> 两项检查和输出报告的流程基本完整。“道具标识”应使用 `アイテムID`，`アイテムタグ` 可能表示分类标签。`必須なアイコン` 应改为 `必須のアイコン`；`誤ったリソースをレポートします` 含义模糊，应明确把有错误的资源列入报告。

> [!tip] 参考回答
> ゲームをビルドする前に、アイテムIDが重複していないか、必須のアイコンが欠けていないかを確認し、エラーのあるアセットをレポートに含めるべきです。

---

### 练习 14

> [!question] 题目
> 我订了七点的餐厅，但可能会晚到十分钟，能帮我先跟店里说一声吗？

> [!example] 自己的答案
>　七時のレストランを予約した、十分ぐらい遅れるかも、店に伝えてくれないか

> [!warning] 指正
> 基本意思可以理解，但多个分句不能只用逗号连接。`七時のレストランを予約した` 会变成“预约了七点的餐厅”，应说 `7時にレストランを予約している`。请求语气的 `くれないか` 略生硬，这里用 `伝えてもらえる？` 更自然。

> [!tip] 参考回答
> 7時にレストランを予約しているんだけど、10分くらい遅れるかもしれない。先にお店へ連絡してもらえる？

---

### 练习 15

> [!question] 题目
> 这个修改还需要设计师确认，我会在合入之前整理问题，并把确认结果发给大家。

> [!example] 自己的答案
>　この変更はデザイナーさんの確認が必要があります。マージする前に問題を整理して、確認結果を皆さんに共有します。

> [!warning] 指正
> 工作流程和共享对象都表达清楚。`確認が必要があります` 是语法错误，应为 `確認が必要です`，或使用动词结构 `確認してもらう必要があります`。`デザイナーさん` 会话中可用，正式职场说明中直接说 `デザイナー` 更简洁。

> [!tip] 参考回答
> この変更については、まだデザイナーに確認してもらう必要があります。マージする前に問題点を整理し、確認結果を皆さんに共有します。

## 2026-08-22

### 练习 1

> [!question] 题目
> 打开宝箱之前，请先确认钥匙数量和宝箱状态，成功切换到开启状态后再消耗钥匙。

> [!example] 自己的答案
>　宝箱を開ける前に、まず鍵の数とボックス状態を確認し、無事にオン状態に切り替えてから鍵を消費します。

> [!warning] 指正
> 确认和消耗的顺序基本正确。`ボックス状態` 不是宝箱状态，应说 `宝箱の状態`；`オン状態` 是开关处于开启，不能表达宝箱已经打开，宜使用 `開いた状態` 或 `開放状態`。`無事に` 可以理解，但技术说明中 `正常に` 更准确。

> [!tip] 参考回答
> 宝箱を開ける前に、まず鍵の数と宝箱の状態を確認し、正常に開いた状態へ切り替わってから鍵を消費します。

---

### 练习 2

> [!question] 题目
> 武器组件应从只读定义中取得基础属性，并把当前弹药量等运行时数据保存在实例中。

> [!example] 自己的答案
>　武器コンポーネントは定義から基礎属性を読み込むべきで、現在の弾薬数などランタイムデータをインスタンスに格納します。

> [!warning] 指正
> 基础数据与运行时数据的保存位置表达清楚，但遗漏了“只读”，应明确为 `読み取り専用の定義`。Gameplay 语境中的“基础属性”用 `基礎ステータス` 比 `基礎属性` 更自然。`〜べきで、〜します` 的连接略生硬，可统一为说明句。

> [!tip] 参考回答
> 武器コンポーネントは読み取り専用の定義から基礎ステータスを取得し、現在の弾薬数などのランタイムデータはインスタンスに保存するべきです。

---

### 练习 3

> [!question] 题目
> 如果 Unreal 中的反射边缘出现断裂，请比较材质粗糙度、反射方式和历史画面的稳定性。

> [!example] 自己的答案
>　Unrealで反射の端に隙が出現する場合は、マテリアルの、反射方法、履歴上の画面結果の安定性を比較してください。

> [!warning] 指正
> 反射方式这一项表达正确，但 `マテリアルの、` 后缺少“粗糙度”，应为 `マテリアルの粗さ`。`反射の端に隙が出現する` 可理解，更自然地说 `反射の境界が途切れる`。`履歴上の画面結果` 不自然，应使用 `履歴フレームの安定性`。

> [!tip] 参考回答
> Unrealで反射の境界が途切れる場合は、マテリアルの粗さ、反射方式、履歴フレームの安定性を比較してください。

---

### 练习 4

> [!question] 题目
> 如果 UI 布局每帧都重新计算，请记录被标记为无效的控件、状态变化来源和布局耗时。

> [!example] 自己的答案
>　毎フレームUIレイアウトを再計算する場合、マーカー付き無効なウィジェット、状態変化の由来、レイアウトのコストを記録してください。

> [!warning] 指正
> 三类记录对象能够看出，但前半应让布局作主体：`UIレイアウトが毎フレーム再計算される場合`。`マーカー付き無効なウィジェット` 不是“被标记为无效的控件”，应说 `無効とマークされたウィジェット`；`状態変化の由来` 宜改为 `発生元`，布局耗时则是 `処理時間`。

> [!tip] 参考回答
> UIレイアウトが毎フレーム再計算される場合は、無効とマークされたウィジェット、<mark style="background: #FF5582A6;">状態変化の発生元</mark>、レイアウト処理<mark style="background: #FF5582A6;">にかかった時間</mark>を記録してください。

---

### 练习 5

> [!question] 题目
> 预加载大型区域资源时，应设置内存预算，并在超过预算后优先释放低优先级资源。

> [!example] 自己的答案
>　大きなエリアリソースをロードする時に、メモリ予算を設定しておいて、予算オーバー後に低優先度リソースを先に解放します。

> [!warning] 指正
> 内存预算和释放优先级都表达正确，但题目是“预加载”，答案中的 `ロードする` 遗漏了“预先”的含义，应使用 `事前に読み込む`。`予算オーバー後に` 略生硬，条件表达用 `予算を超えた場合は` 更自然。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">大規模な</mark>エリアのリソースを事前に読み込むときは、メモリ予算を設定し、予算を超えた場合は<mark style="background: #FF5582A6;">優先度の低い</mark>リソースから解放します。

---

### 练习 6

> [!question] 题目
> 确认弹窗提交后，应暂时禁用按钮，等处理结果返回后再决定关闭弹窗还是显示错误。

> [!example] 自己的答案
>　ポップアップで確認した後、ボタンを無効化にするべきで、処理結果が帰ってきてからポップアップを閉じるかまたはエラーを表示するか決めます。

> [!warning] 指正
> 按处理结果决定后续行为的逻辑正确。`ポップアップで確認した後` 是“在弹窗中确认后”，题目强调提交，应说 `確認ポップアップを送信した後`。`無効化にする` 应改为 `無効にする`；结果“返回”用 `返ってくる`，不是表示回家的 `帰ってくる`。

> [!tip] 参考回答
> 確認ポップアップを送信した後は、ボタンを一時的に無効にし、処理結果が返ってきてから、ポップアップを閉じるかエラーを表示するかを決めます。

---

### 练习 7

> [!question] 题目
> 如果 Blend Space 中的移动速度不正确，请比较实际速度、输入方向和角色缩放值。

> [!example] 自己的答案
>　BlendSpaceでの移動が誤ったら、実際の速度、入力方向、キャラクターのスケールを比較してください。

> [!warning] 指正
> 三个比较对象都已覆盖。题目是 Blend Space 中的“移动速度”不正确，`移動が誤ったら` 变成移动行为错误，应明确为 `移動速度が正しくない場合`。条件表达中 `〜たら` 偏口语，调试说明使用 `場合は` 更合适。

> [!tip] 参考回答
> Blend Space内の移動速度が正しくない場合は、実際の速度、入力方向、キャラクターのスケール値を比較してください。

---

### 练习 8

> [!question] 题目
> 高速投射物穿过薄墙时，请检查连续碰撞检测、物理子步和碰撞体厚度。

> [!example] 自己的答案
> 高速なが壁をすり抜けた時に、連続コリジョン判定、物理演算ステップ、コリジョンの幅さを確認してください。

> [!warning] 指正
> 句首 `高速なが壁` 语法不成立，并且遗漏了投射物，应写成 `高速の投射物が薄い壁`。物理“子步”应使用 `物理サブステップ`，不是普通步骤；碰撞体厚度是 `コライダーの厚さ`，`幅さ` 也不是正确的名词形式。

> [!tip] 参考回答
> 高速の投射物が薄い壁をすり抜ける場合は、<mark style="background: #FF5582A6;">連続衝突判定</mark>、<mark style="background: #FF5582A6;">物理サブステップ</mark>、コライダーの厚さを確認してください。

---

### 练习 9

> [!question] 题目
> AI 丢失目标后，可以在限定时间内保留最后已知位置，超时后再清除追踪状态。

> [!example] 自己的答案
>　AIが目標を失った後に、一定時間内で既存の位置を残してタイムオーバー後追跡状態をクリアします。

> [!warning] 指正
> 保留后再清除的流程正确。`既存の位置` 是现有位置，不能表达“最后已知位置”，应说 `最後に確認された位置`。`一定時間内で` 的 `内` 多余，可用 `一定時間`；`タイムオーバー後` 也应补成 `タイムアウトした後に`。

> [!tip] 参考回答
> AIがターゲットを見失った後は、一定時間だけ最後に確認された位置を保持し、タイムアウトした後に追跡状態を解除します。

---

### 练习 10

> [!question] 题目
> 多段充能技能的恢复计时应由技能组件统一管理，界面只显示剩余层数和下一次恢复时间。

> [!example] 自己的答案
>　複数回チャージスキルの回復タイマーはスキルコンポーネントで一元的に管理し、画面は残りスタック数と次の回復時刻だけを表示します。

> [!warning] 指正
> 责任主体、剩余层数和恢复时刻都表达完整。`複数回チャージスキル` 容易理解为多次执行充能，表示多段充能更自然的是 `複数チャージ式のスキル`。`画面` 可用，但作为系统职责说明时使用 `UI` 更直接。

> [!tip] 参考回答
> 複数<mark style="background: #FF5582A6;">チャージ式</mark>のスキルでは、回復タイマーをスキルコンポーネントが一元的に管理し、UIは残りのチャージ数と次の回復時刻だけを表示します。

---

### 练习 11

> [!question] 题目
> 删除存档槽位成功后，还要移除对应的概要信息、截图和临时缓存。

> [!example] 自己的答案
>　スロットの削除に成功した後、それに対応する概要情報、スクリプト、一時的キャッシュを削除します。

> [!warning] 指正
> 删除相关数据的流程基本正确，但“截图”被误写成了表示脚本的 `スクリプト`，应为 `スクリーンショット`。题目的“存档槽位”也宜明确为 `セーブスロット`；`一時的キャッシュ` 可简化为 `一時キャッシュ`。

> [!tip] 参考回答
> セーブスロットの削除に成功した後は、対応する概要情報、スクリーンショット、一時キャッシュも削除します。

---

### 练习 12

> [!question] 题目
> 如果反复切换场景后内存持续增长，请比较对象数量、引用链和资源卸载的时间点。

> [!example] 自己的答案
>　シーンの切り替えを繰り返してメモリ使用量が増え続ける場合、オブジェクトの数、参照、リソースアンロード時刻を比較してください。

> [!warning] 指正
> 内存增长现象和对象数量都表达正确。题目要求比较“引用链”，答案只写 `参照`，应明确为 `参照チェーン`。资源卸载的“时间点”宜说 `タイミング`，`時刻` 更偏具体钟表时间。

> [!tip] 参考回答
> シーン切り替えを繰り返した後もメモリ使用量が増え続ける場合は、オブジェクト数、参照チェーン、リソースをアンロードするタイミングを比較してください。

---

### 练习 13

> [!question] 题目
> 测试道具转移时，如果目标背包空间不足，源背包的数量和占用状态都不能发生变化。

> [!example] 自己的答案
>　アイテムの移動をテストする時に、移動先のインベントリのスペースが足りない場合、元のインベントリの数と占有状態が変わらないようにします。

> [!warning] 指正
> 目标背包空间不足和源背包状态都表达出来了。说明测试规则时，`テストする時に` 改为 `テストするときは` 更自然。“数量”应明确为 `アイテム数`；测试的目标是确认不发生变化，因此 `変わらないようにします` 应改为 `変化しないことを確認します`。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">アイテム転送</mark>をテストするときは、転送先のインベントリに空きがない場合でも、転送元のアイテム数とセルの占有状態が変化しないことを確認します。

---

### 练习 14

> [!question] 题目
> 手机快没电了，你带充电器了吗？如果方便的话，可以借我用一会儿吗？

> [!example] 自己的答案
>　スマホの電量が切れそう、充電器を持っているか？良ければ、ちょっと貸してくれませんか

> [!warning] 指正
> 请求内容能够理解，但手机电量应说 `バッテリー残量` 或 `充電`，不能使用中文式的 `電量`。`持っているか？` 与后面的礼貌语气不一致；日常会话可统一使用 `持ってる？` 和 `貸してくれない？`。句末还需标点。

> [!tip] 参考回答
> スマホの<mark style="background: #FF5582A6;">充電</mark>が切れそうなんだけど、充電器持ってる？よかったら、少しだけ貸してくれない？

---

### 练习 15

> [!question] 题目
> 我已经补充了复现步骤和日志，发布前可以请你只检查这次修改的部分吗？

> [!example] 自己的答案
>　再現方法とログをすでに補足しました。リリースする前に今回に変更された部分だけを確認していただけませんか

> [!warning] 指正
> 礼貌请求和检查范围都表达正确。技术文档中的“复现步骤”通常说 `再現手順`，不是 `再現方法`；“补充”这里用 `追記しました` 更自然。`今回に変更された部分` 助词错误，应改为 `今回変更した部分`，句末需要问号。

> [!tip] 参考回答
> 再現手順とログを追記しました。リリース前に、今回変更した部分だけを確認していただけますか？

## 2026-08-23

### 练习 1

> [!question] 题目
> 使用消耗品之前，请先确认冷却和角色状态，效果成功应用后再减少道具数量。

> [!example] 自己的答案
>　消耗品を使用する前に、まずクールダウンとキャラクターのステータスを確認し、効果を応用してからアイテム数を減らします。

> [!warning] 指正
> 确认后再减少数量的顺序正确。效果应用应使用 `適用する`，`応用する` 表示活用知识或方法。题目强调“成功应用后”，还需补充 `正常に適用された後で`；`キャラクターのステータス` 可以理解，若指状态条件则 `キャラクターの状態` 更贴切。

> [!tip] 参考回答
> 消耗品を使用する前に、まずクールダウンとキャラクターの状態を確認し、<mark style="background: #FF5582A6;">効果が正常に適用された</mark>後でアイテム数を減らします。

---

### 练习 2

> [!question] 题目
> 护盾不足以吸收全部伤害时，应把溢出的部分传给生命值，并只向界面发送最终结果。

> [!example] 自己的答案
>　シールドがダメージを全部解消できない場合、残り部分をHPに渡し、UIに最後の結果を通知します。

> [!warning] 指正
> 把剩余伤害传给 HP 的逻辑能够理解。“吸收伤害”应说 `ダメージを吸収する`，`解消する` 不适合护盾；溢出的部分宜说 `超過分`，不是较泛的 `残り部分`。最后应明确 UI 只收到 `最終結果だけ`。

> [!tip] 参考回答
> シールドですべてのダメージを<mark style="background: #FF5582A6;">吸収できない</mark>場合は、<mark style="background: #FF5582A6;">超過分</mark>をHPに適用し、UIには最終結果だけを通知します。

---

### 练习 3

> [!question] 题目
> 如果 Unreal 的体积雾出现闪烁，请比较时间重投影、光源移动和采样质量。

> [!example] 自己的答案
>　Unrealのボリュームフォグがちらつく場合、二重シャドウ、光源の移動、サンプル品質を比較してください。

> [!warning] 指正
> 光源移动和采样质量表达出来了，但“时间重投影”被误译为 `二重シャドウ`。正确术语是 `時間的再投影` 或 `テンポラルリプロジェクション`。体积雾更常写作 `ボリュメトリックフォグ`，采样质量可说 `サンプリング品質`。

> [!tip] 参考回答
> Unrealのボリュメトリックフォグがちらつく場合は、時間的再投影、光源の移動、サンプリング品質を比較してください。

---

### 练习 4

> [!question] 题目
> 使用手柄浏览网格菜单时，焦点应跳过隐藏或禁用的按钮，并在返回页面后恢复到原来的项目。

> [!example] 自己的答案
>　ゲームパッドでメニューを操作する時に、フォーカスは隠れるボタンと禁止ボタンをスキップするべきです。元の画面に戻ってきてから元のオプションに復元します。

> [!warning] 指正
> 跳过按钮和恢复焦点的流程基本完整。隐藏、禁用按钮应分别说 `非表示のボタン`、`無効なボタン`，不能用 `隠れる` 和表示禁止的 `禁止`。`元の画面に戻ってきてから` 不自然，应为 `ページに戻った後`；恢复的是焦点，因此要明确 `フォーカスを元の項目に戻す`。

> [!tip] 参考回答
> ゲームパッドでグリッドメニューを操作するときは、非表示または無効なボタンをフォーカス対象から除外し、ページに戻った後はフォーカスを元の項目に戻します。

---

### 练习 5

> [!question] 题目
> 快速切换外观预览时，应取消上一次的加载任务，并释放已经不再需要的资源句柄。

> [!example] 自己的答案
>　外見プレビューを素早く切り替える時に、前回のロードタスクをキャンセルスべきで、その上で不要になったリソースハンドルを解放します。

> [!warning] 指正
> 取消任务和释放句柄都准确表达出来了。`キャンセルスべき` 中的 `ス` 字形错误，应为 `キャンセルするべき`。说明通用规则时，`切り替える時に` 改为 `切り替えるときは`；`〜べきで、その上で` 可简化为并列动作。

> [!tip] 参考回答
> 外見プレビューを素早く切り替えるときは、前回の読み込みタスクをキャンセルし、不要になったリソースハンドルを解放するべきです。

---

### 练习 6

> [!question] 题目
> 为了减少频繁查找组件的成本，可以在初始化时缓存稳定引用，并在复用前检查对象是否仍然有效。

> [!example] 自己的答案
>　コンポーネントの検索コストを減らしすために、初期化する時に参照をキャッシュしておいて、オブジェクトを再利用する前に有効性を確認します。

> [!warning] 指正
> 缓存后验证的顺序正确。`減らしすために` 是拼写错误，应为 `減らすために`。题目强调“稳定引用”，答案只写 `参照`，应补为 `安定した参照`；对象是否仍有效可自然表达为 `オブジェクトがまだ有効か確認する`。

> [!tip] 参考回答
> コンポーネントを頻繁に検索するコストを減らすために、初期化時に安定した参照をキャッシュし、再利用する前にオブジェクトがまだ有効か確認します。

---

### 练习 7

> [!question] 题目
> 原地转身动画被移动输入中断时，应清除旋转偏移，并立即切换回移动状态。

> [!example] 自己的答案
>　その場ターンアニメーションが移動入力によって中断された場合、回転オフセットをクリアしてすぐ移動状態に切り替えるべきです。

> [!warning] 指正
> 中断条件、清除偏移和切回移动状态都表达准确。`その場ターンアニメーション` 可以理解，游戏动画语境中也常用 `ターンインプレースアニメーション`。`すぐ` 没有问题，技术说明中 `即座に` 稍正式。

> [!tip] 参考回答
> ターンインプレースアニメーションが移動入力によって中断された場合は、回転オフセットをクリアし、<mark style="background: #FF5582A6;">即座に</mark>移動状態へ切り替えるべきです。

---

### 练习 8

> [!question] 题目
> 如果物理约束过早断开，请记录冲量、断裂阈值和对象缩放值，再比较它们是否匹配。

> [!example] 自己的答案
>　物理的制限が想定より速く切れた場合、まず切れる閾値、オブジェクトのスケールを記録して、マッチしていないかを比較します。

> [!warning] 指正
> 记录内容遗漏了“冲量”，必须补充 `力積`。物理约束是 `物理制約`，不是泛指限制的 `物理的制限`；“过早断开”宜说 `早く切れる`。题目要求比较是否匹配，应使用 `一致しているか`，而不是只检查不匹配的 `マッチしていないか`。

> [!tip] 参考回答
> 物理制約が想定より早く切れる場合は、<mark style="background: #FF5582A6;">力積</mark>、<mark style="background: #FF5582A6;">破壊閾値</mark>、オブジェクトのスケール値を記録し、それらが一致しているか比較してください。

---

### 练习 9

> [!question] 题目
> AI 的巡逻点被阻挡时，可以暂时标记为不可用，选择备用点，并在等待一段时间后重试。

> [!example] 自己的答案
>　AIの巡回ポイントがブロックされる場合、利用不可能をつけて、他のポイントを探索して、一定時間後にリトライします。

> [!warning] 指正
> 选择其他点和等待后重试的流程正确。`利用不可能をつける` 不自然，应说 `一時的に利用不可としてマークする`。题目是选择“备用点”，`他のポイントを探索する` 可理解，但 `代替ポイントを選ぶ` 更准确。

> [!tip] 参考回答
> AIの巡回ポイントが<mark style="background: #FF5582A6;">塞がれている</mark>場合は、その地点を一時的に利用不可としてマークし、代替ポイントを選んで、一定時間待ってから再試行します。

---

### 练习 10

> [!question] 题目
> 检查点只保存恢复所需的紧凑快照，道具应记录稳定标识，而不是保存对象引用。

> [!example] 自己的答案
>　チェックポイントでは最小限復元スナップだけを保存し、アイテム参照を保存するではなく、アイテムIDを記録するべきです。

> [!warning] 指正
> 保存快照和使用道具 ID 的方向正确。`最小限復元スナップ` 的名词连接不自然，应为 `復元に必要な最小限のスナップショット`。`保存するではなく` 缺少名词化的 `の`；题目还强调不能保存的是 `オブジェクト参照`，不只是一般道具参照。

> [!tip] 参考回答
> チェックポイントには<mark style="background: #FF5582A6;">復元に必要な最小限の</mark>スナップショットだけを保存し、アイテムはオブジェクト参照ではなく、安定したIDを記録するべきです。

---

### 练习 11

> [!question] 题目
> 如果输入动作没有触发，请比较输入环境的优先级、动作绑定和当前焦点是否消耗了输入。

> [!example] 自己的答案
> 入力したアクションが発火しない場合、入力優先度、アクションバインディングを比較して、現在のフォーカスが今回の入力を処理したかを確認してください。

> [!warning] 指正
> 动作绑定和当前焦点都已覆盖，但“输入环境的优先级”应明确为 `入力コンテキストの優先度`。`入力したアクション` 是“输入了的动作”，系统术语应直接使用 `入力アクション`。题目的“消耗输入”也应说 `入力を消費したか`，不能仅说处理了输入。

> [!tip] 参考回答
> 入力アクションが発火しない場合は、入力コンテキストの優先度、アクションのバインディング、現在のフォーカスが入力を消費したかどうかを比較してください。

---

### 练习 12

> [!question] 题目
> Unity 的资源包体积异常增大时，请检查重复依赖、压缩设置和被多个资源包包含的文件。

> [!example] 自己的答案
>　Unityのリソースパッケージが想定以上肥大化した場合、重複の依存、圧縮設定と複数のパッケージに参照されたファイルを確認してください。

> [!warning] 指正
> 压缩设置和多包引用的方向可以理解。Unity 语境中的资源包通常直接说 `AssetBundle`。`重複の依存` 应改为 `重複した依存関係`；题目是被多个资源包“包含”，应使用 `複数のAssetBundleに含まれているファイル`，不是只表示参照。

> [!tip] 参考回答
> UnityのAssetBundleが想定以上に大きくなった場合は、重複した依存関係、圧縮設定、複数のAssetBundleに含まれているファイルを確認してください。

---

### 练习 13

> [!question] 题目
> 测试 Boss 阶段切换时，即使短时间内连续受到多次伤害，阶段进入处理也只能执行一次。

> [!example] 自己的答案
>　ボスのフェーズ切り替えをテストする時に、短時間でダメージを連続して受け取っても、フェーズに入る処理を一度だけ実行します。

> [!warning] 指正
> 短时间连续受伤和只执行一次都表达出来了。说明测试规则时，`テストする時に` 改为 `テストするときは`。角色受到伤害应使用 `ダメージを受ける`，`受け取る` 偏向接收物品或信息；测试句还应明确“确认只执行一次”。

> [!tip] 参考回答
> ボスのフェーズ切り替えをテストするときは、短時間に連続してダメージを受けた場合でも、フェーズ移行処理が一度しか実行されないことを確認します。

---

### 练习 14

> [!question] 题目
> 衣服刚晾出去就开始下雨了，你能帮我把阳台上的衣服收进来吗？

> [!example] 自己的答案
>　洗濯物を乾かしたばかりのに雨が降り始まります。ベランダでの洗濯物を取り戻してくれませんか。

> [!warning] 指正
> 下雨和请求帮忙的意思能够理解，但“晾衣服”是 `洗濯物を干す`，`乾かす` 表示使其干燥。`ばかりのに` 应为 `ばかりなのに`；当前已经开始下雨应用过去形 `降り始めた`。把衣服收进来是 `取り込む`，不是取回的 `取り戻す`。

> [!tip] 参考回答
> 洗濯物を<mark style="background: #FF5582A6;">干した</mark>ばかりなのに、雨が降り始めた。ベランダの洗濯物を<mark style="background: #FF5582A6;">取り込ん</mark>でくれない？

---

### 练习 15

> [!question] 题目
> 我已经更新了会议记录，如果中午之前没有异议，就按这个计划继续推进。

> [!example] 自己的答案
>　議事録をすでに更新しました。午後前に意義なし場合には、そのプランで進みます。

> [!warning] 指正
> 更新记录和继续推进的流程基本正确。中午之前是 `正午までに`，不是 `午後前に`。“异议”应写作 `異議`，`意義` 表示意义；`意義なし場合` 还缺少助词，应为 `異議がない場合`。工作计划通常使用 `計画`，比 `プラン` 更正式。

> [!tip] 参考回答
> 議事録を更新しました。正午までに異議がなければ、この計画で引き続き進めます。
