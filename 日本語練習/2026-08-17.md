## 2026-08-17

### 练习 1

> [!question] 题目
> 在 Unreal 中管理可叠加的状态效果时，应由效果组件统一判断层数上限、持续时间刷新和移除条件。

> [!example] 自己的答案
> Unrealでは重なり状態エフェクトを管理する時に、エフェクトコンポーネントで重なり上限、持続時間の更新、削除条件を一元的に判断するべきです。

> [!warning] 指正
> 意思基本清楚。`重なり状態エフェクト` 不自然，可改为 `スタック可能なステータス効果`；“层数上限”宜说 `スタック上限`。说明通用规则时，`管理する時に` 改为 `管理するときは` 更自然。

> [!tip] 参考回答
> Unrealでスタック可能なステータス効果を管理するときは、エフェクトコンポーネントがスタック上限、持続時間の更新、削除条件を一元的に判断するべきです。

---

### 练习 2

> [!question] 题目
> 菜单输入应先由输入层转换成操作事件，再由当前打开的 UI 决定如何处理该事件。

> [!example] 自己的答案
>　メニューの入力はまず入力階層で入力を操作イベントに変換し、その後、処理を開かれているUIで決めます。

> [!warning] 指正
> 前半意思正确，但 `入力階層` 更自然地说 `入力層`。后半 `処理を開かれているUIで決めます` 语序和助词不自然，应明确为“当前打开的 UI 决定如何处理该事件”。

> [!tip] 参考回答
> メニュー入力は、まず入力層で操作イベントに変換し、その後、現在開いているUIがそのイベントをどのように処理するかを決定します。

---

### 练习 3

> [!question] 题目
> 释放蓄力技能之前，请先确认资源消耗和目标条件，全部满足后再生成攻击效果。

> [!example] 自己的答案
>　力溜めスキルを発動する前に、まずリソース消費とターゲット条件を確認し、全て条件を満たしてから攻撃エフェクトを生成します。

> [!warning] 指正
> 处理顺序表达得很清楚。`力溜めスキル` 可以理解，但游戏语境中 `チャージスキル` 更常见；`全て条件` 缺少助词，应为 `全ての条件`。题目的“全部满足”也可明确为 `両方の条件を満たした後で`。

> [!tip] 参考回答
> チャージスキルを発動する前に、まずリソース消費とターゲット条件を確認し、両方の条件を満たした後で攻撃エフェクトを生成します。

---

### 练习 4

> [!question] 题目
> 如果 Unreal 场景中的纹理突然变模糊，请记录纹理流送池的使用量、所需显存和问题出现的时间点。

> [!example] 自己的答案
>　Unrealシーンでテクスチャが突然にボヤしする場合、テクスチャストリーミングの使用量、GPUメモリ、問題が出るタイミングを記録してください。

> [!warning] 指正
> 调试方向基本传达出来了。`突然にボヤしする` 不自然，应使用自动词 `突然ぼやける`。`テクスチャストリーミングの使用量` 应明确为流送池的使用量；`GPUメモリ` 还遗漏了“所需”的含义。

> [!tip] 参考回答
> Unrealのシーンでテクスチャが突然ぼやける場合は、テクスチャストリーミングプールの使用量、必要なGPUメモリ、問題が発生した時点を記録してください。

---

### 练习 5

> [!question] 题目
> Unity 的异步资源加载被取消时，也要清理等待中的任务并释放已经取得的资源。

> [!example] 自己的答案
>　Unityの非同期のリソースロードがキャンセルされた時に、待ちタスクをクリアしてロード済みリソースを解放します。

> [!warning] 指正
> 题意表达完整。`非同期のリソースロード` 可简化为 `非同期リソース読み込み`，`待ちタスク` 更自然地说 `待機中のタスク`。说明取消时必须执行的规则，用 `キャンセルされたときは` 更合适。

> [!tip] 参考回答
> Unityの非同期リソース読み込みがキャンセルされたときは、待機中のタスクをクリーンアップし、すでに取得したリソースも解放する必要があります。

---

### 练习 6

> [!question] 题目
> 读取跨越多个版本的存档时，应按版本顺序执行迁移，并在每一步保存验证结果。

> [!example] 自己的答案
>　複数のバージョンに渡ってセーブデータを読み込む時に、バージョン順に移行を行い、ステップごとに検証結果を保存します。

> [!warning] 指正
> 迁移顺序和保存验证结果都表达出来了。`複数のバージョンに渡ってセーブデータを読み込む` 略生硬，建议说 `複数のバージョンをまたぐセーブデータを読み込む`。通用规则仍宜使用 `〜するときは`。

> [!tip] 参考回答
> 複数のバージョンをまたぐセーブデータを読み込むときは、バージョン順に移行処理を実行し、各ステップで検証結果を保存します。

---

### 练习 7

> [!question] 题目
> 如果攻击判定比动画提前出现，请比较动画通知的触发帧、判定框的启用时间和播放速率。

> [!example] 自己的答案
>　攻撃判定がアニメーションより早めに発生する場合、Anim Notifyの発火、判定有効タイミング、再生倍速を比較してください。

> [!warning] 指正
> 排查对象基本正确。`早めに発生する` 是“较早发生”，此处应说 `アニメーションより先に発生する`。`Anim Notifyの発火` 遗漏了“触发帧”，`判定有効タイミング` 也应明确是判定框的启用时间；`再生速度` 比 `再生倍速` 更自然。

> [!tip] 参考回答
> 攻撃判定がアニメーションより先に発生する場合は、Anim Notifyの発火フレーム、判定ボックスが有効になるタイミング、アニメーションの再生速度を比較してください。

---

### 练习 8

> [!question] 题目
> 角色从布娃娃状态恢复时，需要先停止物理模拟，再校正角色位置，最后切换到起身动画。

> [!example] 自己的答案
>　キャラクターがRogDollから復元する時に、まず物理演算を停止し、その後キャラクターの位置を修正し、最終に立ち上がるアニメーションを切り替えます。

> [!warning] 指正
> 三个步骤都已表达，但术语和搭配需要调整。`RogDoll` 应为 `Ragdoll`，从该状态恢复宜说 `復帰する`；`最終に` 应改为 `最後に`。切换到某动画要用 `起き上がりアニメーションに切り替える`。

> [!tip] 参考回答
> キャラクターがRagdoll状態から復帰するときは、まず物理シミュレーションを停止し、次にキャラクターの位置を補正して、最後に起き上がりアニメーションへ切り替えます。

---

### 练习 9

> [!question] 题目
> 为了减少战斗中的内存分配，可以复用查询结果容器，并避免在每帧循环里创建临时列表。

> [!example] 自己的答案
>　戦闘中にメモリの割り当てを減らすために、絞り込み結果を再利用できるように、毎フレーム一時的リストを繰り返して生成することを防ぎます。

> [!warning] 指正
> 减少临时列表的方向正确，但 `絞り込み結果` 没有准确表达“查询结果容器”，建议使用 `クエリ結果用のコンテナ`。`生成することを防ぐ` 较生硬，这里用 `生成を避ける` 更自然。

> [!tip] 参考回答
> 戦闘中のメモリ割り当てを減らすために、クエリ結果用のコンテナを再利用し、毎フレームのループ内で一時リストを生成するのを避けます。

---

### 练习 10

> [!question] 题目
> 显示道具提示框时，应根据屏幕边缘调整位置，同时保持手柄焦点不被提示框夺走。

> [!example] 自己的答案
>　アイテムのヒントを表示するときに、モニターの端に応じて位置を調整し、ゲームパッドのフォーカスがヒントに遷移しないようにします。

> [!warning] 指正
> 意思基本准确。UI 语境中的“提示框”通常说 `ツールチップ`，`モニターの端` 应改为 `画面の端`。题目强调焦点“不被夺走”，用 `フォーカスを奪われないようにする` 比 `遷移しない` 更贴切。

> [!tip] 参考回答
> アイテムのツールチップを表示するときは、画面の端に応じて位置を調整し、ゲームパッドのフォーカスがツールチップに奪われないようにします。

---

### 练习 11

> [!question] 题目
> 使用命令模式实现撤销功能时，每个操作都要保存恢复上一状态所需的最小数据。

> [!example] 自己的答案
>　コマンドパターンでキャンセル機能を実装する時に、操作ごとに前回の状態に戻せる最小限データを保存します。

> [!warning] 指正
> 设计意图基本清楚，但“撤销”不是 `キャンセル`，应使用 `元に戻す機能` 或 `取り消し機能`。`最小限データ` 缺少助词 `の`；恢复的是操作前的状态，用 `直前の状態` 更准确。

> [!tip] 参考回答
> コマンドパターンで元に戻す機能を実装するときは、各操作で直前の状態を復元するために必要な最小限のデータを保存します。

---

### 练习 12

> [!question] 题目
> 测试敌人的决策逻辑时，请固定初始状态和随机数种子，并比较每一步选择是否一致。

> [!example] 自己的答案
>　敵の決定ロジックをテストする時に、初期状態と乱数のシードを固定してください。各ステップの選択が一致しているかを比較してください。

> [!warning] 指正
> 测试条件与比较方法都表达完整。`決定ロジック` 可以理解，但表示 AI 的“决策逻辑”时，`意思決定ロジック` 更自然。两句也可以连接起来，使测试流程更紧凑。

> [!tip] 参考回答
> 敵の意思決定ロジックをテストするときは、初期状態と乱数シードを固定し、各ステップでの選択が一致しているか比較してください。

---

### 练习 13

> [!question] 题目
> 战斗日志只负责记录结构化事件，调试界面再根据筛选条件决定显示哪些内容。

> [!example] 自己的答案
>　バトルログはイベントだけを記録し、デバッグ画面は絞り込み条件に応じて内容を表示します。

> [!warning] 指正
> Gameplay 与调试界面的责任边界表达正确，但 `イベントだけ` 遗漏了“结构化”的含义，应说 `構造化されたイベントだけ`。后半可补出“决定显示哪些内容”，使职责描述更完整。

> [!tip] 参考回答
> バトルログは構造化されたイベントの記録だけを担当し、デバッグ画面はフィルター条件に基づいて、どの内容を表示するかを決定します。

---

### 练习 14

> [!question] 题目
> 这家店周末总是排很长的队，我们早点出门怎么样？

> [!example] 自己的答案
>　この店は週末にいつも長く並んでいる、早めに出かけるどうですか。

> [!warning] 指正
> 意图能理解，但 `この店は〜並んでいる` 变成了“店在排队”，应说 `この店はいつも長い行列ができる`。两个分句不能只用逗号连接；`出かけるどうですか` 应为 `出かけるのはどうですか`。

> [!tip] 参考回答
> この店は週末になるといつも長い行列ができるから、少し早めに出かけるのはどう？

---

### 练习 15

> [!question] 题目
> 目前的修改可能影响测试进度，我整理完风险后，想请大家一起确认优先级。

> [!example] 自己的答案
>　今回の修正が進捗に影響するので、リスクをまとめてから皆さんに優先度を確認していただきます。

> [!warning] 指正
> 职场语气基本成立，但遗漏了“测试进度”，应明确为 `テストの進捗`。`影響するので` 表示确定会影响，而题目是“可能影响”，应使用 `影響する可能性があります`。`確認していただきます` 也没有表达“一起确认”的请求。

> [!tip] 参考回答
> 現在の修正がテストの進捗に影響する可能性があります。リスクを整理した後で、優先順位を皆さんと一緒に確認したいです。
