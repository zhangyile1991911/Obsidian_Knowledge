
> [!question] 题目  
> 为了减少远处树木在 LOD 切换时的闪烁，请让不同 LOD 之间的颜色和法线尽量保持一致。

> [!example] 我的回答  
> 遠くの木がLOD切り替え時のチラつきを軽減するため、異なるLODの間で色とノーマルマップを一致するようにしてください

> [!warning] 指正  
> 「遠くの木がチラつきを軽減する」会变成“远处的树木自己减少闪烁”，主语不自然。  
> 「色とノーマルマップを一致する」也不自然，中文是“颜色和法线”，不一定是 Normal Map，因此这里用「法線」更准确。  
> 「一致する」用于自动词时可以说「色と法線が一致する」，如果是人为调整，推荐用「揃える」或「一致させる」。

> [!tips] 推荐回答  
> 遠くの木がLOD切り替え時にチラつかないように、異なるLOD間で色と法線をできるだけ揃えてください。
^tech-0619-lod

---

> [!question] 题目  
> 如果 Shader 关键字发生变化，需要重新生成编译缓存，否则旧缓存可能会导致错误的渲染结果。

> [!example] 我的回答  
> シェーダーのキーワードが変化した場合、古いキャッシュによる間違った描画結果にならないように、コンパイルキャッシュを再生成する必要がある

> [!warning] 指正  
> 这句整体不错，意思准确。  
> 「変化した場合」可以理解，但开发文档里「変更された場合」更自然。  
> 「間違った描画結果」也能懂，不过技术文档中更常用「誤った描画結果」或「不正な描画結果」。

> [!tips] 推荐回答  
> シェーダーキーワードが変更された場合は、古いキャッシュによって<mark style="background: #FF5582A6;">誤った</mark>描画結果にならないように、コンパイルキャッシュを再生成する必要があります。

---

> [!question] 题目  
> 客户端预测产生的位置偏差不能一次性强行修正，否则角色会出现明显的瞬移感。

> [!example] 我的回答  
> クライアントの予測による位置ズレを一回強制に補正しないように、キャラクターが目立って一瞬で移動してしまいます

> [!warning] 指正  
> 这句的主要问题是逻辑关系。  
> 中文是“如果一次性强行修正，就会出现瞬移感，所以不要这样做”。  
> 日语里应该用「〜すると、〜してしまうため、〜しないでください / 避けてください」。  
> 「一回強制に補正」不自然，应改为「一度に強制的に補正する」或「一括で強制補正する」。  
> 「目立って一瞬で移動」也不够自然，技术表现上可以说「明らかなワープ感が出る」。

> [!tips] 推荐回答  
> クライアント予測による位置ズレを一度に強制的に補正すると、キャラクターに明らかなワープ感が出てしまうため、避けてください。
^tech-0619-client-prediction

---

> [!question] 题目  
> 请不要在按钮点击事件里直接写装备切换逻辑，而是通过控制器层调用对应的业务逻辑。

> [!example] 我的回答  
> ボタンクリックイベント内で装備の切り替えをハードコードで実装しないように、コントローラー経由で業務ロジックを呼び出す

> [!warning] 指正  
> 「ハードコード」不是很准确。  
> 中文的“直接写逻辑”不是“写死常量或字符串”，所以更自然的是「直接実装する」「直接書く」「直接記述する」。  
> 「業務ロジック」在企业系统中常见，但游戏开发文脉里稍硬。这里可以说「対応する処理」或「ビジネスロジック」。  
> 「〜しないように、〜呼び出す」也略不自然，推荐用「〜せず、〜してください」。

> [!tips] 推荐回答  
> ボタンクリックイベント内に装備切り替えロジックを直接実装せず、コントローラー層を経由して対応する処理を呼び出してください。
^tech-0619-controller-layer

---

> [!question] 题目  
> 释放战斗场景资源之前，需要确认没有仍在播放的动画或音效引用这些资源。

> [!example] 我的回答  
> バトルシーンリソースをアンロードする前に、再生中のアニメションと効果音がこのシーンに依存するリソースを参照していないことを確認する必要があります

> [!warning] 指正  
> 「アニメション」拼写错误，正确是「アニメーション」。  
> 「アニメーションと効果音」不是错，但这里表示“动画或音效”，用「アニメーションや効果音」更自然。  
> 「このシーンに依存するリソース」意思偏了，中文是“这些资源/目标资源”，不是“依赖这个场景的资源”。  
> 这里推荐用「対象リソース」来表达“当前要释放的资源”。

> [!tips] 推荐回答  
> バトルシーンのリソースを解放する前に、再生中のアニメーションや効果音が対象リソースを参照していないことを確認してください。
^tech-0619-resource-release

---

> [!question] 题目  
> 当多个技能同时触发时，粒子数量会在短时间内急剧增加，所以需要限制同屏显示的特效数量。

> [!example] 我的回答  
> 複数のスキルが同時に発火する場合に、パーティクル数が短時間内で急に増えるため、同時にエフェクト数を制限する必要があります。

> [!warning] 指正  
> 「発火」在程序事件里可以用，但技能触发更自然是「発動する」。  
> 「短時間内で」不自然，推荐「短時間で」或「短時間のうちに」。  
> 「同時にエフェクト数」不对，容易被理解成“同时进行限制”。  
> “同屏显示的特效数量”应该说「画面内に同時表示されるエフェクト数」。

> [!tips] 推荐回答  
> 複数のスキルが同時に発動すると、短時間でパーティクル数が急増するため、画面内に同時表示されるエフェクト数を制限する必要があります。
^tech-0619-particle-limit

---

> [!question] 题目  
> 小地图缩放时，只应该改变地图背景的缩放比例，图标大小和文字大小需要保持不变。

> [!example] 我的回答  
> ミニマップをズームする時に、マップ背景だけをズームし、アイコンと文字サイズを変更しないようにする

> [!warning] 指正  
> 意思基本正确。  
> 「ズームする時に」可以更简洁地写成「ズーム時は」。  
> 「アイコンと文字サイズ」稍微不自然，应该说「アイコンサイズと文字サイズ」或「アイコンと文字のサイズ」。  
> 技术文档中，“保持不变”推荐用「固定する」。

> [!tips] 推荐回答  
> ミニマップのズーム時は、マップ背景のスケールのみを変更し、アイコンサイズと文字サイズは固定してください。

---

> [!question] 题目  
> 存档写入是异步执行的，所以在写入完成之前不要刷新存档列表。

> [!example] 我的回答  
> セーブデータが非同期で実行されているため、書き込みが完了する前にリストを更新しないようにする

> [!warning] 指正  
> 「セーブデータが非同期で実行される」不自然。  
> 被执行的不是“存档数据”，而是“写入处理”或“存档处理”。  
> 应该说「セーブデータの書き込みは非同期で実行される」或「セーブ処理は非同期で行われる」。  
> 「リスト」也最好具体写成「セーブリスト」「セーブスロット一覧」等。

> [!tips] 推荐回答  
> セーブ処理は非同期で行われるため、書き込み完了前にセーブスロット一覧を更新しないでください。
^tech-0619-async-save

---

> [!question] 题目  
> 如果两个动画的 Root Motion 轨迹差异过大，混合过程中角色位置可能会出现不自然的回退。

> [!example] 我的回答  
> 二つアニメションのRootMotionの軌跡差が大きすぎると、ブランド中にキャラクターの位置が不自然にロールバックする可能性がある

> [!warning] 指正  
> 「二つアニメション」应为「二つのアニメーション」。  
> 「ブランド中」是拼写错误，正确是「ブレンド中」。  
> 「RootMotion」推荐写成「Root Motion」。  
> 「ロールバック」在网络同步或事务处理中更常见，这里描述角色位置表现时，用「戻る」或「巻き戻る」更自然。

> [!tips] 推荐回答  
> 二つのアニメーション間でRoot Motionの軌跡差が大きすぎると、ブレンド中にキャラクター位置が不自然に巻き戻る可能性があります。
^tech-0619-root-motion

---

> [!question] 题目  
> 进入伤害结算子流程时，主状态机应该暂停，等子流程结束后再返回原来的状态。

> [!example] 我的回答  
> ダメージ計算サブステートに入る際に、メインステートが一時的に停止し、サブステートが完了後に元のステートに復帰する

> [!warning] 指正  
> 整体意思不错。  
> 但“主状态机”应该是「メインステートマシン」，不是「メインステート」。  
> “伤害结算子流程”比起「ダメージ計算サブステート」，更推荐「ダメージ処理サブフロー」或「ダメージ解決サブフロー」。  
> 「完了後に」可以用，但「完了してから」更顺。

> [!tips] 推荐回答  
> ダメージ処理サブフローに入る際は、メインステートマシンを一時停止し、サブフローが完了してから元のステートに復帰します。
^tech-0619-state-machine
