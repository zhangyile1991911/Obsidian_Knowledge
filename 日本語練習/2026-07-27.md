## 2026-07-27

### 练习 1

> [!question] 题目
> 在 Unreal 中实现锁定目标时，应该把目标选择逻辑和镜头旋转逻辑分开管理。

> [!example] 自己的答案
>　Unrealではターゲットをロックする時に、ターゲットの選択とカメラ回転ロジックを分けて管理するべきです。

> [!warning] 指正
> 意思完整，表达也自然。`カメラ回転ロジック` 能懂，但技术说明里更自然是 `カメラの回転ロジック`。

> [!tip] 参考回答
> Unrealでターゲットロックを実装するときは、ターゲット選択ロジックとカメラの回転ロジックを分けて管理するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的对象池归还子弹时，需要重置速度、生命周期和碰撞状态。

> [!example] 自己的答案
>　Unityではオブジェクトプールに弾を返却する時に、速度、ライフサイクルとコリジョンをリセットする必要があります

> [!warning] 指正
> 主要意思正确。并列时 `速度、ライフサイクルとコリジョン` 建议改为 `速度、生存時間、コリジョン状態`

> [!tip] 参考回答
> Unityのオブジェクトプールに弾を返却するときは、速度、生存時間、コリジョン状態をリセットする必要があります。

---

### 练习 3

> [!question] 题目
> 如果角色的受击动画被打断，请先确认状态机条件和动画层权重。

> [!example] 自己的答案
>　キャラクターの被弾アニメーションが中断された場合、まずステートマーシンの条件とアニメーション層の重みを確認します。

> [!warning] 指正
> 意思清楚，但 `ステートマーシン` 应为 `ステートマシン`。`アニメーション層の重み` 可以理解，更常见说法是 `アニメーションレイヤーのウェイト`。

> [!tip] 参考回答
> キャラクターの被弾アニメーションが中断された場合は、まずステートマシンの条件と<mark style="background: #FF5582A6;">アニメーションレイヤーのウェイト</mark>を確認してください。

---

### 练习 4

> [!question] 题目
> 在设计技能队列时，输入缓冲区只保存短时间内有效的操作。

> [!example] 自己的答案
>　スキルキューを設計する時に、入力バッファーでは短時間内の有効操作だけを保持します。

> [!warning] 指正
> 翻译基本准确。`短時間内の有効操作` 能懂，但更自然是 `短時間だけ有効な入力` 或 `操作`。

> [!tip] 参考回答
> スキルキューを設計するときは、入力バッファには短時間だけ有効な操作だけを保持します。

---

### 练习 5

> [!question] 题目
> 渲染大量透明特效时，请减少重叠面积，并优先检查过度绘制成本。

> [!example] 自己的答案
>　大量の透明エフェクトを描画する時に、重なる面積を減らし、オーバードローを優先的に確認してください。

> [!warning] 指正
> 整体自然，题意也完整。如果要更明确“过度绘制成本”，可以说 `オーバードローのコスト`。

> [!tip] 参考回答
> 大量の透明エフェクトを描画するときは、重なり合う面積を減らし、オーバードローのコストを優先的に確認してください。

---

### 练习 6

> [!question] 题目
> 地图切换前，应该先保存玩家位置、任务进度和临时增益状态。

> [!example] 自己的答案
>　マップを切り替える前に、プレイヤーの位置を保存しておき、クエスト進捗と一時的なバフを保存しておくべきです。

> [!warning] 指正
> 意思正确，但 `保存しておき` 重复了两次，可以合并并列项。`一時的なバフ` 很自然；“增益状态”也可以更明确写成 `一時的なバフ状態`。

> [!tip] 参考回答
> マップを切り替える前に、プレイヤーの位置、クエスト進捗、一時的なバフ状態を保存しておくべきです。

---

### 练习 7

> [!question] 题目
> 背包排序完成后，UI 应该根据新的格子数据刷新显示顺序。

> [!example] 自己的答案
>　インベントリが並び替えた後に、セルデータに応じて表示順を更新するべきです。

> [!warning] 指正
> 题意基本能表达，但主语有偏差。`インベントリが並び替えた後に` 听起来像“背包自己排序了”，更自然是 `インベントリのソートが完了した後`。题目说 UI 刷新显示顺序，建议明确 `UIは...更新する`。

> [!tip] 参考回答
> インベントリのソートが完了した後、UIは新しいセルデータに基づいて表示順を更新するべきです。

---

### 练习 8

> [!question] 题目
> 使用数据表配置敌人参数时，请在启动阶段检查必填字段是否为空。

> [!example] 自己的答案
>　データテーブルでは敵のパラメータを配置するときに、起動段階で必須フィールドが設定されているかを確認してください。

> [!warning] 指正
> 基本意思正确。`データテーブルでは敵のパラメータを配置する` 不太自然，应为 `データテーブルで敵のパラメータを設定する`。题目是“是否为空”，你的 `設定されているか` 可以表达，但 `空になっていないか` 更贴切。

> [!tip] 参考回答
> データテーブルで敵のパラメータを設定するときは、起動段階で必須フィールドが空になっていないか確認してください。

---

### 练习 9

> [!question] 题目
> 当角色进入攀爬状态时，移动组件需要切换到专用的速度计算方式。

> [!example] 自己的答案
>キャラクターが状態に入る時、移動コンポーネントが専用な速度計算方式に切り替える必要があります。

> [!warning] 指正
> 这里漏掉了“攀爬状态”，`状態に入る` 信息不足。`専用な` 应改为 `専用の`。`移動コンポーネントが...切り替える` 主语和动词搭配不自然，应说 `移動コンポーネントを...切り替える` 或 `移動コンポーネント側で...切り替える`。

> [!tip] 参考回答
> キャラクターがクライミング状態に入るときは、移動コンポーネントを専用の速度計算方式に切り替える必要があります。

---

### 练习 10

> [!question] 题目
> 为了降低加载卡顿，可以把大型音效资源拆成按场景使用的小包。

> [!example] 自己的答案
>　ロードかくツキを軽減するため、大きなサイズのオディオリソースをシーン単位で小さいアセットに切り分けます。

> [!warning] 指正
> 意思大致正确，但有几个词需要修正。`ロードかくツキ` 应写成 `ロード時のカクつき`。`オディオ` 应为 `オーディオ`。`シーン単位で小さいアセットに切り分けます` 可以用，但题目是“小包”，更自然是 `小さなパッケージに分割します`。

> [!tip] 参考回答
> ロード時のカクつきを軽減するために、大きなオーディオリソースをシーンごとに使用する小さなパッケージ<mark style="background: #FF5582A6;">に分割</mark>できます。

---

### 练习 11

> [!question] 题目
> 调试 AI 巡逻路线时，请记录当前路点、等待时间和下一条移动指令。

> [!example] 自己的答案
>　AIのパトロール経路をデバッグする時に、現在のポイント、待ち時間と次の行動コマンドを記録します。

> [!warning] 指正
> 基本意思能传达。`現在のポイント` 稍泛，巡逻路线中的“路点”更自然是 `現在のウェイポイント`。`待ち時間と次の行動コマンド` 并列时建议写成 `待ち時間、次の移動指示`。题目是“移动指令”，`移動指示` 更准确。

> [!tip] 参考回答
> AIのパトロール経路をデバッグするときは、現在のウェイポイント、待ち時間、次の移動指示を記録してください。

---

### 练习 12

> [!question] 题目
> 在制作设置界面时，滑块数值变化后应该立即更新预览音量。

> [!example] 自己的答案
>　設定画面を制作する時に、スライドが変化した後、音量を直ちに更新するべきです。

> [!warning] 指正
> 题意有一部分缺失。`スライド` 应为 UI 控件 `スライダー`。题目是“滑块数值变化后”，建议写成 `スライダーの値が変化したら`。另外“预览音量”应明确为 `プレビュー音量`，不是普通的 `音量`。

> [!tip] 参考回答
> 設定画面を作るときは、スライダーの値が変化したら、すぐにプレビュー音量を更新するべきです。

---

### 练习 13

> [!question] 题目
> 如果低端设备帧率不稳定，请先比较阴影质量、后处理和角色数量的影响。

> [!example] 自己的答案
>　フレームレートがローエンド端末で不安定している場合、影の質、ポストプロセスとキャラクター数をそれぞれ比較してください。

> [!warning] 指正
> 意思基本正确。`不安定している` 应为 `不安定になっている` 或 `安定しない`。`ポストプロセス` 可以用，但常见说法是 `ポストプロセス` 或 `ポストエフェクト`；“影响”最好补出 `影響`，不要只说比较三个项目。

> [!tip] 参考回答
> ローエンド端末でフレームレートが安定しない場合は、まず影の品質、ポストプロセス、キャラクター数の影響を比較してください。

---

### 练习 14

> [!question] 题目
> 今天下班后如果还有时间，我们去车站旁边那家店吃晚饭吧。

> [!example] 自己的答案
>　仕事が終わった後時間があれば、駅の近くの店で晩ご飯を食べましょう

> [!warning] 指正
> 日常表达自然，意思也基本完整。`仕事が終わった後時間があれば` 中间建议加 `に` 或改成 `仕事の後でまだ時間があれば`。题目是“车站旁边那家店”，`駅の近くの店` 稍泛，可以说 `駅のそばのあの店`。句末需要句号。

> [!tip] 参考回答
> 今日、仕事の後でまだ時間があれば、駅のそばのあの店で晩ご飯を食べましょう。

---

### 练习 15

> [!question] 题目
> 我已经整理好这次修改的影响范围，稍后会发给大家确认。

> [!example] 自己的答案
>　今回の修正による影響範囲をすでに整理しました、後皆さんに送って確認していただきます。

> [!warning] 指正
> 意思基本完整，但逗号不能直接连接两个完整句子，建议拆开或用 `ので`。`後皆さんに` 应为 `後で皆さんに`，职场语气里更自然是 `後ほど皆さんに共有します`。`確認していただきます` 像安排对方做事，若是“请大家确认”，可用 `確認していただければと思います`。

> [!tip] 参考回答
> 今回の修正による影響範囲はすでに整理しましたので、後ほど皆さんに共有して確認していただければと思います。

## 2026-07-28

### 练习 1

> [!question] 题目
> 在 Unreal 中调整材质实例参数时，应该避免每帧重复创建动态材质实例。

> [!example] 自己的答案
> Unrealでマテリアルのインスタンスを調整する時に、毎フレーム動的なマテリアルインスタンスを生成することを避けるべきです。

> [!warning] 指正
> 意思基本完整。`マテリアルのインスタンスを調整する` 少了“参数”，建议说 `マテリアルインスタンスのパラメータを調整する`。`動的なマテリアルインスタンス` 可以理解，但 Unreal 语境里更自然是 `動的マテリアルインスタンス`。`調整する時に` 可写成 `調整するときは`。

> [!tip] 参考回答
> Unrealでマテリアルインスタンスのパラメータを調整するときは、毎フレーム動的マテリアルインスタンスを作成し直すことは避けるべきです。

---

### 练习 2

> [!question] 题目
> Unity 的技能冷却显示应该由数据变化驱动，而不是在按钮里直接计算战斗规则。

> [!example] 自己的答案
>　Unityのスキルクールダウンではデータの変化で駆動するべきです。ボタン内にバトル計算ルールを実装するのではありません。

> [!warning] 指正
> 题意能传达，但第一句主语不够准确。不是“冷却本身由数据驱动”，而是“冷却显示由数据变化驱动”，建议补出 `表示`。`バトル計算ルール` 不自然，可以说 `戦闘ルールの計算`。第二句可合并成更自然的对比句。

> [!tip] 参考回答
> Unityのスキルクールダウン表示は、<mark style="background: #FF5582A6;">データの変化によって更新されるべきで</mark>、ボタン内で直接戦闘ルールを計算するべきではありません。

---

### 练习 3

> [!question] 题目
> 如果近战命中判定不稳定，请先检查碰撞体大小、动画通知时间点和角色朝向。

> [!example] 自己的答案
>　近接ヒット判定が不安定になる場合は、まずコリジョンの大きさ、アニメーション通知タイミングとキャラクター向き合いを確認します。

> [!warning] 指正
> 主要意思正确。并列时 `タイミングとキャラクター向き合い` 不自然，建议用 `タイミング、キャラクターの向き`。`向き合い` 是“面对/相互面对”的名词化，不适合这里的“朝向”。`アニメーション通知` 可用，游戏开发语境中 `Anim Notify` 或 `アニメーション通知` 都可以。

> [!tip] 参考回答
> 近接攻撃のヒット判定が不安定な場合は、まずコリジョンの大きさ、アニメーション通知のタイミング、キャラクターの向きを確認してください。

---

### 练习 4

> [!question] 题目
> 在实现命令模式时，每个输入命令都应该封装执行条件和回滚处理。

> [!example] 自己的答案
>　コマンドパータンを実装する時に、入力コマンドごとに実行条件とフォールバックをカプセル化するべきです。

> [!warning] 指正
> `コマンドパータン` 应为 `コマンドパターン`。`フォールバック` 是“备用处理”，不等于“回滚处理”；这里应使用 `ロールバック処理`。`カプセル化する` 很合适。

> [!tip] 参考回答
> コマンドパターンを実装するときは、各入力コマンドに実行条件と<mark style="background: #FF5582A6;">ロールバック処理</mark>をカプセル化するべきです。

---

### 练习 5

> [!question] 题目
> 角色换装后，需要刷新骨骼挂点、材质参数和碰撞配置。

> [!example] 自己的答案
>　キャラクターが装備を切り替えた後、ボーンのソケット、マテリアルパラメータとコリジョン設定を更新する必要があります。

> [!warning] 指正
> 意思清楚。`キャラクターが装備を切り替えた後` 可以用，但题目是“角色换装后”，也可说 `キャラクターの衣装を切り替えた後`。`ボーンのソケット` 对“骨骼挂点”很自然。并列时建议用顿号连接：`ボーンソケット、マテリアルパラメータ、コリジョン設定`。

> [!tip] 参考回答
> キャラクターの衣装を切り替えた後は、ボーンソケット、マテリアルパラメータ、コリジョン設定を更新する必要があります。

---

### 练习 6

> [!question] 题目
> 为了减少场景进入时的等待时间，可以按区域预加载必要的特效和角色资源。

> [!example] 自己的答案
>　シーンに入る時の待ち時間を減らすため、エリアに応じる必要なエフェクトとキャラクターリソースを事前にロードしておきます

> [!warning] 指正
> 基本意思正确。`シーンに入る時` 建议写成 `シーンに入るとき` 或更自然的 `シーン遷移時`。`エリアに応じる必要な` 应为 `エリアに応じて必要な`。句末需要句号。

> [!tip] 参考回答
> シーンに入るときの待ち時間を減らすために、エリアごとに必要なエフェクトやキャラクターリソースを事前にロードしておくことができます。

---

### 练习 7

> [!question] 题目
> 当 UI 焦点从背包移动到装备栏时，手柄导航应该保持清晰的选中状态。

> [!example] 自己的答案
>　UIフォーカスがインベントリから装備欄に遷移する時に、ゲームパットは選択中状態を保持しているべきです。

> [!warning] 指正
> 题意有一处偏差。不是“手柄保持选中状态”，而是“手柄导航保持清晰的选中状态”，建议主语用 `ゲームパッドナビゲーション`。`ゲームパット` 应为 `ゲームパッド`。`遷移する時に` 可改为 `移動するときは`。

> [!tip] 参考回答
> UIフォーカスがインベントリから装備欄へ移動するときは、ゲームパッドナビゲーションで分かりやすい選択状態を維持するべきです。

---

### 练习 8

> [!question] 题目
> 保存数据版本升级时，请为旧字段准备默认值和兼容处理。

> [!example] 自己的答案
>　セーブデータをアップグレードする時に、旧フィールドにデフォルト値と互換処理を用意してください。

> [!warning] 指正
> 意思基本准确。`セーブデータをアップグレードする時` 可以表达，但题目是“保存数据版本升级时”，更完整是 `セーブデータのバージョンを上げるとき`。`旧フィールドにデフォルト値と互換処理を用意する` 可理解，不过 `旧フィールド向けに` 更自然。

> [!tip] 参考回答
> セーブデータのバージョンを上げるときは、古いフィールド向けにデフォルト値と互換処理を用意してください。

---

### 练习 9

> [!question] 题目
> 如果敌人的寻路结果绕远路，请可视化导航网格并确认障碍物标记。

> [!example] 自己的答案
>　敵が経路探索する時に遠回りする場合、ナビメッシュを可視化して障害物マーカを確認してください

> [!warning] 指正
> 主要意思正确。`敵が経路探索する時に遠回りする` 能懂，但题目强调“寻路结果绕远路”，更自然是 `敵の経路探索結果が遠回りになる場合`。`マーカ` 建议写成长音 `マーカー`。句末需要句号。

> [!tip] 参考回答
> <mark style="background: #FFB86CA6;">敵の経路探索結果が遠回りになる場合は</mark>、ナビメッシュを可視化して、障害物のマーカーを確認してください。

---

### 练习 10

> [!question] 题目
> 在优化粒子系统时，应该限制发射数量，并检查贴图尺寸是否过大。

> [!example] 自己的答案
>　パーティクルシステムを最適化する時に、発射数を制限するべきです。その上でテクスチャのサイズが大きすぎるかを確認します。

> [!warning] 指正
> 表达自然，意思也完整。`発射数` 可以用，如果强调粒子数量，`エミッション数` 或 `発生数` 更贴近粒子系统语境。`大きすぎるかを確認します` 建议写成 `大きすぎないか確認します`。

> [!tip] 参考回答
> パーティクルシステムを最適化するときは、<mark style="background: #FF5582A6;">エミッション数</mark>を制限し、テクスチャサイズが大きすぎないか確認するべきです。

---

### 练习 11

> [!question] 题目
> 关卡机关触发后，状态变化应该先写入玩法数据，再通知表现层播放动画。

> [!example] 自己的答案
>　レベルのギミックが発火した後、状態変化をゲームプレイーデータに書き込み、表現層に送信してアニメーションを再生します。

> [!warning] 指正
> 大意正确，但 `ゲームプレイーデータ` 应为 `ゲームプレイデータ`。`発火した後` 在程序语境可用，不过机关触发更自然是 `作動した後` 或 `トリガーされた後`。`表現層に送信して` 不够准确，应是“通知表现层”，可说 `表現層に通知して`。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">レベルギミックが作動した</mark>後は、状態変化を先にゲームプレイデータへ書き込み、その後で表現層に通知してアニメーションを再生させるべきです。

---

### 练习 12

> [!question] 题目
> 打包前，请检查未使用资源、调试日志和平台专用配置。

> [!example] 自己的答案
>　ビルド前に、未使用のリソース、デバッグログとプラトフォーム設定を確認してください。

> [!warning] 指正
> 意思基本完整。`プラトフォーム` 应为 `プラットフォーム`。`デバッグログとプラットフォーム設定` 并列时建议用 `デバッグログ、プラットフォーム固有の設定`。题目里的“专用配置”最好译成 `固有の設定`。

> [!tip] 参考回答
> パッケージ化の前に、未使用のリソース、デバッグログ、プラットフォーム固有の設定を確認してください。

---

### 练习 13

> [!question] 题目
> 当相机穿过墙面时，可以用射线检测调整相机距离。

> [!example] 自己的答案
>　カメラが壁をすり抜けた場合、レイトレースしてカメラの距離を調整できます。

> [!warning] 指正
> 意思能表达。`壁をすり抜けた場合` 是“已经穿过去了”，题目更像一般条件，建议说 `壁をすり抜ける場合` 或 `壁にめり込む場合`。`レイトレースして` 可用，但 gameplay 相机碰撞里常说 `レイキャスト`；如果在 Unreal 语境也可以说 `ライントレース`。

> [!tip] 参考回答
> カメラが壁をすり抜ける場合は、<mark style="background: #FF5582A6;">レイキャスト</mark>を使ってカメラ距離を調整できます。

---

### 练习 14

> [!question] 题目
> 周末如果天气好的话，我们一起去公园散步吧。

> [!example] 自己的答案
>　週末に天気が良ければ、一緒に公園で散歩しましょう

> [!warning] 指正
> 日常表达自然，意思完整。`公園で散歩しましょう` 可以用，但“去公园散步”更自然是 `公園を散歩しましょう` 或 `公園に散歩に行きましょう`。句末需要句号。

> [!tip] 参考回答
> 週末、天気が良ければ、一緒に公園へ散歩に行きましょう。

---

### 练习 15

> [!question] 题目
> 我明天上午会先确认问题原因，下午再向你汇报调查结果。

> [!example] 自己的答案
>　明日午前にまず問題の原因を確認します。午後にあなたに調査結果を報告します。

> [!warning] 指正
> 意思清楚。`明日午前に` 建议写成 `明日の午前中に`。`あなたに` 在职场日语里过于直白，汇报对象明确时可省略，或用 `ご報告します` 提升礼貌度。两句合并会更自然。

> [!tip] 参考回答
> 明日の午前中にまず問題の原因を確認し、午後に調査結果をご報告します。

## 2026-07-29

### 练习 1

> [!question] 题目
> 在 Unreal 中制作冲刺技能时，应该把输入判定、体力消耗和移动速度变化分成独立模块。

> [!example] 自己的答案
> Unrealでダッシュスキルを制作する時に、入力判定、スタミナーコストと移動速を独立したモジュールに分割するべきです。

> [!warning] 指正
> 意思基本完整。`スタミナーコスト` 应为 `スタミナコスト`。`移動速` 不自然，建议用 `移動速度の変化`。`制作する時に` 可改为更自然的 `作るときは` 或 `実装するときは`。

> [!tip] 参考回答
> Unrealでダッシュスキルを実装するときは、入力判定、スタミナ消費、移動速度の変化を独立したモジュールに分けるべきです。

---

### 练习 2

> [!question] 题目
> Unity 的 UI 动画如果和按钮状态耦合太紧，后续扩展新的交互效果会变得很困难。

> [!example] 自己的答案
>　UnityのUIアニメーションとボタン状態の結合度が高い場合、後からインタリエクションが拡張しにくくなります。

> [!warning] 指正
> 题意能表达，但 `インタリエクション` 拼写应为 `インタラクション`。`インタラクションが拡張しにくくなります` 主语不太自然，建议说 `新しいインタラクション効果を追加しにくくなります`。`結合度が高い` 很适合技术语境。

> [!tip] 参考回答
> UnityのUIアニメーションがボタン状態と強く結合していると、後から新しい<mark style="background: #FF5582A6;">インタラクション</mark>効果を拡張するのが難しくなります。

---

### 练习 3

> [!question] 题目
> 如果技能预览范围显示不准确，请先检查碰撞半径、地面投影和角色朝向。

> [!example] 自己的答案
> スキルプレビュー範囲が不正に表示している場合、まずコリジョン半径、地面シャドウとキャラクターの向きを確認してください。

> [!warning] 指正
> 主要意思能传达。`不正に表示している` 更像“不正当地显示”，这里应说 `正しく表示されない`。`地面シャドウ` 不等于“地面投影”，建议用 `地面への投影` 或 `地面投影`。

> [!tip] 参考回答
> スキルのプレビュー範囲が正しく表示されない場合は、まずコリジョン半径、地面への投影、キャラクターの向きを確認してください。

---

### 练习 4

> [!question] 题目
> 在实现状态机时，进入状态和离开状态的处理应该尽量保持对称。

> [!example] 自己的答案
>　ステートマシンを実装する時に、状態に入ると状態から出る処理をできるだけペアーします。

> [!warning] 指正
> 意思接近，但 `状態に入ると状態から出る処理` 结构不够自然，应为 `状態に入る処理と状態から出る処理`。`ペアーします` 可以理解，但“保持对称”更自然是 `対称に保つ`。

> [!tip] 参考回答
> ステートマシンを実装するときは、状態に入る処理と状態から出る処理をできるだけ対称に保つべきです。

---

### 练习 5

> [!question] 题目
> 渲染角色轮廓时，需要注意描边宽度在不同分辨率下是否保持一致。

> [!example] 自己的答案
>　キャラクターアウトラインを描画する時に、アウトラインの粗さは異なる解像度でも一致していないを意識する必要があります。

> [!warning] 指正
> 题意有偏差。`アウトラインの粗さ` 是“粗糙度”，不是“描边宽度”，应使用 `アウトラインの幅`。`一致していないを意識する` 语法不成立，建议说 `一定に保たれているか注意する`。

> [!tip] 参考回答
> キャラクターのアウトラインを描画するときは、<mark style="background: #FF5582A6;">アウトラインの幅</mark>が異なる解像度でも一定に保たれているか注意する必要があります。

---

### 练习 6

> [!question] 题目
> 为了降低资源加载峰值，可以把首屏必需资源和后续可延迟资源分开处理。

> [!example] 自己的答案
>　リソースロード時のピークを軽減するために、第一画面に必要なリソースと遅延ロードのリソースを分けて処理できるようにします。

> [!warning] 指正
> 意思基本正确。`第一画面` 不太自然，“首屏”在界面/加载语境里可说 `初期画面` 或 `最初に表示される画面`。`遅延ロードのリソース` 能懂，但题目是“后续可延迟资源”，更自然是 `後から遅延ロードできるリソース`。

> [!tip] 参考回答
> リソースロード時のピークを抑えるために、最初に表示される画面で必要なリソースと、後から遅延ロードできるリソースを分けて処理できます。

---

### 练习 7

> [!question] 题目
> 当玩家打开地图界面时，角色移动输入应该被暂停，但镜头缩放仍然可以保留。

> [!example] 自己的答案
>　プレイヤーがワールドマップを開いた時に、キャラクターの移動入力を停止させ、カメラズーム機能を残し続けます。

> [!warning] 指正
> 主要意思正确。`停止させ` 可以用，但题目是“暂停输入”，技术表达里 `一時停止する` 更贴切。`カメラズーム機能を残し続けます` 稍生硬，建议说 `カメラズームはそのまま残せます`。

> [!tip] 参考回答
> プレイヤーがマップ画面を開いたときは、キャラクターの移動入力を一時停止するべきですが、カメラズームはそのまま残せます。

---

### 练习 8

> [!question] 题目
> 保存装备数据时，请同时记录物品编号、强化等级和耐久度。

> [!example] 自己的答案
>　装備データを保存する時に、オブジェクトのID、強化レベルと耐久度を同時に記録してください。

> [!warning] 指正
> 意思基本完整。装备数据里的“物品编号”用 `アイテムID` 比 `オブジェクトのID` 更准确。并列时 `強化レベルと耐久度` 建议写成 `強化レベル、耐久度`。

> [!tip] 参考回答
> 装備データを保存するときは、アイテムID、強化レベル、耐久度を同時に記録してください。

---

### 练习 9

> [!question] 题目
> 如果动画混合看起来很生硬，请调整过渡时间，并确认根运动是否参与计算。

> [!example] 自己的答案
>　アニメーションブレンドが、トランジションの長さを調整し、ルートモーションが計算に反映されていないかを確認してください。

> [!warning] 指正
> 句子缺少“看起来很生硬”的条件部分。`アニメーションブレンドが、トランジション...` 中主语后直接接处理不完整。`反映されていないか` 表示“是否没有反映”，题目是确认是否参与计算，建议用 `計算に含まれているか`。

> [!tip] 参考回答
> アニメーションブレンドが<mark style="background: #FF5582A6;">硬く見える</mark>場合は、トランジション時間を調整し、ルートモーションが計算に含まれているか確認してください。

---

### 练习 10

> [!question] 题目
> 在优化战斗特效时，应该优先减少屏幕中央反复出现的高亮粒子。

> [!example] 自己的答案
>　バトルエフェクトを最適化する時に、まず画面中央に重複して出ているハイライトなパーティクルを減らすべきです。

> [!warning] 指正
> 意思基本能传达。`重複して出ている` 偏“重复生成”，题目强调“反复出现”，可用 `繰り返し表示される`。`ハイライトなパーティクル` 不自然，建议说 `明るいパーティクル` 或 `高輝度のパーティクル`。

> [!tip] 参考回答
> バトルエフェクトを最適化するときは、まず画面中央に繰り返し表示される<mark style="background: #FF5582A6;">高輝度</mark>のパーティクルを減らすべきです。

---

### 练习 11

> [!question] 题目
> 任务系统更新目标进度后，日志界面应该只刷新发生变化的条目。

> [!example] 自己的答案
>　クエストシステムはタスクの進捗を更新した後、ログ画面では更新されたアイテムだけを更新するべきです。

> [!warning] 指正
> 大意正确，但主语切换稍乱。题目是“日志界面刷新条目”，建议把 `ログ画面では` 作为主语。`タスクの進捗` 在游戏任务系统中可以用，但 `目標の進捗` 更贴合“目标进度”。`更新されたアイテムだけを更新` 重复，`変化した項目だけを更新` 更自然。

> [!tip] 参考回答
> クエストシステムが目標の進捗を更新した後、ログ画面では変化した項目だけを更新するべきです。

---

### 练习 12

> [!question] 题目
> 调试掉落表时，请输出怪物编号、权重配置和最终抽中的物品。

> [!example] 自己的答案
>　ドロップテーブルをデバッグする時に、モンスターID、重みと当たったオブジェクトを出力してください。

> [!warning] 指正
> 意思基本正确。`重み` 可以用，但配置项更自然是 `重み設定` 或 `ウェイト設定`。`当たったオブジェクト` 能懂，但掉落表语境里“抽中的物品”更准确是 `最終的に選ばれたアイテム`。

> [!tip] 参考回答
> ドロップテーブルをデバッグするときは、モンスターID、重み設定、最終的に選ばれたアイテムを出力してください。

---

### 练习 13

> [!question] 题目
> 在设计 Boss 阶段切换时，表现演出不应该阻塞核心战斗状态的更新。

> [!example] 自己的答案
>　Bossのフェーズ切り替えを設計する時に、コアバトル状態の更新が演出にブロックされるべきではありません。

> [!warning] 指正
> 意思清楚。`ブロックされるべきではありません` 可以表达，但日语技术说明里更自然是 `ブロックしてはいけません`。另外题目主语是“表现演出不应该阻塞更新”，可把 `演出が...更新をブロックしないようにする` 说得更直接。

> [!tip] 参考回答
> Bossのフェーズ切り替えを設計するときは、演出がコアな戦闘状態の更新をブロックしないようにするべきです。

---

### 练习 14

> [!question] 题目
> 如果你晚上有空，我们一起去附近新开的咖啡店坐一会儿吧。

> [!example] 自己的答案
> 夜に都合が良ければ、近くの新しくできた喫茶店に一緒に行きましょう

> [!warning] 指正
> 日常表达自然，意思基本完整。`夜に都合が良ければ` 可以用，不过口语里 `今夜時間があれば` 更自然。题目里的“坐一会儿”没有明确表达，可以加 `少しゆっくりしましょう`。句末需要句号。

> [!tip] 参考回答
> 今夜時間があれば、近くに新しくできたカフェに一緒に行って、少しゆっくりしましょう。

---

### 练习 15

> [!question] 题目
> 我已经把今天的调查结果整理成文档，会议前会发给大家。

> [!example] 自己的答案
>　本日の調査結果をすでにドキュメントに整理しました。会議前に皆さんに送ります。

> [!warning] 指正
> 职场表达清楚。`すでに` 放在这里没问题，但 `もう` 或直接省略也自然。`皆さんに送ります` 稍直接，工作场景更常用 `共有します`。若想更礼貌，可以用 `会議前に皆さんへ共有します`。

> [!tip] 参考回答
> 本日の調査結果はすでにドキュメントに整理しましたので、会議前に皆さんへ共有します。

## 2026-07-30

### 练习 1

> [!question] 题目
> 在 Unreal 的连击系统中，输入缓冲应该记录按键时间，并在当前攻击结束前判断是否允许进入下一段。

> [!example] 自己的答案
> Unrealのコンボシステムでは、入力バッファが押し時間を記録するべきです。その上で現在の攻撃が終了する前に次のフェーズに入れるかを判定します。

> [!warning] 指正
> 意思基本完整。`押し時間` 不太自然，题目是“按键时间”，建议用 `入力された時刻` 或 `ボタンが押されたタイミング`。`次のフェーズ` 可以理解，但连击语境里 `次の段` 或 `次の攻撃` 更贴切。

> [!tip] 参考回答
> Unrealのコンボシステムでは、入力バッファにボタンが<mark style="background: #FF5582A6;">押されたタイミング</mark>を記録し、現在の攻撃が終わる前に<mark style="background: #FF5582A6;">次の段へ進めるか</mark>を判定するべきです。

---

### 练习 2

> [!question] 题目
> Unity 中的相机震动效果最好按武器类型配置强度和持续时间，而不是写死在角色脚本里。

> [!example] 自己的答案
>　カメラシェークが武器のタイプに応じる強度と持続時間を配置し、キャラクタースクリプトにハードーコードしないようにします。

> [!warning] 指正
> 主要意思能传达，但语法需要调整。`武器のタイプに応じる強度と持続時間を配置し` 不自然，应说 `武器タイプに応じて強度と持続時間を設定し`。`ハードーコード` 应为 `ハードコード`。

> [!tip] 参考回答
> Unityのカメラシェイク効果は、キャラクタースクリプトにハードコードするのではなく、武器タイプごとに強度と持続時間を設定するのが望ましいです。

---

### 练习 3

> [!question] 题目
> 如果技能冷却时间显示和实际可用时间不一致，请先确认计时来源和 UI 刷新时机。

> [!example] 自己的答案
>　スキルクールダウン時間と実際の時間が不一致している場合、まずタイマー由来とUI更新タイミングを確認してください

> [!warning] 指正
> 意思清楚。`実際の時間` 略泛，题目是“实际可用时间”，建议用 `実際に使用可能になる時間`。`タイマー由来` 不自然，应说 `時間の参照元` 或 `計測元`。句末需要句号。

> [!tip] 参考回答
> スキルのクールダウン表示と実際に使用可能になる時間が一致しない場合は、まず時間の<mark style="background: #FF5582A6;">参照元</mark>とUIの更新タイミングを確認してください。

---

### 练习 4

> [!question] 题目
> 在设计背包排序功能时，应该把排序规则和格子显示逻辑分开，方便之后增加新的排序条件。

> [!example] 自己的答案
>　インベントリーの並び替え機能を設定する時に、ソートルールと表示ロジックを分けるべきです。後から新しいソートを追加しやすくします。

> [!warning] 指正
> 意思基本正确。`設定する時` 这里应为“设计”功能，`設計するとき` 更准确。`表示ロジック` 前最好补出 `グリッド`。`後から新しいソートを追加` 可改为 `新しいソート条件を追加`，更贴合题目。

> [!tip] 参考回答
> インベントリの並び替え機能を設計するときは、ソートルールとグリッドの表示ロジックを分けておくと、後から新しいソート条件を追加しやすくなります。

---

### 练习 5

> [!question] 题目
> 渲染半透明特效时，需要注意排序问题和过度绘制对帧率的影响。

> [!example] 自己的答案
>　半透明エフェクトを描画する時に、ソート問題を意識してオーバドローがフレームレートに影響します

> [!warning] 指正
> 题意能表达，但句子连接不自然。`オーバドロー` 应为 `オーバードロー`。后半应说“注意过度绘制对帧率的影响”，不是“过度绘制会影响帧率”直接并列，建议用 `影響にも注意する必要があります`。

> [!tip] 参考回答
> 半透明エフェクトを描画するときは、描画順の問題と、オーバードローがフレームレートに与える影響に注意する必要があります。

---

### 练习 6

> [!question] 题目
> 资源表导入后，请检查缺失引用、重复编号和未使用字段，避免运行时才发现配置错误。

> [!example] 自己的答案
>　リソーステーブルをインポットする時に、不足している参照、重複したIDと未使用のフィールドを確認してください。実行中の設定ミスを避けられるようにします。

> [!warning] 指正
> 意思基本完整。`インポット` 应为 `インポート`。`不足している参照` 可以用，但“缺失引用”更自然是 `欠落している参照`。`実行中の設定ミス` 更像“运行中的配置错误”，题意是避免运行时才发现，建议用 `実行時に設定ミスが発覚することを防ぐ`。

> [!tip] 参考回答
> リソーステーブルをインポートした後は、<mark style="background: #FF5582A6;">欠落している参照</mark>、重複したID、未使用のフィールドを確認し、実行時に設定ミスが<mark style="background: #FF5582A6;">発覚しない</mark>ようにします。

---

### 练习 7

> [!question] 题目
> 当角色进入瞄准状态时，移动速度、镜头距离和准星扩散都应该切换到对应的配置。

> [!example] 自己的答案
>　キャラクターがエイム状態に入る時に、移動速度、カメラ距離と照準拡散を対応する設定に切り替えます。

> [!warning] 指正
> 意思清楚。并列时 `カメラ距離と照準拡散` 建议写成 `カメラ距離、照準の拡散`。`対応する設定に切り替えます` 很自然。`入る時に` 可改为 `入ったときは`。

> [!tip] 参考回答
> キャラクターがエイム状態に入ったときは、移動速度、カメラ距離、照準の拡散をそれぞれ対応する設定に切り替えるべきです。

---

### 练习 8

> [!question] 题目
> 在实现任务奖励时，先计算奖励内容，再播放获得道具的表现，可以让逻辑更容易测试。

> [!example] 自己的答案
>　クエスト報賞を実装する時に、まず報賞内容を計算し、アイテムを取得する表現を再生します。そうすることで、ロジックをテストしやすくなります。

> [!warning] 指正
> 主要意思正确。`報賞` 不常用于这里，游戏奖励一般用 `報酬`。`アイテムを取得する表現` 能懂，但更自然是 `アイテム獲得演出`。句子最好体现“先计算，再播放”，职责顺序会更清楚。

> [!tip] 参考回答
> クエスト報酬を実装するときは、先に<mark style="background: #FF5582A6;">報酬</mark>内容を計算してからアイテム<mark style="background: #FF5582A6;">獲得演出</mark>を再生すると、ロジックをテストしやすくなります。

---

### 练习 9

> [!question] 题目
> 如果寻路角色在转角处频繁卡住，请确认碰撞胶囊大小和导航区域是否匹配。

> [!example] 自己的答案
>　経路探索キャラクターがコーナーで頻繁にボロックされる場合、カプセルのサイズとナビーエリアが合うかどうかを確認してください。

> [!warning] 指正
> 意思接近，但有几个词需要修正。`ボロック` 应为 `ブロック`，不过“卡住”更自然是 `引っかかる`。`ナビーエリア` 应为 `ナビエリア` 或 `ナビゲーションエリア`。`合うかどうか` 可改为技术语境更自然的 `一致しているか`。

> [!tip] 参考回答
> 経路探索を行うキャラクターが角で頻繁に<mark style="background: #FF5582A6;">引っかかる</mark>場合は、カプセルのサイズとナビゲーションエリアが一致しているか確認してください。

---

### 练习 10

> [!question] 题目
> 为了减少每帧分配，伤害数字组件可以复用已有实例，并在显示前重置位置和颜色。

> [!example] 自己的答案
>　毎フレーム割り当てを減らすため、既存のダメージ数字のコンポーネントを再利用するように、表示する前にポジションと色をリセットします。

> [!warning] 指正
> 意思基本正确。`割り当て` 在性能语境里常说 `アロケーション` 或 `メモリ割り当て`。`再利用するように、表示する前に...` 连接不自然，建议拆成“复用实例，并在显示前重置”。`ポジション` 可用，但 `位置` 更自然。

> [!tip] 参考回答
> 毎フレームのアロケーションを減らすために、ダメージ数字コンポーネントは既存のインスタンスを再利用し、表示前に位置と色をリセットできます。

---

### 练习 11

> [!question] 题目
> 在调试材质参数时，请同时记录参数名、当前值和修改它的系统，方便追踪问题来源。

> [!example] 自己的答案
>　マテリアルパラメータをデバッグする時に、パラメータ名、現在値と変更由来を同時に記録し、問題原因を追跡しやすいようにします。

> [!warning] 指正
> 意思能表达。`変更由来` 不太自然，题目是“修改它的系统”，建议用 `変更したシステム` 或 `変更元のシステム`。`問題原因` 前加 `の` 更自然：`問題の原因`。

> [!tip] 参考回答
> マテリアルパラメータをデバッグするときは、パラメータ名、現在値、変更したシステムを同時に記録すると、問題の原因を追跡しやすくなります。

---

### 练习 12

> [!question] 题目
> 存档读取失败时，不要直接进入游戏流程，应该显示错误提示并保留返回标题界面的选项。

> [!example] 自己的答案
>　セーブデータを読み込むに失敗した場合、ゲームフローに直接入らなく、エラーを提示してタイトル画面に戻るオプションを表示します。

> [!warning] 指正
> 题意清楚，但语法需要调整。`読み込むに失敗した` 应为 `読み込みに失敗した` 或 `読み込めなかった`。`直接入らなく` 不自然，应说 `直接入らず`。`エラーを提示して` 可用，但 UI 语境里 `エラーメッセージを表示して` 更自然。

> [!tip] 参考回答
> セーブデータの読み込みに失敗した場合は、ゲームフローに直接入らず、エラーメッセージを表示して、タイトル画面へ戻る選択肢を残すべきです。

---

### 练习 13

> [!question] 题目
> 在制作技能图标冷却遮罩时，遮罩进度应该由剩余时间计算，而不是只依赖动画播放进度。

> [!example] 自己的答案
>　スキルアイコンのクールダウンマスクを制作する時に、マスク進捗はアニメーション進行に依存せず、残り時間によって更新します。

> [!warning] 指正
> 意思基本正确。`制作する時` 可改为 `作成するとき` 或 `実装するとき`。`マスク進捗` 不够自然，建议用 `マスクの進行度`。`残り時間によって更新します` 可以，但题目是“由剩余时间计算”，`残り時間から計算する` 更准确。

> [!tip] 参考回答
> スキルアイコンのクールダウンマスクを作成するときは、マスクの進行度を残り時間から計算し、アニメーションの再生進度だけに依存しないようにするべきです。

---

### 练习 14

> [!question] 题目
> 今天下班后如果不累，我们去车站附近吃点热的东西吧。

> [!example] 自己的答案
>　仕事が終わった後に疲れないなら、駅の近くで暖かいものを食べに行きましょう

> [!warning] 指正
> 意思基本完整。`疲れないなら` 听起来像条件过硬，日常邀请更自然是 `疲れていなければ`。`暖かいもの` 指温暖抽象或衣物时也可用，食物建议用 `温かいもの`。句末需要句号。

> [!tip] 参考回答
> 今日、仕事が終わった後に疲れていなければ、駅の近くで温かいものを食べに行きましょう。

---

### 练习 15

> [!question] 题目
> 我会在下午三点前整理好测试结果，如果发现高风险问题，会马上通知你。

> [!example] 自己的答案
>　午後三時前にテスト結果を整理し、リスクが高い問題を見つけたら、すぐに報告します。

> [!warning] 指正
> 职场表达清楚。`午後三時前に` 可以用，但 `午後3時までに` 更自然，表示截止时间。`リスクが高い問題` 可以，更常见是 `高リスクの問題`。`報告します` 没问题，如对同事或上级可用 `共有します` 或 `ご報告します`。

> [!tip] 参考回答
> 午後3時までにテスト結果を整理し、高リスクの問題が見つかった場合は、すぐにご報告します。

## 2026-07-31

### 练习 1

> [!question] 题目
> 在 Unreal 中实现互动提示时，检测范围和 UI 显示状态应该由不同组件负责。

> [!example] 自己的答案
>　Unrealでインタラクションを提示する時に、別々のコンポーネントに検知範囲とUI表示状態を持たせます。

> [!warning] 指正
> 意思清楚。`インタラクションを提示する` 稍偏“提示互动本身”，题目是“互动提示”，更自然是 `インタラクション提示`。`検知範囲とUI表示状態を持たせます` 能懂，但“由不同组件负责”建议明确为 `別々のコンポーネントに担当させます`。

> [!tip] 参考回答
> Unrealでインタラクション提示を実装するときは、検知範囲とUIの表示状態を別々のコンポーネントに担当させるべきです。

---

### 练习 2

> [!question] 题目
> Unity 的角色控制器切换移动模式时，应该先清除上一种模式留下的输入状态。

> [!example] 自己的答案
>　Unityのキャラクターコントローラーが移動モードに切り替える時に、先に前回使用時に残っている入力状態をクリアするべきです。

> [!warning] 指正
> 题意基本正确。`キャラクターコントローラーが移動モードに切り替える` 主语搭配不太自然，建议说 `移動モードを切り替えるとき`。`前回使用時に残っている入力状態` 能表达，但题目是“上一种模式留下的输入状态”，更贴切是 `前のモードに残っている入力状態`。

> [!tip] 参考回答
> Unityのキャラクターコントローラーで移動モードを切り替えるときは、先に前のモードに残っている入力状態をクリアするべきです。

---

### 练习 3

> [!question] 题目
> 如果命中反馈延迟明显，请确认伤害计算、动画事件和音效播放的执行顺序。

> [!example] 自己的答案
>　ヒットフィードバックが明らかに遅ている場合、ダメージ計算、アニメーションイベントとオーディオ再生順番で確認してください。

> [!warning] 指正
> 大意能传达，但有几个明显问题。`遅ている` 应为 `遅れている`。并列时 `アニメーションイベントとオーディオ再生順番で確認` 不自然，应写成 `アニメーションイベント、効果音の再生順を確認`。`明らかに` 可以用，但这里 `目立つ` 更自然。

> [!tip] 参考回答
> ヒットフィードバックの遅延が目立つ場合は、ダメージ計算、アニメーションイベント、効果音の再生順を確認してください。

---

### 练习 4

> [!question] 题目
> 在设计道具使用流程时，消耗逻辑应该先于表现演出完成，避免重复使用。

> [!example] 自己的答案
>　アイテム使用フローを設計する時に、消耗ロジックは演出より早めに完了させ、同一アイテムが重複して使用されないようにします。

> [!warning] 指正
> 意思完整，表达也比较自然。`消耗ロジック` 能懂，但道具数量或资源消耗的处理更常说 `消費処理`。`演出より早めに完了させ` 可以理解，不过技术流程里更自然是 `演出より先に完了させる`。

> [!tip] 参考回答
> アイテム使用フローを設計するときは、消費処理を演出より先に完了させ、同じアイテムが重複して使用されないようにします。

---

### 练习 5

> [!question] 题目
> 渲染角色轮廓描边时，需要注意后处理顺序和深度缓冲的读取成本。

> [!example] 自己的答案
>　キャラクターの輪郭線を描画する時に、ポストプロセス処理順と深度バッファ読み込み負荷を意識する必要があります。

> [!warning] 指正
> 主要意思正确。`ポストプロセス処理順` 可以用，但更自然是 `ポストプロセスの順序`。`深度バッファ読み込み負荷` 能懂，不过“读取成本”可说 `深度バッファの読み取りコスト`，技术语气更准确。

> [!tip] 参考回答
> キャラクターの輪郭線を描画するときは、ポストプロセスの順序と深度バッファの読み取りコストに注意する必要があります。

---

### 练习 6

> [!question] 题目
> 资源加载失败时，请记录资源路径、请求来源和失败原因，方便之后排查。

> [!example] 自己的答案
>　リソースの読み込みに失敗した場合、リソースのパス、呼び出し元と原因を記録してください。そうすることで後から調査しやすくなります。

> [!warning] 指正
> 意思基本完整。`呼び出し元` 能表达“请求来源”，但资源加载语境里 `リクエスト元` 更贴切。`原因` 前最好补出“失败原因”，写成 `失敗理由` 会更明确。`そうすることで` 的后续表达自然。

> [!tip] 参考回答
> リソースの読み込みに失敗した場合は、リソースパス、リクエスト元、失敗理由を記録してください。そうすることで、後から調査しやすくなります。

---

### 练习 7

> [!question] 题目
> 当技能被打断时，冷却时间是否开始计算应该由技能状态机统一决定。

> [!example] 自己的答案
>　スキルが中断された時に、クールダウンが始まるかどうかはステートマシンによって統一決定します。

> [!warning] 指正
> 意思清楚。`統一決定します` 不自然，应改为 `一元的に決定する` 或 `統一して決定する`。另外题目是“是否开始计算冷却时间”，建议明确 `クールダウン時間の計算を開始するかどうか`。

> [!tip] 参考回答
> スキルが中断されたときにクールダウン時間の計算を開始するかどうかは、スキルステートマシンが一元的に決定するべきです。

---

### 练习 8

> [!question] 题目
> 在制作装备详情界面时，属性差异应该用统一的数据格式传给 UI。

> [!example] 自己的答案
>　装備詳細画面を作成している時に、属性差分を統一されたデータ構造でUIに渡します。

> [!warning] 指正
> 基本意思正确。`作成している時に` 更像“正在制作期间”，这里用 `作成するときは` 更自然。`属性差分` 很贴切；`統一されたデータ構造` 可以，但题目是“数据格式”，`統一したデータ形式` 更准确。

> [!tip] 参考回答
> 装備詳細画面を作成するときは、属性差分を統一したデータ形式でUIに渡すべきです。

---

### 练习 9

> [!question] 题目
> 如果场景切换后角色状态异常，请先确认初始化顺序和保存数据的覆盖时机。

> [!example] 自己的答案
>　シーンの切り替えた後にキャラクター状態が異常になった場合、まず初期順とデータの上書きタイミングを確認してください。

> [!warning] 指正
> 题意能传达，但语法需要调整。`シーンの切り替えた後に` 应为 `シーンを切り替えた後に` 或 `シーン切り替え後に`。`キャラクター状態` 前加 `の` 更自然。`初期順` 信息不足，应说 `初期化順序`。

> [!tip] 参考回答
> シーン切り替え後にキャラクターの状態が異常になった場合は、まず初期化順序とセーブデータの上書きタイミングを確認してください。

---

### 练习 10

> [!question] 题目
> 为了减少战斗日志的性能开销，可以只在调试模式下输出详细事件。

> [!example] 自己的答案
>　バトルログによる負荷を減らすために、デバッグモードだけで詳細イベントを出力するようにします。

> [!warning] 指正
> 表达自然，意思也完整。`デバッグモードだけで` 可以用，技术说明里更常见是 `デバッグモードの場合だけ` 或 `デバッグモード時のみ`。`詳細イベント` 也可以说成 `詳細なイベントログ`。

> [!tip] 参考回答
> バトルログによる負荷を減らすために、デバッグモード時のみ詳細なイベントログを出力するようにします。

---

### 练习 11

> [!question] 题目
> 在实现观察者模式时，事件发送方不应该依赖具体的 UI 控件类。

> [!example] 自己的答案
>　オブザーバーパターンを実装する時に、イベント発送元は具体的のUIクラスに依存するべきではありません。

> [!warning] 指正
> 主要意思正确。`イベント発送元` 不自然，应为 `イベント送信側` 或 `イベント発行元`。`具体的のUIクラス` 应改为 `具体的なUIクラス`。如果要表达“控件类”，可以用 `UIウィジェットクラス`。

> [!tip] 参考回答
> オブザーバーパターンを実装するときは、イベント送信側が具体的なUIウィジェットクラスに依存するべきではありません。

---

### 练习 12

> [!question] 题目
> 调试技能范围预览时，请同时检查半径计算、地面投影和颜色透明度。

> [!example] 自己的答案
>　スキル範囲プレビューをデバッグする時に、半径計算、地面投影と色の透明度を同時に検証してください。

> [!warning] 指正
> 意思完整。并列时 `地面投影と色の透明度` 建议统一用顿号连接：`半径計算、地面投影、色の透明度`。`検証してください` 可以，但“调试时请检查”用 `確認してください` 更自然。

> [!tip] 参考回答
> スキル範囲プレビューをデバッグするときは、半径計算、地面投影、色の透明度を同時に確認してください。

---

### 练习 13

> [!question] 题目
> 如果低配设备上 UI 打开变慢，请优先检查纹理尺寸和首次创建控件的成本。

> [!example] 自己的答案
>　ローエンド端末でUIの開きが重くなった場合、テクスチャのサイズと初回に生成されるウィジェット負荷を優先的に確認してください。

> [!warning] 指正
> 意思基本正确。`UIの開きが重くなった` 能懂，但更自然是 `UIを開くのが遅くなった`。`初回に生成されるウィジェット負荷` 稍生硬，建议说 `ウィジェットを初回生成するコスト`。

> [!tip] 参考回答
> ローエンド端末でUIを開くのが遅くなった場合は、テクスチャサイズとウィジェットを初回生成するコストを優先的に確認してください。

---

### 练习 14

> [!question] 题目
> 明天如果天气不错，我们去附近的公园散步，然后顺便买点咖啡吧。

> [!example] 自己的答案
>　明日は天気が良ければ、近くの公園で散歩してついでにコーヒーを買いましょう

> [!warning] 指正
> 日常表达自然，意思也完整。`公園で散歩して` 可以用，不过“去公园散步”更自然是 `公園に散歩に行って`。句末需要句号。

> [!tip] 参考回答
> 明日、天気が良ければ、近くの公園に散歩に行って、ついでにコーヒーを買いましょう。

---

### 练习 15

> [!question] 题目
> 我会先确认这次修改是否影响现有功能，确认后再把结果发到群里。

> [!example] 自己的答案
>　まず今回の修正が既存の機能に影響するかどうかを確認し、確認終わる次第に結果をチャンネルで共有します

> [!warning] 指正
> 职场表达基本清楚。`確認終わる次第に` 应为 `確認が終わり次第`。题目是“发到群里”，`チャンネルで共有します` 很自然，若是聊天群也可说 `グループに共有します`。句末需要句号。

> [!tip] 参考回答
> まず今回の修正が既存機能に影響するかどうかを確認し、確認が終わり次第、結果をチャンネルで共有します。

## 2026-08-01

### 练习 1

> [!question] 题目
> 在 Unreal 中调整动画蒙太奇时，需要确认根运动是否会覆盖角色移动组件的速度。

> [!example] 自己的答案
> UnrealでMontageを調整する時に、ロートモーションがキャラクター移動コンポーネント速度を上書きするかを確認する必要があります。

> [!warning] 指正
> 意思基本正确。`ロートモーション` 应为 `ルートモーション`。`キャラクター移動コンポーネント速度` 少了连接，建议说 `キャラクター移動コンポーネントの速度`。`上書きするか` 可以用，但题目是“是否会覆盖”，技术说明里 `上書きしていないか` 更自然。

> [!tip] 参考回答
> Unrealでアニメーションモンタージュを調整するときは、ルートモーションがキャラクター移動コンポーネントの速度を上書きしていないか確認する必要があります。

---

### 练习 2

> [!question] 题目
> Unity 的材质实例如果在运行时频繁生成，可能会增加内存压力和批处理成本。

> [!example] 自己的答案
>　Unityのマテリアルインスタンスが実行中に頻繁に生成するとメモリ使用量とバッチ処理が増えるかもしれません

> [!warning] 指正
> 大意清楚。`マテリアルインスタンスが...生成すると` 主语搭配不自然，应改为 `マテリアルインスタンスを...生成すると`。题目里的“内存压力和批处理成本”可以更明确地说成 `メモリ負荷` 和 `バッチ処理のコスト`。句末需要句号。

> [!tip] 参考回答
> Unityでマテリアルインスタンスを実行時に頻繁に生成すると、メモリ負荷やバッチ処理のコストが増える可能性があります。

---

### 练习 3

> [!question] 题目
> 设计敌人感知逻辑时，视野角度、听觉范围和警戒等级最好写成可调整的数据。

> [!example] 自己的答案
>　敵の検知ロジックを設計する時に、視野の角度、聴力範囲と警戒レベルが調整できるようなデータにします

> [!warning] 指正
> 意思基本完整。`聴力範囲` 能懂，但 AI 感知语境里更自然是 `聴覚範囲`。并列时 `視野の角度、聴覚範囲、警戒レベル` 更顺。`調整できるようなデータ` 可改成 `調整可能なデータ`，技术表达更简洁。

> [!tip] 参考回答
> 敵の感知ロジックを設計するときは、視野角、<mark style="background: #FF5582A6;">聴覚</mark>範囲、警戒レベルを<mark style="background: #FF5582A6;">調整可能なデータ</mark>として定義するのが望ましいです。

---

### 练习 4

> [!question] 题目
> 如果技能特效提前消失，请检查粒子生命周期、对象池回收时机和父节点状态。

> [!example] 自己的答案
>　スキルエフェクトが早めに消えた場合、パーティクルのライフサイクル、オブジェクトプールに返却するタイミングと親ノード状態を確認してください

> [!warning] 指正
> 主要意思正确。`早めに消えた` 可以理解，但“不该消失却提前消失”更自然是 `想定より早く消える`。`ライフサイクル` 可以用，粒子语境也可说 `寿命`。`親ノード状態` 前加 `の` 更自然。

> [!tip] 参考回答
> スキルエフェクトが<mark style="background: #FF5582A6;">想定より早く</mark>消える場合は、パーティクルの寿命、オブジェクトプールへ回収するタイミング、親ノードの状態を確認してください。

---

### 练习 5

> [!question] 题目
> 在制作装备强化界面时，成功率、材料数量和预览结果应该来自同一份计算结果。

> [!example] 自己的答案
>　装備強化画面を作成する時に、成功率、素材数とプレビュー結果が同じ結果由来です。

> [!warning] 指正
> 题意接近，但 `素材数とプレビュー結果` 并列时建议用顿号连接。`同じ結果由来です` 不自然，应说 `同じ計算結果から取得する` 或 `同じ計算結果に基づく`。

> [!tip] 参考回答
> 装備強化画面を作成するときは、成功率、素材数、プレビュー結果を同じ計算結果から取得するべきです。

---

### 练习 6

> [!question] 题目
> 为了避免帧率抖动，加载大型贴图前可以先显示占位图，并把实际替换延后到安全时机。

> [!example] 自己的答案
>　フレームレートがちらつかないように、大きなテクスチャをロードする前にホルドを先に表示し、切り替えを安全なタイミングまでに遅延します。

> [!warning] 指正
> 意思能传达，但几个词需要修正。`フレームレートがちらつかない` 不自然，帧率抖动常说 `フレームレートのばらつき` 或 `フレームレートの揺れ`。`ホルド` 应为 `プレースホルダー`。`までに遅延します` 应改为 `まで遅らせます`。

> [!tip] 参考回答
> フレームレートの揺れを避けるために、大きなテクスチャを読み込む前に<mark style="background: #FF5582A6;">プレースホルダー</mark>を表示し、実際の差し替えは安全なタイミングまで遅らせることができます。

---

### 练习 7

> [!question] 题目
> 当 Boss 进入第二阶段时，行为树应该切换黑板参数，而不是直接改写每个节点的逻辑。

> [!example] 自己的答案
>　ボスが二番目フェーズに入る時に、BehaviourTreeがブラックボードパラメータを切り替えるべきです。各ノードのロジックを書き換えるのではありません。

> [!warning] 指正
> 大意正确。`二番目フェーズ` 应为 `第2フェーズ` 或 `第二段階`。`BehaviourTreeが...切り替える` 可以表达，但日语资料中常写 `ビヘイビアツリー`。后半独立句没问题，不过合成一句会更自然。

> [!tip] 参考回答
> ボスが第2フェーズに入ったときは、各ノードのロジックを直接書き換えるのではなく、ビヘイビアツリー側でブラックボードパラメータを切り替えるべきです。

---

### 练习 8

> [!question] 题目
> 调试 UI 焦点丢失问题时，请记录当前选中的控件、输入设备和界面层级。

> [!example] 自己的答案
>　UIフォーカスがなくなった不具合をデバッグする時に、選ばれたウェジェット、入力設備と画面階層を記録してください

> [!warning] 指正
> 意思基本完整。`フォーカスがなくなった不具合` 能懂，但更自然是 `フォーカスが失われる問題`。`ウェジェット` 应为 `ウィジェット`。`入力設備` 不自然，UI 输入语境用 `入力デバイス`。

> [!tip] 参考回答
> UIフォーカスが失われる問題をデバッグするときは、現在選択されているウィジェット、<mark style="background: #FF5582A6;">入力デバイス</mark>、画面階層を記録してください。

---

### 练习 9

> [!question] 题目
> 在实现受击硬直时，硬直时间应该由攻击数据决定，而不是写在角色蓝图里。

> [!example] 自己的答案
>　被弾硬直を実装する際に、硬直時間は攻撃で決めるべきで、キャラクターのBlueprintでハードコードするのではありません。

> [!warning] 指正
> 题意清楚。`攻撃で決める` 略泛，题目是“由攻击数据决定”，建议明确为 `攻撃データで決める` 或 `攻撃データに基づいて決める`。

> [!tip] 参考回答
> 被弾硬直を実装する際は、硬直時間を攻撃データに基づいて決めるべきで、キャラクターのブループリントにハードコードするべきではありません。

---

### 练习 10

> [!question] 题目
> 如果场景中阴影成本过高，请先比较动态光源数量、阴影距离和角色数量。

> [!example] 自己的答案
>　シーンの中でシャドウ負荷が高い場合、動的の光源数、シャドウ距離とキャラクター数を比較してください。

> [!warning] 指正
> 意思完整。`シーンの中で` 可简化为 `シーン内で`。`動的の光源数` 应为 `動的光源の数`。并列时 `動的光源の数、シャドウ距離、キャラクター数` 更自然。

> [!tip] 参考回答
> シーン内でシャドウのコストが高い場合は、まず動的光源の数、シャドウ距離、キャラクター数を比較してください。

---

### 练习 11

> [!question] 题目
> 存档版本升级时，需要保留旧字段的转换逻辑，避免玩家数据读取失败。

> [!example] 自己的答案
>　セーブデータをアップグレードする時に、旧フィールドの移行ロジックを残し、プレイヤーデータの読み込みに失敗しないようにします。

> [!warning] 指正
> 主要意思正确。`セーブデータをアップグレードする時` 可以理解，但题目是“存档版本升级时”，更自然是 `セーブデータのバージョンを上げるとき`。`旧フィールドの移行ロジック` 很好。整体只需让“避免读取失败”更贴题。

> [!tip] 参考回答
> セーブデータのバージョンを上げるときは、旧フィールドの変換ロジックを残し、プレイヤーデータの読み込みに失敗しないようにする必要があります。

---

### 练习 12

> [!question] 题目
> Unity 的 LOD 切换距离应该根据相机距离和目标平台性能一起调整。

> [!example] 自己的答案
>　UnityのLOD切り替え距離はカメラとの距離とプラットフォーム性能と一緒に調整するべきです。

> [!warning] 指正
> 意思能表达。`カメラとの距離とプラットフォーム性能と一緒に` 连接不自然，建议改为 `カメラとの距離と対象プラットフォームの性能を考慮して`。`ターゲットプラットフォーム` 也可以。

> [!tip] 参考回答
> UnityのLOD切り替え距離は、カメラとの距離とターゲットプラットフォームの性能を考慮して調整するべきです。

---

### 练习 13

> [!question] 题目
> 在性能分析报告里，请把采样场景、平均帧时间和最高峰值分开记录。

> [!example] 自己的答案
>　パフォーマンスレポートでは、シーンのサンプル、平均フレーム時間、ピークをそれぞれにきろくしてください。

> [!warning] 指正
> 题意基本正确。`シーンのサンプル` 容易理解成样本而不是采样场景，建议说 `サンプリングしたシーン` 或 `計測シーン`。`きろく` 应写成汉字 `記録`。`最高峰值` 可译为 `最大ピーク値`。

> [!tip] 参考回答
> パフォーマンス分析レポートでは、サンプリングしたシーン、平均フレーム時間、<mark style="background: #FF5582A6;">最大ピーク値</mark>を分けて記録してください。

---

### 练习 14

> [!question] 题目
> 周末如果你有时间，我们可以一起整理书架，再做一顿简单的晚饭。

> [!example] 自己的答案
>　週末に時間があれば、一緒に本棚を整理したり、簡単な晩飯を作ったりします

> [!warning] 指正
> 意思大致正确，但语气不像邀请。`作ったりします` 是陈述习惯，不适合“我们可以一起...吧”。`晩飯` 偏随意粗口，日常自然表达建议用 `夕食` 或 `晩ご飯`。可以用 `一緒に...しませんか`。

> [!tip] 参考回答
> 週末に時間があれば、一緒に本棚を整理して、それから簡単な夕食を作りませんか。

---

### 练习 15

> [!question] 题目
> 我会在会议前整理好风险清单，如果有遗漏，请直接在文档里补充。

> [!example] 自己的答案
>　会議前にリスクを整理し終わり、もし何か漏れたら、ドキュメントに直接補足してください

> [!warning] 指正
> 职场意思基本清楚。`整理し終わり` 不自然，表示“整理好”建议用 `整理しておきます`。`漏れたら` 主语不明，题目是“如果有遗漏”，可说 `漏れがあれば`。`補足` 可以用，但文档里补充更自然是 `追記`。

> [!tip] 参考回答
> 会議前にリスク一覧を整理しておきますので、漏れがあればドキュメントに直接追記してください。

## 2026-08-02

### 练习 1

> [!question] 题目
> 在 Unreal 中实现可破坏物体时，生命值计算和碎片表现应该保持解耦。

> [!example] 自己的答案
> Unrealで破壊可能のオブジェクトを実装する時に、HP計算と断片表現を疎結合にするべきです。

> [!warning] 指正
> 意思基本清楚。`破壊可能のオブジェクト` 应改为 `破壊可能なオブジェクト`。`断片表現` 能懂，但可破坏物的碎片表现更自然是 `破片の表現` 或 `破片演出`。`疎結合にする` 可以用，不过题目是“保持解耦”，用 `切り離しておく` 更直接。

> [!tip] 参考回答
> Unrealで破壊可能なオブジェクトを実装するときは、HP計算と破片の表現を切り離しておくべきです。

---

### 练习 2

> [!question] 题目
> Unity 的技能指示器应该根据地形高度调整投影位置，避免看起来浮在空中。

> [!example] 自己的答案
>　Unityのスキルインジケーターは地形の高さに応じて投影位置を調整し、空中に浮いてるに見えることを避けます。

> [!warning] 指正
> 大意正确。`空中に浮いてるに見える` 语法不对，应为 `空中に浮いているように見える`。`避けます` 稍硬，技术说明里可说 `避けるべきです` 或 `避けます` 但前面要接自然。

> [!tip] 参考回答
> Unityのスキルインジケーターは、地形の高さに応じて投影位置を調整し、空中に浮いているように見えないようにするべきです。

---

### 练习 3

> [!question] 题目
> 如果角色转身动作不自然，请检查旋转插值速度和动画混合空间的参数。

> [!example] 自己的答案
>　キャラクターの回転が不自然に見える場合、回転補間速度とアニメーションブレンドスペースのパラメータを確認してください

> [!warning] 指正
> 意思完整。`キャラクターの回転` 会偏向“旋转本身”，题目是“转身动作”，可说 `振り向き動作`。句末少了句号。`回転補間速度` 和 `アニメーションブレンドスペース` 都是可接受的技术表达。

> [!tip] 参考回答
> キャラクターの振り向き動作が不自然に見える場合は、回転補間速度とアニメーションブレンドスペースのパラメータを確認してください。

---

### 练习 4

> [!question] 题目
> 在设计技能取消规则时，需要明确哪些动作可以被闪避打断。

> [!example] 自己的答案
>　スキルのキャンセルルールを設計する時に、回避による中断可能のアクションを明確にする必要があります。

> [!warning] 指正
> 题意基本表达出来了。`中断可能のアクション` 不自然，应改为 `中断可能なアクション` 或 `中断できるアクション`。`回避による` 可以用，但这里“被闪避打断”更自然是 `回避でキャンセルできる`。

> [!tip] 参考回答
> スキルのキャンセルルールを設計するときは、どのアクションを回避でキャンセルできるのかを明確にする必要があります。

---

### 练习 5

> [!question] 题目
> 渲染草地时，可以通过减少阴影投射和调整密度来降低开销。

> [!example] 自己的答案
>　芝地を描画する時に、シャドウキャストを減らすと密度を調整することで負荷を軽減します。

> [!warning] 指正
> 意思能传达。`芝地` 更像“草坪”，游戏渲染里的“草地/草”通常用 `草` 或 `草地`。`シャドウキャスト` 可理解，但自然说法是 `影の投射`。`減らすと密度を調整することで` 应改为 `減らしたり、密度を調整したりすることで`。

> [!tip] 参考回答
> 草を描画するときは、影の投射を減らしたり密度を調整したりすることで、描画コストを下げることができます。

---

### 练习 6

> [!question] 题目
> 资源卸载前，请确认场景中没有组件继续持有它的引用。

> [!example] 自己的答案
>　リソースをアンロードする前に、シーン内でこのリソースの参照を持っているコンポーネントがあるかを確認してください。

> [!warning] 指正
> 主要意思有了，但逻辑反了。题目要求确认“没有组件继续持有引用”，你的句子是“确认是否有组件持有引用”，可能听起来只是查有没有而不是确保没有。`このリソース` 可说，若指代前面的资源，`そのリソース` 更自然。

> [!tip] 参考回答
> リソースをアンロードする前に、シーン内のコンポーネントがそのリソースへの参照を保持していないか確認してください。

---

### 练习 7

> [!question] 题目
> 当玩家切换武器时，准星样式、射击间隔和后坐力参数都应该一起更新。

> [!example] 自己的答案
>　プレイヤーが武器を切り替える時に、照準仕様、撃ち間隔、打ち返しパラメータを一括で更新するべきです。

> [!warning] 指正
> 题意大致能懂，但技术词需要调整。`照準仕様` 不够自然，“准星样式”可说 `照準の見た目` 或 `レティクルの種類`。`撃ち間隔` 可理解，但 `射撃間隔` 更正式。`打ち返し` 是“还击/反击”，后坐力应为 `反動` 或 `リコイル`。

> [!tip] 参考回答
> プレイヤーが武器を切り替えるときは、照準の見た目、射撃間隔、反動パラメータをまとめて更新するべきです。

---

### 练习 8

> [!question] 题目
> 在制作任务追踪 UI 时，已完成目标和进行中的目标应该有不同的显示状态。

> [!example] 自己的答案
> クエスト追跡のUIを作成する時に、完了済みターゲットと進行中のターゲットが別々の表示状態があります。

> [!warning] 指正
> 意思基本清楚。`完了済みターゲット` 在任务 UI 里容易误解为目标对象，建议用 `完了済みの目標`。`が別々の表示状態があります` 不自然，应改为 `には別々の表示状態を用意するべきです`。

> [!tip] 参考回答
> クエスト追跡UIを作成するときは、完了済みの目標と進行中の目標に別々の表示状態を用意するべきです。

---

### 练习 9

> [!question] 题目
> 如果碰撞检测结果和画面表现不一致，请先确认可见模型和碰撞形状是否对齐。

> [!example] 自己的答案
>　コリジョン結果と画面表示が不一致している場合、まず見えるモデルとコリジョンの形はそろっていないかを確認してください

> [!warning] 指正
> 题意接近。`不一致している` 应为 `一致していない`。`見えるモデル` 可理解，但技术表达更自然是 `表示モデル` 或 `見た目のモデル`。`そろっていないか` 是“是否没有对齐”，题目要确认是否对齐，可说 `一致しているか`。

> [!tip] 参考回答
> コリジョン検出の結果と画面上の表示が一致していない場合は、まず表示モデルとコリジョン形状がそろっているか確認してください。

---

### 练习 10

> [!question] 题目
> 为了减少战斗结算错误，伤害来源和目标对象都应该在计算前做有效性检查。

> [!example] 自己的答案
>　バトル計算ミスを減らすために、ダメージ元とターゲットの有効性を計算する前にチャックする必要があります。

> [!warning] 指正
> 大意正确。`バトル計算ミス` 可以懂，但题目是“战斗结算错误”，更自然是 `戦闘の集計ミス` 或 `戦闘処理の計算ミス`。`ダメージ元` 建议说 `ダメージソース` 或 `ダメージ発生元`。`チャック` 是拼写错误，应为 `チェック`。

> [!tip] 参考回答
> 戦闘処理の計算ミスを減らすために、ダメージ発生元と対象オブジェクトは、計算前に有効性チェックを行う必要があります。

---

### 练习 11

> [!question] 题目
> 在实现教程步骤时，触发条件和提示文本最好由配置数据管理。

> [!example] 自己的答案
>　チュートリアルを実装する時に、トリガー条件と提示テキストを設定データで配置できるのが望ましいです。

> [!warning] 指正
> 主要意思能看懂。`提示テキスト` 可用，但日语 UI 语境更自然是 `ヒントテキスト` 或 `表示テキスト`。`設定データで配置できる` 不符合“由配置数据管理”，应改成 `設定データで管理する`。

> [!tip] 参考回答
> チュートリアルのステップを実装するときは、トリガー条件とヒントテキストを設定データで管理するのが望ましいです。

---

### 练习 12

> [!question] 题目
> 如果加载界面停留太久，请分别记录资源准备时间和场景初始化时间。

> [!example] 自己的答案
>　ロード画面が長時間に止まっている場合、準備とシーン初期化にかかる時間をそれぞれに記録してください。

> [!warning] 指正
> 意思基本明确。`長時間に止まっている` 不自然，应为 `長時間止まっている` 或 `長く表示されたままになる`。`準備` 少了“资源”，建议写 `リソース準備時間`。`それぞれに記録` 应为 `それぞれ記録`。

> [!tip] 参考回答
> ロード画面が長く表示されたままになる場合は、リソースの準備時間とシーンの初期化時間を分けて記録してください。

---

### 练习 13

> [!question] 题目
> 在调整敌人攻击范围时，需要同时检查判定半径、动画距离和玩家反馈。

> [!example] 自己的答案
>　敵の攻撃範囲を調整する時に、判定半径、アニメーション距離、プレイヤーフィードバックを同時に確認する必要があります。

> [!warning] 指正
> 题意完整。`アニメーション距離` 能懂但略硬，可以说 `アニメーション上の距離`。`プレイヤーフィードバック` 可用，不过“玩家反馈”若指受击感/提示，也可说 `プレイヤーへのフィードバック`，更明确。

> [!tip] 参考回答
> 敵の攻撃範囲を調整するときは、判定半径、アニメーション上の距離、プレイヤーへのフィードバックを同時に確認する必要があります。

---

### 练习 14

> [!question] 题目
> 如果明天不下雨，我们就早点出门，顺便买早餐。

> [!example] 自己的答案
>　明日雨が降らないなら、早めに出かけて、ついでに朝ご飯を買いましょう。

> [!warning] 指正
> 日常表达自然，意思也完整。`明日` 稍偏书面，口语里 `明日` 读作 `あした` 没问题；写作日常对话可用 `明日` 或 `あした`。整体只需补上更自然的条件语气即可。

> [!tip] 参考回答
> 明日雨が降らなければ、早めに出かけて、ついでに朝ご飯を買いましょう。

---

### 练习 15

> [!question] 题目
> 我会先确认修复范围，再和负责测试的同事对齐验证计划。

> [!example] 自己的答案
>　先に修正範囲を確認し、テストの担当者と検証プランを揃えます。

> [!warning] 指正
> 职场意思清楚。`テストの担当者` 可用，但“负责测试的同事”更自然是 `テスト担当の同僚` 或 `テスト担当者`。`検証プランを揃えます` 能理解，不过“对齐计划”常说 `認識を合わせる` 或 `すり合わせる`。

> [!tip] 参考回答
> 先に修正範囲を確認してから、テスト担当の同僚と検証計画をすり合わせます。
