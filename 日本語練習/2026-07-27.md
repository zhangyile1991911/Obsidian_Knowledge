## 2026-07-27

### 练习 1

> [!question] 题目
> 在 Unreal 中实现锁定目标时，应该把目标选择逻辑和镜头旋转逻辑分开管理。

> [!example] 自己的答案
>　Unrealではターゲットをロックする時に、ターゲットの選択とカメラ回転ロジックを分けて管理するべきです。

> [!warning] 指正
> 意思完整，表达也自然。`カメラ回転ロジック` 能懂，但技术说明里更自然是 `カメラの回転ロジック`。

> [!tip] 参考回答
> Unrealでターゲットロックを実装するときは、ターゲット選択ロジックとカメラの回転ロジックを分けて管理するべきです。

---

### 练习 2

> [!question] 题目
> Unity 的对象池归还子弹时，需要重置速度、生命周期和碰撞状态。

> [!example] 自己的答案
>　Unityではオブジェクトプールに弾を返却する時に、速度、ライフサイクルとコリジョンをリセットする必要があります

> [!warning] 指正
> 主要意思正确。并列时 `速度、ライフサイクルとコリジョン` 建议改为 `速度、生存時間、コリジョン状態`

> [!tip] 参考回答
> Unityのオブジェクトプールに弾を返却するときは、速度、生存時間、コリジョン状態をリセットする必要があります。

---

### 练习 3

> [!question] 题目
> 如果角色的受击动画被打断，请先确认状态机条件和动画层权重。

> [!example] 自己的答案
>　キャラクターの被弾アニメーションが中断された場合、まずステートマーシンの条件とアニメーション層の重みを確認します。

> [!warning] 指正
> 意思清楚，但 `ステートマーシン` 应为 `ステートマシン`。`アニメーション層の重み` 可以理解，更常见说法是 `アニメーションレイヤーのウェイト`。

> [!tip] 参考回答
> キャラクターの被弾アニメーションが中断された場合は、まずステートマシンの条件と<mark style="background: #FF5582A6;">アニメーションレイヤーのウェイト</mark>を確認してください。

---

### 练习 4

> [!question] 题目
> 在设计技能队列时，输入缓冲区只保存短时间内有效的操作。

> [!example] 自己的答案
>　スキルキューを設計する時に、入力バッファーでは短時間内の有効操作だけを保持します。

> [!warning] 指正
> 翻译基本准确。`短時間内の有効操作` 能懂，但更自然是 `短時間だけ有効な入力` 或 `操作`。

> [!tip] 参考回答
> スキルキューを設計するときは、入力バッファには短時間だけ有効な操作だけを保持します。

---

### 练习 5

> [!question] 题目
> 渲染大量透明特效时，请减少重叠面积，并优先检查过度绘制成本。

> [!example] 自己的答案
>　大量の透明エフェクトを描画する時に、重なる面積を減らし、オーバードローを優先的に確認してください。

> [!warning] 指正
> 整体自然，题意也完整。如果要更明确“过度绘制成本”，可以说 `オーバードローのコスト`。

> [!tip] 参考回答
> 大量の透明エフェクトを描画するときは、重なり合う面積を減らし、オーバードローのコストを優先的に確認してください。

---

### 练习 6

> [!question] 题目
> 地图切换前，应该先保存玩家位置、任务进度和临时增益状态。

> [!example] 自己的答案
>　マップを切り替える前に、プレイヤーの位置を保存しておき、クエスト進捗と一時的なバフを保存しておくべきです。

> [!warning] 指正
> 意思正确，但 `保存しておき` 重复了两次，可以合并并列项。`一時的なバフ` 很自然；“增益状态”也可以更明确写成 `一時的なバフ状態`。

> [!tip] 参考回答
> マップを切り替える前に、プレイヤーの位置、クエスト進捗、一時的なバフ状態を保存しておくべきです。

---

### 练习 7

> [!question] 题目
> 背包排序完成后，UI 应该根据新的格子数据刷新显示顺序。

> [!example] 自己的答案
>　インベントリが並び替えた後に、セルデータに応じて表示順を更新するべきです。

> [!warning] 指正
> 题意基本能表达，但主语有偏差。`インベントリが並び替えた後に` 听起来像“背包自己排序了”，更自然是 `インベントリのソートが完了した後`。题目说 UI 刷新显示顺序，建议明确 `UIは...更新する`。

> [!tip] 参考回答
> インベントリのソートが完了した後、UIは新しいセルデータに基づいて表示順を更新するべきです。

---

### 练习 8

> [!question] 题目
> 使用数据表配置敌人参数时，请在启动阶段检查必填字段是否为空。

> [!example] 自己的答案
>　データテーブルでは敵のパラメータを配置するときに、起動段階で必須フィールドが設定されているかを確認してください。

> [!warning] 指正
> 基本意思正确。`データテーブルでは敵のパラメータを配置する` 不太自然，应为 `データテーブルで敵のパラメータを設定する`。题目是“是否为空”，你的 `設定されているか` 可以表达，但 `空になっていないか` 更贴切。

> [!tip] 参考回答
> データテーブルで敵のパラメータを設定するときは、起動段階で必須フィールドが空になっていないか確認してください。

---

### 练习 9

> [!question] 题目
> 当角色进入攀爬状态时，移动组件需要切换到专用的速度计算方式。

> [!example] 自己的答案
>キャラクターが状態に入る時、移動コンポーネントが専用な速度計算方式に切り替える必要があります。

> [!warning] 指正
> 这里漏掉了“攀爬状态”，`状態に入る` 信息不足。`専用な` 应改为 `専用の`。`移動コンポーネントが...切り替える` 主语和动词搭配不自然，应说 `移動コンポーネントを...切り替える` 或 `移動コンポーネント側で...切り替える`。

> [!tip] 参考回答
> キャラクターがクライミング状態に入るときは、移動コンポーネントを専用の速度計算方式に切り替える必要があります。

---

### 练习 10

> [!question] 题目
> 为了降低加载卡顿，可以把大型音效资源拆成按场景使用的小包。

> [!example] 自己的答案
>　ロードかくツキを軽減するため、大きなサイズのオディオリソースをシーン単位で小さいアセットに切り分けます。

> [!warning] 指正
> 意思大致正确，但有几个词需要修正。`ロードかくツキ` 应写成 `ロード時のカクつき`。`オディオ` 应为 `オーディオ`。`シーン単位で小さいアセットに切り分けます` 可以用，但题目是“小包”，更自然是 `小さなパッケージに分割します`。

> [!tip] 参考回答
> ロード時のカクつきを軽減するために、大きなオーディオリソースをシーンごとに使用する小さなパッケージ<mark style="background: #FF5582A6;">に分割</mark>できます。

---

### 练习 11

> [!question] 题目
> 调试 AI 巡逻路线时，请记录当前路点、等待时间和下一条移动指令。

> [!example] 自己的答案
>　AIのパトロール経路をデバッグする時に、現在のポイント、待ち時間と次の行動コマンドを記録します。

> [!warning] 指正
> 基本意思能传达。`現在のポイント` 稍泛，巡逻路线中的“路点”更自然是 `現在のウェイポイント`。`待ち時間と次の行動コマンド` 并列时建议写成 `待ち時間、次の移動指示`。题目是“移动指令”，`移動指示` 更准确。

> [!tip] 参考回答
> AIのパトロール経路をデバッグするときは、現在のウェイポイント、待ち時間、次の移動指示を記録してください。

---

### 练习 12

> [!question] 题目
> 在制作设置界面时，滑块数值变化后应该立即更新预览音量。

> [!example] 自己的答案
>　設定画面を制作する時に、スライドが変化した後、音量を直ちに更新するべきです。

> [!warning] 指正
> 题意有一部分缺失。`スライド` 应为 UI 控件 `スライダー`。题目是“滑块数值变化后”，建议写成 `スライダーの値が変化したら`。另外“预览音量”应明确为 `プレビュー音量`，不是普通的 `音量`。

> [!tip] 参考回答
> 設定画面を作るときは、スライダーの値が変化したら、すぐにプレビュー音量を更新するべきです。

---

### 练习 13

> [!question] 题目
> 如果低端设备帧率不稳定，请先比较阴影质量、后处理和角色数量的影响。

> [!example] 自己的答案
>　フレームレートがローエンド端末で不安定している場合、影の質、ポストプロセスとキャラクター数をそれぞれ比較してください。

> [!warning] 指正
> 意思基本正确。`不安定している` 应为 `不安定になっている` 或 `安定しない`。`ポストプロセス` 可以用，但常见说法是 `ポストプロセス` 或 `ポストエフェクト`；“影响”最好补出 `影響`，不要只说比较三个项目。

> [!tip] 参考回答
> ローエンド端末でフレームレートが安定しない場合は、まず影の品質、ポストプロセス、キャラクター数の影響を比較してください。

---

### 练习 14

> [!question] 题目
> 今天下班后如果还有时间，我们去车站旁边那家店吃晚饭吧。

> [!example] 自己的答案
>　仕事が終わった後時間があれば、駅の近くの店で晩ご飯を食べましょう

> [!warning] 指正
> 日常表达自然，意思也基本完整。`仕事が終わった後時間があれば` 中间建议加 `に` 或改成 `仕事の後でまだ時間があれば`。题目是“车站旁边那家店”，`駅の近くの店` 稍泛，可以说 `駅のそばのあの店`。句末需要句号。

> [!tip] 参考回答
> 今日、仕事の後でまだ時間があれば、駅のそばのあの店で晩ご飯を食べましょう。

---

### 练习 15

> [!question] 题目
> 我已经整理好这次修改的影响范围，稍后会发给大家确认。

> [!example] 自己的答案
>　今回の修正による影響範囲をすでに整理しました、後皆さんに送って確認していただきます。

> [!warning] 指正
> 意思基本完整，但逗号不能直接连接两个完整句子，建议拆开或用 `ので`。`後皆さんに` 应为 `後で皆さんに`，职场语气里更自然是 `後ほど皆さんに共有します`。`確認していただきます` 像安排对方做事，若是“请大家确认”，可用 `確認していただければと思います`。

> [!tip] 参考回答
> 今回の修正による影響範囲はすでに整理しましたので、後ほど皆さんに共有して確認していただければと思います。

## 2026-07-28

### 练习 1

> [!question] 题目
> 在 Unreal 中调整材质实例参数时，应该避免每帧重复创建动态材质实例。

> [!example] 自己的答案
> Unrealでマテリアルのインスタンスを調整する時に、毎フレーム動的なマテリアルインスタンスを生成することを避けるべきです。

> [!warning] 指正
> 意思基本完整。`マテリアルのインスタンスを調整する` 少了“参数”，建议说 `マテリアルインスタンスのパラメータを調整する`。`動的なマテリアルインスタンス` 可以理解，但 Unreal 语境里更自然是 `動的マテリアルインスタンス`。`調整する時に` 可写成 `調整するときは`。

> [!tip] 参考回答
> Unrealでマテリアルインスタンスのパラメータを調整するときは、毎フレーム動的マテリアルインスタンスを作成し直すことは避けるべきです。

---

### 练习 2

> [!question] 题目
> Unity 的技能冷却显示应该由数据变化驱动，而不是在按钮里直接计算战斗规则。

> [!example] 自己的答案
>　Unityのスキルクールダウンではデータの変化で駆動するべきです。ボタン内にバトル計算ルールを実装するのではありません。

> [!warning] 指正
> 题意能传达，但第一句主语不够准确。不是“冷却本身由数据驱动”，而是“冷却显示由数据变化驱动”，建议补出 `表示`。`バトル計算ルール` 不自然，可以说 `戦闘ルールの計算`。第二句可合并成更自然的对比句。

> [!tip] 参考回答
> Unityのスキルクールダウン表示は、<mark style="background: #FF5582A6;">データの変化によって更新されるべきで</mark>、ボタン内で直接戦闘ルールを計算するべきではありません。

---

### 练习 3

> [!question] 题目
> 如果近战命中判定不稳定，请先检查碰撞体大小、动画通知时间点和角色朝向。

> [!example] 自己的答案
>　近接ヒット判定が不安定になる場合は、まずコリジョンの大きさ、アニメーション通知タイミングとキャラクター向き合いを確認します。

> [!warning] 指正
> 主要意思正确。并列时 `タイミングとキャラクター向き合い` 不自然，建议用 `タイミング、キャラクターの向き`。`向き合い` 是“面对/相互面对”的名词化，不适合这里的“朝向”。`アニメーション通知` 可用，游戏开发语境中 `Anim Notify` 或 `アニメーション通知` 都可以。

> [!tip] 参考回答
> 近接攻撃のヒット判定が不安定な場合は、まずコリジョンの大きさ、アニメーション通知のタイミング、キャラクターの向きを確認してください。

---

### 练习 4

> [!question] 题目
> 在实现命令模式时，每个输入命令都应该封装执行条件和回滚处理。

> [!example] 自己的答案
>　コマンドパータンを実装する時に、入力コマンドごとに実行条件とフォールバックをカプセル化するべきです。

> [!warning] 指正
> `コマンドパータン` 应为 `コマンドパターン`。`フォールバック` 是“备用处理”，不等于“回滚处理”；这里应使用 `ロールバック処理`。`カプセル化する` 很合适。

> [!tip] 参考回答
> コマンドパターンを実装するときは、各入力コマンドに実行条件と<mark style="background: #FF5582A6;">ロールバック処理</mark>をカプセル化するべきです。

---

### 练习 5

> [!question] 题目
> 角色换装后，需要刷新骨骼挂点、材质参数和碰撞配置。

> [!example] 自己的答案
>　キャラクターが装備を切り替えた後、ボーンのソケット、マテリアルパラメータとコリジョン設定を更新する必要があります。

> [!warning] 指正
> 意思清楚。`キャラクターが装備を切り替えた後` 可以用，但题目是“角色换装后”，也可说 `キャラクターの衣装を切り替えた後`。`ボーンのソケット` 对“骨骼挂点”很自然。并列时建议用顿号连接：`ボーンソケット、マテリアルパラメータ、コリジョン設定`。

> [!tip] 参考回答
> キャラクターの衣装を切り替えた後は、ボーンソケット、マテリアルパラメータ、コリジョン設定を更新する必要があります。

---

### 练习 6

> [!question] 题目
> 为了减少场景进入时的等待时间，可以按区域预加载必要的特效和角色资源。

> [!example] 自己的答案
>　シーンに入る時の待ち時間を減らすため、エリアに応じる必要なエフェクトとキャラクターリソースを事前にロードしておきます

> [!warning] 指正
> 基本意思正确。`シーンに入る時` 建议写成 `シーンに入るとき` 或更自然的 `シーン遷移時`。`エリアに応じる必要な` 应为 `エリアに応じて必要な`。句末需要句号。

> [!tip] 参考回答
> シーンに入るときの待ち時間を減らすために、エリアごとに必要なエフェクトやキャラクターリソースを事前にロードしておくことができます。

---

### 练习 7

> [!question] 题目
> 当 UI 焦点从背包移动到装备栏时，手柄导航应该保持清晰的选中状态。

> [!example] 自己的答案
>　UIフォーカスがインベントリから装備欄に遷移する時に、ゲームパットは選択中状態を保持しているべきです。

> [!warning] 指正
> 题意有一处偏差。不是“手柄保持选中状态”，而是“手柄导航保持清晰的选中状态”，建议主语用 `ゲームパッドナビゲーション`。`ゲームパット` 应为 `ゲームパッド`。`遷移する時に` 可改为 `移動するときは`。

> [!tip] 参考回答
> UIフォーカスがインベントリから装備欄へ移動するときは、ゲームパッドナビゲーションで分かりやすい選択状態を維持するべきです。

---

### 练习 8

> [!question] 题目
> 保存数据版本升级时，请为旧字段准备默认值和兼容处理。

> [!example] 自己的答案
>　セーブデータをアップグレードする時に、旧フィールドにデフォルト値と互換処理を用意してください。

> [!warning] 指正
> 意思基本准确。`セーブデータをアップグレードする時` 可以表达，但题目是“保存数据版本升级时”，更完整是 `セーブデータのバージョンを上げるとき`。`旧フィールドにデフォルト値と互換処理を用意する` 可理解，不过 `旧フィールド向けに` 更自然。

> [!tip] 参考回答
> セーブデータのバージョンを上げるときは、古いフィールド向けにデフォルト値と互換処理を用意してください。

---

### 练习 9

> [!question] 题目
> 如果敌人的寻路结果绕远路，请可视化导航网格并确认障碍物标记。

> [!example] 自己的答案
>　敵が経路探索する時に遠回りする場合、ナビメッシュを可視化して障害物マーカを確認してください

> [!warning] 指正
> 主要意思正确。`敵が経路探索する時に遠回りする` 能懂，但题目强调“寻路结果绕远路”，更自然是 `敵の経路探索結果が遠回りになる場合`。`マーカ` 建议写成长音 `マーカー`。句末需要句号。

> [!tip] 参考回答
> <mark style="background: #FFB86CA6;">敵の経路探索結果が遠回りになる場合は</mark>、ナビメッシュを可視化して、障害物のマーカーを確認してください。

---

### 练习 10

> [!question] 题目
> 在优化粒子系统时，应该限制发射数量，并检查贴图尺寸是否过大。

> [!example] 自己的答案
>　パーティクルシステムを最適化する時に、発射数を制限するべきです。その上でテクスチャのサイズが大きすぎるかを確認します。

> [!warning] 指正
> 表达自然，意思也完整。`発射数` 可以用，如果强调粒子数量，`エミッション数` 或 `発生数` 更贴近粒子系统语境。`大きすぎるかを確認します` 建议写成 `大きすぎないか確認します`。

> [!tip] 参考回答
> パーティクルシステムを最適化するときは、<mark style="background: #FF5582A6;">エミッション数</mark>を制限し、テクスチャサイズが大きすぎないか確認するべきです。

---

### 练习 11

> [!question] 题目
> 关卡机关触发后，状态变化应该先写入玩法数据，再通知表现层播放动画。

> [!example] 自己的答案
>　レベルのギミックが発火した後、状態変化をゲームプレイーデータに書き込み、表現層に送信してアニメーションを再生します。

> [!warning] 指正
> 大意正确，但 `ゲームプレイーデータ` 应为 `ゲームプレイデータ`。`発火した後` 在程序语境可用，不过机关触发更自然是 `作動した後` 或 `トリガーされた後`。`表現層に送信して` 不够准确，应是“通知表现层”，可说 `表現層に通知して`。

> [!tip] 参考回答
> <mark style="background: #FF5582A6;">レベルギミックが作動した</mark>後は、状態変化を先にゲームプレイデータへ書き込み、その後で表現層に通知してアニメーションを再生させるべきです。

---

### 练习 12

> [!question] 题目
> 打包前，请检查未使用资源、调试日志和平台专用配置。

> [!example] 自己的答案
>　ビルド前に、未使用のリソース、デバッグログとプラトフォーム設定を確認してください。

> [!warning] 指正
> 意思基本完整。`プラトフォーム` 应为 `プラットフォーム`。`デバッグログとプラットフォーム設定` 并列时建议用 `デバッグログ、プラットフォーム固有の設定`。题目里的“专用配置”最好译成 `固有の設定`。

> [!tip] 参考回答
> パッケージ化の前に、未使用のリソース、デバッグログ、プラットフォーム固有の設定を確認してください。

---

### 练习 13

> [!question] 题目
> 当相机穿过墙面时，可以用射线检测调整相机距离。

> [!example] 自己的答案
>　カメラが壁をすり抜けた場合、レイトレースしてカメラの距離を調整できます。

> [!warning] 指正
> 意思能表达。`壁をすり抜けた場合` 是“已经穿过去了”，题目更像一般条件，建议说 `壁をすり抜ける場合` 或 `壁にめり込む場合`。`レイトレースして` 可用，但 gameplay 相机碰撞里常说 `レイキャスト`；如果在 Unreal 语境也可以说 `ライントレース`。

> [!tip] 参考回答
> カメラが壁をすり抜ける場合は、<mark style="background: #FF5582A6;">レイキャスト</mark>を使ってカメラ距離を調整できます。

---

### 练习 14

> [!question] 题目
> 周末如果天气好的话，我们一起去公园散步吧。

> [!example] 自己的答案
>　週末に天気が良ければ、一緒に公園で散歩しましょう

> [!warning] 指正
> 日常表达自然，意思完整。`公園で散歩しましょう` 可以用，但“去公园散步”更自然是 `公園を散歩しましょう` 或 `公園に散歩に行きましょう`。句末需要句号。

> [!tip] 参考回答
> 週末、天気が良ければ、一緒に公園へ散歩に行きましょう。

---

### 练习 15

> [!question] 题目
> 我明天上午会先确认问题原因，下午再向你汇报调查结果。

> [!example] 自己的答案
>　明日午前にまず問題の原因を確認します。午後にあなたに調査結果を報告します。

> [!warning] 指正
> 意思清楚。`明日午前に` 建议写成 `明日の午前中に`。`あなたに` 在职场日语里过于直白，汇报对象明确时可省略，或用 `ご報告します` 提升礼貌度。两句合并会更自然。

> [!tip] 参考回答
> 明日の午前中にまず問題の原因を確認し、午後に調査結果をご報告します。
