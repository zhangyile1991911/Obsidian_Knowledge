## 2026-06-15

### 练习 1

> [!question] 题目
> 为了减少过度绘制，我们只在相机进入室内区域后启用室内特效。

> [!example] 自己的答案
> 過剰なドローコールを軽減するため、カメラが室内エリアに入ってからエフェクトを再生する

> [!warning] 指正
> 「過剰なドローコール」是过多的绘制调用，而“过度绘制”应使用「オーバードロー」或「過剰描画」。「軽減する」可以使用，但搭配占用或次数时「減らす」更直接。“室内特效”漏掉了「室内」，而「再生する」侧重播放，原文是启用，应使用「有効にする」。还需用「～後にのみ」体现“只在……后”。

> [!tip] 参考回答
> オーバードローを減らすため、カメラが室内エリアに入った後にのみ、<mark style="background: #FF5582A6;">室内用のエフェクトを有効にします</mark>。

---

### 练习 2

> [!question] 题目
> 客户端在收到服务器确认之前，会暂时保留尚未确认的操作记录。

> [!example] 自己的答案
>　クライアントがサーバーから確認を受信するまで未確認入力履歴を一時的に保存する

> [!warning] 指正
> 整体意思基本正确。「クライアントが」也可使用，但描述客户端的固定行为时用主题「クライアントは」更自然。「確認を受信する」略显生硬，可说「確認応答を受け取る」。

> [!tip] 参考回答
> クライアントは、サーバーから<mark style="background: #FF5582A6;">確認応答</mark>を受け取るまで、未確認の入力履歴を一時的に保持します。

---

### 练习 3

> [!question] 题目
> 请把角色移动的计算从输入处理模块中分离出来，以便以后更换控制方式。

> [!example] 自己的答案
> キャラクターの移動計算ロジックをインプット処理モジュールから分離し、後から差し替えるようにする

> [!warning] 指正
> 「インプット」可以理解，但软件模块通常称为「入力処理モジュール」。“以后更换控制方式”不是单纯替换移动计算逻辑；原答案的「後から差し替える」没有说明替换对象，应明确为「後から操作方式を変更できるようにする」。题目是请求，因此句尾应使用「分離してください」。

> [!tip] 参考回答
> 後から操作方式を変更できるように、キャラクターの移動計算を入力処理モジュールから分離してください。

---

### 练习 4

> [!question] 题目
> 如果同一个资源被多个场景使用，就不要在切换场景时立即释放它。

> [!example] 自己的答案
>　同一リソースが複数シーンに使用されている場合、シーンの切り替え際に直ちに解放しないようにする

> [!warning] 指正
> 「複数シーンに使用されている」中的助词不自然，应为「複数のシーンで使用されている」。「シーンの切り替え際に」缺少「の」，可改为「シーン切り替え時に」。“不要立即释放”可表达为「すぐには解放しない」或「即座に解放しない」。题目是请求，句尾应使用「してください」。

> [!tip] 参考回答
> 同じリソースが複数のシーン<mark style="background: #FF5582A6;">で</mark>使用されている場合は、シーン切り替え時にすぐには解放しないでください。

---

### 练习 5

> [!question] 题目
> 性能分析结果显示，频繁创建临时字符串是垃圾回收抖动的主要原因。

> [!example] 自己的答案
>　パフォーマンス分析結果により、ガベージコレクションのスパイク原因は頻繁に一時テキストが生成されるとなります。

> [!warning] 指正
> 「パフォーマンス分析結果により」表示依据分析结果采取动作，这里是分析结果表明某事，应说「パフォーマンス分析の結果」。「一時テキスト」是临时文本，程序中的临时字符串应为「一時文字列」。「スパイク原因は～となります」搭配错误，可用「スパイクの主な原因は～であることが分かりました」。“垃圾回收抖动”也可根据语境表达为「GCスパイク」。

> [!tip] 参考回答
> パフォーマンス分析の結果、<mark style="background: #FF5582A6;">一時文字列の頻繁な生成</mark>が、ガベージコレクションによるスパイクの主な原因<mark style="background: #FF5582A6;">であることが分かりました</mark>。

---

### 练习 6

> [!question] 题目
> 当玩家拖动滑块时，只更新预览数值，不要立刻发送设置变更请求。

> [!example] 自己的答案
>　プレイヤーがスクロールする際に、プレビュー数値だけ更新し、すぐ設定変更を送信しないようにする

> [!warning] 指正
> 「スクロールする」是滚动页面或列表，不是拖动滑块；应说「スライダーをドラッグする」。“设置变更请求”漏掉了“请求”，应完整表达为「設定変更リクエスト」。原文强调拖动过程中只更新预览值，且不立即发送请求。

> [!tip] 参考回答
> プレイヤーが<mark style="background: #FF5582A6;">スライダーをドラッグ</mark>している間は、プレビュー値だけを更新し、設定変更リクエストはすぐに送信しないでください。

---

### 练习 7

> [!question] 题目
> 自动存档失败时，请保留上一次成功写入的存档，并向玩家显示错误原因。

> [!example] 自己的答案
> 自動セーブに失敗した際に、直前の成功したセーブを保持し、プレイヤーにエラー原因を表示する

> [!warning] 指正
> 整体意思基本正确。「直前の成功したセーブ」不够自然，而且原文强调成功写入，宜说「前回正常に書き込まれたセーブデータ」。「エラー原因」可以理解，但加助词写成「エラーの原因」更自然。题目是请求，因此两个动作应以「保持し」「表示してください」连接。

> [!tip] 参考回答
> 自動セーブに失敗した場合は、<mark style="background: #FF5582A6;">前回正常に書き込まれたセーブデータ</mark>を保持し、プレイヤーにエラーの原因を表示してください。

---

### 练习 8

> [!question] 题目
> 我们在资源导入脚本中加入了检查，以防止压缩设置不符合平台规范。

> [!example] 自己的答案
>　リソースインポート処理スクリプトにチェックを追加し、圧縮設定を誤ってプラットフォームのルールに違反することを防ぐ

> [!warning] 指正
> 「リソースインポート処理スクリプト」略显重复，可简化为「リソースインポートスクリプト」。「圧縮設定を誤って～違反する」语法错误，压缩设置是“不符合规范”，应表达为「圧縮設定がプラットフォームの規定に適合しない」。原文是已经完成的动作，句尾应使用过去式。

> [!tip] 参考回答
> 圧縮設定がプラットフォームの規定に適合しない状態を防ぐため、リソースインポートスクリプトにチェックを追加しました。

---

### 练习 9

> [!question] 题目
> 我顺路去便利店，你有什么想让我帮你买的吗？

> [!example] 自己的答案
>　ついでにコンビニに行くけど、何か欲しいものがあるか

> [!warning] 指正
> 「ついでにコンビニに行く」表示顺便去便利店，但“顺路去”更自然地说「途中でコンビニに寄る」或「コンビニに寄っていく」。「何か欲しいものがあるか」以间接疑问形式结束，句子不完整，也没有体现“让我帮你买”；可说「何か買ってきてほしいものはある？」。

> [!tip] 参考回答
> 途中でコンビニに寄るけど、<mark style="background: #FF5582A6;">何か買ってきてほしいものはある</mark>？

---

### 练习 10

> [!question] 题目
> 关于昨天发现的问题，我已经整理好重现步骤，稍后会发到项目群里。

> [!example] 自己的答案
> 昨日に見つけた問題について、再現手順をまとめた、後ほどプロジェクトグループで共有します

> [!warning] 指正
> 表示“昨天”时直接使用「昨日」，不加助词「に」。「再現手順をまとめた」是普通体过去式，与后面的礼貌体不统一；应改为「まとめました」或表示已经准备好的「まとめてあります」。项目群通常说「プロジェクトのグループチャット」，向群里发送应使用助词「に」以及动词「投稿する」或「共有する」。

> [!tip] 参考回答
> 昨日見つかった問題について、再現手順をまとめました。後ほど<mark style="background: #FF5582A6;">プロジェクトのグループチャットに投稿します</mark>。

---

## 2026-06-16

### 练习 1

> [!question] 题目
> 为了避免阴影边缘闪烁，请把级联阴影的切换距离稍微调远一点。

> [!example] 自己的答案
> シャド境界がチラつくを避けるため、カストーシャドの切り替え距離を大きくしてください

> [!warning] 指正
> 「シャド」应为「シャドウ」。「チラつくを避ける」语法错误，应说「ちらつきを防ぐ」或「ちらつくのを避ける」。“级联阴影”应为「カスケードシャドウ」或「カスケードシャドウマップ」，不是「カストーシャド」。“调远一点”不是单纯变大，宜表达为「少し遠くに設定する」。

> [!tip] 参考回答
> シャドウ境界のちらつきを防ぐため、カスケードシャドウの切り替え距離を少し遠くしてください。

---

### 练习 2

> [!question] 题目
> 这个同步逻辑只发送状态差异，而不是每一帧都发送完整的位置数据。

> [!example] 自己的答案
>　ロジック同期は状態の差分だけを送信し、フレームごとに完全な位置データを送信するではありません

> [!warning] 指正
> 「ロジック同期」语序不自然，应为「同期ロジック」。「フレームごと」可以理解，但技术语境中「毎フレーム」更自然。「送信するではありません」是否定形式错误，应说「送信するのではありません」。原句强调“不是完整位置数据，而是差异”，可用「完全な位置データではなく、状態差分だけを送信する」。

> [!tip] 参考回答
> この同期ロジックでは、毎フレーム完全な位置データを送信するのではなく、<mark style="background: #FF5582A6;">状態差分だけを送信します</mark>。

---

### 练习 3

> [!question] 题目
> 如果以后要支持手柄操作，输入映射表最好不要写死在角色脚本里。

> [!example] 自己的答案
>　後からゲームパッドを適用できるように、入力マップをキャラクタークラス内に実装しないようにする

> [!warning] 指正
> 「ゲームパッドを適用」不自然，“支持手柄操作”应为「ゲームパッド操作に対応する」。「入力マップ」可以理解，但“输入映射表”更准确为「入力マッピングテーブル」。「写死」在技术语境中常说「ハードコードする」。“最好不要”可用「～しないほうがよい」或「～は避けたほうがよい」。

> [!tip] 参考回答
> 後からゲームパッド操作に対応する可能性があるなら、<mark style="background: #FF5582A6;">入力マッピングテーブル</mark>をキャラクタークラス内にハードコードしないほうがよいです。

---

### 练习 4

> [!question] 题目
> 资源包下载完成后，请先校验哈希值，再把它加入可加载列表。

> [!example] 自己的答案
>　アセットバンドルダウンロードが完了後に、ハッシュサムを検証してからロードリストに投入する

> [!warning] 指正
> 「アセットバンドルダウンロードが完了後」缺少助词，应为「アセットバンドルのダウンロードが完了した後」。「ハッシュサム」不是常用表达，题目是哈希值，应为「ハッシュ値」。「ロードリスト」不够明确，“可加载列表”可说「ロード可能リスト」或「読み込み可能なリスト」。「投入する」偏口语或把东西投进去，列表追加应使用「追加する」。题目是请求，句尾应为「してください」。<mark style="background: #FF5582A6;">先に......その後で</mark>

> [!tip] 参考回答
> アセットバンドルのダウンロードが完了したら、先に<mark style="background: #FF5582A6;">ハッシュ値を検証し</mark>、その後でロード可能リストに追加してください。

---

### 练习 5

> [!question] 题目
> 这次掉帧不是渲染线程造成的，而是主线程上有大量阻塞式文件读取。

> [!example] 自己的答案
>　フレーム低下原因はレンダースレッドの負荷ではなく、大容量のファイルの読み取るにより、メインスレッドがブロックされます。

> [!warning] 指正
> 「フレーム低下」不如「フレーム落ち」自然。“不是渲染线程造成的”应说「レンダースレッドが原因ではない」。「大容量のファイル」变成大文件，原文是大量阻塞式文件读取，应表达为「大量のブロッキングなファイル読み込み」。「読み取るにより」语法错误，应为「読み込みによって」。句末「ブロックされます」会变成主线程被阻塞这个事实，而原文强调这是掉帧原因，宜整合为「原因です」。

> [!tip] 参考回答
> 今回のフレームレート低下はレンダースレッドではなく、メインスレッドが大量のファイル読み込みによってブロックされていることが原因です。

---

### 练习 6

> [!question] 题目
> 在设置界面关闭垂直同步后，玩家需要重新启动游戏才能让变更生效。

> [!example] 自己的答案
>　設定画面で同期を無効化した後、プレイヤーがゲームを再起動してから設定が有効になります

> [!warning] 指正
> 「同期」范围太广，垂直同步应明确为「垂直同期」或「VSync」。「ゲームを再起動してから設定が有効になります」意思接近，但原文强调玩家需要重启才能生效，宜使用「再起動する必要があります」。“变更生效”可说「変更を反映する」或「変更を有効にする」。

> [!tip] 参考回答
> 設定画面で<mark style="background: #FF5582A6;">垂直同期</mark>を無効にした後、その変更を反映するには、プレイヤーがゲームを再起動する必要があります。

---

### 练习 7

> [!question] 题目
> 读取存档时，如果版本号太旧，就显示迁移失败的提示并停止加载。

> [!example] 自己的答案
>　セーブデータを読み取る際に、バージョンが古すぎるを検知すると遷移失敗を表示してロードを停止します

> [!warning] 指正
> 「読み取る」偏读取文本或数值，读取存档通常说「読み込む」。「バージョンが古すぎるを検知」语法错误，应为「バージョン番号が古すぎる場合」或「古すぎることを検出した場合」。“迁移”应为「移行」，不是「遷移」。“提示”应译为「メッセージ」或「通知」，因此「遷移失敗を表示」不够完整。题目是请求，句尾应为「してください」。

> [!tip] 参考回答
> セーブデータを読み込む際にバージョン番号が古すぎる場合は、<mark style="background: #FF5582A6;">移行に失敗したことを示すメッセージ</mark>を表示し、ロードを停止してください。

---

### 练习 8

> [!question] 题目
> 我给关卡编辑器加了一个按钮，可以一键检查所有缺失的碰撞体。

> [!example] 自己的答案
>　ステージエディタにボタンを追加し、コライダーが設定されてない件を一括でチェックできる

> [!warning] 指正
> 「ステージエディタ」可理解，但工具名通常用「レベルエディタ」。“缺失的碰撞体”可说「未設定のコライダー」。原文是已经加了按钮，句尾宜使用过去式。

> [!tip] 参考回答
> レベルエディタに専用ボタンを追加し、未設定のコライダーを一括チェックできるようにしました。

---

### 练习 9

> [!question] 题目
> 今天外面太热了，我们等太阳小一点再出门吧。

> [!example] 自己的答案
>　それは暑すぎだから、太陽が下がってから出かけましょ

> [!warning] 指正
> 「それは暑すぎ」中的「それ」指代不清，外面应为「外」。「暑すぎだから」语法错误，应为「暑すぎるから」。“太阳小一点”不是太阳下降本身，而是日照变弱，可说「日差しが弱くなる」。「出かけましょ」应写作「出かけましょう」。

> [!tip] 参考回答
> 今日は外が暑すぎるから、<mark style="background: #FF5582A6;">日差しが少し弱くなってから</mark>出かけましょう。

---

### 练习 10

> [!question] 题目
> 我想确认一下，这个任务最晚需要在周五下班前完成吗？

> [!example] 自己的答案
>　金曜日退勤前にこのタスクを完了するではないかを確認したいです

> [!warning] 指正
> 「金曜日退勤前」应加助词，写作「金曜日の退勤前」；职场表达中「金曜の業務終了まで」更自然。「完了するではないか」是否定疑问结构错误，应为「完了する必要があるか」或「完了すればよいか」。“我想确认一下”可用「確認したいのですが」来引出问题。

> [!tip] 参考回答
> 確認したいのですが、このタスクは<mark style="background: #FF5582A6;">遅くとも金曜の業務終了までに完了する必要がありますか</mark>？
