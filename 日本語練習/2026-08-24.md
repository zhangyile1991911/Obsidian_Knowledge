## 2026-08-24

### 练习 1

> [!question] 题目
> 角色尝试发动终结技时，技能组件应先确认当前状态和所需资源，只有进入施放状态成功后才扣除资源。

> [!example] 自己的答案
> キャラクターがスキルを発動してみると機に、まず現在の状態と必要なコストを確認して、正常に発動してからコストを消費します。

> [!warning] 指正
> 大意能够理解，但 `発動してみると機に` 不成立，应改为 `発動しようとしたときは`。题目中的主体“技能组件”和“成功进入施放状态后才扣除”没有完整表达；`正常に発動してから` 也不够精确。

> [!tip] 参考回答
> キャラクターがフィニッシュ技を発動しようとしたときは、スキルコンポーネントがまず現在の状態と必要なリソースを確認し、発動状態への移行に成功した後でのみリソースを消費します。

---

### 练习 2

> [!question] 题目
> 伤害计算模块负责生成最终数值，伤害数字 UI 只负责选择显示样式和播放出现动画。

> [!example] 自己的答案
>　ダメージ計算モージュルでは決定数値だけを生成し、ダメージUIは表示の式とアニメーション再生だけを担当します。

> [!warning] 指正
> 职责划分表达清楚。`モージュル` 应为 `モジュール`，`決定数値` 宜改为 `最終的な数値`；“显示样式”是 `表示形式`，不是 `表示の式`。还应明确是播放“出现动画”。

> [!tip] 参考回答
> ダメージ計算モジュールが最終的な数値の生成を担当し、ダメージ数値UIは表示形式の選択と出現アニメーションの再生だけを担当します。

---

### 练习 3

> [!question] 题目
> 如果 Unreal 的 World Partition 在移动过程中出现卡顿，请记录被加载的单元数量、I/O 等待时间和当时的内存使用量。

> [!example] 自己的答案
>　UnrealのWorld Partitionは移動中にカクつく場合、ロード中のObject数、IO待ち時間、メモリ使用量を記録してください。

> [!warning] 指正
> 调查方向基本正确，但 `World Partitionは` 应改为表示场景的 `World Partitionで`。题目的观测对象是“已加载的单元数量”，不能替换成较宽泛的 `ロード中のObject数`；`I/O` 的写法更规范。

> [!tip] 参考回答
> UnrealのWorld Partitionで移動中にカクつきが発生する場合は、読み込まれたセルの数、I/Oの待ち時間、<mark style="background: #FF5582A6;">その時点での</mark>メモリ使用量を記録してください。

---

### 练习 4

> [!question] 题目
> 长列表 UI 应只生成画面内需要的项目，并在项目被回收时清除选择状态和临时事件。

> [!example] 自己的答案
>　長いUIリストは表示領域内に必要なアイテムだけを生成し、アイテムが返却される時に選択状態とイベントバインディングをクリアしてください。

> [!warning] 指正
> 题意表达得较完整。一般规则用 `返却される時に` 不如 `再利用のために回収されるときは` 自然；“临时事件”最好明确为 `一時的なイベントバインディング`。句末用 `〜するべきです` 更符合题目语气。

> [!tip] 参考回答
> 長いリストUIでは、画面内で必要な項目だけを生成し、項目が再利用のために回収されるときは、選択状態と一時的なイベントバインディングをクリアするべきです。

---

### 练习 5

> [!question] 题目
> 当 Gameplay Tag 阻止某个动作时，请把失败原因传给提示模块，但不要让提示模块重新判断角色状态。

> [!example] 自己的答案
>　Gameplay Tagはアクションを塞げる時に、失敗の原因を提示モージュルに通知し、提示モージュルがキャラクター状態を再判断しないようにします。

> [!warning] 指正
> 处理方向正确。`Gameplay Tagはアクションを塞げる` 不自然，可说 `Gameplay Tagの条件によってアクションがブロックされた`。`モージュル` 应为 `モジュール`；后半应突出提示模块只接收原因，不重新判定状态。

> [!tip] 参考回答
> Gameplay Tagの条件によってアクションがブロックされたときは、失敗理由を通知モジュールに渡しますが、通知モジュール側ではキャラクターの状態を再判定しないようにします。

---

### 练习 6

> [!question] 题目
> 对象工厂应根据配置创建对应类型，并把初始化失败的原因返回给请求方，同时销毁已经创建的临时对象。

> [!example] 自己的答案
>　オブジェクトファクトリーでは設定に応じて生成するべきです。そのうえで初期化に失敗した原因を返して生成済み一時的なオブジェクトを廃棄します。

> [!warning] 指正
> 失败后的清理表达出来了，但 `設定に応じて生成する` 缺少宾语，应明确创建“对应类型的对象”。`原因を返して` 还需写出返回对象是请求方；`生成済み一時的なオブジェクト` 应加 `の`，也可简化为 `生成済みの一時オブジェクト`。

> [!tip] 参考回答
> オブジェクトファクトリーは設定に応じて対応する型のオブジェクトを生成し、初期化に失敗した場合は、その理由を要求元に返すとともに、すでに生成した一時オブジェクトを破棄します。

---

### 练习 7

> [!question] 题目
> 使用 Motion Warping 调整攻击动作时，如果目标点已经失效，应取消位移修正并让动画自然结束。

> [!example] 自己的答案
>　Motion Warpingを使って攻撃アクションを調整する時に、ターゲットが無効になった場合、移動修正をキャンセルしてアニメーションを正常に完了させるべきです。

> [!warning] 指正
> 主要逻辑正确。一般条件用 `調整する時に` 不如 `調整するときに` 自然；题目是“目标点”，宜说 `目標地点`。`移動修正` 可改为 `位置補正`，而“自然结束”不是 `正常に完了`，应说 `自然に終了する`。

> [!tip] 参考回答
> Motion Warpingを使って攻撃モーションを調整するときに、目標地点がすでに無効になっている場合は、位置補正をキャンセルし、アニメーションを自然に終了させるべきです。

---

### 练习 8

> [!question] 题目
> 调查车辆悬挂抖动时，请比较弹簧强度、阻尼、车轮接触状态以及每个物理步长的变化。

> [!example] 自己的答案
>　車の揺れることを調査する時に、スプリングno強度、ホイールの接触状態、物理サブステップの変化を比較してください。

> [!warning] 指正
> `スプリングno強度` 是输入错误，应为 `スプリングの強度`。`車の揺れること` 过于宽泛，题目是 `車両のサスペンションの振動`；同时遗漏了“阻尼”，即 `減衰`。最后应表达每个物理时间步中的变化，而不只是笼统的子步变化。

> [!tip] 参考回答
> 車両のサスペンションの振動を調査するときは、スプリングの強度、減衰、ホイールの接触状態、各物理タイムステップでの変化を比較してください。

---

### 练习 9

> [!question] 题目
> AI 丢失感知目标后，应清除黑板中的旧引用，并在确认还有有效候选后再选择下一个目标。

> [!example] 自己的答案
> AIが感知ターゲットを失った後に、ブラックボード内の旧参照をクリアするべきです。そのうえで有効な候補から次のターゲットを選びます。

> [!warning] 指正
> 前半准确，后半也能理解，但省略了“确认仍有有效候选”这一独立步骤。`旧参照` 可以理解，`古い参照` 更自然；应使用 `〜ことを確認してから` 明确选择下一个目标之前的条件。

> [!tip] 参考回答
> AIが感知対象を失った後は、ブラックボード内の古い参照をクリアし、有効な候補がまだ残っていることを確認してから、次の対象を選択します。

---

### 练习 10

> [!question] 题目
> 移动物品之前，背包系统应先验证所有目标格；只要有一个格子不可用，就保持原来的占用状态。

> [!example] 自己的答案
>　アイテムを移動する前に、インベントリーシステムでは全ての目標セルを確認します。いずれが利用不可だったら、元の占有状態を維持します。

> [!warning] 指正
> 处理原则表达正确。常用写法是 `インベントリシステムは`，不是 `インベントリーシステムでは`。`いずれが利用不可だったら` 不自然，应说 `一つでも利用できないセルがあれば`，以准确表达“只要有一个”。

> [!tip] 参考回答
> アイテムを移動する前に、インベントリシステムはすべての移動先セルを検証し、一つでも利用できないセルがあれば、元の占有状態を維持します。

---

### 练习 11

> [!question] 题目
> 同类音效超过并发上限时，音频管理器应根据优先级停止一个旧音效，而不是直接拒绝所有新请求。

> [!example] 自己的答案
>　同じ効果音が同時再生数を超える場合、オディオは優先度に基づいて旧効果音を停止するべきです。新しいリクエストを全部拒否するのではありません。

> [!warning] 指正
> 对比关系和主要处理都表达出来了。`オディオ` 是拼写错误，应为 `オーディオマネージャー`；`旧効果音` 宜说 `再生中の古い効果音`。后一句用 `〜のではなく` 与前句连接会更自然。

> [!tip] 参考回答
> 同じ種類の効果音が同時再生数の上限を超えた場合、オーディオマネージャーは新しい要求をすべて拒否するのではなく、優先度に基づいて再生中の古い効果音を一つ停止するべきです。

---

### 练习 12

> [!question] 题目
> 如果当前语言缺少某条文本，文本系统应依次尝试默认语言和开发用文本，并把缺失的键记录到日志中。

> [!example] 自己的答案
> 現在の言語にはテキストが欠落する場合、テキストシステムはデフォルト言語と開発テキストから試すべきです。そして欠落しているキーをログに記録します。

> [!warning] 指正
> 流程基本完整。`現在の言語にはテキストが欠落する` 不自然，可改为 `現在の言語に該当するテキストがない`。`デフォルト言語と開発テキストから試す` 没有清楚表达顺序，应使用 `順に試す`；“开发用文本”是 `開発用テキスト`。

> [!tip] 参考回答
> 現在の言語に該当するテキストがない場合、テキストシステムはデフォルト言語と開発用テキスト<mark style="background: #FF5582A6;">を順に試し</mark>、欠落しているキーをログに記録します。

---

### 练习 13

> [!question] 题目
> 面试中说明帧耗时突然增加的排查方法时，请按“确认发生区间、比较主要线程、缩小可疑功能、验证修改结果”的顺序回答。

> [!example] 自己的答案
>　フレーム時間が急に増える原因を特定するために、まずロジックとレンダリングにかかる時間を分けて計測します。ロジックが問題である場合、プロファイラーでかかる時間順で並び替えて展開することで具体的なメソッドを絞り込みます。

> [!warning] 指正
> 回答给出了实际的 Profiler 排查方法，技术内容合理，但没有按题目要求覆盖完整四步：缺少“确认发生区间”和“验证修改结果”，也没有明确说出“缩小可疑功能”。`具体的なメソッド` 可换成更自然的 `具体的な処理`。

> [!tip] 参考回答
> まず問題が発生した時間区間を確認し、次に主要なスレッドの処理時間を比較します。その後、疑わしい機能を一つずつ絞り込み、最後に変更前後の計測結果を比較して、修正の効果を検証します。

---

### 练习 14

> [!question] 题目
> 我出门后才发现忘了带伞。天气预报说傍晚会下雨，所以我想先向同事借一把。

> [!example] 自己的答案
>　出掛け後に傘を忘れたことが気づいた。天気予報により夜から雨が降るから、同僚から借りると思います。

> [!warning] 指正
> 想表达的情境可以理解，但有多处搭配问题。`出掛け後` 应为 `出かけた後`，`忘れたことが気づいた` 应为 `忘れたことに気づいた`。天气预报用 `天気予報によると`；题目是傍晚，应使用 `夕方`。`同僚から借りると思います` 应改为表达意愿的 `同僚に借りようと思う`。

> [!tip] 参考回答
> 出かけた後で、傘を忘れた<mark style="background: #FF5582A6;">ことに気づいた</mark>。天気予報によると夕方から雨が降るそうだから、先に同僚に一本借りようと思う。

---

### 练习 15

> [!question] 题目
> 目前实现已经完成，但测试被一个规格问题卡住了。如果今天下午三点前能确定处理方针，我会更新预计完成时间。

> [!example] 自己的答案
>　現在実現がすでに完了しました。しかしテストが規格問題に止まりました。本日午後三時前に対応策があれば、予定完成時間を更新します。

> [!warning] 指正
> 时间点和后续动作基本传达出来了。软件开发中的“实现”应说 `実装`，不是 `実現`；`規格問題に止まりました` 应改为 `仕様上の問題で止まっています`。`対応策があれば` 是“如果有对策”，没有表达“方针确定”；预计完成时间宜说 `完了見込み`。

> [!tip] 参考回答
> 現在、実装はすでに完了していますが、仕様上の問題によりテストが止まっています。本日午後3時までに対応方針が決まれば、<mark style="background: #FF5582A6;">完了見込み</mark>を更新します。

## 2026-08-25

### 练习 1

> [!question] 题目
> 状态机收到延迟到达的动画事件时，应先确认来源状态仍然有效，再决定是否执行状态转换。

> [!example] 自己的答案
> ステートマシンは遅れて来たAnim Notifyを受け取った場合、まず発生元の状態が有効かどうかを確認し

> [!warning] 指正
> 前半的条件确认表达正确，但句子停在 `確認し`，没有完成题目中的“再决定是否执行状态转换”。`遅れて来た` 可以理解，技术说明中 `遅れて届いた` 更自然；“来源状态”宜说 `遷移元のステート`。

> [!tip] 参考回答
> ステートマシンが遅れて届いたAnim Notifyを受け取った場合は、まず遷移元のステートがまだ有効であることを確認してから、ステート遷移を実行するかどうかを判断します。

---

### 练习 2

> [!question] 题目
> 提示框的数据提供者负责准备标题、数值和说明文本，显示组件只负责布局与视觉效果。

> [!example] 自己的答案
> ポップアップのプロバイダーはタイトル、数値、説明テキスト、表示コンポーネントはレイアウトとビジュアル効果を担当します。

> [!warning] 指正
> 职责划分基本正确，但前半缺少谓语，`タイトル、数値、説明テキストを用意し` 才能与后半连接。“提示框”在此更适合用 `ツールチップ`；`ビジュアル効果` 可以接受，也可说 `視覚効果`。

> [!tip] 参考回答
> ツールチップのデータプロバイダーはタイトル、数値、説明テキストを用意し、表示コンポーネントはレイアウトと視覚効果だけを担当します。

---

### 练习 3

> [!question] 题目
> 调查 Unreal 中 Lumen 反射的噪点时，请记录画面比例、反射质量、光源变化以及镜头停止后的收敛时间。

> [!example] 自己的答案
> UnrealでLumenの反射のノイズを調査するときは、画面比率、反射の品質、光源の変化及びカメラが停止後の収束時間を記録してください。

> [!warning] 指正
> 四个观测项均已表达，整体很好。`カメラが停止後` 缺少动词连接，应为 `カメラが停止した後`。Unreal 的具体设置语境中，“画面比例”也可明确说成 `スクリーンパーセンテージ`。

> [!tip] 参考回答
> UnrealでLumen反射のノイズを調査するときは、スクリーンパーセンテージ、反射品質、光源の変化、カメラが停止した後の収束時間を記録してください。

---

### 练习 4

> [!question] 题目
> 相机模式栈只能由角色控制器修改，UI 可以提出切换请求，但不能直接增删栈中的模式。

> [!example] 自己的答案
>　カメラモードスタックはキャラクターコントローラーによる変更するだけで、UI側は切り替えを要求できるけど、スタック内のモードを直接増減してはいけません。

> [!warning] 指正
> 主体和限制关系表达出来了，但 `キャラクターコントローラーによる変更するだけ` 语法不成立，应改为 `キャラクターコントローラーだけが変更できます`。`できるけど` 偏口语，与后面的敬体不统一；栈中模式的增删宜说 `追加や削除`。

> [!tip] 参考回答
> カメラモードスタックはキャラクターコントローラーだけが変更できます。UIは切り替えを要求できますが、スタック内のモードを直接追加したり削除したりすることはできません。

---

### 练习 5

> [!question] 题目
> 如果技能预览创建失败，应隐藏尚未完成的界面，解除输入锁定，并把具体失败原因交给错误提示组件。

> [!example] 自己的答案
> スキルプレビューの生成に失敗した場合、未完了画面を隠すべきで、入力ロックを解除し、具体的な失敗原因をヒントコンポーネントに通知します。

> [!warning] 指正
> 失败后的三个处理都已覆盖。`未完了画面` 不自然，可说 `未完成のUI`；并列动作应使用 `隠し、解除し`，而不是 `隠すべきで`。`ヒントコンポーネント` 更像一般提示，题目的“错误提示组件”宜用 `エラー表示コンポーネント`。

> [!tip] 参考回答
> スキルプレビューの生成に失敗した場合は、未完成のUIを非表示にし、入力ロックを解除して、具体的な失敗理由をエラー表示コンポーネントに渡します。

---

### 练习 6

> [!question] 题目
> 建造系统应把每次放置操作保存为可撤销的命令，并在撤销前确认对应建筑仍然存在。

> [!example] 自己的答案
> ビルドシステムでは各置く操作をアンドゥコマンドとして保存し、取り消す前に対応する建物はまだ存在するかを確認します。

> [!warning] 指正
> 可撤销和存在性检查都表达到了。`各置く操作` 不自然，应说 `各配置操作`；`アンドゥコマンド` 最好明确为 `取り消し可能なコマンド`。从句的主体是建筑，应使用 `対応する建物がまだ存在するか`。

> [!tip] 参考回答
> 建築システムは各<mark style="background: #FF5582A6;">配置操作</mark>を取り消し可能なコマンドとして保存し、取り消す前に対応する建物がまだ存在することを確認します。

---

### 练习 7

> [!question] 题目
> 攻击 Montage 被中断后，即使迟到的 Notify 到达，也不能再次打开判定窗口。

> [!example] 自己的答案
>　攻撃Montageが中断された後、遅れて来たNotifyが届いても、再び判定ウィンドを開けてはいけません。

> [!warning] 指正
> 题目的让步条件和禁止事项都表达得很准确。主要问题是 `判定ウィンド`，应写成 `判定ウィンドウ`；`遅れて来たNotify` 也可改为更自然的 `遅れて届いたNotify`。

> [!tip] 参考回答
> 攻撃Montageが中断された後は、<mark style="background: #FF5582A6;">遅れて届いた</mark>Notifyを受け取っても、判定ウィンドウを再び開いてはいけません。

---

### 练习 8

> [!question] 题目
> 角色站在移动平台上发生抖动时，请比较平台速度、地面检测结果和两者的更新顺序。

> [!example] 自己的答案
>　キャラクターが移動プラットフォームで振動する時に、プラットフォームの速度、地面検出結果、両者の更新順を比較してください。

> [!warning] 指正
> 三个比较项目完整。`移動プラットフォームで振動する` 宜改为 `移動プラットフォームの上で揺れる`，以明确角色站在平台上；一般条件用 `揺れる場合は` 更自然。`更新順` 可以理解，正式表达为 `更新順序`。

> [!tip] 参考回答
> キャラクターが移動プラットフォームの上で揺れる場合は、プラットフォームの速度、地面検出の結果、両者の更新順序を比較してください。

---

### 练习 9

> [!question] 题目
> AI 的环境查询被取消时，应清除查询句柄，并在获得新的有效结果之前保留当前目标。

> [!example] 自己的答案
>　AIの環境検索がキャンセルされる時に、検索ハンドルをクリアし、新しく有効な結果を取得する前に現在の目標を保持します。

> [!warning] 指正
> 取消后的清理和保留当前目标都表达完整。`キャンセルされる時に` 应改为 `キャンセルされたときは`；游戏 AI 语境中的“环境查询”宜说 `環境クエリ`，相应地使用 `クエリハンドル`。

> [!tip] 参考回答
> AIの環境クエリがキャンセルされたときは、クエリハンドルをクリアし、新しい有効な結果<mark style="background: #FF5582A6;">が得られるまで</mark>現在の目標を保持します。

---

### 练习 10

> [!question] 题目
> 写入检查点时，应先保存临时文件并验证内容，替换旧文件成功后才更新显示用的保存时间。

> [!example] 自己的答案
>　チェックポイントを書き込む時は、まず一時的なファイルを保存して内容を確認するべきです。古いファイルを正常に置き換えた後保存時刻の表示を更新します。

> [!warning] 指正
> 保存、验证、替换和更新时间的顺序表达得很好。`一時的なファイル` 可简化为固定说法 `一時ファイル`；后一句建议使用 `成功した後でのみ`，更突出“成功后才更新”。“显示用的保存时间”是 `表示用の保存時刻`。

> [!tip] 参考回答
> チェックポイントを書き込むときは、まず一時ファイルに保存して内容を検証し、古いファイルの置き換えに成功した後でのみ、表示用の保存時刻を更新します。

---

### 练习 11

> [!question] 题目
> Niagara 组件返回对象池之前，必须重置用户参数、移除父子关系并停止残留的粒子发射。

> [!example] 自己的答案
>　Niagaraコンポーネントがオブジェクトプールに返却する前に、ユーザーパラメータをリセットし、親関係を削除して残るパーティクルエミッターを停止します。

> [!warning] 指正
> 三项清理内容均已覆盖，但 `コンポーネントが〜に返却する` 的主客关系不对，应为被动式 `返却される前に`。`親関係を削除する` 宜说 `親子関係を解除する`；`残るパーティクルエミッター` 可改为 `残っているパーティクルの放出`。

> [!tip] 参考回答
> Niagaraコンポーネントがオブジェクトプールに返却される前に、ユーザーパラメータをリセットし、親子関係を解除して、残っているパーティクルの放出を停止する必要があります。

---

### 练习 12

> [!question] 题目
> 如果遮挡剔除导致物体突然出现，请记录物体包围盒、镜头速度和每帧的可见性判定结果。

> [!example] 自己的答案
> オクルージョンカリングによるオブジェクトが突然に現れた場合、オブジェクトのバウンディングボックス、カメラ速度、毎フレーム表示の判定結果を記録してください。

> [!warning] 指正
> 需要记录的三个项目都没有遗漏。`オクルージョンカリングによるオブジェクトが` 助词关系错误，应说 `オクルージョンカリングによってオブジェクトが`；`突然に現れた` 宜改为 `突然表示される`。“可见性判定”是 `可視性の判定`。

> [!tip] 参考回答
> オクルージョンカリングによってオブジェクトが突然表示される場合は、オブジェクトのバウンディングボックス、カメラの速度、各フレームの<mark style="background: #FF5582A6;">可視性判定</mark>の結果を記録してください。

---

### 练习 13

> [!question] 题目
> 面试中说明如何减少大量 Tick 的开销时，请先给出测量结果，再说明哪些处理可以停用、降频或集中更新，最后验证优化后的帧时间。

> [!example] 自己的答案
> 大量のTickの処理時間を軽減するため、毎フレーム各オブジェクトのTickメソッドを実行するによるCPUキャッシュヒット率が低くなります。例えば、大量のオブジェクト移動処理がある場合、オブジェクトの移動コンポーネントを連続したメモリ領域に入れることでCPUキャッシュヒット率を向上させます。

> [!warning] 指正
> 回答提出了通过连续内存改善缓存命中率的具体方案，技术思路有价值，但没有按题目要求回答：缺少测量结果、停用／降频／集中更新三种分类，以及优化后的帧时间验证。语法上 `実行するによる` 应为 `実行することで`。面试时应先回应指定框架，再补充缓存优化实例。

> [!tip] 参考回答
> まずProfilerで各Tickの<mark style="background: #FF5582A6;">実行回数と処理時間</mark>を計測します。その結果に基づいて、不要なTickを停止し、更新頻度を下げられる処理を間引き、大量の同種処理を一か所にまとめます。最後に最適化前後のフレーム時間を比較して、効果を検証します。

---

### 练习 14

> [!question] 题目
> 我借的书今天到期，但还没有读完。我想问图书馆工作人员能不能再续借一周。

> [!example] 自己的答案
> 借りる本が本日まで切れるけど、読み終わってないから、図書館の係員に一週間を延期してもらえないかと聞きます。

> [!warning] 指正
> 情境可以理解，但 `借りる本` 是“要借的书”，已借的书应说 `借りた本`。到期用 `返却期限が今日まで`，不是 `本日まで切れる`；续借一周应说 `もう一週間延長する` 或 `もう一週間借りる`。询问工作人员时宜使用礼貌表达。

> [!tip] 参考回答
> 借りた本の返却期限が今日までなのですが、まだ読み終わっていません。もう一週間延長できるか、図書館の職員に聞いてみたいです。

---

### 练习 15

> [!question] 题目
> 代码评审中的大部分意见已经处理完毕，但剩下的一项需要先确认设计方针。可以请你在明天的会议上一起讨论吗？

> [!example] 自己的答案
>　コードリビューでの主な意見をすでに対応しました、残り項目はまず仕様方針を確認する必要があります。明日会議で検討できませんか？

> [!warning] 指正
> 主要信息完整，但 `主な意見` 是“主要意见”，题目是“大部分意见”，宜说 `ほとんどの指摘`。`意見を対応する` 应为 `指摘に対応する`；两个分句不能只用逗号连接，应使用 `対応しましたが`。`残り項目` 应加 `の一件`，请求共同讨论可用 `一緒に検討していただけませんか`。

> [!tip] 参考回答
> コードレビューの指摘にはほとんど対応しましたが、残りの一件については、まず設計方針を確認する必要があります。明日の会議で一緒に検討していただけませんか。

## 2026-08-26

### 练习 1

> [!question] 题目
> 执行交互操作之前，交互组件应再次确认目标仍在有效距离内；如果条件不满足，只把失败原因交给提示 UI。

> [!example] 自己的答案
>　インタラクト操作を実行する前に、インタラクトコンポーネントは目標が有効範囲内にいるかを再びに確認するべきです。条件を満たされない場合、失敗原因をUIに渡します。

> [!warning] 指正
> 处理顺序基本正确。`再びに確認する` 不自然，应说 `再度確認する`。目标未必是人物，因此 `有効範囲内にいる` 可改为 `有効範囲内にある`；`条件を満たされない` 的主客关系错误，应为 `条件が満たされていない`。还应补出“只传递失败原因”的 `失敗理由だけ`。

> [!tip] 参考回答
> インタラクト操作を実行する前に、インタラクトコンポーネントは対象がまだ有効範囲内にあることを再度確認します。条件が満たされていない場合は、失敗理由だけを通知UIに渡します。

---

### 练习 2

> [!question] 题目
> 菜单关闭时，焦点管理器应恢复之前选中的控件；如果该控件已经隐藏，则选择同一组中的第一个有效控件。

> [!example] 自己的答案
> メニュを閉じるときに、フォーカスマネージャーは元の選択項目に復元するべきです。ウィジェットが非表示された場合、同じグループ内で第一有効ウィジェットを選択します。

> [!warning] 指正
> 条件分支表达出来了，但 `メニュ` 应为 `メニュー`。`元の選択項目に復元する` 不自然，应明确为“把焦点恢复到之前的控件”。`非表示された` 宜用状态表达 `非表示になっている`；`第一有効ウィジェット` 应说 `最初の有効なウィジェット`。

> [!tip] 参考回答
> メニューを閉じるときは、フォーカスマネージャーが以前選択されていたウィジェットにフォーカスを戻します。そのウィジェットが非表示になっている場合は、同じグループ内の<mark style="background: #FF5582A6;">最初の有効な</mark>ウィジェットを選択します。

---

### 练习 3

> [!question] 题目
> 调查 Virtual Shadow Maps 的缓存失效时，请记录移动物体的数量、失效页面数和阴影阶段的 GPU 耗时。

> [!example] 自己的答案
>　Virtual Shadow Mapsが無効になった原因を調査する時に、移動中オブジェクトの数、無効になったページ数、シャド描画にかかった時間を記録してください。

> [!warning] 指正
> 记录项目大致齐全，但把“Virtual Shadow Maps 的缓存失效”误写成了 Virtual Shadow Maps 本身失效。应使用 `キャッシュの無効化`。`移動中オブジェクト` 应为 `移動するオブジェクト`，`シャド` 应为 `シャドウ`；题目还明确要求记录 GPU 耗时。

> [!tip] 参考回答
> Virtual Shadow Mapsのキャッシュ無効化を調査するときは、移動するオブジェクトの数、無効化されたページ数、シャドウ処理にかかったGPU時間を記録してください。

---

### 练习 4

> [!question] 题目
> 事件观察者被销毁之前，必须解除所有订阅，并确保事件发布者不再保留该对象的引用。

> [!example] 自己的答案
>　オブザーバーが廃棄される前に、すべてのイベント購読を解除し、送信側が該当オブジェクトへの参照を保持しないこと確保します。

> [!warning] 指正
> 订阅解除和引用清理都表达完整。`送信側` 可以理解，但事件系统中更准确的是 `イベント発行元`。`保持しないこと確保します` 缺少助词且搭配不自然，应改为 `保持していないことを確認します` 或 `保持しないようにします`。

> [!tip] 参考回答
> イベントオブザーバーが破棄される前に、すべての購読を解除し、イベント発行元がそのオブジェクトへの参照を保持しないようにする必要があります。

---

### 练习 5

> [!question] 题目
> 录制战斗回放时，只保存能够重新构成结果的输入数据和随机种子，不要保存临时特效对象。

> [!example] 自己的答案
>　バトルリプレーを録画する時には、結果を再生成できる入力データと乱数のシードだけを保存し、一時的なエフェクトおぶじぇくとを保存しません。

> [!warning] 指正
> 保存范围和排除对象都表达正确。`リプレー` 更常写作 `リプレイ`，系统数据的录制宜用 `記録する`。`結果を再生成する` 可以理解，回放语境中 `結果を再現する` 更自然。`おぶじぇくと` 必须改为片假名 `オブジェクト`。

> [!tip] 参考回答
> バトルリプレイを記録するときは、<mark style="background: #FF5582A6;">結果を再現できる</mark>入力データと乱数シードだけを保存し、一時的なエフェクトオブジェクトは保存しません。

---

### 练习 6

> [!question] 题目
> 行动队列应先处理高优先级命令；被取消的命令必须从队列中移除，且不能改变角色状态。

> [!example] 自己的答案
>　アクションキューはコマンドを優先順に処理するべきです。キャラクターの状態を変更せず、キャンセルされたコンマンを必ずにキューから削除し、

> [!warning] 指正
> 回答没有写完，句子停在 `削除し、`。`優先順` 宜明确为 `優先度の高い順`；`コンマン` 是拼写错误，应为 `コマンド`，`必ずに` 应为 `必ず`。还需完整表达“被取消的命令不能改变角色状态”。

> [!tip] 参考回答
> アクションキューは、優先度の高いコマンドから処理します。キャンセルされたコマンドは必ずキューから削除し、キャラクターの状態を変更しないようにします。

---

### 练习 7

> [!question] 题目
> 使用分层骨骼混合播放上半身动作时，应由移动状态驱动下半身，并用骨骼遮罩限制动作影响范围。

> [!example] 自己的答案
>　スケルトンブレンドで上半身アニメーションを再生するときに、移動状態で下半身を駆使してスケルトンマスクで影響範囲を制限します。

> [!warning] 指正
> 上下半身职责和影响范围都表达到了。`スケルトンブレンド` 过于宽泛，此处宜说 `レイヤードボーンブレンド`；`下半身を駆使する` 中的 `駆使する` 是“熟练运用”，应改为 `下半身を駆動する`。骨骼遮罩通常说 `ボーンマスク`。

> [!tip] 参考回答
> レイヤードボーンブレンドで上半身のアニメーションを再生するときは、移動状態で下半身を駆動し、ボーンマスクでアニメーションの影響範囲を制限します。

---

### 练习 8

> [!question] 题目
> 检测角色能否抓住边缘时，请使用胶囊扫描确认路径没有障碍，再用射线确定边缘表面的位置和法线。

> [!example] 自己的答案
>　キャラクターが端を掴めるかを検出するときは、カプセイルを使って経路に障害物なしを確認してから　レイキャストを通して境界の位置と法線を確認します。

> [!warning] 指正
> 两阶段检测的顺序正确。`カプセイル` 应为 `カプセル`，题目的“胶囊扫描”宜说 `カプセルスイープ`。`障害物なしを確認する` 缺少从句，应为 `障害物がないことを確認する`；`レイキャストを通して` 可改为更自然的 `レイキャストで`。

> [!tip] 参考回答
> キャラクターが縁をつかめるか判定するときは、まずカプセルスイープで経路に障害物がないことを確認し、その後レイキャストで縁の表面位置と法線を特定します。

---

### 练习 9

> [!question] 题目
> AI 群体避障出现来回摆动时，请比较期望速度、附近单位数量和当前避障权重。

> [!example] 自己的答案
>　AI群回避は揺れる時は、希望速度、周りのオブジェクト数、現在回避の重みを比較してください。

> [!warning] 指正
> 三个比较项基本传达出来了。`AI群回避` 不自然，应说 `AIの群集回避`；来回摆动可表达为 `左右に揺れる`。`希望速度` 可以理解，但技术语境常用 `目標速度`；“附近单位”不能泛化为所有对象，应说 `周辺のユニット数`。

> [!tip] 参考回答
> AIの群集回避で左右の揺れが発生する場合は、目標速度、<mark style="background: #FF5582A6;">周辺のユニット数</mark>、現在の回避ウェイトを比較してください。

---

### 练习 10

> [!question] 题目
> 资源依赖分析器应在加载之前检测循环依赖，并输出构成循环的完整资源路径。

> [!example] 自己的答案
>　リソース依存分析はロードする前に循環依存を検出し、完全な循環依存パスを出力します。

> [!warning] 指正
> 检测时机和输出动作表达正确。主体应是“分析器”，宜说 `リソース依存関係アナライザー`。`完全な循環依存パス` 容易理解成一个抽象的循环路径；题目要求的是构成循环的每一条完整资源路径，应明确写出复数对象。

> [!tip] 参考回答
> リソース依存関係アナライザーは、読み込み前に循環依存を検出し、その循環を構成するすべてのリソースの完全なパスを出力します。

---

### 练习 11

> [!question] 题目
> 应用画面设置后，请先显示十五秒预览；只有玩家确认时才保存，否则恢复原来的设置。

> [!example] 自己的答案
>　画面設定を適用した後、まず十五秒間プレビューを表示しておきます。プレイヤーが確認した後にのみ保存し、そうではないと元の設定に復帰します。

> [!warning] 指正
> 预览、确认后保存、未确认时恢复的流程完整。`表示しておきます` 带有“预先显示并保持”的语感，此处直接用 `表示します` 即可。`そうではないと` 不自然，应说 `確認されなければ`；设置由系统恢复时，用他动词 `元に戻します` 比 `復帰します` 合适。

> [!tip] 参考回答
> 画面設定を適用した後は、まず15秒間プレビューを表示します。プレイヤーが確認した場合のみ保存し、確認されなければ元の設定に戻します。

---

### 练习 12

> [!question] 题目
> 调查 Unity 的 GC 峰值时，请记录该帧的分配量、主要对象类型和产生分配的堆栈位置。

> [!example] 自己的答案
>　UnityのGCスパイクを調査する時に、該当フレームで割り当て量、オブジェクト型、ヒプ設定を記録してください。

> [!warning] 指正
> `GCスパイク` 使用正确，但后半遗漏和误译较多。`該当フレームで割り当て量` 应为 `該当フレームの割り当て量`；题目要求“主要对象类型”，需加 `主要な`。`ヒプ設定` 不是“产生分配的堆栈位置”，应使用 `割り当てが発生したスタックトレース上の位置`。

> [!tip] 参考回答
> UnityのGCスパイクを調査するときは、該当フレームの割り当て量、主要なオブジェクト型、割り当てが発生したスタックトレース上の位置を記録してください。

---

### 练习 13

> [!question] 题目
> 面试中设计冷却系统时，请说明统一的时间来源、避免每个技能逐帧更新的方法，以及暂停和时间倍率变化时的验证方式。

> [!example] 自己的答案
> クールダウンシステムを設計する時には、時間の由来を統一してスキルごとにマイフレームの更新を避けて及びポーズ、倍速による変化を検証する方法を説明します。
> まずクールダウンマネージャーですべてのクールダウンタスクを一元的に管理します。まず停止する前の時刻を記録し、停止を解除した後に比較します。

> [!warning] 指正
> 已经提出用管理器集中管理，并尝试说明暂停时的验证，方向不错。`時間の由来` 应为 `時間源`，`マイフレーム` 应为 `毎フレーム`；`避けて及び` 无法连接并列项。后半只比较了暂停前后时刻，还需说明时间倍率变化时冷却进度是否符合规则。

> [!tip] 参考回答
> クールダウンには統一した時間源を使用し、各スキルを毎フレーム更新するのではなく、終了時刻をクールダウン管理側で一元管理します。検証では、ポーズ中に残り時間が進まないことと、時間倍率を変更したときに設計どおりの速度で残り時間が変化することを確認します。

---

### 练习 14

> [!question] 题目
> 超市没有做晚饭需要的材料。要不要先给室友发消息，问问他愿不愿意换一道菜？

> [!example] 自己的答案
> スーパーには夕飯の食材が無いから、他の料理に変えていいかってルームメイトに聞いときます。

> [!warning] 指正
> 日常口语整体自然，`〜って` 和 `聞いとく` 的语体也一致。不过当前句子是“我会去问”，没有表达题目的提议“要不要先问问”。另外 `夕飯の食材が無い` 像是完全没有晚饭材料，宜明确为 `夕飯に必要な食材がなかった`。

> [!tip] 参考回答
> スーパーに夕飯に必要な食材がなかったんだけど、先にルームメイトにメッセージして、別の料理でもいいか聞いてみない？

---

### 练习 15

> [!question] 题目
> 明天下午我因为医院预约需要提前离开。负责的任务已经整理好当前状态，也会在离开前把注意事项交接给同事。

> [!example] 自己的答案
> 明日午後に通院予定があるので事前に退勤させていただきます。担当しているタスクをすでに整理しました、退勤する前に注意事項を同僚に渡します。

> [!warning] 指正
> 原因、任务整理和交接都表达到了。`事前に退勤` 不是“提前下班”，应使用 `早退`。题目是整理任务的“当前状态”，宜说 `担当タスクの現在の状況を整理しました`。两个句子不能只用逗号连接；“交接注意事项”用 `注意事項を同僚に共有します` 更自然。

> [!tip] 参考回答
> 明日の午後は通院の予約があるため、<mark style="background: #FF5582A6;">早退させて</mark>いただきます。担当タスクの現在の状況はすでに整理しており、退勤前に注意事項を同僚へ共有します。

## 2026-08-27

### 练习 1

> [!question] 题目
> 修改角色属性时，属性组件应先计算并限制最终值，提交成功后再发送一次变更事件。

> [!example] 自己的答案
> キャラクターの属性を変更するときには、属性コンポーネントは先に計算して最終値を制限し、正常にコミットした後に変更イベントを送信します。

> [!warning] 指正
> 流程基本完整。技术说明中主体用 `属性コンポーネントが` 比 `は` 更直接；`正常にコミットした後` 不自然，应说 `コミットに成功した後`。题目强调只发送“一次”事件，可补充 `一度だけ`。

> [!tip] 参考回答
> キャラクターの属性を変更するときは、属性コンポーネントがまず最終値を計算して有効範囲内に制限し、コミットに成功した後で変更イベントを一度だけ送信します。

---

### 练习 2

> [!question] 题目
> 游戏逻辑只向小地图提供世界坐标和图标类型，UI 负责转换位置、限制边界并选择显示尺寸。

> [!example] 自己的答案
>　ゲームロジックはミニマップに世界座標とアイコンタイプだけを提供します。UIは位置変換、範囲制限、表示サイズの選択だけを担当します。

> [!warning] 指正
> 游戏逻辑与 UI 的职责划分表达得很清楚，主要含义完整。为了让并列结构更自然，后半可写成 `位置の変換、境界内への制限、表示サイズの選択を担当します`；前半的“只提供”也可用 `のみを提供します`。

> [!tip] 参考回答
> ゲームロジックはミニマップに世界座標とアイコンの種類のみを提供し、UIは位置の変換、境界内への制限、表示サイズの選択を担当します。

---

### 练习 3

> [!question] 题目
> 调查 Nanite 场景的 GPU 开销时，请比较可见实例数、材质复杂度和遮挡前后的渲染时间。

> [!example] 自己的答案
>　NaniteシーンのGPUコストを調査する時に、表示されるインスタンスの数、マテリアル複雑度、隠蔽前後描画にかかる時間を比較してください。

> [!warning] 指正
> 三个比较项都表达到了。一般条件用 `調査するときは` 比 `調査する時に` 自然；`表示されるインスタンスの数` 可以理解，技术表达宜说 `可視インスタンス数`。`隠蔽前後描画` 缺少连接，建议用 `オクルージョン前後の描画時間`。

> [!tip] 参考回答
> NaniteシーンのGPUコストを調査するときは、可視インスタンス数、マテリアルの複雑度、<mark style="background: #FF5582A6;">オクルージョン</mark>前後の描画時間を比較してください。

---

### 练习 4

> [!question] 题目
> 关卡加载后，出生点组件应向管理器登记自己；关卡卸载前，必须注销并清除管理器中的引用。

> [!example] 自己的答案
>　レベルをロードした後に、スパンコンポーネントはマネジャーに自分を登録するべきです。レベルをアンロードする前に、マネジャー内で対応する参照をクリアするべきです。

> [!warning] 指正
> 登记和清除引用表达出来了，但 `スパンコンポーネント` 应为 `スポーンポイントコンポーネント`，`マネジャー` 通常写作 `マネージャー`。题目还要求卸载前“注销”，不能只说清除引用，应明确加入 `登録を解除し`。

> [!tip] 参考回答
> レベルの読み込み後、スポーンポイントコンポーネントは自身をマネージャーに登録します。レベルをアンロードする前に登録を解除し、マネージャー内の参照をクリアする必要があります。

---

### 练习 5

> [!question] 题目
> 材质定义只保存共享参数，渲染组件根据运行时状态创建实例，不能反过来修改原始定义。

> [!example] 自己的答案
>　マテリアル定義はシェーアパラメータだけを保存し、描画コンポーネントはランタイム時に状態に応じてインスタンスを生成します。逆に元の定義を変更してはいけません。

> [!warning] 指正
> 定义与运行时实例的边界表达完整。`シェーアパラメータ` 是拼写错误，应为 `共有パラメータ`；`ランタイム時に状態に応じて` 略显重复，可简化为 `実行時の状態に応じて`。后一句最好明确由实例或组件不能修改原始定义。

> [!tip] 参考回答
> マテリアル定義は共有パラメータだけを保持し、描画コンポーネントは実行時の状態に応じてインスタンスを生成します。生成したインスタンスから元の定義を変更してはいけません。

---

### 练习 6

> [!question] 题目
> 应用装备预设之前，请先验证所有物品的所有权和槽位；任意一项失败时都保持当前装备不变。

> [!example] 自己的答案
>　装備の事前設定を適用する前に、まずすべての所有権とスロットを検出します。いずれも失敗した場合にそのままに保持します。

> [!warning] 指正
> 主要方向可以理解，但 `装備の事前設定` 宜说 `装備プリセット`。题目是验证“所有物品的所有权”，应为 `すべてのアイテムの所有権`；`検出` 应改为 `検証`。`いずれも失敗した` 容易理解为全部失败，题目是任意一项失败，应说 `いずれか一つでも失敗した場合`。

> [!tip] 参考回答
> 装備<mark style="background: #FF5582A6;">プリセット</mark>を適用する前に、すべてのアイテムの所有権と装備スロットを検証します。<mark style="background: #FF5582A6;">いずれか一つでも</mark>検証に失敗した場合は、現在の装備を変更しません。

---

### 练习 7

> [!question] 题目
> Root Motion 被碰撞阻挡时，应把角色实际移动的距离反馈给动作逻辑，并丢弃未完成的剩余位移。

> [!example] 自己的答案
>　Root Motionがブロックされる時に、キャラクターの実際に移動した距離をアニメーションロジックに反映し、未完成部分を実行するべきではありません。

> [!warning] 指正
> 碰撞阻挡后的反馈方向正确。`ブロックされる時に` 应改为 `ブロックされたときは`；`キャラクターの実際に移動した距離` 的助词位置不自然，应为 `キャラクターが実際に移動した距離`。`未完成部分を実行しない` 太宽泛，需明确丢弃 `残りの移動量`。

> [!tip] 参考回答
> Root Motionが衝突によってブロックされたときは、キャラクターが実際に移動した距離をアクションロジックに反映し、適用されなかった<mark style="background: #FF5582A6;">残りの移動量</mark>を破棄します。

---

### 练习 8

> [!question] 题目
> 播放脚步声之前，请读取命中结果中的表面类型；如果没有有效材质，则使用默认脚步音效。

> [!example] 自己的答案
>　足音を再生する前に、ヒットした表面タイプを取得しておき、有効なマテリアルがない場合、デフォルト効果音を使います。

> [!warning] 指正
> 条件分支和默认处理都表达正确。题目明确从“命中结果”读取，因此宜说 `ヒット結果から表面タイプを取得する`。`デフォルト効果音` 范围过宽，应明确为 `デフォルトの足音`；这里直接用 `取得し` 即可，无需 `取得しておき`。

> [!tip] 参考回答
> 足音を再生する前に、ヒット結果から表面タイプを取得します。有効な物理マテリアルがない場合は、デフォルトの足音を使用します。

---

### 练习 9

> [!question] 题目
> 行为树任务被中止时，应取消等待中的处理并清除临时引用，完成清理后再报告中止结果。

> [!example] 自己的答案
>　ビヘイビアツリーが中断された時に、待ち中処理をキャンセルして一時的な参照をクリアすべきです。その上で中断結果をリポートします。

> [!warning] 指正
> 清理后再报告的顺序基本表达出来了，但题目主体是“行为树任务”，不能写成整个 `ビヘイビアツリー`。`待ち中処理` 不自然，应说 `待機中の処理`；`その上で` 可进一步明确为 `クリーンアップが完了した後で`。

> [!tip] 参考回答
> ビヘイビアツリータスクが中止されたときは、<mark style="background: #FF5582A6;">待機中の処理</mark>をキャンセルして一時参照をクリアし、クリーンアップが完了した後で中止結果を報告します。

---

### 练习 10

> [!question] 题目
> UI 文本放大后，布局系统应重新计算控件尺寸和换行，不能通过裁剪文字来保持原来的高度。

> [!example] 自己的答案
>　UIテキストが拡大された後には、レイアウトはウィジェットサイズと折り返しを再計算するべきです。テキストをカットすることで元の高さを維持するのではありません。

> [!warning] 指正
> 重新计算尺寸和换行表达完整。题目主体是“布局系统”，宜明确写成 `レイアウトシステム`。`テキストをカットする` 不够专业，可用 `テキストをクリップする`；禁止事项应写成 `〜ことで元の高さを維持してはいけません`。

> [!tip] 参考回答
> UIテキストを拡大した後は、レイアウトシステムがウィジェットのサイズと改行位置を再計算します。テキストをクリップすることで元の高さを維持してはいけません。

---

### 练习 11

> [!question] 题目
> 调查音频遮挡变化过快的问题时，请记录射线结果、遮挡系数和音量插值所需的时间。

> [!example] 自己的答案
>　オーディオの変化が早すぎる問題を調査する時には、レイキャスト結果、遮蔽減衰、音量補完時間を記録してください。

> [!warning] 指正
> 三个记录项大致对应，但开头把“音频遮挡变化”泛化成了“音频变化”，应明确为 `オーディオオクルージョンの変化`。`遮蔽減衰` 不是题目的“遮挡系数”，宜说 `オクルージョン係数`；`補完` 是补充，插值应写作 `補間`。

> [!tip] 参考回答
> オーディオ<mark style="background: #FF5582A6;">オクルージョン</mark>の変化が速すぎる問題を調査するときは、レイキャストの結果、オクルージョン<mark style="background: #FF5582A6;">係数</mark>、音量の補間にかかる時間を記録してください。

---

### 练习 12

> [!question] 题目
> Unity 的资源验证器应区分错误和警告：错误会阻止导入，警告只记录问题并允许继续。

> [!example] 自己的答案
>　Unityのリソース検出ツールはエラーと警告を分けるべきです。エラーはインポートを阻む、警告は問題を記録して続けます。

> [!warning] 指正
> 错误和警告的不同处理已经表达出来。`リソース検出ツール` 是检测工具，题目的验证器宜说 `アセット検証ツール`。后半语体不统一，`阻む` 应改为 `阻止します`；“警告允许继续”应明确是只记录问题并继续处理。

> [!tip] 参考回答
> Unityの<mark style="background: #FF5582A6;">アセット検証ツール</mark>は、エラーと警告を区別するべきです。エラーの場合はインポートを阻止し、警告の場合は問題を記録するだけで<mark style="background: #FF5582A6;">処理を続行</mark>します。

---

### 练习 13

> [!question] 题目
> 面试中说明事件处理顺序时，请解释队列、优先级和稳定排序的作用，并说明如何用测试验证相同输入总会得到相同结果。

> [!example] 自己的答案
>　イベント処理順を明確にする時にはキューを使って入れ順番によって処理します。　検証では同じ入力で同じ結果が得られます。

> [!warning] 指正
> 回答只说明了按进入队列的顺序处理，遗漏了“优先级”和“稳定排序”的作用。`入れ順番` 不自然，应说 `追加された順番`。验证部分只是陈述结果相同，没有说明方法；应表达重复输入相同数据并比较输出。

> [!tip] 参考回答
> イベントをキューに入れ、まず優先度で並べ替えます。同じ優先度のイベントには安定ソートを使い、追加順を維持します。テストでは同じ入力を複数回与え、処理順と最終結果が毎回一致することを検証します。

---

### 练习 14

> [!question] 题目
> 快递好像送到了邻居家。我想先去问一下，如果对方不在，再联系配送公司。

> [!example] 自己的答案
>　宅便が隣に届いたみたい、まず隣の人に聴いて、留守場合なら、宅便会社に連絡します。

> [!warning] 指正
> 先询问邻居、对方不在时再联系配送公司的顺序正确。`宅便` 应为 `宅配便`；“送到邻居家”宜说 `隣の家に届いた`。询问用 `聞く`，不是表示倾听的 `聴く`；`留守場合` 缺少 `の`，应为 `留守の場合`。

> [!tip] 参考回答
> 宅配便が隣の家に届いたみたいだから、まず隣の人に聞いてみる。留守だったら、その後で配送会社に連絡するよ。

---

### 练习 15

> [!question] 题目
> 关于这个功能，我整理了两个方案和各自的风险。为了不影响下周的实现，可以请你明天中午前确定方针吗？

> [!example] 自己的答案
>　この機能について、二つ提案のリストをそれぞれ整理しました。来週に実現に差し支えないように、明日午前に方針を決めていただけませんか。

> [!warning] 指正
> 提出请求的方向正确，但 `二つ提案のリストをそれぞれ整理しました` 没有表达“两种方案和各自风险”，应说 `二つの案とそれぞれのリスクを整理しました`。开发中的实现是 `実装`，不是 `実現`；题目期限是“明天中午前”，应为 `明日の正午まで`，不能改成上午。

> [!tip] 参考回答
> この機能について、二つの案とそれぞれのリスクを整理しました。来週の実装に影響が出ないよう、明日の正午までに方針を決めていただけますか。

## 2026-08-28

### 练习 1

> [!question] 题目
> 锁定目标时，评分模块只负责计算候选分数，角色控制器应在确认目标仍可见后才应用选择结果。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 2

> [!question] 题目
> 拖动物品时，UI 可以显示预览位置，但只有背包系统验证成功后才能提交实际移动。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 3

> [!question] 题目
> 调查 TSR 的残影时，请记录运动矢量、遮挡解除区域和镜头移动前后的画面结果。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 4

> [!question] 题目
> 事件分发期间不要直接修改订阅列表；新增和移除请求应排队，并在本轮处理结束后统一应用。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 5

> [!question] 题目
> 过场动画被跳过时，应停止序列、恢复原来的相机模式，并销毁只为该过场创建的临时对象。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 6

> [!question] 题目
> 制作物品之前先预留材料；制作成功后才消耗，失败时必须解除预留并保持数量不变。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 7

> [!question] 题目
> 高优先级受击动作可以中断低优先级动作，但结束后必须恢复由移动状态驱动的基础姿势。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 8

> [!question] 题目
> 被抓取的物体销毁时，物理抓取组件应立即释放约束、清除目标引用并关闭交互提示。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 9

> [!question] 题目
> AI 收到寻路结果时，应比较请求编号；如果目的地已经改变，就丢弃过期结果并保留当前移动状态。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 10

> [!question] 题目
> 跨越关卡卸载保存对象引用时，请使用弱引用，并在每次访问前确认对象仍然有效。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 11

> [!question] 题目
> 多个系统同时请求降低背景音乐音量时，音频管理器应记录请求数量，只有全部请求结束后才恢复音量。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 12

> [!question] 题目
> 精简 Shader 变体时，请记录处理前后的变体数量和构建耗时，并确认常用材质组合没有被删除。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 13

> [!question] 题目
> 面试中设计计时器管理器时，请说明如何安排大量到期事件、如何取消指定计时器，以及如何验证同一帧集中到期时的处理成本。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 14

> [!question] 题目
> 咖啡店好像多收了一份甜点的钱。我想先确认收据，再礼貌地请店员帮我检查。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>

---

### 练习 15

> [!question] 题目
> 追加验证后，预计工时比原计划多一天。我已经整理了缩小范围的方案，请在今天下班前确认是否采用。

> [!example] 自己的答案
>

> [!warning] 指正
>

> [!tip] 参考回答
>
